# PRD: cb몰 (전자책 PDF 판매 플랫폼) v2.0

> 작성일: 2026-09-16 · 최종 수정일: 2026-09-20
> 상태: **제품 요구사항 + 기술 스택(8.1~8.51) 전 항목 확정, 상호 호환성 감사 완료** — 상세 설계(API/데이터 모델, 화면설계) 및 구현 착수 가능 단계
>
> **문서 구성**: 1~7절은 제품 요구사항(무엇을 만드는가), 8절은 기술 스택 결정(어떻게 만드는가, 주제별 색인은 8절 서두 참고), 9~10절은 일정·팀·다음 단계입니다. 스키마/마이그레이션 실제 파일은 `apps/api/prisma/` 아래에 있습니다.

## 요약 (Executive Summary)

| 항목 | 결정 |
|---|---|
| 판매 형태 | 자사몰(단일 판매자) — 오픈마켓/입점 정산 기능 없음 |
| 타겟 카테고리 | 전자책(e-book) PDF 디지털 상품, **자사 직접 제작** 콘텐츠만 판매 |
| 플랫폼 범위 | 반응형 웹만 (MVP), 네이티브 앱은 추후 검토 |
| 결제 | 토스페이먼츠 — 신용카드 + 카카오페이/네이버페이 |
| 개발 방식 | 자체 개발 (커스텀 웹앱) |
| 아키텍처 | Next.js(프론트) + NestJS(백엔드) 분리, TypeScript 단일 언어 |
| DB | PostgreSQL (Supabase 관리형) + Prisma |
| 배포 | 프론트 → Vercel / 백엔드 → AWS ECS Fargate / 파일스토리지 → AWS S3 |
| 저장소/ORM | 모노레포(Turborepo + pnpm) / Prisma |
| 트래픽 규모 | 초기 회원 1,000명, 동시접속 100명 — 소규모 기준, 과설계 지양 |
| 일정/인력 | **개발 16주**(초기 목표 12주에서 재산정 — FR-2.7 본문검색 +2주, 관리자 4건·MFA +1.5주, 이메일 인증·웹푸시·검토대기주문 +1주), 내부 개발팀 권장 5~6명 유지. **1주차에 도메인·환경 구축과 기술 스파이크를 선행**(M0, M1과 병행) — 9.1절 참고 |
| 디지털 상품 정책 | 재다운로드 1년/최대 10회, 비회원은 이메일 일회성 링크, 파일보호는 비공개 저장소+만료형 링크(MVP), 환불은 다운로드 링크 클릭 전까지만 가능 |
| Signed URL 상세 | S3 presigned URL(SDK v3), 유효기간 **5분**(8.73절 V-1), 저장 암호화 **SSE-S3**, 리전 서울 — 8.4/8.73절 참고 |
| DB 함수/RLS | 다운로드·쿠폰·포인트 원자적 증감은 **PL/pgSQL 저장 함수**로 처리, Supabase **RLS 전 테이블 적용** — 목적은 앱 경로 이중화가 아니라 **공개 `anon` 키가 쓰는 PostgREST 경로 차단**, 통과는 BYPASSRLS가 아닌 **`cbmall_app` 전용 정책**, **Data API 노출 스키마에서 `public` 제거** — 8.5~8.6/8.68절 참고 |
| 인증 | 비밀번호 **Argon2id** 해싱, **Access+Refresh 이중 토큰**(Refresh는 httpOnly 쿠키+회전), 프론트/백엔드 **서브도메인 통일**, **가입 시 이메일 인증 추가**(FR-1.6) — 8.7절 참고 |
| 토스페이먼츠 연동 | 서버 승인(confirm) 2단계 필수, **Idempotency-Key** 적용, **웹훅 안전망 포함**(재조회 검증), 시크릿키는 **AWS Secrets Manager**, PENDING 주문 **30분** 후 자동취소 — 8.8~8.9절 참고 |
| S3 업로드/다운로드 | 업로드는 **백엔드 프록시**(스트리밍), **원본 PDF만 비공개**·샘플/표지는 **공개 버킷 분리**, 다운로드 API는 **JSON으로 presigned URL 반환** — 8.10절 참고 |
| 속도 제한/ECS 태스크 | ECS **2개 이상** 태스크 운영, 보안 민감 엔드포인트는 **Postgres 기반 공유 속도 제한**, 다운로드 URL 발급 **분당 3회 제한** — 8.11절 참고 |
| 예약 작업(Cron) | **`@nestjs/schedule` 인프로세스**로 처리. 중복 실행 방지는 **작업 성격별 이원화**(8.52절): 짧은 DB 작업은 **트랜잭션 단위 Advisory Lock**, 외부 I/O 작업(알림·PDF 재시도)은 **`FOR UPDATE SKIP LOCKED` 행 클레이밍** — 8.12절 참고 |
| 이메일 발송 | ESP는 **MVP: Resend → 추후 AWS SES 재검토**(`EmailProvider` 인터페이스로 추상화), 템플릿은 **React Email**, 발송 안정성은 **`notification_outbox`(channel=EMAIL) + 재시도 Cron** — 8.13/8.17절 참고 |
| SMS 발송 | 아그리게이터 **솔라피(Solapi)**, **짧은 주문완료 확인만**(민감정보는 이메일로만), 실패 시 **로그만 남김**(재시도 없음) — 8.14절 참고 |
| DB 롤/커넥션 | 런타임은 신규 **`cbmall_app`**(최소권한+명시적 BYPASSRLS) + **Supavisor 트랜잭션 모드**, 마이그레이션은 기존 **postgres 롤 + 다이렉트 커넥션** — 8.15절 참고 |
| 웹 푸시 알림 | **Firebase Cloud Messaging(FCM)**으로 발송 인프라만 우선 구축(트리거 미정, 향후 네이티브 앱 대비), 등록 토큰은 `push_subscriptions`에 저장 — 8.16절 참고 |
| 통합 알림 아키텍처 | 이메일/SMS/푸시를 **이벤트 기반(`@nestjs/event-emitter`)**으로 트리거, 발송 이력은 **`notification_outbox`로 통합**(channel 컬럼) — 8.17절 참고 |
| 알림 재시도 정책 | **긴급(14분 이내)/일반(8.5시간 이내) 2단계 백오프 프로파일**, 소진 시 인터랙티브 알림은 사용자 재요청, 주문완료 등은 **관리자 수동 재발송(FR-7.7)** — 8.18절 참고 |
| MFA | **관리자 계정 TOTP 필수**(otplib, 백업코드 10개), 일반 회원은 이번 범위 제외 — 8.19절 참고 |
| 관리자 계정/권한 | **초대 기반 생성**(최초 1명은 시드 스크립트), **SUPER_ADMIN/ADMIN 2단계 권한**, **감사 로그** 도입 — 8.20절 참고 |
| 백엔드 내부 구조 | **도메인별 모듈 폴더**(모듈형 모놀리스), 조회=직접호출/부작용=이벤트, **`dependency-cruiser`로 경계 자동 검증**, CQRS 미도입 — 8.21절 참고 |
| Next.js BFF | **SSR 토큰 릴레이 전용**(좁은 BFF, Route Handler 1개), 클라이언트 상호작용은 8.7절대로 NestJS 직접 호출, `middleware.ts`는 쿠키 존재 여부만 가벼운 1차 체크 — 8.22절 참고 |
| 캐시 전략 | **Redis 미도입**, Next.js Data Cache(태그 기반)로 공개 조회만 캐싱, **온디맨드+5분 시간기반 병행 무효화**, 주문/결제는 항상 캐시 우회 — 8.23절 참고 |
| 모니터링/로깅 | **`nestjs-pino`**(구조화 JSON) → CloudWatch Logs(30일 보존), 민감정보 자동 마스킹, Sentry Logs 병행, **외부 업타임 모니터링** 도입 — 8.24절 참고 |
| 반복호출 방어 종합점검 | 결제는 **PENDING 주문 부분 유니크 인덱스**로 중복주문 방지(8.51절 C-1로 Advisory Lock에서 변경), 쿠폰은 **분당 10회 속도제한**으로 코드 무차별대입 방어 — 8.25절 참고 |
| API 스타일 | **REST 유지, GraphQL 미도입** — 8.23 캐싱/8.11·8.25 속도제한 설계와의 정합성 및 팀 규모(5~6명) 고려 — 8.26절 참고 |
| 검색 방식 | **`pg_trgm` 확장**(오타 허용 유사도 검색) + **초성 검색**(계산된 컬럼), 카탈로그 3,000~5,000건 초과 시 전용 검색엔진 재검토 — 8.27절 참고 |
| Rate Limiting 종합 | 전역 기본 **분당 100회**(인메모리) + 엔드포인트별 오버라이드(8.7/8.11/8.25/8.27), 엣지에 **AWS WAF** 추가 도입 — 8.28절 참고 |
| 샘플 PDF 뷰어 | **`react-pdf`**(PDF.js 래퍼), `next/dynamic`+`ssr:false`로 지연 로드, 다운로드 버튼 포함, 워터마크 불필요 — 8.29절 참고 |
| 키 로테이션 | MFA secret은 **AWS KMS 봉투암호화**로 전환(재암호화 불필요), JWT 시크릿 **90일**, 벤더 API 키 **연 1회**, DB 비밀번호 **분기별** — 8.30절 참고 |
| PDF 본문 전문검색 | `pdf-parse`로 **비동기 텍스트 추출**, **pg_trgm GIN으로 가속한 `ILIKE` 부분문자열 검색**(8.54절 재수정 — 한국어 조사 때문에 tsvector 부적합), 스니펫은 **애플리케이션에서 생성** — 8.31절 참고 |
| PDF 추출 모니터링 | **최대 2회 자동 재시도**, 1시간 초과 PENDING 정체 감지, **실패율 20% 초과 시 집계 알림**, 관리자 대시보드 위젯 추가 — 8.32절 참고 |
| PDF 추출 성능격리 | **`worker_threads`(piscina)로 메인 이벤트 루프에서 분리**, 태스크 vCPU 기준 풀 크기 설정, 60초 타임아웃 — 8.33절 참고 |
| 환경 구성 | **PR 프리뷰(프론트만 격리, 백엔드·DB는 스테이징 공유)→스테이징→운영** 3단계, Supabase 프로젝트는 **스테이징·운영 2개**(브랜칭 미사용, 8.56절), 운영 배포는 **수동 승인 게이트** — 8.34절 참고 |
| CI/CD | **GitHub Actions**, AWS 인증은 **OIDC**(`environment:production` 기반), **승인→마이그레이션→배포** 순서 고정, **확장-수축 규율 + 파괴적 DDL CI 검출**, 배포 직렬화 — 8.35/8.57절 참고 |
| 모노레포 구조 | `apps/web`(Next.js, 관리자는 라우트 그룹)·`apps/api`(NestJS, **`prisma/`가 이 안으로 이동**)·`packages/shared-types` 등 — 8.36절 참고 |
| 상태관리 | 서버 상태는 **TanStack Query**, 클라이언트 전역 상태는 **Zustand**(Access Token 비영속, **장바구니는 localStorage 영속화**) — 8.37절 참고 |
| 테스트 전략 | 프론트 **Vitest+Playwright**, 백엔드 **Jest**(NestJS 기본값), 전체 커버리지보다 **결제/다운로드/알림재시도 등 핵심 로직 우선** — 8.38절 참고 |
| UI 스타일링 | **Tailwind CSS v4 + shadcn/ui**(소스 복사 방식, 신규 의존성 최소화), 고객/관리자 화면이 컴포넌트 세트 공유 — 8.39절 참고 |
| 폼 처리/검증 | **React Hook Form + Zod**, 스키마는 `packages/shared-types`에서 공유, 백엔드는 **`nestjs-zod`로 전환**해 프론트와 동일 검증 규칙 사용 — 8.40절 참고 |
| API 문서화 | `nestjs-zod`의 `createZodDto`+Swagger 연동, **운영에서는 비활성화**(스테이징까지만 노출) — 8.41절 참고 |
| 환경변수 검증 | 프론트 **`@t3-oss/env-nextjs`**(빌드 실패), 백엔드 **Zod 기반 `ConfigModule.validate`**(부팅 즉시 종료) — 8.42절 참고 |
| SEO/메타데이터 | Next.js 내장 Metadata API + **JSON-LD(Book+Offer)**, 동적 sitemap, robots.txt(관리자/개인화 차단, **AI 크롤러 허용**), noindex+canonical — 8.43절 참고 |
| 분석/애널리틱스 | **Vercel Analytics**(트래픽/성능, 신규벤더 아님) + **GA4**(전환 퍼널, `@next/third-parties`) 병행, FR-7.1 대시보드와 역할 분리 — 8.44절 참고 |
| Vitest 상세 설정 | 환경 **jsdom**, API 모킹 **MSW**, 테스트 전용 QueryClient, Zustand 스토어 리셋, CI는 `turbo run test` — 8.45절 참고 |
| Playwright 상세 설정 | **PR 프리뷰 URL** 대상(정리 불필요, 8.34절 임시 브랜치 재사용), **`storageState`** 역할별 재사용, 결제는 **`page.route()` 모킹**, **Chromium 단독** — 8.46절 참고 |
| Testing Library 상세 | shadcn/ui 자체는 제외·비즈니스 컴포넌트만 대상, **`renderWithProviders()`** 공통 헬퍼, `getByRole` 우선, 핵심 폼엔 **`jest-axe`** — 8.47절 참고 |
| ESLint | 최신 버전 **flat config**, `packages/eslint-config`(base/next/nestjs 분리), Prettier·dependency-cruiser와 역할 분리, **Husky+lint-staged** 커밋 전 검사, oxlint 미도입 — 8.48절 참고 |
| Prettier | 루트 **단일 설정**, **기본값 유지**, `prettier-plugin-tailwindcss`로 클래스 자동정렬, import 정렬은 ESLint 담당, **CI에 `prettier --check` 안전망 추가** — 8.49절 참고 |
| GA4 이벤트 설계 | 탐색성 이벤트는 클라이언트, **`purchase`는 NestJS가 Measurement Protocol로 서버 전송**(client_id 연결), 이벤트 정의는 **`packages/shared-types`에서 공유** — 8.50절 참고 |
| 상호 호환성 감사 | 교차 검증으로 찾은 **9건 전부 수정 반영**(락 방식·쿠키 Path·본문검색 엔진·프리뷰 롤·S3 CORS·revalidate 인증·태스크 2vCPU/4GB·storageState·실효한도) — 8.51절 참고 |
| 락·풀러 심층 검토 | C-1 수정안을 재검증해 **작업 성격별로 이원화**(짧은 DB 작업=xact 락 / 외부 I/O 작업=행 클레이밍), 조용한 실패 감지 추가 — 8.52절 참고 |
| 쿠키·SSR 릴레이 심층 검토 | C-2 재검증 결과 **SSR 경로는 비회전 전용 엔드포인트**(서버 간 시크릿) 사용, 브라우저 회전에 **30초 유예** 추가 — 8.53절 참고 |
| C-3~C-9 재검증 | **C-3 재수정**(tsvector→pg_trgm ILIKE+앱단 스니펫, 한국어 조사 문제), **C-5 재수정**(CORS에 Vercel 프리뷰 오리진 추가), C-7은 2vCPU 유지(비용 2배 인지), 나머지 4건 유지 — 8.54절 참고 |
| 2차 전면 점검 | **알림 outbox를 결제 트랜잭션에 묶음**(유실 방지), MFA 미등록 차단·SUPER_ADMIN 잠김 방지, **다운로드 한도 재부여(FR-7.10)**, 업로드 경로 제약. 비회원 중복주문은 **리스크 수용** — 8.55절 참고 |
| 브랜칭·프리뷰 심층 검토 | **Supabase Branching 포기**(Prisma 마이그레이션·ECS 백엔드와 구조적 불일치), 프리뷰는 프론트만 격리하고 백엔드·DB는 스테이징 공유 → C-4 소멸, 대신 **E2E 데이터 정리 로직 필요** — 8.56절 참고 |
| CI/CD 심층 검토 | **확장-수축 마이그레이션 강제**(롤링 중 구코드×신스키마 장애 방지), **E2E 캐시 제외**(캐시가 테스트를 건너뛰는 문제), 스키마 변경 PR 정책, `directUrl` 누락 수정 — 8.57절 참고 |
| 알림 아키텍처 심층 검토 | **중복 발송·유령 알림 차단**(즉시 발송도 행 클레임 통일 + 이벤트는 커밋 이후 발행), 웹훅이 결제 후 워크플로를 **멱등하게 완결**, 재클레임 대기 1분(알림)/10분(PDF 추출), **outbox `payload`에 민감값 저장 금지** — 8.58절 참고 |
| 본문검색 엔진 심층 검토 | pg_trgm 유지가 맞으나 **색인 단위를 '책 1권 1행'→'청크 다중 행'으로 전환**(GIN recheck가 전수 스캔으로 퇴화하는 문제), **본문검색은 3자 이상만**(한국어 2자 검색어는 인덱스 불가), tsvector는 **1MB 하드 에러로 선택지에서 배제**, 전환 기준을 상품 수→청크 수·p95로 교체 — 8.59절 참고 |
| 청크 분할 로직 | **PDF 줄바꿈으로 쪼개진 검색어 복구**(NFC·유령문자·하이픈·한글간 개행 결합), **경계 50자 오버랩**(경계에 걸친 검색어 실종 방지), 문단/문장 경계 우선 분할, **재추출은 DELETE 후 전량 재삽입**(옛 청크 잔존 방지), 1,000자 + `STORAGE MAIN`으로 TOAST 회피 — 8.60절 참고 |
| 스니펫 생성 | **DB에서 `SUBSTRING`으로 잘라 반환**(본문 전체 유출을 쿼리 레벨에서 차단), `strpos(lower(...))`로 대소문자 오프셋 불일치 방지, `DISTINCT ON`으로 상품당 1건, **검색어 50자 상한**으로 이어붙이기 공격 억제, 하이라이트는 **오프셋만 반환**(XSS 차단) — 8.61절 참고 |
| 검색 API 설계 | 단일 `GET /products/search`(+`matchedIn`/`searchedFields`), **검색 결과 페이지는 robots 차단+noindex+비SSR 3중 차단**(스니펫 색인 방지), **`%`·`_` 이스케이프**(전수 스캔 유발 차단), 질의·색인 정규화 공유, `no-store`, 상한 카운트 200+ — 8.62절 참고 |
| 검색 결과 화면 | **제출형 검색**(실시간 검색은 정상 사용자를 분당 30회 제한에 걸리게 함), **검색 상태는 URL이 단일 진실 원천**(SSR 제거의 대가 보완), 하이라이트는 `slice` 3조각 + **`react/no-danger` 린트 강제**, 4xx 재시도 금지, 번호 페이지네이션 — 8.63절 참고 |
| App Router 호환성 점검 | **`ssr: false`는 `'use client'` 래퍼 안에서만**(Server Component에서 빌드 실패), **Zustand는 요청별 Provider + 서버 쓰기 금지**(SSR 토큰 릴레이와 만나면 요청 간 토큰 유출), 장바구니 `persist`는 **`skipHydration`**, pnpm 대응(pdf.js 워커 자체 호스팅·zod 단일 버전), RHF는 `reset()`로 초기값 주입 — 8.64절 참고 |
| 서브도메인 통일 적용 범위 | **프리뷰에도 `pr-N.preview.` 도메인 부여**(`*.vercel.app`은 다른 사이트라 프리뷰 인증이 통째로 실패), `Domain` 속성은 **SSR 릴레이 때문에 필요**함을 명시(`__Host-` 불가 → `__Secure-`), **서브도메인 위임·방치 정책**, 쿠키 엔드포인트에 **`Origin` 검증**, 로컬도 `app.localhost` — 8.65절 참고 |
| BFF 범위 재확정 | **BFF Route Handler 0개**(8.53절이 쿠키 쓰기 필요를 없애 존재 이유가 소멸) — SSR 릴레이는 **서버 전용 헬퍼 함수**, 브라우저 회전은 `api.` 직접 호출. `app/api/`에는 **인바운드 웹훅(`revalidate`) 하나만**(BFF와 별개 범주), 미들웨어에서 NestJS 호출 금지, `'use server'` 금지, **CI가 허용 목록과 대조해 강제** — 8.66절 참고 |
| Prisma × 풀러 검증 | **`?pgbouncer=true` 유지 확정**(Supavisor의 prepared statement 지원은 고객이 켤 수 없는 서버 측 플래그) — 공식 문서들이 "제거해도 된다"로 읽히므로 **제거 금지와 해제 조건을 명시**, `connection_limit=1`(서버리스용) 복사 금지, 누락은 SQLSTATE 42P05·26000 알림으로 감지 — 8.67절 참고 |
| SSR 릴레이 성능·장애 | **Vercel 함수 리전을 `icn1`(서울)로 고정**(기본 `iad1`이면 릴레이가 태평양을 2회 왕복), `maxDuration=10`·`AbortSignal.timeout(3초)`로 **빠르게 실패하고 우아하게 저하**, 개인화 영역은 **`<Suspense>` 스트리밍**, 헬퍼는 **`cache()`로 렌더당 1회** — 8.69절 참고 |
| 마이그레이션 커넥션 | "다이렉트 커넥션"은 **Supavisor 세션 모드 풀러**를 뜻함 — 진짜 다이렉트(`db.<ref>`)는 **IPv6 전용**이고 GitHub Actions는 IPv4뿐이라 CI가 실패. 정확한 규칙은 **"트랜잭션 모드(6543)를 피하라"**. 두 URL 혼동은 **환경변수 형식 검증**으로 차단 — 8.70절 참고 |
| 서명 없는 결제 웹훅 | 전제 재확인(토스 서명은 **지급대행 웹훅 전용**). **응답 코드 3갈래 분리**(중복=200 / 일시장애=5xx로 토스 재시도를 자동복구로 활용 / 불일치=200+경보), **30분 자동취소 × 3일 19시간 재시도 충돌을 `NEEDS_REVIEW`+관리자 알림으로** 처리, 재조회 대조 4항목(금액 포함) — 8.71절 참고 |
| 긴급 알림 재시도 예산 | 백오프를 **`created_at + 누적 지연`(절대 시각)** 으로 계산해 Cron 1분 주기가 누적되지 않게 → 마지막 시도 **최대 15분**(기존 "14분"은 실제로 17분), **발송 직전 토큰 유효성 확인**(만료 링크 발송 차단), "다시 받기" 시 진행 중 outbox 취소, 외부 API 타임아웃 10초 — 8.72절 참고 |
| 다운로드 한도·URL 수명 | **presigned URL 30분→5분**(만료 전까지 재사용·공유가 가능해 유효기간이 곧 콘텐츠 공개 창), **유효 URL 재사용 시 횟수 미소모**(반복 클릭으로 한도가 깎이던 문제), 속도 제한을 **엔타이틀먼트 단위 분당 3회로 통일**, 개정판 발행 시 **한도 +1** — 8.73절 참고 |
| 웹 푸시 트리거 | **거래 정보성 3건 확정**(문의 답변·개정판 발행·만료 D-30), 광고성은 MVP 제외(정보통신망법 동의·야간 제한), **푸시는 항상 이메일과 쌍**(단독 전달 없음), **본문에 책 제목 미표기**(잠금화면 노출) — 8.75절 참고 |
| 3차 호환성 감사 | 최근 절들끼리의 충돌 3건 수정 — 다운로드 **속도 제한을 차감/전체 둘로 분리**(재사용 구제가 제한에 막히던 문제), 웹훅 제한을 **재조회 수반 요청에만**(유도한 재시도를 스스로 막던 문제), 알림 `CANCELLED`를 **실패 집계·재발송 목록에서 제외** — 8.74절 참고 |

---

## 1. 배경 및 목적

'cb몰'은 **전자책(PDF) 디지털 상품**을 자사가 직접 제작하여 온라인으로 판매하는 이커머스 플랫폼이다. 실물 배송이 없는 디지털 재화 특성상, 결제 즉시 다운로드로 상품을 인도하는 흐름이 핵심이다.

### 1.1 문제 정의
- 자체 제작한 전자책(PDF) 콘텐츠를 판매할 온라인 채널 부재
- 고객이 검색 → 미리보기 → 결제 → 즉시 다운로드까지 이탈 없이 완료할 수 있는 경험 필요
- 디지털 파일의 무단 재배포(불법 공유) 리스크 최소화 필요

### 1.2 목표 (Goals)
- 회원가입 없이도 상품을 탐색하고, 회원가입 후 구매까지 완료할 수 있는 최소 기능 구현
- 결제 완료 즉시 안전한 다운로드 링크 제공, 이후 마이페이지에서 재다운로드 지원
- 운영자가 전자책 파일과 주문을 관리할 수 있는 관리자 기능 제공
- 안정적인 결제 연동 및 주문/다운로드 데이터 정합성 확보

### 1.3 성공 지표 (Success Metrics, 가정)
| 지표 | 목표(예시) |
|---|---|
| 회원가입 전환율 | 방문자 대비 5% 이상 |
| 장바구니→결제 완료율 | 40% 이상 |
| 결제 성공률(오류 제외) | 99% 이상 |
| 평균 페이지 로드 시간 | 2초 이내 |
| 고객센터 문의 처리 SLA | 24시간 이내 1차 응답 |

---

## 2. 범위 (Scope)

### 2.1 In Scope (MVP)
- 회원(고객): 회원가입/로그인, 상품 탐색/검색, 장바구니, 주문/결제, 마이페이지(구매내역·다운로드·리뷰)
- 상품 관리: 카테고리, 전자책(PDF) 파일 등록/수정/노출, 미리보기(샘플) 제공
- 주문/결제: 장바구니 → 주문서 → 결제(PG 연동) → 주문 완료 → 즉시 다운로드 인도
- 관리자(백오피스): 상품(파일)/주문/회원/쿠폰 관리, 매출 통계 기초
- 고객지원: 1:1 문의, FAQ, 리뷰/Q&A

### 2.2 Out of Scope (MVP 제외)
- 다중 판매자(오픈마켓) 정산 시스템
- 실물 배송/택배 연동 (디지털 상품이므로 해당 없음)
- 해외 결제/다국어/다통화
- 라이브커머스, 개인화 추천 AI
- 네이티브 앱(iOS/Android)
- 구독형(정기 콘텐츠 제공) 상품
- 워터마킹/DRM 등 고급 파일 보호 (MVP는 비공개 저장소 + 만료형 링크만 적용)
- 미리보기 자동 생성 (별도 제작한 샘플 PDF 수동 업로드 방식만 지원)

---

## 3. 사용자 정의 (Personas)

| 구분 | 설명 | 주요 니즈 |
|---|---|---|
| 일반 고객 | 전자책을 검색/구매하는 최종 소비자 | 빠른 탐색, 미리보기, 신뢰할 수 있는 결제, 즉시·재다운로드 |
| 비회원 구매자 | 회원가입 없이 구매하는 고객 | 최소 정보 입력으로 빠른 결제, 이메일로 다운로드 링크 수신 |
| 운영자(관리자) | 상품(전자책 파일)/주문/회원을 관리하는 내부 직원 | 효율적인 파일 등록, 주문 처리, 다운로드/저작권 관리 |
| (확장) 입점 판매자 | 자체 상품을 등록/판매하는 셀러 | *MVP 이후 검토* |

---

## 4. 핵심 사용자 흐름 (User Flows)

1. **탐색 및 구매**: 메인 진입 → 카테고리/검색 → 상품 상세(미리보기 확인) → 장바구니 담기 → 주문서 작성(결제수단, 배송지 불필요) → 결제 → 주문완료 → 다운로드 링크 즉시 제공
2. **회원가입/로그인**: 이메일 또는 소셜 로그인(추후) → 약관 동의 → 가입 완료 → 자동 로그인
3. **다운로드 및 사후 처리**: 마이페이지 → 구매내역 → 다운로드(재다운로드 가능) / 다운로드 링크 미클릭 건에 한해 환불 신청 → 처리 상태 확인
4. **관리자 상품 등록**: 관리자 로그인 → 전자책 등록(카테고리/가격/설명/표지이미지/PDF 파일/샘플 파일) → 노출 상태 설정 → 저장
5. **관리자 주문 처리**: 신규 주문 확인 → 결제 확인(자동) → 다운로드 발급 이력 확인 → 환불/취소 요청 시 클릭 이력 확인 후 처리

---

## 5. 기능 요구사항 (Functional Requirements)

### 5.1 회원(Membership)
- FR-1.1: 이메일/휴대폰 기반 회원가입 — 비밀번호는 **Argon2id**로 해싱하여 저장. 비밀번호 재설정은 1회용 시간제한(30분) 토큰을 이메일로 발송(8.7절)
- FR-1.2: 로그인/로그아웃, 자동 로그인 — **Access Token(단기, 15~30분)+Refresh Token(장기, httpOnly 쿠키)** 이중 토큰 구조, Refresh는 회전(rotation) 및 재사용 탐지 적용(8.7절)
- FR-1.3: 회원정보 수정(현금영수증/세금계산서 발행용 정보 — 선택 입력, 배송지 불필요)
- FR-1.4: 비회원 구매자는 이메일로 전송된 일회성 다운로드 링크로 인증(로그인 불필요). 링크 분실 시 주문번호+이메일로 재발송 요청 가능
- FR-1.5 (추후): 소셜 로그인(카카오/네이버/구글)
- FR-1.6: 회원가입 시 이메일 인증 링크 발송 — 클릭 시 계정 활성화(`email_verified_at` 기록). 오탈자 이메일 가입으로 인한 다운로드 링크·비밀번호 재설정 미수신 사고 예방 (8.7절)
- FR-1.7: **관리자 계정 MFA(TOTP) 필수** — 인증 앱 등록(QR코드) + 1회용 백업코드 10개 발급, 로그인 시 비밀번호 검증 후 TOTP 코드 추가 입력 (8.19절)

### 5.2 상품(전자책/Digital Product)
- FR-2.1: 카테고리(대/중/소분류) 관리
- FR-2.2: 전자책 등록 — 제목, 저자/설명, 표지 이미지, 가격/할인가, PDF 원본 파일 업로드, 별도 제작한 샘플 PDF 파일 업로드(미리보기용)
- FR-2.3: 상품 검색(키워드, `pg_trgm` 기반 오타 허용 유사도 검색 + 초성 검색) 및 필터(카테고리, 가격대, 판매순/신상품순 정렬). **검색은 엔터·버튼 제출형**(타이핑 중 자동 검색 없음, 8.63절 L-1), 결과는 **번호 페이지네이션**(최대 10페이지, 총 건수는 200건 초과 시 "200+") (8.27/8.62/8.63절)
- FR-2.7: **원본 PDF 본문 전문검색** — 검색어가 책 본문에 포함되면 결과에 노출, 매칭 지점 앞뒤 15~20자 **스니펫만** 제공(전체 본문 절대 미노출)하며 **매칭 지점의 "PDF 기준 N페이지"를 함께 표시**(8.60절 I-6: 표지·서문 때문에 인쇄 쪽번호와 다를 수 있어 기준을 명시). 업로드 완료 후 비동기로 텍스트 추출·정규화해 **목표 1,000자 청크 단위로 색인**. **검색어 3글자 이상 50자 이하**에만 적용되며(8.61절 J-4) 2자 이하는 제목·설명·초성 검색만 수행. 같은 책이 여러 번 매칭되어도 **상품당 1건**(책에서 가장 먼저 나오는 위치)만 노출 (8.31/8.59/8.61절)
- FR-2.4: 상품 상세 페이지(표지, 상세설명, 목차, 페이지 수/용량, 리뷰, 샘플 PDF 뷰어/다운로드) — 뷰어는 **`react-pdf`**로 구현, 미리보기 버튼 클릭 시 지연 로드 (8.29절)
- FR-2.5: 판매 상태 관리(판매중/판매중지 — 디지털 상품이므로 재고 개념 없음, 무제한 판매 기본)
- FR-2.6: 파일 버전 관리 — **오탈자 수정본**: 원본 파일 교체 후 기존 구매자에게 갱신 알림. 이때 **해당 상품 구매자 전원의 다운로드 한도를 +1 부여**한다(8.73절 V-4: 고객 잘못이 아닌 사유로 한도가 깎이면 부당하고 CS를 유발). 알림·한도 부여 대상은 **유효한 엔타이틀먼트 보유자**(구매일+1년 이내)로 한정한다(8.73절 V-5: 만료된 고객은 받을 수 없는 파일의 알림만 받게 됨). **전면 개정판**: 별도 신규 상품으로 등록·판매(기존 구매자 자동 제공 없음)

### 5.3 장바구니 및 주문
- FR-3.1: 장바구니 담기/수량변경(동일 항목 중복구매 방지)/삭제 (비회원도 세션 기반 이용 가능)
- FR-3.2: 주문서 작성 — 구매자 정보 확인, 쿠폰/포인트 적용 (배송지 입력 불필요). 주문 생성 API는 **5초 Advisory Lock**으로 중복 클릭에 의한 중복 주문 생성을 방지 (8.25절)
- FR-3.3: 결제수단 선택 및 PG 연동 — 토스페이먼츠, 신용카드 + 카카오페이/네이버페이 간편결제
- FR-3.4: 결제 완료 즉시 다운로드 링크 발급 및 주문확인 알림(이메일/SMS)
- FR-3.5: 주문 취소/환불 — **다운로드 링크를 클릭하지 않은 건에 한해 환불 가능**, 클릭 이력이 있으면 환불 불가(시스템이 클릭 시각을 기록해 자동 판정)

### 5.4 디지털 상품 인도 (다운로드 관리)
- FR-4.1: 결제 완료 시 비공개 저장소에서 만료형(서명된) 다운로드 URL 발급
- FR-4.2: **구매일로부터 1년, 최대 10회**까지 재다운로드 허용 — 요청마다 새 만료형 URL 발급. **"10회"는 URL 발급 횟수**를 뜻한다(8.73절 V-1: presigned URL은 유효기간 안에서 재사용 횟수 제한이 없으므로 실제 파일 수신 횟수와는 다르다). 유효기간 내 반복 요청은 **기존 URL을 반환하고 횟수를 소모하지 않는다**(8.73절 V-2)
- FR-4.3: 회원은 마이페이지에서, 비회원은 이메일 일회성 링크로 접근하여 한도 내 재다운로드. **비회원 안내 이메일에는 문의 경로(고객센터 이메일 + 주문번호)를 함께 표기**한다(8.73절 V-6: 비회원은 계정이 없어 FR-7.10 한도 재부여를 요청할 창구가 없다)
- FR-4.4: 잔여 다운로드 횟수 및 만료일(구매일+1년) 안내 표시
- FR-4.5: 다운로드 이력 로그(최초 클릭 시각 포함, 관리자 조회 가능) — FR-3.5 환불 판정에 사용
- FR-4.6: **다운로드 기간 만료 임박 알림** — 만료(구매일+1년) **30일 전**에 이메일(주 채널) + 웹 푸시(보조)로 안내. 만료되면 재다운로드가 영구 불가하므로 사전 고지가 필요 (8.12/8.75절)
- (MVP 이후) 워터마킹 등 구매자 식별 정보 삽입을 통한 무단 재배포 추적

### 5.5 리뷰/고객지원
- FR-5.1: 구매 확정 후 리뷰 작성(텍스트+사진)
- FR-5.2: 1:1 문의 게시판, FAQ
- FR-5.3: 상품 Q&A

### 5.6 프로모션
- FR-6.1: 쿠폰 발급/적용(정액/정률, 사용조건). 쿠폰 적용 API는 **분당 10회** 속도 제한으로 코드 무차별 대입 시도를 방어 (8.25절)
- FR-6.2: 적립금(포인트) 지급 및 사용

### 5.7 관리자(Admin/Back-office)
- FR-7.1: 대시보드(일별 매출, 주문건수 요약, **PDF 본문추출 대기/실패 건수 위젯** — 8.32절, **검토 대기 주문(`NEEDS_REVIEW`) 건수 위젯** — 8.71절 T-2)
- FR-7.2: 상품(전자책 파일)/카테고리 CRUD, 파일 교체(개정판) 업로드
- FR-7.3: 주문 관리(상태 변경, 검색/필터), 다운로드 이력 조회. **`NEEDS_REVIEW` 주문 처리** — 자동취소된 주문에 뒤늦은 웹훅으로 결제가 확인된 건(8.71절 T-2)을 관리자가 조회해 **수동 환불 또는 주문 복구 중 하나를 선택**하고, 그 판단을 FR-7.9 감사 로그에 남긴다
- FR-7.4: 회원 관리(조회, 등급/제재)
- FR-7.5: 쿠폰/프로모션 관리
- FR-7.6: 매출/정산 기초 리포트(엑셀 다운로드)
- FR-7.7: 알림 발송 이력 조회 및 **수동 재발송**(대상은 `FAILED`만 — `CANCELLED`는 사용자가 "다시 받기"로 취소한 정상 종료이므로 제외, 8.74절 W-3) — 재시도가 모두 소진되어 `FAILED` 상태로 남은 알림(특히 주문완료 메일)을 관리자가 확인하고 즉시 재발송 (8.18절)
- FR-7.8: **관리자 계정 관리**(SUPER_ADMIN 전용) — 신규 관리자 초대, 계정 비활성화, 권한(SUPER_ADMIN/ADMIN) 변경 (8.20절)
- FR-7.9: **관리자 활동 감사 로그 조회** — 주문상태변경/환불/상품수정/쿠폰발급/회원제재/관리자계정변경 등 주요 행위를 행위자·시각·대상 기준으로 조회 (8.20절)
- FR-7.10: **다운로드 한도 재부여** — 네트워크 실패 등으로 실제 수신 없이 횟수만 차감된 고객을 위해 관리자가 잔여 다운로드 횟수를 조정(감사 로그 기록 대상, 8.55절 D-4)

---

## 6. 비기능 요구사항 (Non-Functional Requirements)

| 구분 | 요구사항 |
|---|---|
| 성능 | 주요 페이지 로드 2초 이내, 동시접속 100명 기준 안정적 응답(초기 회원 1,000명 규모 — 경량 인프라로 충분). **로그인 사용자의 SSR 페이지는 Vercel 함수 리전을 `icn1`(서울)로 맞추고 개인화 영역을 `<Suspense>`로 분리해 이 목표를 지킨다**(8.69절 — 기본 리전 `iad1`이면 릴레이가 태평양을 2회 왕복해 렌더 전에 360ms+가 깔린다) |
| 보안 | 결제/개인정보는 PG사 및 관련 법규(전자상거래법, 개인정보보호법) 준수, 비밀번호 암호화 저장, HTTPS 필수 |
| 파일 저장/전송 | PDF 원본은 비공개 스토리지(S3 비공개 버킷)에 저장, 다운로드는 만료형 서명 URL로만 제공 |
| 저작권 보호 | MVP는 비공개 저장소+만료형 링크 수준. 워터마킹 등은 MVP 이후 검토 |
| 확장성 | 상품/주문 데이터 증가에 대비한 DB 설계, 추후 다중 판매자 확장 고려 |
| 가용성 | 목표 가동률 99.5% 이상(추후 확정) |
| 반응형 | 모바일/태블릿/데스크톱 반응형 웹 지원 |
| 브라우저 호환 | 최신 Chrome, Safari, Edge, 모바일 브라우저 — **기준선은 각 브라우저의 최근 2개 메이저 버전**(8.64절 M-7). 이 범위를 벗어나면 핵심 동선(구매·다운로드)은 동작하되 부가 기능(PDF 미리보기·웹 푸시)은 그레이스풀 디그레이드 |
| 로깅/모니터링 | 주문/결제 오류에 대한 로깅 및 알림 체계 |
| 접근성 | 웹 접근성 기본 준수(대체텍스트, 키보드 네비게이션) — 우선순위 낮음 |

---

## 7. 결제 및 법적 고려사항
- PG사: 토스페이먼츠 — 가맹점 계약 체결 및 API 연동(결제위젯 SDK v2) 필요
- 통신판매업 신고 필요
- **디지털 콘텐츠 청약철회 특례**: 전자상거래법상 디지털 콘텐츠는 소비자가 다운로드/열람에 동의한 경우 청약철회가 제한될 수 있음. 결제 전 명확한 고지 UI 필요. **다운로드 링크 클릭 시점을 기준으로 환불 판정** — 클릭 전까지는 환불 가능, 이후 불가
- 콘텐츠 저작권: 자사 직접 제작 확정 — 외부 저자/출판사 계약 불필요. 내부 제작 인력(직원/외주 작가)의 저작권 귀속 계약은 별도 확인 필요
- 개인정보처리방침, 이용약관 페이지 필요
- 현금영수증/세금계산서 발행 정책 확인 필요

---

## 8. 기술 스택

> 검토 순서대로 절 번호를 붙여왔기 때문에 주제가 흩어져 있습니다. **번호를 재배열하면 문서 전반의 상호 참조(예: "8.11절 참고")가 모두 깨지므로 번호는 유지**하고, 대신 아래 주제별 색인으로 찾아볼 수 있게 했습니다.

| 주제 | 해당 절 |
|---|---|
| **기반 아키텍처** | 8.1 아키텍처 · 8.2 스택 상세 · 8.3 세부 기술 결정 · 8.21 모듈형 모놀리스 · 8.26 GraphQL 미도입 · 8.36 모노레포 구조 |
| **DB·데이터 접근** | 8.5 PostgreSQL 함수 정책 · 8.6 PL/pgSQL 함수·RLS · 8.15 DB 롤/커넥션 |
| **인증·계정 보안** | 8.7 이메일+비밀번호 인증 · 8.19 MFA · 8.20 관리자 계정/권한/감사 · 8.22 BFF 계층 · 8.30 키 로테이션 · 8.53 SSR 토큰 릴레이 · 8.65 서브도메인 통일 · 8.66 BFF 범위 재확정 |
| **결제** | 8.8 토스페이먼츠 연동 · 8.9 웹훅 중복 방지 |
| **파일·PDF** | 8.4 만료형 Signed URL · 8.10 S3 업로드/다운로드 · 8.29 샘플 PDF 뷰어 · 8.31 본문 텍스트 추출/색인 · 8.32 추출 실패율 모니터링 · 8.33 추출 성능 격리 |
| **알림(메일·SMS·푸시)** | 8.13 이메일 · 8.14 SMS · 8.16 웹 푸시 · 8.17 통합 알림 아키텍처 · 8.18 재시도 정책 · 8.58 알림 아키텍처 심층 검토 · 8.75 웹 푸시 트리거 |
| **검색** | 8.27 검색 방식(제목·초성) · 8.59 본문검색 엔진 · 8.60 청크 분할 로직 · 8.61 스니펫 생성 · 8.62 검색 API · 8.63 검색 결과 화면 |
| **성능·보호** | 8.11 파일 요청 속도 제한 · 8.12 Cron/예약 작업 · 8.23 캐시 전략 · 8.25 반복호출 방어 점검 · 8.28 Rate Limiting 종합 · 8.52 락 방식 이원화 |
| **프론트엔드** | 8.37 상태관리 · 8.39 UI 스타일링 · 8.40 폼 처리/검증 · 8.43 SEO/메타데이터 · 8.64 App Router 호환성 |
| **품질·테스트 도구** | 8.38 테스트 전략 · 8.45 Vitest · 8.46 Playwright · 8.47 Testing Library · 8.48 ESLint · 8.49 Prettier |
| **운영·인프라** | 8.24 모니터링/로깅 · 8.34 환경 구성 · 8.35 CI/CD · 8.41 API 문서화 · 8.42 환경변수 검증 · 8.56 브랜칭·프리뷰 · 8.57 CI/CD 심층 검토 |
| **분석** | 8.44 분석 도구 선정 · 8.50 GA4 이벤트 설계 |
| **전체 정합성 점검** | 8.51 1차 호환성 감사(9건) · 8.52~8.54 수정안 재검증 · 8.55 2차 전면 점검 · 8.56~8.58 개별 심층 검토 · 8.64~8.73 교차 점검(App Router·서브도메인·BFF·Prepared Statement·RLS·SSR 타임아웃·마이그레이션 커넥션·웹훅·재시도 예산·다운로드 한도) · **8.74 3차 호환성 감사** |

#### 결정 간 의존 관계 — "이 절을 바꾸면 함께 봐야 할 절"

> 이 표를 두는 이유는 실제로 **결정이 표류한 사례가 반복됐기 때문**입니다. 8.51절 C-3은 두 번 뒤집혔고(8.54 → 8.59), C-4는 전제가 무너져 아예 소멸했으며(8.56), 8.53절이 SSR 릴레이 방식을 바꿨는데 **8.22절이 갱신되지 않아 존재 이유가 없는 Route Handler가 13개 절 동안 남아 있었습니다**(8.66절). 아래는 **바뀌면 파급이 큰 기반 결정**만 추린 것으로, 전체 상호참조 목록이 아닙니다.

| 기반 결정 | 무엇을 떠받치고 있나 | 바뀌면 함께 봐야 할 절 |
|---|---|---|
| **8.7 서브도메인 통일 + 쿠키 `Domain` 속성** | 쿠키가 `app.`↔`api.` 사이에서 전송된다는 전제. `Domain` 속성은 **SSR 릴레이 때문에** 필요(8.65절 N-2) | 8.22(BFF 0개의 근거) · 8.34(환경별 도메인) · 8.46(프리뷰 E2E 로그인) · 8.51 C-5(CORS 오리진) · 8.53 · 8.56(프리뷰 도메인) · 8.65 · 8.66 |
| **8.15 Supavisor 트랜잭션 모드 풀러** | "세션 단위 상태를 쓸 수 없다"는 제약의 출처 | 8.6(PL/pgSQL) · 8.12(Cron 락) · 8.52(락 이원화) · 8.57 F-5(`directUrl`) |
| **8.53 비회전 SSR 전용 엔드포인트** | 쿠키 쓰기를 불필요하게 만들어 BFF를 없앨 수 있게 한 결정. SSR 경로의 **네트워크 왕복 수**를 결정하기도 함 | 8.22 · 8.66(되돌리면 Route Handler가 다시 필요) · 8.69(리전·타임아웃·스트리밍) |
| **8.59 청크 단위 색인(`product_text_chunks`)** | 검색 줄기 전체의 토대 | 8.31(추출) · 8.32(파이프라인) · 8.60(분할) · 8.61(스니펫) · 8.62(API) · 8.63(화면) · 8.24(전환 판단 지표) |
| **8.31 본문 전체 미노출 원칙** | 유료 콘텐츠 보호의 기준선. SEO 결정과 정면으로 만남 | 8.43(robots·noindex) · 8.61 J-1/J-4(스니펫·검색어 상한) · 8.62 K-1(검색 페이지 3중 차단) |
| **8.22 "브라우저가 `api.` 직접 호출"** | 요청 경로 단일화. 우회 경로가 생기면 함께 깨짐 | 8.10(업로드 경로 — Vercel 4.5MB 제한 회피) · 8.62 K-1(검색 클라이언트 페치) · 8.63 L-2(URL 상태) · 8.66 |
| **8.17 `notification_outbox` 통합** | 모든 채널이 한 테이블·한 재시도 경로를 공유 | 8.13/8.14/8.16(채널별 구현) · 8.18(재시도 프로파일) · 8.52(행 클레임) · 8.58 |
| **검색 결과 상한 3종(페이지 상한 10 × 페이지당 20건 = 총 건수 표기 200)** | 세 값이 하나의 관계식으로 묶여 있음(8.74절 W-6) | 8.62 K-5 · 8.63 L-5 — **하나를 바꾸면 나머지 둘도 함께** |
| **8.28 검색 분당 30회 제한** | 서버 방어선이자 **프론트 구현 방식을 강제하는 제약** | 8.61 J-4(검색어 상한) · 8.62 K-6(`no-store`) · 8.63 L-1(제출형 검색)·L-4(4xx 재시도 금지) |
| **8.52 Cron 락 이원화(A형/B형)** | 외부 I/O를 포함한 작업의 동시성 모델 | 8.12(스케줄) · 8.18(알림 재시도) · 8.32(추출 재시도) · 8.58 G-4(재클레임 대기) |
| **8.36 pnpm + Turborepo** | 의존성 해석 방식과 캐시 동작 | 8.57(캐시가 E2E를 건너뛰는 문제) · 8.64 M-4(pdf.js 워커 경로·zod 단일 버전) |
| **8.37 Zustand 상태 경계** | 무엇을 어디에 담는지의 기준 | 8.7(Access Token 보관) · 8.63 L-2(URL 상태) · 8.64 M-2/M-3(요청별 스토어·`persist`) |
| **8.43 robots·AI 크롤러 허용** | 무엇이 색인되어도 되는지의 기준선 | 8.31(본문 보호와 충돌 지점) · 8.62 K-1 · 8.66 O-6 |

**사용법**: 위 표의 기반 결정을 수정하는 PR은 오른쪽 열의 절을 **함께 열어 확인**하고, 영향이 있으면 같은 PR에서 갱신합니다. 새로운 심층 검토 절을 추가할 때 기반 결정을 건드렸다면 이 표의 오른쪽 열도 함께 갱신합니다. 이 확인은 **`.github/PULL_REQUEST_TEMPLATE.md`의 체크박스로 리마인드**되며, 표를 수정할 때는 템플릿 안의 요약 표도 함께 고칩니다(두 곳에 중복되어 있으므로 한쪽만 고치면 어긋납니다).

### 8.1 아키텍처
**Next.js(프론트) + NestJS(백엔드) 분리** 구조, TypeScript 단일 언어. 결제 승인·다운로드 토큰·presigned URL 발급 등 보안에 민감한 로직을 백엔드가 전담하도록 프론트/백엔드를 분리했다.

### 8.2 스택 상세

| 영역 | 선택 | 근거 |
|---|---|---|
| 프론트엔드 | Next.js 15 (App Router) + TypeScript | 국내 자료·커뮤니티 풍부, 토스페이먼츠 SDK 공식 지원 |
| 백엔드 | NestJS + TypeScript | 모듈/DI 구조로 결제·다운로드·관리자 API를 체계적으로 분리, 추후 앱 API 재사용 용이 |
| DB | PostgreSQL (Supabase 관리형) | 회원-주문-상품-결제-다운로드이력 간 관계형 데이터에 적합, 인프라 구축 부담 최소화 |
| ORM | Prisma | NestJS 공식 통합 예제 풍부, 스키마 관리·마이그레이션 용이 |
| 인증 | NestJS + Passport/JWT | 이메일 인증 + 향후 소셜로그인 확장 용이, 비회원 일회성 링크는 별도 토큰 방식 |
| 결제 | 토스페이먼츠 결제위젯 SDK v2(프론트) + NestJS 결제 승인/웹훅 처리 | v1은 업데이트 종료, v2 권장. 승인 로직을 백엔드에 분리해 보안 강화 |
| 파일 스토리지 | AWS S3 비공개 버킷 + NestJS에서 만료형 presigned URL 발급 | CDN 없이도 100명 동시접속 규모는 충분히 처리, 비용/구성 단순화 |
| 이메일 발송 | Resend 또는 SendGrid | 비회원 일회성 링크, 주문확인 메일 발송용 |
| 배포 | 프론트 → Vercel, 백엔드 → AWS(ECS Fargate **2개 이상 태스크**, 8.11절), 파일스토리지 → AWS S3 | Vercel-Next.js 궁합, 백엔드·스토리지를 AWS로 묶어 IAM/네트워크 단순화. 태스크 2개 이상은 무중단 배포·장애 대응 목적 |
| 모니터링 | Sentry | 결제/다운로드 오류 조기 감지 |

### 8.3 세부 기술 결정 (확정)

| 항목 | 결정 | 근거 |
|---|---|---|
| 백엔드 AWS 배포 방식 | **ECS Fargate** | 2026년 기준 신규 프로젝트는 대부분 Fargate를 우선 선택하는 추세. 컨트롤 플레인 비용 없이 소규모 컨테이너(0.5vCPU/1GB)를 월 $14~18 수준으로 운영 가능하며, 서버 패치·오토스케일링 등 운영 부담이 EC2보다 훨씬 낮음. 100명 동시접속 규모에서는 EC2 대비 비용 차이가 크지 않아 운영 단순화가 더 이득 |
| 저장소 구조 | **모노레포 (Turborepo + pnpm workspaces)** | 프론트(Next.js)와 백엔드(NestJS)를 같은 팀이 개발하므로, 모노레포로 묶어 **주문/결제/다운로드 DTO·TypeScript 타입을 패키지로 공유**하면 API 응답 구조가 바뀔 때 프론트에서 즉시 타입 에러로 감지되어 3개월 일정에서 버그를 줄이는 데 유리. 캐시 기반 빌드로 CI 시간도 단축 |
| ORM | **Prisma** | NestJS 공식 통합 문서가 잘 되어 있고 스키마 마이그레이션이 쉬움. 현재 데이터 모델(회원-주문-상품-결제-다운로드이력)은 복잡한 다형성 관계가 없어 Prisma로 충분 |

> 참고: 자체 개발 vs 커머스 SaaS 비교 근거는 부록 A 참고.

### 8.4 만료형 Signed URL 상세 설계 (확정)

> FR-4.1(만료형 다운로드 URL 발급)의 구체적인 구현 방식입니다.

**두 종류의 링크를 구분해서 설계해야 합니다** (현재 FR 문구상 혼동 소지가 있어 명확화):

| 구분 | 이메일 발송 링크(비회원) / 마이페이지 진입(회원) | 실제 파일 다운로드 URL |
|---|---|---|
| 성격 | 구매 엔타이틀먼트를 식별하는 **장기 토큰** (구매일+1년 유효) | S3에서 **직접 파일을 받는 단기 URL** |
| 발급 시점 | 결제 완료 시 1회 생성, 이메일로 발송 | 사용자가 "다운로드" 버튼을 누를 때마다 매번 새로 생성 |
| 유효기간 | 1년 (FR-4.2 재다운로드 정책과 동일) | **수십 분 단위**로 매우 짧게 (아래 참고) |
| 재사용 | 여러 번 재접근 가능 (횟수 한도 내) — "일회성"은 *비밀번호 없이 이메일로 1개 링크만 발급*한다는 의미이지, 1회용 소모성 링크라는 뜻은 아님 | 발급된 URL 자체는 유효기간 내 여러 번 접근 가능 (S3 presigned URL의 특성상 사용 횟수 자체는 제한 불가) |

**확정 스택 및 설계**

| 항목 | 결정 | 근거 |
|---|---|---|
| 구현 방식 | **AWS SDK v3** — `@aws-sdk/client-s3` + `@aws-sdk/s3-request-presigner`의 `getSignedUrl(GetObjectCommand)` | S3 표준 방식, NestJS와 통합 쉬움. CloudFront Signed URL/Cookie는 CDN 미사용 결정(6절)과 일관되게 MVP에서 제외 |
| URL 유효기간 | **5분**(8.73절 V-1로 30분에서 단축) | presigned URL은 **만료 전까지 재사용 횟수에 제한이 없고 타인에게 공유도 가능**하므로, 유효기간이 곧 **유료 콘텐츠가 사실상 공개되는 창**이다. 전자책 PDF(최대 100MB)는 정상 회선에서 **수 초~수십 초**면 받으므로 5분으로도 충분하고, 공유 창이 6분의 1로 줄어든다. 근본 차단(백엔드 프록시 스트리밍)은 100MB를 ECS가 중계해야 해 대역폭·메모리 부담이 커 MVP에는 과하다. 2분 미만은 서버-클라이언트 시계 오차로 위험하다. **단축이 대용량 다운로드를 깨지 않는 근거(8.74절 W-5)**: S3는 **요청 시작 시점에만 서명을 검증**하므로 100MB 전송 도중 URL이 만료돼도 **진행 중인 다운로드는 끊기지 않는다**. 다만 **연결이 끊겨 재개(Range 재요청)하는 경우는 새 요청**이라 만료되었으면 실패하는데, 5분 안이면 8.73절 V-2의 재사용 창이 횟수 소모 없이 새 URL을 주므로 완충된다 |
| 다운로드 횟수 카운트 시점 | **presigned URL 발급 시점**에 카운트 증가 | S3는 실제 파일 수신 완료 여부를 알 수 없으므로, "다운로드 버튼 클릭(URL 발급 요청)"을 1회로 간주 — FR-4.2와 일치 **단, 직전 발급 URL이 아직 유효하면 새로 발급하지 않고 그 URL을 그대로 반환하며 카운트를 올리지 않는다(8.73절 V-2).** 발급 시점 카운트만 두면 **다운로드가 시작되지 않는 것 같아 버튼을 세 번 누른 정상 사용자가 한도 3회를 잃는다** — 8.11절의 분당 3회 제한은 이를 막아주기는커녕 분당 3회씩 소진되도록 허용하므로 10회가 3~4분 만에 바닥날 수 있고, 그때마다 FR-7.10 CS가 발생한다. 유효기간(5분) 안의 반복 클릭을 한 번으로 집계하면 이 소모가 구조적으로 사라진다. UI에도 "이미 발급된 링크가 N분간 유효합니다"를 표시한다. |
| 파일명 처리 | `ResponseContentDisposition: attachment; filename="상품명.pdf"` 파라미터 적용 | S3 내부 객체 키(임의 문자열) 대신 사용자 친화적 파일명으로 저장되도록 강제 |
| 저장 시 암호화 | **SSE-S3(기본 제공)** | 별도 키 관리 없이 저장 데이터 암호화 가능. 더 강한 접근감사/키 회전이 필요하면 SSE-KMS로 상향 가능(비용/복잡도 소폭 증가) |
| 버킷 리전 | **ap-northeast-2 (서울)** | 국내 사용자 대상 서비스이므로 지연시간 최소화 |
| 버킷 접근 정책 | Block Public Access 전체 활성화, 버킷 정책으로 presigned URL 외 모든 접근 차단 | 비공개 저장소 요구사항(6절) 충족 |
| 남용 방지(MVP 수준) | 동일 엔타이틀먼트에 대해 **분당 3회** 발급 제한(8.11절과 통일 — 8.73절 V-3에서 기존 "예: 1분당 1회" 표기를 정정) | 자동화된 대량 다운로드 시도를 완화. 워터마킹/DRM 등 강한 보호는 기존 결정대로 MVP 이후 검토 |

Sources:
- [Download and upload objects with presigned URLs (AWS 공식 문서)](https://docs.aws.amazon.com/AmazonS3/latest/userguide/using-presigned-url.html)
- [Sharing objects with presigned URLs (AWS 공식 문서)](https://docs.aws.amazon.com/AmazonS3/latest/userguide/ShareObjectPreSignedURL.html)
- [AWS S3 Presigned URLs 가이드](https://medium.com/@speaktoharisudhan/aws-s3-presigned-urls-2da5abeefb4a)

### 8.5 PostgreSQL 함수/DB 로직 사용 정책 (확정)

> 커스텀 PL/pgSQL 함수·트리거를 DB에 둘지, 로직을 NestJS 애플리케이션 계층에 둘지에 대한 리서치 기반 제안입니다. **실제 구축이 아닌 결정 단계**입니다.

**결론(확정): `updated_at`/검색은 애플리케이션 계층으로 충분하나, 원자적 증감은 PL/pgSQL 저장 함수로 처리하고, Supabase RLS는 방어적 이중화 차원에서 전 테이블에 적용합니다.**

| 항목 | 검토 대상 | 결정 | 근거 |
|---|---|---|---|
| `updated_at` 자동 갱신 | DB 트리거 함수(`BEFORE UPDATE`) vs Prisma `@updatedAt` | **Prisma `@updatedAt` 유지, DB 트리거 불필요** | 모든 쓰기가 NestJS(Prisma)를 통해서만 발생하므로 애플리케이션 계층만으로 충분 |
| 다운로드 횟수(`download_count`), 쿠폰 사용 수(`used_count`), 포인트 잔액 원자적 증감 | PL/pgSQL 저장 함수 vs 애플리케이션 단건 조건부 UPDATE | **PL/pgSQL 저장 함수 사용** (`increment_download_count`, `redeem_coupon`, `adjust_point_balance` — 8.6절) | 검증 로직(한도·유효기간·잔액 체크)과 원자적 갱신을 DB 함수 안에 캡슐화해 애플리케이션 코드가 실수로 조건절을 누락해도 DB 레벨에서 정합성이 보장됨. 여러 서비스 코드 경로(관리자 API, 배치 등)에서 재사용하기도 용이 |
| 상품 검색(FR-2.3) | Postgres 전문검색(`tsvector`/`GIN` 인덱스) vs 단순 `ILIKE` 키워드 검색 | **단순 `ILIKE`/`contains` 검색 (Prisma 기본 기능)** | 자사 직접 제작 전자책 위주로 카탈로그 규모가 크지 않을 것으로 예상되어 전문검색 인프라는 과설계. 카탈로그가 커지면 그때 도입 검토 |
| Supabase Row Level Security(RLS) | 전 테이블 RLS 정책 작성 vs 미적용 | **적용 — 목적은 "앱 경로의 이중 방어"가 아니라 "PostgREST/anon 경로 차단"**(8.68절 Q-1). 전 테이블 RLS 활성화 + **`cbmall_app` 전용 허용 정책만** 생성하고 **anon·authenticated용 정책은 0개로 유지** | **(8.68절로 설명 교정)** 이전 표현인 "방어적 이중화"는 실제 효과와 달랐다 — 애플리케이션 롤이 RLS를 통과하므로 **앱 경로에는 아무 방어도 하지 않는다**. RLS가 실제로 막는 것은 **Supabase가 자동 노출하는 PostgREST Data API**이며, `anon` 키는 **설계상 공개되는 값**이다. 우리는 Supabase Auth를 쓰지 않고 자체 인증(8.7절)을 쓰므로 **anon 정책 0개 = 전면 거부**가 정확히 맞는 구성이다. **⚠ 정책을 추가하지 말 것** — "정책이 하나도 없네?"라며 선의로 추가하면 그 순간 공개 키로 읽히는 테이블이 생긴다. 통과 방식은 BYPASSRLS가 아니라 **`TO cbmall_app` 전용 정책**을 쓴다(8.68절 Q-3: PostgreSQL은 BYPASSRLS 부여를 superuser에게만 허용하는데 Supabase의 `postgres`는 superuser가 아니라 실패할 수 있고, 정책 방식이 "누가 통과하는지"를 스키마에 남겨 더 낫다) |

Sources:
- [How to Handle Race Conditions in PostgreSQL Functions](https://oneuptime.com/blog/post/2026-01-25-postgresql-race-conditions/view)
- [Best way to increment/decrement a field? (Prisma GitHub Discussion)](https://github.com/prisma/prisma/discussions/3176)
- [Row Level Security | Supabase Docs](https://supabase.com/docs/guides/database/postgres/row-level-security)

### 8.6 PL/pgSQL 함수 및 RLS 마이그레이션 (확정)

8.5절 결정에 따른 실제 함수 정의는 `apps/api/prisma/migrations/20260919130000_atomic_functions_and_rls/migration.sql`에 작성했습니다. NestJS에서는 Prisma의 `$queryRaw`로 `SELECT * FROM increment_download_count($1)` 형태로 호출하고, 함수가 `RAISE EXCEPTION`을 던지면 이를 캐치해 비즈니스 예외(예: 다운로드 한도 초과, 쿠폰 사용 불가, 포인트 부족)로 변환합니다.

| 함수 | 용도 | 실패 시 동작 |
|---|---|---|
| `increment_download_count(entitlement_id)` | FR-4.2 다운로드 횟수 원자적 증가 (한도·유효기간 검증 포함) | 조건 불충족 시 `DOWNLOAD_LIMIT_EXCEEDED_OR_EXPIRED` 예외 |
| `redeem_coupon(coupon_id)` | FR-6.1 쿠폰 사용 수 원자적 증가 (사용한도·유효기간 검증 포함) | 조건 불충족 시 `COUPON_UNAVAILABLE` 예외 |
| `adjust_point_balance(user_id, delta)` | FR-6.2 포인트 적립/사용 원자적 증감 (음수 잔액 방지) | 잔액 부족 시 `INSUFFICIENT_POINT_BALANCE` 예외 |

함수는 모두 **`SECURITY INVOKER`(기본값)** 로 두며 **`SECURITY DEFINER`는 사용하지 않습니다**(8.68절 Q-6: `DEFINER`를 붙이면 소유자인 `postgres` 권한으로 실행되어 `cbmall_app`의 최소권한 설계가 무너집니다. 불가피한 경우 함수 내에서 `search_path`를 고정해야 하며, CI가 이 문자열을 검출합니다). RLS는 전 테이블에 `ENABLE ROW LEVEL SECURITY`를 적용하고 **`cbmall_app` 전용 허용 정책만** 둡니다(= anon/authenticated 롤은 기본적으로 모든 접근 거부, NestJS가 사용하는 DB 롤은 BYPASSRLS로 영향 없음).

> **8.11절 반영**: `increment_download_count()`는 이후 분당 3회 속도 제한과 `download_logs` 기록까지 원자적으로 처리하도록 개정되었습니다(`20260919160000_rate_limiting` 마이그레이션). 또한 로그인/비밀번호 재설정용 공유 속도 제한 저장소로 `rate_limit_buckets` 테이블을 추가했습니다.

### 8.7 이메일+비밀번호 인증 기술 스택 (확정)

> FR-1.1~1.2(이메일/비밀번호 회원가입·로그인)의 구체적인 구현 방식입니다. 프론트(Vercel)와 백엔드(AWS ECS)가 서로 다른 오리진이라는 점이 쿠키 기반 설계에 영향을 줍니다.

| 항목 | 검토 대상 | 결정 | 근거 |
|---|---|---|---|
| 비밀번호 해싱 | Argon2id vs bcrypt | **Argon2id** (`argon2` 패키지, m=64MB, t=3, p=4) | OWASP 2024+ 권장 1순위. bcrypt는 72바이트 길이 제한과 GPU 병렬화 취약점이 있음. 백엔드가 ECS Fargate(컨테이너)라 네이티브 바이너리 컴파일 제약(서버리스/Lambda에서 흔한 이슈)이 없어 Argon2id 도입에 문제 없음 |
| 라이브러리 | Passport 조합 | **`@nestjs/passport` + `passport-local`(로그인 검증) + `passport-jwt`(라우트 보호) + `@nestjs/jwt`** | NestJS 공식 인증 가이드의 표준 조합, 자료·예제 풍부 |
| 토큰 구조 | 단순 장기 Access JWT 1개 vs **Access(단기)+Refresh(장기) 이중 토큰** | **이중 토큰**: Access 15~30분(클라이언트 메모리 보관, `Authorization: Bearer` 헤더로 전송) + Refresh 30일(서버가 **httpOnly 쿠키**로 발급). **Path 제한은 두지 않음**(8.51절 C-2: `/auth/refresh` Path 제한 시 8.22절 BFF 핸들러·미들웨어에 쿠키가 전송되지 않아 SSR 인증이 동작 불가 — 보호는 HttpOnly·SameSite·Domain이 담당) | 탈취 시 피해 범위를 최소화(Access는 짧게, Refresh는 JS로 읽을 수 없음). 2026년 기준 NestJS+Next.js 분리 아키텍처의 표준 패턴 |
| Refresh Token 저장/무효화 | Access Token만 stateless로 두고 Refresh는 무상태 vs **DB에 해시로 저장 + 회전(rotation)** | **DB 저장 + 회전 + 재사용 탐지** — 매 갱신마다 새 Refresh 발급, 이전 토큰 무효화, 이미 사용된 토큰 재사용 시도 감지 시 해당 사용자의 전체 세션 강제 로그아웃. **단 회전 직후 30초의 유예 기간**을 두어, 그 안에 구 토큰이 다시 오면 침해로 보지 않고 이미 발급된 새 토큰을 반환(8.53절: 동시 다중 refresh 요청으로 정상 사용자가 오탐 로그아웃되는 것을 방지). **SSR 경로는 회전하지 않는 전용 엔드포인트를 별도로 사용**(8.53절) | 로그아웃/탈취 대응이 즉시 반영되어야 하는데, Access Token은 만료 전까지 무효화가 불가능한 stateless 방식이므로 Refresh 쪽에서 통제권을 가져야 함 |
| 프론트-백엔드 도메인 구성 | 서로 다른 도메인 그대로 사용(`SameSite=None; Secure`) vs **같은 상위 도메인의 서브도메인으로 통일** | **서브도메인 통일**: 프론트 `app.cb몰도메인`, 백엔드 `api.cb몰도메인` — Refresh 쿠키를 `Domain=.cb몰도메인; SameSite=Lax(또는 Strict); Secure; HttpOnly`로 설정 | `SameSite=None`은 크로스사이트 쿠키 전송을 완전히 허용해 CSRF 방어가 약해지고, 일부 브라우저의 서드파티 쿠키 차단 정책에 걸릴 위험도 있음. 같은 상위 도메인이면 `Lax`/`Strict`로 안전하게 쿠키 공유 가능 — **실제 서비스 도메인 확정은 9.1절 1주차 작업**(8.65절 N-7: 도메인이 없으면 PR 프리뷰·인증 E2E가 아예 돌지 않고, 8.13절 SPF/DKIM·토스페이먼츠 상점 등록도 막힘). **`Domain` 속성을 쓰는 이유를 못박아 둔다(8.65절 N-2)** — `app.`→`api.` 호출만 보면 Domain 없이 `api.`에 host-only로 두는 편이 범위가 좁아 더 안전하다. Domain이 **반드시 필요한 유일한 이유는 8.22/8.53절의 SSR 토큰 릴레이**(Next.js 서버가 `app.` 도메인으로 들어온 요청에서 쿠키를 읽어야 함)이므로, 임의로 떼면 SSR 경로가 조용히 깨진다. 따라오는 제약: **`__Host-` 접두사는 Domain과 함께 쓸 수 없어 사용 불가**하며 **`__Secure-` 접두사**를 쓴다. **서브도메인 운영 정책(8.65절 N-3)**: 이 쿠키는 현재·미래의 모든 서브도메인에 전송되므로 ① 외부 SaaS(헬프센터·블로그·상태페이지)에 서브도메인을 위임할 때는 **Refresh Token이 그 업체로 전송된다는 전제로 검토**하고 가급적 별도 상위 도메인을 쓴다, ② **사용 중단한 DNS 레코드는 즉시 삭제**한다(dangling DNS 선점 방지), ③ 새 서브도메인 추가는 인증 담당자 확인을 거친다 |
| 브루트포스 방지 | 없음 vs **Rate Limiting** | **`@nestjs/throttler`**로 로그인·비밀번호 재설정 요청을 IP+이메일 기준 분당 5회 등으로 제한. 저장소는 **Postgres 기반 커스텀 ThrottlerStorage**(8.11절 — ECS 2개 이상 태스크에서도 정확히 동작) | 크리덴셜 스터핑/무차별 대입 공격 완화, NestJS 공식 모듈이라 도입 비용 낮음 |
| 비밀번호 재설정(FR-1.1) | 재사용 가능한 링크 vs **1회용 시간제한 토큰** | 재설정 토큰을 **해시로 DB 저장, 30분 만료, 1회 사용 후 즉시 무효화** (`password_reset_tokens` 테이블) | 재다운로드 토큰(8.4절)과 마찬가지로, 토큰 원문은 이메일로만 전달하고 DB에는 해시만 저장해 유출 시 피해 최소화 |
| 이메일 인증(FR-1.6, 범위 추가) | 가입 시 이메일 인증 없음 vs **가입 시 이메일 인증 링크 발송** | **추가** — 인증 전까지 계정은 `email_verified_at = NULL` 상태로 제한적 이용(구매는 가능, 이후 정책은 운영 중 조정), 인증 링크 클릭 시 활성화 (`email_verification_tokens` 테이블) | 비회원 다운로드 링크(FR-1.4)와 비밀번호 재설정 모두 이메일 전달에 의존하므로, 오탈자 이메일 가입을 막아 사고를 예방 |

Sources:
- [NestJS JWT Refresh Token: 6 Proven, Safe Steps](https://codifysaas.com/blog/authentication-security/nestjs-jwt-refresh-token-production-implementation/)
- [Password Hashing in 2026: Argon2 vs bcrypt vs scrypt Comparison](https://reintech.io/blog/password-hashing-2026-argon2-bcrypt-scrypt-comparison)
- [Password Hashing Argon2id vs Bcrypt in 2026: Secure Defaults](https://khimananda.com/blog/password-hashing-argon2id-vs-bcrypt-in-2026)

### 8.8 토스페이먼츠 결제 연동 상세 설계 (확정)

> FR-3.3~3.5(결제/환불)의 서버 연동 방식입니다. 우리 결제수단(신용카드·카카오페이·네이버페이)은 모두 결제창에서 즉시 승인되는 방식이라 가상계좌(무통장입금) 관련 비동기 처리는 범위 밖입니다.

| 항목 | 검토 대상 | 결정 | 근거 |
|---|---|---|---|
| 결제 승인(Confirm) 흐름 | 프론트 성공 응답만으로 결제완료 처리 vs **요청-승인 2단계** | **2단계 필수**: 프론트가 결제위젯 호출 → 성공 시 `successUrl`로 `paymentKey`/`orderId`/`amount` 전달 → **백엔드가 이 값을 서버 저장값과 대조 후 시크릿키로 승인(confirm) API 호출**, 승인 성공 응답을 받아야 비로소 주문을 PAID 처리 | 토스페이먼츠 공식 가이드가 요구하는 필수 패턴. 클라이언트 응답만 믿으면 위변조된 성공 콜백으로 결제 없이 주문 완료 처리되는 취약점이 생김 |
| 금액 위변조 방지 | 클라이언트가 보낸 amount 신뢰 vs **서버 저장 금액과 대조** | 주문 생성 시점에 `orders.total_amount`를 서버가 먼저 확정해 저장하고, confirm 직전 `successUrl`의 `amount`가 이 값과 **일치하지 않으면 승인 거부 및 즉시 결제취소 처리** | 결제 금액 조작(예: 프론트 스크립트 변조로 더 싼 금액 결제 시도)을 원천 차단 |
| 멱등성(Idempotency) | 재시도 시 중복 승인/중복 취소 위험 감수 vs **Idempotency-Key 사용** | 승인/취소 API 호출 시 `Idempotency-Key` 헤더에 `orderId` 기반 고유값 사용 | 토스 API가 공식 지원하는 기능. 네트워크 타임아웃으로 백엔드가 같은 승인 요청을 재시도해도 중복 결제/중복 취소가 발생하지 않음 |
| 웹훅(안전망) | 미구현(리다이렉트 흐름만 신뢰) vs **`PAYMENT_STATUS_CHANGED` 웹훅 수신** | **포함** — 사용자가 결제 성공 직후 브라우저를 닫아 `successUrl` 리다이렉트가 유실되는 경우를 대비. 이 웹훅 타입은 서명 헤더가 없으므로 **페이로드를 신뢰하지 않고, paymentKey로 조회 API(`GET /v1/payments/{paymentKey}`)를 재호출해 실제 상태를 확인한 뒤에만 주문 상태에 반영**. 이때 **단순 상태 반영이 아니라 결제 이후 워크플로 전체(주문 PAID 전이 + 엔타이틀먼트 생성 + `notification_outbox` 행 생성)를 멱등하게 완결**하고, 이미 처리된 부분은 건너뛰고 누락된 부분만 채움(8.58절 G-3: 토스 승인 성공 직후·DB 커밋 직전에 프로세스가 죽으면 **결제는 됐는데 주문·엔타이틀먼트·알림이 모두 없는 상태**가 되며, 이를 복구할 수 있는 경로는 웹훅뿐). 수신 이벤트는 `payment_webhook_events`에 기록해 중복 수신을 걸러냄(8.9절 스키마) | payout.changed/seller.changed 웹훅만 HMAC 서명이 제공되고 결제 상태 웹훅은 서명이 없어, "재조회 검증" 패턴이 안전한 대안 **응답 코드를 세 갈래로 분리한다(8.71절 T-1)** — ① **중복·이미 처리됨 → 200**(재시도가 무의미), ② **재조회 API·DB 일시 장애 → 5xx**(토스가 **3일 19시간 동안 최대 7회** 재시도하므로 이것이 곧 자동 복구 장치가 되어 **별도 폴링 안전망이 불필요**하다), ③ **재조회 결과 불일치 → 200 + Sentry 경보**(재시도해도 결과가 같으므로 붙잡아둘 이유가 없고 위조 시도는 알림으로 다룬다). 토스는 **200을 받으면 재시도를 멈추므로** 이 구분을 빠뜨리면 일시 장애 한 번에 결제가 영구 유실된다. **재조회에서 대조할 항목(8.71절 T-4)**: `orderId` 일치 / `totalAmount`가 **우리가 계산한 주문 금액**과 일치(클라이언트가 보낸 값이 아님) / `status`가 `DONE` / `paymentKey` 일치 — 하나라도 어긋나면 ③ 경로. **부수 효과(8.71절 T-6)**: 토스 웹훅은 순서를 보장하지 않아 일반적인 구현에서는 오래된 상태가 새 상태를 덮어쓰는 사고가 생기는데, 우리는 페이로드가 아니라 **재조회 결과(항상 최신)** 를 쓰므로 이 문제가 **구조적으로 발생하지 않는다** — 재조회는 성능이 아니라 **정합성** 때문에 하는 것이며, "비용이 드니 페이로드를 믿자"는 제안의 반박 근거다. **해제 조건(8.71절 T-5)**: 결제 웹훅에 `tosspayments-webhook-signature`가 제공되면 **서명 검증을 추가**한다(재조회 검증은 유지 — 서명은 "토스가 보냈다"만 증명할 뿐 금액 대조와 순서 문제는 해결하지 못한다). |
| 환불(FR-3.5) | 관리자가 토스 콘솔에서 수동 취소 vs **백엔드 API로 취소 자동화** | 관리자가 다운로드 미클릭 조건(FR-3.5) 확인 후 버튼 클릭 시 백엔드가 결제취소 API(`POST /v1/payments/{paymentKey}/cancel`) 호출, Idempotency-Key 적용 | 운영 효율성, 취소 이력이 우리 DB(`orders.status = REFUNDED`)에도 정확히 남음 |
| 시크릿 키 보관 | ECS 태스크 정의에 평문 환경변수 vs **AWS Secrets Manager** | **AWS Secrets Manager** — 토스 시크릿 키/테스트·라이브 키를 환경별로 분리 저장하고 ECS 태스크가 실행 시점에 주입받도록 구성 | 시크릿이 코드 저장소나 태스크 정의 파일에 평문 노출되는 것을 방지, 8.3절 ECS Fargate 배포와 자연스럽게 통합 |
| 미완료 주문 정리 | PENDING 상태로 무기한 방치 vs **자동 취소 배치** | 결제창 이탈 등으로 **30분** 이상 PENDING 상태인 주문을 CANCELLED로 자동 전환하는 배치(간단한 스케줄 작업) | 재고 개념은 없지만(디지털 상품), 통계·정산 리포트(FR-7.6)에 미완료 주문이 섞이는 것을 방지. 결제창은 보통 수분 내 완료되므로 30분이면 충분 **뒤늦게 도착한 웹훅과의 충돌을 반드시 처리한다(8.71절 T-2)** — 토스 웹훅은 **최대 3일 19시간까지 재시도**되므로 30분 자동취소보다 **훨씬 늦게 도착할 수 있다**. 웹훅 재조회 결과가 **결제 `DONE`인데 주문이 `CANCELLED`** 인 경우, 고객은 돈을 냈고 상품은 받지 못한 상태다. 이때는 **자동으로 판단하지 않는다** — ① 주문을 **`NEEDS_REVIEW`** 상태로 표시, ② 8.17절 통합 알림의 **긴급 프로파일로 관리자에게 즉시 통지**, ③ 고객에게는 "확인 중" 안내. 자동 환불이나 주문 자동 복구는 하지 않는다 — 그 사이 상품이 판매중지·가격변경되었거나 쿠폰이 재사용되었을 수 있어 **검증할 예외가 많고, 자동 판단이 틀리면 돈이 얽힌다**. 발생 빈도가 낮아 수동 처리의 운영 부담도 작다. |

Sources:
- [결제 흐름 이해하기 | 토스페이먼츠 개발자센터](https://docs.tosspayments.com/guides/v2/get-started/payment-flow)
- [웹훅 이벤트 | 토스페이먼츠 개발자센터](https://docs.tosspayments.com/reference/using-api/webhook-events)
- [인증 및 기타 헤더 설정(Idempotency-Key) | 토스페이먼츠 개발자센터](https://docs.tosspayments.com/reference/using-api/authorization)

### 8.9 결제 웹훅 중복 방지 스키마 (확정)

8.8절의 웹훅 안전망을 위해 `payment_webhook_events` 테이블을 추가했습니다. 토스가 보낸 이벤트 식별자(또는 식별자가 없을 경우 `paymentKey`+수신된 상태값 조합)로 중복 수신 여부를 판별하고, 이미 처리한 이벤트는 무시합니다. 실제 웹훅 페이로드의 정확한 필드명은 연동 시점에 토스 개발자센터 문서로 재확인이 필요합니다.

### 8.10 S3 파일 업로드/다운로드 API 설계 (확정)

> FR-2.2(전자책/샘플/표지 등록), FR-7.2(파일 교체)의 업로드 측과, 8.4절 다운로드(만료형 URL)의 API 형태입니다.

**버킷/경로 구조**: 파일 성격에 따라 접근 정책이 달라야 하므로 공개/비공개를 분리합니다.

| 자산 | 접근 정책 | 근거 |
|---|---|---|
| 원본 PDF (유료 콘텐츠) | **비공개 버킷** (8.4절 만료형 presigned URL로만 접근) | 판매 상품 본체이므로 반드시 결제·엔타이틀먼트 검증을 거쳐야 함 |
| 샘플 PDF (미리보기) | **공개 버킷/프리픽스** (누구나 URL로 직접 접근 가능) | 마케팅 목적으로 누구나 자유롭게 열람해야 하는 자료라, 매번 서명 URL을 발급하는 것은 불필요한 오버헤드 |
| 표지 이미지 | **공개 버킷/프리픽스** | 상품 목록/상세 페이지에 노출되는 이미지라 공개가 자연스러움 |

| 항목 | 검토 대상 | 결정 | 근거 |
|---|---|---|---|
| 업로드 방식 | **NestJS 백엔드 프록시 업로드** (관리자→백엔드→S3) vs 프리사인드 PUT(관리자 브라우저→S3 직접) | **백엔드 프록시 업로드** | 프리사인드 PUT는 대역폭을 절약하지만 업로드되는 파일이 실제 PDF인지(매직바이트) 서버가 사전 검증하기 어렵고 크기 제한도 S3 정책만으로는 허술함. 관리자 전용 기능이라 트래픽이 낮고(9.2절 팀 규모 대비 소수 운영자), ECS Fargate는 Lambda 같은 6MB 페이로드 제한이 없어 프록시 방식이 부담 없음 |
| 공개/비공개 분리 | 전부 비공개+서명URL vs **원본 PDF만 비공개, 샘플/표지는 공개 버킷·경로 분리** | **분리** | 샘플/표지는 누구나 열람 가능해야 하는 마케팅 자료라, 매번 서명 URL을 발급하는 것은 불필요한 오버헤드이자 구조적으로도 맞지 않음 |
| 업로드 요청 경로 | Next.js Route Handler 경유 vs **`api.` 도메인(ALB/ECS) 직접 호출** | 관리자 브라우저가 **`api.` 도메인으로 직접 업로드**(Next.js를 경유하지 않음) | **(8.55절 D-5)** Vercel은 요청 본문 크기에 약 4.5MB 제한이 있어, 무심코 Next.js Route Handler를 거치면 이 절에서 정한 **100MB 업로드가 실패**함. 8.7절 서브도메인 구성 덕분에 직접 호출이 이미 가능하므로 이를 **명시적 제약으로 고정** |
| 스트리밍 처리 | 파일 전체를 메모리에 버퍼링 후 업로드 vs **스트리밍 업로드** | `@aws-sdk/lib-storage`의 `Upload` 클래스로 요청 스트림을 그대로 S3에 스트리밍 | 대용량 PDF를 Fargate 태스크 메모리에 통째로 올리지 않아 메모리 사용량과 지연을 줄임 |
| 파일 검증 | 클라이언트가 보낸 Content-Type 신뢰 vs **매직바이트 검증** | 업로드된 파일의 시작 바이트가 `%PDF-`인지 서버에서 확인, 확장자/Content-Type만으로 판단하지 않음 | Content-Type 헤더는 클라이언트가 임의로 지정 가능해 위조에 취약 |
| 파일 크기 제한 | 무제한 vs **유형별 상한** | 원본 PDF 100MB, 샘플 PDF 20MB, 표지 이미지 5MB (NestJS `FileInterceptor` 옵션으로 강제) | 전자책 특성상 이 범위를 크게 벗어나지 않을 것으로 예상, 과도한 업로드로 인한 스토리지/대역폭 남용 방지 |
| 악성코드 스캔 | 스캔 도입 vs **MVP 미도입** | 관리자(내부 직원)만 업로드 권한이 있고 자사 직접 제작 콘텐츠만 다루므로 MVP 범위에서 제외 | 외부 저자/제3자 업로드 권한이 생기면(현재는 Out of Scope) 그때 재검토 |
| 다운로드 API 응답 형식 | HTTP 302 리다이렉트 vs **JSON으로 presigned URL 반환** | 다운로드 엔드포인트가 `{ downloadUrl, remainingDownloads, expiresAt }` 형태의 JSON을 반환하고, 프론트가 이를 받아 다운로드를 트리거 | FR-4.4(잔여 횟수·만료일 안내)를 프론트에서 표시하려면 리다이렉트보다 JSON 응답이 유리하고, 에러(한도초과 등) 처리도 자연스러움 |

Sources:
- [Implementing Secure AWS S3 File Uploads in NestJS with Presigned URLs](https://sandipwrites.medium.com/implementing-secure-aws-s3-file-uploads-in-nestjs-with-presigned-urls-573dfefe8f65)
- [S3 Uploads — Proxies vs Presigned URLs vs Presigned POSTs](https://zaccharles.medium.com/s3-uploads-proxies-vs-presigned-urls-vs-presigned-posts-9661e2b37932)
- [NestJS S3 대용량 파일 업로드 (with PreSigned URL, S3 MultiPart Upload)](https://joojae.com/nestjs-large-file-upload-with-presigned-url-and-s3-multipart-upload/)

### 8.11 파일 업로드/다운로드 요청 속도 제한 (확정)

> 8.10절이 파일 크기·검증을 다뤘다면, 이번엔 **요청 빈도 제한**(자동화 스크립트로 다운로드 URL을 반복 발급받아 10회 한도를 순식간에 소진하거나, 액세스 토큰을 무차별 대입하는 등의 남용 방지)입니다.

**핵심 이슈**: NestJS 기본 `@nestjs/throttler`는 인메모리 저장소를 쓰기 때문에, ECS Fargate 태스크가 2개 이상이면 각 태스크가 카운터를 따로 가져 제한이 사실상 무력화됩니다(예: 분당 5회 제한이 태스크 2개면 최대 10회까지 허용됨). 태스크를 1개만 운영하면 이 문제가 없지만, 배포 시 무중단이 안 되고 장애 시 즉시 대체 인스턴스가 없다는 가용성 트레이드오프가 생깁니다.

| 항목 | 검토 대상 | 결정 | 근거 |
|---|---|---|---|
| ECS 태스크 최소 개수 | 1개(비용 최소) vs **2개 이상(가용성 확보)** | **2개 이상** | 무중단 배포·장애 대응을 위한 일반적 권장 구성. 6절 가용성 목표(99.5%)와도 부합. 이 결정에 따라 아래 속도 제한 방식이 "인메모리로 충분" 대신 "공유 저장소 필요"로 확정됨 |
| 다운로드 URL 발급 속도 제한 | 인메모리 카운터 vs **Postgres 기반 원자적 체크** | 8.6절 패턴 확장 — `increment_download_count()` 함수 안에서 `download_logs`를 조회해 **엔타이틀먼트(상품) 단위 속도 제한을 두 개로 분리**해 적용(8.74절 W-1) — **차감되는 발급은 분당 3회**(남용 방지의 본래 목적), **전체 요청(재사용 발급 포함)은 분당 10회**(자동화 스크립트만 걸리는 느슨한 상한). 단일 제한으로 재사용까지 함께 세면 8.73절 V-2가 구하려던 "반복 클릭하는 정상 사용자"가 **한도는 안 깎여도 4번째 클릭에서 429를 맞아** 목적이 절반만 달성된다(8.73절 V-3: 사용자 단위로 세면 **5권을 산 고객이 마이페이지에서 3권만 받고 막힌다** — 여러 상품 동시 수령은 정상 사용이고, 걸러야 할 것은 "한 상품을 반복 요청"하는 남용이다) | FR-4.2 자체가 이미 DB 트랜잭션(엔타이틀먼트 카운터 증가)을 거치므로, 같은 원자적 함수 안에서 속도 제한까지 처리하면 Redis 같은 신규 인프라 없이도 여러 ECS 태스크에 걸쳐 정확하게 동작. 5분 URL 유효기간(8.4절, 8.73절 V-1) 대비 분당 3회면 정상적인 재시도는 충분히 허용되며, 8.73절 V-2의 "유효 URL 재사용"이 적용되면 정상 사용자가 이 제한에 닿는 일 자체가 드물어진다 |
| 로그인/비밀번호 재설정 속도 제한(8.7절 보완) | 기본 인메모리 `@nestjs/throttler` vs **Postgres 기반 커스텀 ThrottlerStorage** | **Postgres 기반으로 변경** — `@nestjs/throttler`의 `ThrottlerStorage` 인터페이스를 구현해 카운터를 Supabase Postgres 테이블에 저장 | 로그인/비밀번호 재설정은 크리덴셜 스터핑 등 실제 공격 대상이 되는 민감 엔드포인트라, 태스크 2개 이상에서 정확한 제한이 필요. Redis(ElastiCache)를 새로 들이는 대신 이미 쓰는 Postgres를 공유 저장소로 재사용해 인프라·비용을 늘리지 않음 |
| 관리자 업로드 속도 제한 | Postgres 기반 vs **기본 인메모리 throttler로 충분** | 기본 `@nestjs/throttler`(인메모리) 유지 | 내부 소수 운영자만 접근하는 저위험·저빈도 엔드포인트라, 태스크별 카운터 분리로 인한 실질적 위험이 낮음 — 굳이 DB 부하를 늘릴 필요 없음 |
| S3/CDN 레벨 제한 | 별도 조치 vs **불필요** | 조치 없음 | S3는 자체적으로 매우 높은 요청률을 처리하며(prefix당 자동 확장), 우리 규모(동시접속 100명)에서는 S3 자체가 병목이 되지 않음. CloudFront 미사용 결정(6절)과도 일관 |

Sources:
- [How to Build NestJS Rate Limiting with Redis](https://oneuptime.com/blog/post/2026-03-31-redis-nestjs-rate-limiting/view)
- [API with NestJS #187. Rate limiting using Throttler](https://wanago.io/2025/03/03/api-nestjs-rate-limiting-throttler/)

### 8.12 Cron/예약 작업 기술 스택 (확정)

> PRD에서 식별된 주기적 배치 작업은 세 가지입니다: ① PENDING 주문 자동 취소(8.8절, 30분 경과), ② `rate_limit_buckets` 만료 정리(8.11절), ③ 만료된 Refresh/비밀번호재설정/이메일인증 토큰 정리(8.7절). 8.11절에서 ECS를 2개 이상 태스크로 운영하기로 한 결정이 여기에도 그대로 영향을 줍니다(같은 스케줄 작업이 태스크 수만큼 중복 실행될 수 있음).

| 항목 | 검토 대상 | 결정 | 근거 |
|---|---|---|---|
| 실행 방식 | NestJS 인프로세스 스케줄러(`@nestjs/schedule`) vs ECS Scheduled Task(EventBridge) vs Lambda+EventBridge | **NestJS 인프로세스 `@nestjs/schedule`** | 세 작업 모두 가볍고 짧은 DB 집합 연산이라, 이미 떠 있는 ECS 서비스 안에서 처리하면 별도 배포 파이프라인·태스크 정의가 필요 없음. ECS Scheduled Task나 Lambda는 관리 포인트가 하나 더 늘어나 3개월·소규모 팀에는 과함 |
| 다중 태스크 중복 실행 방지 | 모든 작업에 동일한 락 적용 vs **작업 성격에 따라 두 가지 방식으로 분리**(8.52절) | **A형(짧은 순수 DB 작업)**: `@Cron()` 핸들러를 트랜잭션으로 감싸고 `SELECT pg_try_advisory_xact_lock(hashtext('job-name'))` 시도 → 실패 시 즉시 skip(트랜잭션 단위라 자동 해제, unlock 불필요). **B형(외부 I/O 포함 — 알림 재시도 8.18, PDF 추출 재시도 8.32)**: 락을 쓰지 않고 **`FOR UPDATE SKIP LOCKED` 행 클레이밍**으로 처리 대상만 선점(`claimed_at` 기록) 후 트랜잭션 밖에서 외부 호출 | 세션 단위 락은 8.15절 트랜잭션 모드 풀러와 호환되지 않고(8.51절 C-1), 트랜잭션 단위 락도 **외부 호출 내내 트랜잭션을 열어두게 되는 작업에는 부적합**(8.52절). B형은 태스크들이 서로 다른 행을 나눠 처리해 처리량도 함께 개선됨 | 8.11절에서 ECS 태스크를 2개 이상 운영하기로 했으므로 동일 스케줄이 태스크 수만큼 동시 실행됨. 현재 세 작업은 멱등적 UPDATE/DELETE라 중복 실행되어도 데이터 정합성 문제는 없지만, **모든 작업에 락을 통일 적용**해두면 향후 이메일 발송처럼 중복 시 실제 문제가 되는 작업이 추가돼도 규칙을 안 바꿔도 됨. 이미 쓰는 Postgres로 처리해 신규 인프라(분산 락 서비스 등) 불필요 |
| 작업 스케줄 | — | PENDING 주문 취소: **5분마다** 확인(30분 경과 건 처리) / `rate_limit_buckets` 정리: **1시간마다** / 만료 토큰(Refresh·재설정·이메일인증) 정리: **매일 1회**(새벽) / **알림 재시도(`notification_outbox`): 1분마다**(8.18절) / **PDF 텍스트 추출 재시도+정체감지(`product_full_texts`): 10분마다**(8.32절, 최대 2회 재시도·1시간 초과 PENDING은 FAILED 전환) / **PDF 텍스트 추출 실패율 집계: 매일 1회**(8.32절, 20% 초과 시 Sentry 경고) / **발송 완료된 `notification_outbox` payload 정리: 매일 1회**(8.58절 G-5, SENT 상태이면서 민감값이 남은 행의 payload를 비움) / **다운로드 만료 임박(D-30) 알림 대상 조회: 매일 1회, 09~21시 사이**(FR-4.6, 8.75절 — 발송 시점을 우리가 고르는 알림이므로 야간을 피한다) | 주문 취소·알림 재시도는 사용자 경험에 직접 영향을 주므로 자주, 나머지 정리성/집계 작업은 부담 없는 주기로 충분 |
| 실패 모니터링 | 없음 vs **Sentry 연동** | `@Cron()` 핸들러 내 예외를 Sentry로 전송(8.2절에서 이미 도입 결정된 Sentry 재사용) | 배치 작업이 조용히 실패해 PENDING 주문이 계속 쌓이는 등의 상황을 조기에 발견 |

Sources:
- [Task Scheduling | NestJS 공식 문서](https://docs.nestjs.com/techniques/task-scheduling)
- [Managing Cron Jobs with NestJS: Solving Multi-Instance Issues and Locking Mechanisms](https://medium.com/@femresirvan/managing-cron-jobs-with-nestjs-solving-multi-instance-issues-and-locking-mechanisms-b2675d28a292)

### 8.13 이메일 발송(회원가입/주문/알림) 기술 스택 (확정)

> 지금까지 PRD에서 이메일 발송이 필요한 지점은 모두 **트랜잭션(거래성) 이메일**입니다 — 마케팅/뉴스레터가 아니므로 광고성 정보 사전동의·수신거부 관리는 해당 없음. ① 비밀번호 재설정(FR-1.1), ② 회원가입 이메일 인증(FR-1.6), ③ 비회원 다운로드 링크(FR-1.4), ④ 주문완료(FR-3.4), ⑤ 오탈자 수정본 재다운로드 안내(FR-2.6). **실제 구축이 아닌 결정 단계**입니다. FR-3.4의 "SMS" 발송은 별도 국내 문자 아그리게이터(알리고/NHN Cloud 등) 선정이 필요한 다른 주제라 이번 검토에서는 제외했습니다.

| 항목 | 검토 대상 | 결정 | 근거 |
|---|---|---|---|
| 발송 서비스(ESP) | AWS SES vs **Resend(우선 구현) → 프로덕션에서 AWS SES 재검토** | **MVP/초기 구현은 Resend**, 트래픽·운영 안정화 이후 **AWS SES 전환을 재검토** | Resend는 React Email과 궁합이 가장 좋고 무료 티어로 개발 속도가 빠름. 다만 SES는 AWS 통합(IAM, 비용)과 2026년 추가된 전달률 모니터링 이점이 있어, 발송량이 늘거나 비용 최적화가 필요해지면 재검토 대상으로 남겨둠 |
| ESP 교체 대비 설계 | 발송 로직에 Resend SDK 직접 호출 산재 vs **`EmailProvider` 인터페이스로 추상화** | NestJS에 `EmailProvider` 인터페이스(`send(to, subject, html)`)를 두고 Resend 구현체를 주입, 추후 SES 구현체로 교체 시 이 계층만 변경 | React Email 템플릿은 이미 ESP 중립적(HTML만 생성)이므로, 발송 계층만 인터페이스로 감싸두면 향후 Resend→SES 전환이 비즈니스 로직 변경 없이 가능 |
| 이메일 템플릿 엔진 | **React Email**(JSX 컴포넌트) vs 단순 문자열/Handlebars 템플릿 | **React Email** | ESP에 종속되지 않고 HTML만 생성하므로 Resend/SES 등 어떤 발송 서비스와도 조합 가능. 우리 프론트가 이미 React/TypeScript라 팀이 새로 배울 것이 적고, 일관된 디자인의 이메일(주문완료, 다운로드 링크 등)을 컴포넌트로 재사용 가능 |
| 발송 안정성(재시도) | 요청 처리 중 즉시 1회 발송(실패 시 미대응) vs **outbox 테이블 + 재시도 Cron** | 비즈니스 이벤트 발생 시 `notification_outbox`(channel=EMAIL, `max_attempts=5`)에 먼저 기록 후 즉시 발송 시도 → 실패하면 상태를 PENDING으로 남기고, **8.12절과 같은 패턴의 Cron 작업**(Advisory Lock 적용)이 주기적으로 PENDING 건을 재시도(최대 5회, 지수 백오프) | FR-1.4(다운로드 링크)·FR-3.4(주문완료)는 고객이 반드시 받아야 하는 메일이라 일시적 네트워크 오류로 유실되면 안 됨. 발송 이력이 DB에 남아 "메일을 못 받았다"는 CS 문의(FR-5.2) 대응도 쉬워짐. 새 메시지 큐(SQS 등) 없이 기존 Postgres+Cron 인프라 재사용. provider 컬럼에 ESP를 값으로 기록하므로 Resend→SES 전환 시에도 이력 스키마는 그대로 유지(8.17절에서 SMS/푸시와 테이블 통합) |
| 발신 도메인 인증 | 미설정 vs **SPF/DKIM/DMARC 설정 + 발신 전용 서브도메인**(예: `mail.`) | 실제 서비스 도메인이 확정되면 발신 전용 서브도메인에 SPF/DKIM/DMARC를 설정 | 네이버·다음 등 국내 웹메일은 낯선 발신 도메인에 스팸 처리가 엄격한 편이라, ESP 선택과 무관하게 인증 설정과 평판 관리가 전달률에 더 크게 영향을 줌. 8.7절에서 이미 서브도메인 구성을 결정했으므로 같은 흐름으로 처리하고, **ESP를 SES로 전환하더라도 도메인 인증을 다시 설정**해야 함(전환 시 체크리스트에 반영) |

Sources:
- [Amazon SES, 이제 받은 편지함 배치 지표 및 차단 목록 모니터링 기능 제공 - AWS](https://aws.amazon.com/ko/about-aws/whats-new/2026/05/amazon-ses-global-deliverability/)
- [Resend vs Amazon SES vs Postmark Transactional Email 2026](https://www.buildmvpfast.com/blog/resend-vs-ses-vs-postmark-transactional-email-deliverability-saas-2026)

### 8.14 SMS 발송 채널 기술 스택 (확정)

> FR-3.4(주문완료 알림 이메일/SMS)의 SMS 측 설계입니다. 국내 이동통신 규정상 해외 서비스(Twilio, AWS SNS/End User Messaging SMS 등)는 국내 발신번호 사전등록 체계와 맞지 않아 실질적으로 **국내 아그리게이터**가 필수입니다.

| 항목 | 검토 대상 | 결정 | 근거 |
|---|---|---|---|
| 아그리게이터 | 솔라피(Solapi) vs 알리고(Aligo) vs NCP SENS/NHN Cloud | **솔라피** | 공식 Node.js SDK 제공, REST API가 현대적이고 문서화가 잘 되어 있음. 우리가 NCP·NHN Cloud를 다른 용도로 쓰고 있지 않으므로 그 생태계에 종속될 이유가 없고, 솔라피는 가입만으로 바로 시작 가능. **카카오 알림톡을 함께 지원**하며 "알림톡 실패 시 SMS 자동 대체발송" 기능이 API에 내장돼 있어, 향후 알림톡 도입 시(아래 참고) 벤더 교체 없이 확장 가능 |
| 발신번호 사전등록 | 생략 불가 | 사업자 서류 인증을 통한 **발신번호 사전등록 필수**(영업일 1~2일 소요, 개인 계정으로는 이용 불가) | 국내 통신 규정상 모든 아그리게이터 공통 요구사항. 서비스 오픈 일정에 영향을 줄 수 있어 조기 신청 필요(10절 다음 단계 반영) |
| 메시지 범위 | 주문 상세·다운로드 링크까지 SMS로 전달 vs **짧은 확인 문자만(90자 이내 SMS)** | **SMS는 짧은 주문완료 확인용으로만 사용**(예: "[cb몰] 주문이 완료되었습니다. 결제금액 10,000원"), 다운로드 링크 등 민감 정보는 8.13절 이메일로만 전달 | 90자를 넘으면 LMS(단가 상승)로 전환해야 하고, 다운로드 링크 같은 민감 정보를 SMS 평문으로 보내는 것은 8.4절의 만료형 URL 설계 취지와도 맞지 않음. 이메일을 신뢰 가능한 주 채널로 유지하고 SMS는 보조 알림으로 역할 분리 |
| 발송 안정성 | 이메일과 동일한 outbox+재시도 큐 vs **실패 시 로그만 남기고 차단하지 않음** | SMS 발송 실패는 Sentry로 알림만 하고 **주문 처리 자체를 막지 않음**(재시도는 하지 않음 — 8.17절에서 발송 이력 자체는 `notification_outbox`(channel=SMS, `max_attempts=1`)에 남기도록 통합됨) | SMS는 보조 알림이고 핵심 정보(주문내역·다운로드 링크)는 이미 이메일로 안전하게 전달되므로, SMS까지 이메일 수준의 재시도 인프라를 갖추는 것은 과설계. 실패가 반복되면 운영자가 인지할 수 있도록 모니터링만 유지 |
| API 키 보관 | 코드/환경변수 평문 vs **AWS Secrets Manager** | 8.8절과 동일한 패턴으로 솔라피 API 키를 Secrets Manager에 저장 | 토스페이먼츠 시크릿 키 관리와 일관된 보안 정책 |
| 카카오 알림톡(신규 검토, 범위 확장) | 이번 MVP는 SMS만 vs **알림톡 도입** | (제안, 현재 FR 범위 밖) 이번 MVP는 SMS만 구현, 알림톡은 벤더가 이미 지원하니 이후 필요 시 도입 | 알림톡은 카카오톡 채널 개설 + 메시지 템플릿 사전 심사가 필요해 별도 준비 기간이 필요하므로, 3개월 일정의 MVP 범위에는 포함하지 않음 |

Sources:
- [SMS - NHN Cloud 사용자 가이드](https://docs.nhncloud.com/ko/Notification/Notification%20Hub/ko/service-policy-and-precondition/sms/)
- [SMS API - NCP SENS API 가이드](https://api.ncloud-docs.com/docs/ai-application-service-sens-smsv2)
- [알리고 문자 | 문자 발송 RESTful API](https://smartsms.aligo.in/smsapi.html)

### 8.15 RLS 및 데이터베이스 접근 보안 — 역할/커넥션 설계 (확정)

> 8.5/8.6절에서 "RLS를 전 테이블에 적용하되 Prisma 연결은 BYPASSRLS라 영향 없다"고 결정했는데, **정확히 어떤 DB 롤로 연결하는지**를 명시하지 않으면 이 전제가 성립하지 않을 수 있습니다. 이번 리서치로 발견한 사실: 지금까지처럼 Supabase의 기본 `postgres`(슈퍼유저) 롤을 백엔드가 그대로 쓰면 편하지만 최소권한 원칙에 어긋나고, 반대로 최소권한 롤을 새로 만들면 기본적으로 BYPASSRLS가 없어 우리가 걸어둔 deny-all RLS에 백엔드 자신이 막힐 수 있습니다. **실제 구축이 아닌 결정 단계**입니다.

| 항목 | 검토 대상 | 결정 | 근거 |
|---|---|---|---|
| DB 롤 분리 | `postgres` 슈퍼유저 롤을 마이그레이션·런타임 모두에 사용 vs **런타임/마이그레이션 롤 분리** | **분리**: 마이그레이션(`prisma migrate deploy`)은 Supabase가 기본 제공하는 **`postgres` 롤을 그대로 사용**(신규 생성 불필요), 런타임은 **신규 생성한 `cbmall_app` 롤**(애플리케이션 테이블 CRUD와 8.6절 함수 실행 권한만 GRANT, DDL/역할관리 권한 없음)을 사용 | 슈퍼유저를 상시 운영 중인 ECS 서비스의 커넥션으로 계속 쓰면, 애플리케이션 코드에 취약점이 생겼을 때 테이블 삭제·스키마 변경까지 가능해져 피해 범위가 커짐. 마이그레이션은 배포 파이프라인에서만 잠깐 쓰이므로 기존 `postgres` 롤을 유지해도 노출 표면이 작음 |
| RLS 우회 방식 | `cbmall_app`에 슈퍼유저 권한으로 우회 vs **명시적으로 `BYPASSRLS` 속성만 부여** | `cbmall_app` 생성 시 `CREATE ROLE ... BYPASSRLS`로 **RLS 우회 속성만 별도 부여**(슈퍼유저 아님) — `apps/api/prisma/setup/roles.sql` 참고 | 2026년 Supabase 패치로 슈퍼유저가 아니어도 `BYPASSRLS` 속성을 가진 롤을 만들 수 있게 됨. 이렇게 하면 8.6절의 "anon/authenticated는 차단, 백엔드는 통과" 전제가 특정 롤의 명시적 속성으로 뒷받침되어 더 이상 암묵적 가정이 아니게 됨 |
| 런타임 커넥션 방식 | 다이렉트 커넥션(5432) vs **Supavisor 트랜잭션 모드 풀러(6543)** | 앱 런타임은 **Supavisor 트랜잭션 모드(포트 6543)** + Prisma 연결 문자열에 `?pgbouncer=true` 추가. **이 플래그는 제거하지 않는다(8.67절 P-1)** — Supavisor는 `named_prepared_statements` 기능 플래그가 켜져 있으면 트랜잭션 모드에서도 prepared statement를 지원하지만, 그것은 **Supavisor 서버 측 설정**(`NAMED_PREPARED_STATEMENTS_ENABLED`)이라 **Supabase 호스팅 고객이 켤 수 없다**. Prisma 공식 문서의 "PgBouncer 1.21+ 에서는 `pgbouncer=true`를 쓰지 마라"도 우리에겐 해당하지 않는다(우리는 PgBouncer가 아니라 Supavisor를 쓰고, 그 조건인 `max_prepared_statements > 0` 역시 우리가 제어할 수 없는 서버 설정). **해제 조건: Supabase가 이 설정을 고객에게 노출할 때.** **커넥션 총량 산정(8.74절 W-4)**: 런타임 **태스크당 5개**(2 vCPU 기준) × **태스크 2개 = 10개**, 여기에 CI 마이그레이션용 세션 모드 커넥션 1개가 더해진다. **태스크를 늘리거나 vCPU를 올리면 이 값이 함께 커진다.** 실제 Supavisor 풀 한도는 플랜·설정에 따라 다르므로 **M0 ②에서 총 커넥션 수 대 풀 한도를 실측**한다. **`connection_limit`은 지정하지 않고 Prisma 기본값(`물리 CPU 수 × 2 + 1`)을 쓴다(8.67절 P-3)** — 인터넷 예시는 대부분 `?pgbouncer=true&connection_limit=1`을 함께 제시하지만 **`connection_limit=1`은 Vercel·Lambda 같은 서버리스 전제**이고, 우리 백엔드는 ECS 상주 프로세스(8.3절)라 그대로 쓰면 **태스크당 DB 커넥션이 1개로 묶여 동시성이 무너진다**. 8.51절 C-7의 2 vCPU 기준 태스크당 5개 × 태스크 2개 = 총 10개로 Supavisor 풀 한도 안에서 여유가 있다 | 8.11절에서 ECS를 2개 이상 태스크로 운영하기로 했고, 각 태스크가 Prisma 자체 커넥션 풀까지 가지므로 다이렉트 커넥션만 쓰면 Postgres 커넥션 한도를 빠르게 소진할 수 있음. `pgbouncer=true`는 트랜잭션 모드가 Prepared Statement를 지원하지 않는 Prisma 이슈를 우회하기 위해 필요 |
| 마이그레이션 커넥션 방식 | 런타임과 동일한 풀러 사용 vs **다이렉트 커넥션(5432) 별도 사용** | `prisma migrate deploy`는 **다이렉트 커넥션**으로만 실행 | Supavisor 트랜잭션 모드는 DDL을 포함한 마이그레이션과 호환성 문제가 보고되어 있어, 마이그레이션 전용 다이렉트 커넥션 문자열을 CI/CD 파이프라인에만 별도 보관 **(8.70절 S-1로 호스트 수정)** 여기서 말하는 "다이렉트 커넥션"은 **Supavisor 세션 모드 풀러**(`aws-0-<region>.pooler.supabase.com:5432`)를 가리킨다. 진짜 다이렉트 커넥션(`db.<ref>.supabase.co:5432`)은 **Supabase에서 IPv6 전용**인데 **GitHub Actions 러너는 IPv6 아웃바운드가 없어** CI 마이그레이션이 `ENETUNREACH`로 실패한다. 세션 모드 풀러는 **전 플랜에서 IPv4를 지원**하고, **연결이 세션 내내 전용 서버 연결에 고정**되므로 8.57절 F-5가 요구한 세션 advisory lock이 정상 동작한다. 즉 정확한 규칙은 **"풀러를 피하라"가 아니라 "트랜잭션 모드(6543)를 피하라"** 이다. (M0 ②에서 실측하며, 문제가 드러나면 차선책은 유료 **IPv4 애드온** + 진짜 다이렉트 커넥션) |
| 환경별 롤 적용 범위 | 환경마다 다른 롤 vs **모든 환경이 동일하게 `cbmall_app` 사용** | **스테이징·운영 두 프로젝트 모두 `cbmall_app`** 사용 | **(8.51절 C-4 → 8.56절에서 해소)** C-4는 "PR 프리뷰 브랜치에 커스텀 롤이 없다"는 문제였는데, 8.56절에서 **Supabase 브랜칭 자체를 포기**하고 프리뷰가 스테이징 DB를 공유하게 되면서 문제가 사라짐. 오히려 **프리뷰부터 운영과 동일한 최소권한 롤로 검증**되므로 권한 누락을 더 일찍 발견할 수 있음 |
| 자격증명 보관 | 두 커넥션 문자열 모두 동일 취급 vs **분리 보관** | `cbmall_app` 커넥션 문자열(Supavisor 풀러)은 ECS 태스크 정의(Secrets Manager, 8.8절과 동일 패턴)에, `postgres` 롤의 다이렉트 커넥션 문자열은 **CI/CD 파이프라인 시크릿에만** 저장하고 ECS 태스크에는 주입하지 않음 | 실행 중인 서비스가 마이그레이션 자격증명(슈퍼유저)을 아예 들고 있지 않게 하여, 런타임 침해 시에도 스키마 변경 권한까지 유출되는 것을 막음 |

Sources:
- [Postgres Roles | Supabase Docs](https://supabase.com/docs/guides/database/postgres/roles)
- [Prisma | Supabase Docs](https://supabase.com/docs/guides/database/prisma)
- [Connection pooling and limits | Supabase Docs](https://supabase.com/docs/guides/database/connecting-to-postgres/pooling-and-limits)
- [Configure Prisma Client with PgBouncer | Prisma Documentation](https://www.prisma.io/docs/orm/prisma-client/setup-and-configuration/databases-connections/pgbouncer)

### 8.16 웹 푸시 알림 기술 스택 (확정)

> "FCM/APNs"로 문의하셨지만, 이 프로젝트는 반응형 웹만 지원(2.2절)하고 네이티브 앱이 없어 **웹 푸시 알림**(브라우저 Push API)으로 범위를 확인했습니다. 이번 단계에서는 **발송 인프라만 구축**하고, 실제로 무엇을 알릴지(트리거)는 별도 FR로 추후 확정합니다.

| 항목 | 검토 대상 | 결정 | 근거 |
|---|---|---|---|
| 표준/라이브러리 | 표준 Web Push API(VAPID) — `web-push` 패키지 vs **Firebase Cloud Messaging(FCM)** | **FCM** (백엔드 `firebase-admin` SDK + 프론트 Firebase JS SDK) | `web-push`(VAPID 단독)가 신규 벤더 없이 더 가볍지만, **향후 네이티브 앱 가능성**을 고려해 웹·iOS·Android를 하나의 SDK/콘솔로 통합 관리할 수 있는 FCM으로 결정. Firebase 프로젝트라는 신규 벤더가 늘어나는 트레이드오프는 감수 |
| 알림 트리거(범위) | FR-5.2(문의 답변) 알림으로 즉시 도입 vs 인프라만 구축 | **(8.75절에서 확정)** 트리거 **3건** — `inquiry_answered`(FR-5.2 문의 답변) · `revision_published`(FR-2.6 개정판 발행) · `entitlement_expiring`(FR-4.6 만료 D-30). **푸시는 항상 보조 채널**이라 세 건 모두 이메일과 쌍으로 발송하며, **본문에 책 제목을 넣지 않는다**(잠금화면 노출). 광고성 알림(쿠폰·프로모션·리뷰 요청)은 **MVP 제외**. 이전 결정이던 "인프라만 구축"에서 확장됨 — 구독 등록/토큰 저장/발송 API까지만 구현하고, 실제 알림을 언제 보낼지(문의 답변, 주문 상태 등)는 이후 별도 FR로 확정 | 아직 트리거가 정해지지 않았으므로 발송 파이프라인과 프론트 권한 요청 UX만 우선 준비 |
| 구독 저장 | 원시 PushSubscription(endpoint/p256dh/auth) 저장 vs **FCM 등록 토큰 저장** | `push_subscriptions` 테이블에 **FCM 등록 토큰**(`fcm_token`)을 저장 (userId, fcmToken, userAgent) — 한 사용자가 여러 브라우저/기기에서 등록 가능하도록 1:N | FCM은 브라우저의 raw PushSubscription을 SDK 내부에서 단일 토큰 문자열로 추상화해주므로, 우리가 endpoint/키를 직접 다룰 필요가 없어짐 |
| 발송 실패 처리 | 재시도 큐 도입 vs **실패 시 로그만 남기고 토큰 정리** | FCM 발송 응답이 `UNREGISTERED`(토큰 무효) 등을 반환하면 해당 `push_subscriptions` 레코드를 즉시 삭제. 그 외 실패는 Sentry 기록만 하고 재시도하지 않음(8.14절 SMS와 동일한 패턴, 발송 이력은 8.17절 `notification_outbox`(channel=PUSH, `max_attempts=1`)에 통합 기록) | 푸시 알림은 보조 채널이라 이메일 수준의 보장이 불필요하나, 죽은 토큰을 방치하면 이후 발송도 계속 실패하므로 정리 로직만 필수 |
| 키/자격증명 관리 | 평문 보관 vs **Firebase 서비스 계정 자격증명을 Secrets Manager 보관** | `firebase-admin` 초기화에 필요한 서비스 계정 JSON 키를 8.8절과 동일하게 AWS Secrets Manager에 저장, 프론트에는 Firebase 웹 설정값(공개 정보)만 노출 | 서비스 계정 키는 서버 전용 비밀이므로 시크릿 매니저 보관이 필요하고, 프론트용 Firebase config는 원래 공개되어도 안전한 값 |
| 브라우저 호환 | 전 브라우저 동일 지원 가정 vs **지원 브라우저에서만 조용히 활성화** | Chrome/Edge/Firefox는 완전 지원, Safari는 PWA로 홈 화면에 추가한 경우에만 제한적으로 지원(FCM 웹 푸시도 내부적으로 브라우저 Push API 기반이라 동일한 제약을 가짐). 미지원 브라우저에서는 알림 관련 UI를 아예 노출하지 않음(그레이스풀 디그레이드) | 6절 브라우저 호환 요구사항과 일관되게, 알림을 못 받는 사용자도 핵심 기능(구매·다운로드)에는 전혀 지장이 없어야 함 |
| 권한 요청 시점 | 회원가입 직후 즉시 권한 요청 vs **실제 필요한 순간에 컨텍스트형 요청** | 트리거 FR이 확정되는 시점에, 해당 액션 맥락에서 권한을 요청하는 방식으로 설계(예: 향후 문의 등록 시 "답변 오면 알려드릴까요?") | 페이지 진입 직후의 무맥락 권한 팝업은 대부분 거부되고 이후 재요청도 어려워짐(브라우저 정책). 실제 필요 맥락에서 요청하면 수락률이 크게 높아짐 |

Sources:
- [web-push vs OneSignal vs firebase-admin 2026](https://www.pkgpulse.com/guides/web-push-vs-onesignal-vs-firebase-push-notifications-2026)
- [web-push - npm](https://www.npmjs.com/package/web-push)
- [Web Push Interoperability Wins | Chrome for Developers](https://developer.chrome.com/blog/web-push-interop-wins)

### 8.17 통합 알림 발송 아키텍처 (확정)

> 8.13(이메일)·8.14(SMS)·8.16(웹 푸시)에서 채널별 벤더/정책은 각각 확정했지만, 지금 상태로는 주문 서비스·인증 서비스 등 비즈니스 로직 코드 곳곳에서 세 채널을 따로따로 호출하게 됩니다. 이번엔 **채널을 하나의 알림 발송 계층으로 묶는 아키텍처**를 검토했습니다. **실제 구축이 아닌 결정 단계**입니다.

| 항목 | 검토 대상 | 결정 | 근거 |
|---|---|---|---|
| 발송 트리거 방식 | 비즈니스 서비스가 이메일/SMS/푸시 발송 함수를 직접 호출 vs **이벤트 기반(`@nestjs/event-emitter`)** | 주문결제·비밀번호재설정 등에서 `order.paid`, `password.reset_requested` 같은 도메인 이벤트를 발행하고, `NotificationModule`이 이 이벤트를 구독해 어떤 채널로 무엇을 보낼지 한곳에서 결정. **단 `notification_outbox` 행 INSERT 자체는 비즈니스 트랜잭션(예: 결제 승인) 안에서 함께 커밋**하고, 이벤트는 "즉시 발송을 앞당기는 최적화"로만 사용(8.55절 D-2). 여기에 **두 가지 규칙을 반드시 함께 적용**(8.58절): ① **이벤트는 트랜잭션 커밋 이후에만 발행**(EventEmitter2는 동기 실행이라 커밋 전 발행 시 롤백된 결제의 알림이 나갈 수 있음), ② **즉시 발송 경로도 8.52절의 행 클레임 절차를 동일하게 거침**(리스너가 직접 발송하면 재시도 워커와 같은 행을 동시에 처리해 중복 발송됨) | "이 이벤트에 어떤 알림이 나가는가"를 리스너 한 파일에서 파악할 수 있어, 채널이 늘어나도(예: 향후 카카오 알림톡) 비즈니스 로직을 건드리지 않고 리스너만 수정하면 됨. NestJS 공식 모듈이라 신규 인프라 불필요 |
| 발송 이력 저장 | 채널별로 별도 테이블(이메일만 이력 존재, SMS/푸시는 무기록) vs **`notification_outbox`로 통합**(channel 컬럼) | 8.13절에서 설계했던 이메일 전용 이력 테이블을 **`notification_outbox`로 일반화**하여 SMS·푸시도 같은 테이블에 1건씩 기록. **`payload`에는 엔타이틀먼트 ID 등 식별자만 저장하고 실제 다운로드 링크는 발송 시점에 생성**(8.58절 G-5: 이력이 영구 보관되므로 링크를 그대로 담으면 유효한 다운로드 URL이 DB에 평문으로 누적되어 8.4/8.7절의 토큰 보호 원칙과 충돌). 채널별 재시도 정책은 `max_attempts` 값으로 구분(이메일=5, SMS/푸시=1 → 사실상 재시도 없음, 8.14/8.16 결정과 동일한 동작) | 지금은 "문자 못 받았어요" CS 문의(FR-5.2) 시 SMS/푸시 발송 이력을 조회할 방법이 없음. 테이블을 통합하면 채널별 재시도 정책(8.13~8.16에서 이미 확정한 정책)은 그대로 유지하면서 이력 조회만 한곳에서 가능해짐 |
| 재시도 Cron | 채널별 별도 Cron vs **8.12절 Cron 1개가 `notification_outbox` 전체 처리** | `attempts < max_attempts AND status = 'PENDING'`인 행을 채널 무관하게 조회해 재시도 시도(Advisory Lock 동일 적용) | 이메일만 실질적으로 재시도되고 SMS/푸시는 `max_attempts=1`이라 1회 시도 후 자연히 멈춤 — 새 Cron을 추가하지 않고 기존 것을 재사용 |
| 채널 라우팅 정의 | 코드에 흩어진 if문으로 채널 결정 vs **이벤트→채널 매핑을 한 곳에 선언** | `NotificationModule` 안에 이벤트 타입별 채널 매핑을 선언적으로 정의(예: `order.paid` → [EMAIL, SMS], `email.verification_requested` → [EMAIL]) | 8.13~8.16에서 정한 "무엇을 어떤 채널로 보낼지" 정책이 코드 여기저기 흩어지지 않고 한 파일에서 한눈에 보임 |

Sources:
- [Event Emitter | NestJS 공식 문서](https://docs.nestjs.com/techniques/events)

### 8.18 알림 발송 실패 재시도 정책 (확정)

> 8.13/8.17에서 "이메일은 최대 5회 재시도"라고만 정하고, **정확히 몇 분 간격으로 재시도할지**, **어떤 알림에 몇 분 창을 쓸지**는 구체화하지 않았습니다. 특히 비밀번호 재설정 메일(8.7절, 토큰 30분 만료)처럼 **긴급한 알림에 몇 시간짜리 재시도 창을 그대로 적용하면 재시도가 성공해도 이미 토큰이 만료되어 무의미**해지는 문제를 발견했습니다. **실제 구축이 아닌 결정 단계**입니다.

| 항목 | 검토 대상 | 결정 | 근거 |
|---|---|---|---|
| 재시도 스케줄링 필드 | 매 Cron tick마다 모든 PENDING 재시도 vs **`next_retry_at` 컬럼으로 지수 백오프 시점 관리** | `notification_outbox`에 `next_retry_at` 추가, Cron은 `status='PENDING' AND next_retry_at <= now()`만 조회(**1분마다** 실행, 8.12절 목록에 추가) | 컬럼이 없으면 지수 백오프가 실질적으로 의미가 없어짐(매번 재시도하거나 매번 건너뛰거나 둘 중 하나) **`next_retry_at`은 `now() + 지연`이 아니라 `created_at + 누적 지연`(절대 시각)으로 계산한다(8.72절 U-1)** — Cron이 1분 주기라 각 재시도는 예정보다 최대 1분 늦게 집히는데, `now()` 기준이면 이 지연이 **회차마다 누적**되어 총 14분이 최대 17분이 된다. 절대 시각 기준이면 누적되지 않고 각 회차가 최대 1분만 늦어져 **마지막 시도가 t+15로 수렴**한다. |
| 알림 유형별 재시도 창 | 모든 알림에 동일한 재시도 스케줄 적용 vs **긴급/일반 2단계 프로파일 분리** | **긴급(urgent)**: `password_reset`·`email_verification`·`download_link_guest` — 실패 시 1분→3분→10분 후 재시도(최대 4회, **최대 15분** — 8.72절 U-1에서 Cron 1분 주기를 반영해 "14분 이내"에서 정정). **긴급 프로파일은 이메일 전용**이다(8.72절 U-6: 8.14/8.16절이 SMS·푸시를 `maxAttempts=1`로 정했고, 이 세 템플릿은 모두 이메일로만 발송된다. 향후 SMS로 확장하면 재시도 정책을 함께 재검토). **일반(normal)**: `order_confirmation`·`revision_notice` — 5분→30분→2시간→6시간 후 재시도(최대 5회 시도, 총 8.5시간 이내) | 비밀번호 재설정·이메일 인증 토큰은 30분 만료(8.7절)라 재시도 창이 이보다 길면 성공해도 무의미. 반대로 주문완료 확인처럼 급하지 않은 알림은 일시적 장애가 길어져도 여유 있게 재시도해 최종 성공률을 높이는 것이 유리 |
| 프로파일 정의 위치 | DB 컬럼으로 관리 vs **애플리케이션 코드의 상수 맵** | `template` 값 → 지연 배열(`[1,3,10]` 분 등)과 **`maxAttempts`**(8.72절 U-4: 채널 기본값이 아니라 **프로파일에서 주입** — 같은 이메일이라도 `password_reset`은 4회, `order_confirmation`은 5회)를 매핑하는 설정 객체를 `NotificationModule` 안에 상수로 정의 | 템플릿 종류가 소수(5~6개)이고 자주 바뀌지 않아 DB 테이블화는 과설계. 코드 리뷰로 변경 이력 추적이 더 쉬움 |
| 최종 실패(재시도 소진) 처리 — 인터랙티브 알림 | 별도 조치 vs **조치 불필요** | `password_reset`·`email_verification`·`download_link_guest`는 사용자가 화면에서 "다시 받기"를 눌러 새 토큰/이메일을 생성하는 기존 FR 흐름(FR-1.1, FR-1.4)이 이미 존재 | 사용자가 직접 재요청할 수 있는 경로가 있으므로, 시스템이 자동 재시도를 몇 번 더 하는 것보다 사용자가 재요청하는 편이 오히려 빠름 **발송 직전에 토큰 유효성을 확인한다(8.72절 U-2)** — 이미 만료되었으면 발송하지 않고 `FAILED`로 처리하고 `last_error`에 `token_expired`를 남긴다. outbox 행이 장애로 묶여 있다가 복구 후 처리되면 **만료된 링크가 담긴 메일이 나가고**, 사용자는 클릭 후 "만료되었습니다"를 보게 된다 — 아무 메일도 안 오는 것보다 나쁜 경험이다. 8.58절 G-5에서 payload에 식별자만 저장하고 링크를 발송 시점에 생성하기로 했으므로 이 검사가 구조적으로 가능하다. **또한 "다시 받기"로 새 토큰을 발급할 때는 같은 사용자·같은 템플릿의 진행 중인 outbox 행을 먼저 취소한다(8.72절 U-3)** — 8.7절상 재설정 토큰은 1회용이라 새 토큰 발급이 **이전 토큰을 무효화**하므로, 진행 중이던 재시도를 그대로 두면 죽은 토큰이 담긴 메일을 보내게 된다. |
| 최종 실패(재시도 소진) 처리 — 비인터랙티브 알림 | 방치 vs **관리자 수동 재발송 기능(신규, FR-7.7)** | `order_confirmation`이 모든 재시도(8.5시간) 후에도 실패하면 고객은 자동으로 다시 받을 방법이 없음 — 관리자 주문 상세 화면에 **"알림 재발송"** 버튼 추가 | 주문완료 메일에는 다운로드 링크가 포함되므로(FR-3.4), 발송이 완전히 실패하면 고객이 구매한 상품을 받을 방법이 없어짐. 이는 email/SMS와 무관하게 반드시 사람이 개입할 수 있는 안전망이 필요 |
| 지터(Jitter) 적용 | 지연에 무작위성 추가 vs **미적용** | 우리 규모(초기 회원 1,000명)에서는 동시다발적 재시도로 인한 부하 폭증(thundering herd) 우려가 없어 생략 | 지터는 대량 트래픽에서 재시도가 특정 순간에 몰리는 것을 막기 위한 기법인데, 우리 알림 발송량은 매우 적어 도입 실익이 없음. 트래픽이 커지면 재검토 |

Sources:
- [Transactional Email Timing: SLA Guide for Developers](https://resources.mailertogo.com/guide/transactional-email-timing-best-practices)
- [Retrying and Exponential Backoff: Smart Strategies for Robust Software](https://www.pullrequest.com/blog/retrying-and-exponential-backoff-smart-strategies-for-robust-software/)

### 8.19 MFA(다중 인증) 계정 보호 기술 스택 (확정)

> 현재 8.7절 인증 설계는 이메일+비밀번호 단일 요소입니다. 관리자 계정은 환불 처리·회원정보 조회·파일 교체 등 강한 권한을 가지므로(FR-7절), 탈취 시 피해가 큰 계정부터 MFA로 보호하는 것을 검토했습니다. **실제 구축이 아닌 결정 단계**입니다.

| 항목 | 검토 대상 | 결정 | 근거 |
|---|---|---|---|
| MFA 방식 | SMS OTP vs Email OTP vs **TOTP(인증 앱)** | **TOTP**(RFC 6238, Google Authenticator/Authy 호환) | 2024년 개정된 NIST SP 800-63B부터 **SMS는 SIM 스와핑·SS7 공격 위험으로 AAL2(보증수준2) 인증요소로 인정되지 않음** — 업계 표준이 SMS에서 TOTP/패스키로 이동 중. TOTP는 기기 오프라인에서도 동작하고 우리 서비스에 추가 발송 비용(솔라피 SMS 단가 등)이 들지 않음 |
| MFA 미등록 상태 처리 | 규칙 없음 vs **등록 전까지 관리자 기능 차단** | 초대 수락 후 아직 TOTP를 등록하지 않은 관리자는 **MFA 등록 엔드포인트 외 모든 관리자 API 접근을 차단**(8.55절 D-3) | "관리자는 MFA 필수"라고 정해도 미등록 상태의 접근을 막지 않으면, 초대 직후 구간이 그대로 우회 경로가 됨 |
| 적용 대상 범위 | 전체 회원 대상(선택 적용) vs **관리자 계정만 필수** | **관리자만 필수**, 일반 고객은 이번 MVP 범위 제외 | 관리자는 8.1절 기준 5~6명뿐이라 도입 마찰이 작고, 환불·회원정보·파일교체 등 피해 범위가 큰 권한을 가짐. 일반 고객까지 강제하면 회원가입/로그인 전환율에 불필요한 마찰이 생기고, 전자책 구매 서비스의 위협 모델상 우선순위가 낮음 |
| 라이브러리 | 직접 구현 vs **`otplib`** | `otplib`(TypeScript, RFC 6238/4226 준수, Node/Bun/Deno 지원) + QR 코드 생성용 `qrcode` 패키지 | RFC 표준 구현체를 직접 만들 이유가 없고, otplib는 활발히 유지보수되는 표준 라이브러리 |
| TOTP Secret 저장 | 평문 저장 vs **암호화 저장** | 사용자별 TOTP secret을 암호화해 저장(비밀번호처럼 단방향 해시 불가 — 로그인 시마다 복호화해 코드 검증 필요). 암호화 방식은 **AWS KMS 봉투암호화**로 확정(8.30절에서 상세·로테이션 정책과 함께 재검토) | TOTP secret은 검증을 위해 원문 비교가 필요해 Argon2id 같은 단방향 해시(8.7절)를 쓸 수 없음. 평문 DB 저장은 DB 유출 시 모든 관리자 계정의 MFA가 즉시 무력화되므로 암호화가 필수. KMS를 쓰면 8.30절에서 다루듯 키 로테이션이 재암호화 없이 가능해짐 |
| 기기 분실 대비 | 대비책 없음 vs **1회용 백업 코드 발급** | MFA 등록 시 **10개**의 1회용 복구 코드 발급, **8.7절과 동일하게 해시로 저장**(원문은 발급 시 1회만 표시) | 인증 앱이 설치된 기기를 분실하면 로그인 자체가 불가능해지는 상황을 방지. 백업 코드는 로그인 자격증명에 준하므로 재설정 토큰(8.7절)과 동일한 해시 저장 원칙 적용 |
| 로그인 흐름 | 비밀번호 검증만으로 즉시 토큰 발급 vs **2단계 로그인** | 이메일+비밀번호 검증 통과 시 **단기(5분) "MFA 대기" 토큰**만 우선 발급 → TOTP 코드 검증 성공 시에야 8.7절의 Access+Refresh 토큰 발급 | 비밀번호만으로 이미 로그인이 완료된 것처럼 보이면 MFA가 형식적으로만 존재하게 됨. 중간 상태를 짧은 만료시간의 별도 토큰으로 명확히 분리 |
| MFA 코드 무차별 대입 방지 | 별도 조치 없음 vs **기존 속도 제한 재사용** | MFA 검증 엔드포인트에도 8.11절에서 이미 구축한 **Postgres 기반 커스텀 ThrottlerStorage** 적용(예: 분당 5회) | 6자리 TOTP 코드는 무차별 대입 가능성이 있어 속도 제한이 필수. 신규 인프라 없이 8.11절 구조를 그대로 재사용 |

Sources:
- [TOTP vs SMS OTP: And Why One Is Being Deprecated](https://guptadeepak.com/ciam-compass/guides/totp-vs-sms-otp/)
- [TOTP vs SMS: Which one is better for two-factor authentication (2FA)?](https://stytch.com/blog/totp-vs-sms/)
- [otplib - npm](https://www.npmjs.com/package/otplib)

### 8.20 관리자별 개별 계정 및 권한/감사 체계 (확정)

> 현재 FR-7절은 "관리자가 상품/주문/회원을 관리한다"고만 되어 있고, **관리자 계정을 어떻게 만들고, 여러 관리자가 동일 권한을 가지는지, 누가 무엇을 했는지 추적할 수 있는지**는 정의돼 있지 않았습니다. 8.1절 기준 관리자(운영자) 인원이 여러 명이 될 수 있어 이번에 함께 짚었습니다. **실제 구축이 아닌 결정 단계**입니다.

| 항목 | 검토 대상 | 결정 | 근거 |
|---|---|---|---|
| 관리자 계정 생성 방식 | 일반 회원가입과 동일한 셀프 가입 폼 vs **초대 기반 생성** | 기존 관리자가 신규 관리자의 이메일로 **초대 링크**를 발송 → 초대받은 사람이 링크를 통해 비밀번호(+MFA, 8.19절)를 직접 설정 | 관리자 계정을 일반 회원가입 폼으로 셀프 가입할 수 있게 두면 권한 상승 취약점이 됨. 초대 기반이면 "누가 이 관리자를 추가했는지"도 자연히 기록됨 |
| 최초 관리자 생성(부트스트랩) | 초대 시스템에만 의존 vs **배포 시 시드 스크립트로 최초 1명 생성** | 관리자가 0명인 초기 상태를 위해 CLI 시드 스크립트(환경변수로 이메일/임시비밀번호 지정)로 최초 SUPER_ADMIN 1명을 생성 | 초대는 "초대할 관리자가 이미 있어야" 동작하므로, 시스템 최초 구축 시의 순환 문제(치킨-에그)를 시드 스크립트로 해결 |
| 관리자 권한 세분화 | 단일 `ADMIN` 역할 유지 vs **`SUPER_ADMIN`/`ADMIN` 2단계 구분** | `Role` enum에 `SUPER_ADMIN` 추가. **SUPER_ADMIN만** 관리자 계정 초대·비활성화·권한 변경 가능, 일반 `ADMIN`은 상품/주문/쿠폰 등 기존 FR-7 운영 기능만 사용 | 8.1절 팀 규모(5~6명)가 작아 세분화된 RBAC(상품담당/CS담당 등 다단계 권한)까지는 과설계지만, "관리자 계정 자체를 늘리고 줄일 권한"만큼은 최소 분리해야 임의의 운영자가 다른 관리자를 마음대로 추가/삭제하지 못함 |
| 관리자 활동 추적 | 없음 vs **감사 로그(Audit Log) 기록** | 관리자의 주요 변경 행위(주문 상태 변경, 환불 처리, 상품 등록/수정, 쿠폰 발급, 회원 제재, 관리자 계정 초대/비활성화)를 `admin_audit_log`에 기록(행위자, 행위 종류, 대상, 변경 전후 요약, IP, 시각) | "개별 계정으로 로그인하게 하는" 목적 자체가 책임 추적성 확보인데, 감사 로그 없이는 개별 계정을 써도 사후 추적이 불가능함. 환불·회원정보 접근처럼 민감한 동작이 많아(FR-3.5, FR-7.4) 분쟁 발생 시 근거 자료로도 필요 |
| 관리자 잠김(lockout) 방지 | 제한 없음 vs **활성 SUPER_ADMIN 최소 1명 보장** | 마지막 남은 활성 SUPER_ADMIN에 대한 **비활성화·권한 강등 요청을 거부**(8.55절 D-3) | 모든 SUPER_ADMIN이 비활성화되면 관리자 계정을 초대·복구할 수 있는 주체가 사라져, 8.20절 시드 스크립트를 다시 돌리는 것 외에는 복구 수단이 없어짐 |
| 관리자 계정 비활성화(퇴사 등) | 삭제만 가능 vs **비활성화(soft-disable) 지원** | `User`에 `isActive`/`deactivatedAt` 추가, 비활성화된 관리자는 로그인 차단(단, 과거 감사 로그·주문 처리 이력의 행위자 참조는 유지) | 관리자 계정을 삭제하면 과거 감사 로그·처리 이력의 "누가 처리했는지" 참조가 끊어짐. 퇴사 시에도 이력 보존을 위해 삭제 대신 비활성화가 표준적 방식 |

### 8.21 모듈형 모놀리스(Modular Monolith) 내부 구조 (확정)

> 8.3절에서 "Next.js(프론트)+NestJS(백엔드) 분리" 아키텍처를 정했지만, **NestJS 백엔드 내부를 어떻게 나눌지**는 아직 정하지 않았습니다. 지금까지 늘어난 도메인(회원/상품/주문/결제/다운로드/쿠폰/포인트/리뷰/문의/알림/관리자)을 하나의 배포 단위 안에서 **모듈형 모놀리스**로 구조화하는 방안을 검토했습니다. **실제 구축이 아닌 결정 단계**입니다.

| 항목 | 검토 대상 | 결정 | 근거 |
|---|---|---|---|
| 모듈 경계 | 계층별 폴더(controllers/services 전역 분리) vs **도메인별 모듈 폴더** | `src/modules/<domain>/`(auth, products, orders, payments, entitlements, coupons, points, reviews, support, notification, admin-accounts 등)로 분리, 각 모듈이 자신의 컨트롤러·서비스·DTO를 소유 | 지금까지 PRD의 FR 1~7이 이미 이 도메인 경계와 자연스럽게 일치함(FR-1→auth, FR-2→products, FR-3→orders 등). 계층별 분리는 프로젝트가 커질수록 "이 기능이 어디 있는지" 찾기 어려워짐 |
| 관리자 API 배치 | 모든 관리자 기능을 별도 `AdminModule`에 몰아넣기 vs **각 도메인 모듈이 자신의 관리자 API를 소유** | 예: 상품 승인/수정 API는 `ProductsModule` 안의 `AdminProductsController`로, 주문 상태변경은 `OrdersModule` 안에 위치 | 기능을 별도 AdminModule에 모으면 결국 모든 도메인 로직을 다시 참조하게 되어 모듈 경계가 무의미해짐(사실상 재중앙집중화). 도메인 모듈이 자기 데이터의 관리자 API까지 책임지는 편이 경계를 지킴 |
| 모듈 간 통신 방식 | 전부 이벤트로 통일 vs **조회=직접 호출, 부작용=이벤트로 이원화** | 같은 요청 안에서 필요한 조회(예: 주문 생성 시 상품 가격 조회)는 다른 모듈 서비스를 직접 주입해 호출. 여러 모듈에 걸친 후속 처리(결제 승인 후 엔타이틀먼트 생성+알림 발송 등)는 8.17절처럼 **도메인 이벤트**(`@nestjs/event-emitter`)로 통일 | 모든 것을 이벤트로 강제하면 단순 조회에도 불필요한 간접화가 생겨 코드 추적이 어려워짐. 반대로 부작용까지 직접 호출로 엮으면 모듈이 서로 강하게 결합되어 8.3절에서 원했던 "추후 확장/분리 용이성"이 사라짐 |
| 모듈 경계 위반 방지 | 코드 리뷰로만 관리 vs **`dependency-cruiser`로 자동 검증** | CI에 `dependency-cruiser` 규칙 추가 — "모듈 폴더 간 직접 import 금지(공개된 모듈 index/서비스는 예외)", "도메인 코드는 자기 도메인 폴더 밖을 import할 수 없음" 등을 자동 검사 | 설정 비용이 30분 정도로 낮은 반면, 사람이 리뷰에서 놓치기 쉬운 경계 위반(예: OrdersModule이 PaymentsModule 내부 리포지토리를 직접 참조)을 자동으로 잡아 장기적인 리팩터링 비용을 줄임 |
| CQRS(`@nestjs/cqrs`) 도입 | 도입 vs **미도입, 서비스+이벤트로 유지** | 커맨드/쿼리/이벤트 핸들러를 완전히 분리하는 CQRS는 도입하지 않고, 지금까지처럼 일반 서비스 클래스 + 도메인 이벤트 조합을 유지 | 8.1절 팀 규모(5~6명)와 9.1절 일정(16주)에서 CQRS의 보일러플레이트(커맨드/쿼리/핸들러 클래스 증식)는 과설계. 트래픽·조직이 커져 도메인별 팀 분리가 필요해질 때 재검토 |
| 향후 마이크로서비스 추출 대비 | 고려하지 않음 vs **모듈 경계를 추출 단위로 미리 정렬** | 지금의 도메인 모듈 경계(특히 PaymentsModule, EntitlementsModule)를 향후 별도 서비스로 분리할 가능성이 있는 단위로 미리 정렬해두되, **지금 당장 분리하지는 않음** | "인프로세스에서 모듈을 옮기는 것은 리팩터링이지만, 네트워크 너머로 옮기는 것은 API 버전관리까지 포함한 별도 프로젝트"라는 원칙에 따라, 지금 결합도만 낮춰두면 실제 분리는 필요해졌을 때 훨씬 저렴해짐 |

Sources:
- [Modular Monolith Architecture: The Complete Guide](https://medium.com/viascom/the-modular-monolith-a-field-guide-36dcf21a477b)
- [NestJS Project Structure Best Practices 2026](https://encore.dev/articles/nestjs-project-structure-best-practices)
- [Modular Monolith vs Microservices in NestJS](https://dev.to/geampiere/modular-monolith-vs-microservices-in-nestjs-223g)

### 8.22 Next.js Route Handler 기반 BFF 계층 (확정)

> 8.3절의 "NestJS가 API 전담" 결정은 유지합니다. 이번엔 Next.js에 **얇은 BFF(Backend for Frontend) 계층**을 어디에, 얼마나 둘지 검토했습니다. 리서치 중 8.7절 설계에 실제로 구멍이 있는 것을 발견했습니다: **Server Component는 쿠키를 읽을 수만 있고 쓰거나 갱신할 수 없어서**, "Access Token은 브라우저 메모리에 보관"이라는 8.7절 설계로는 SSR(서버 렌더링) 시점에 로그인한 사용자의 데이터를 가져올 방법이 없습니다. **실제 구축이 아닌 결정 단계**입니다.

| 항목 | 검토 대상 | 결정 | 근거 |
|---|---|---|---|
| BFF 적용 범위 | 모든 인증 API 호출을 프록시(풀 BFF) vs **SSR 토큰 릴레이 전용**(좁은 BFF) | **SSR 토큰 릴레이 전용** — 로그인 후 클라이언트 상호작용(폼 제출·결제·다운로드 요청 등)은 8.7절 그대로 브라우저가 Access Token으로 **NestJS를 직접 호출**. Route Handler는 "로그인한 사용자의 초기 SSR 데이터(마이페이지 등)를 가져오기 위해 Refresh 쿠키를 Access Token으로 교환"하는 용도로만 사용 | 풀 BFF(브라우저→Next.js→NestJS 이중 홉)는 XSS 방어는 더 강하지만 모든 인터랙션에 지연이 추가되고, Vercel↔AWS 간 프록시 코드를 유지보수해야 함. 8.7절에서 이미 서브도메인+SameSite=Lax 쿠키로 직접 호출이 가능하도록 설계해뒀으므로, 굳이 모든 호출을 되짚어 프록시할 이유가 약함 |
| 구현 위치 | 범용 프록시 Route Handler(`/api/bff/[...path]`) vs 좁은 목적의 Route Handler 2개 vs **BFF Route Handler 0개 + 서버 전용 헬퍼 함수** | **(8.66절 O-1로 재확정)** **BFF Route Handler는 만들지 않는다(0개).** SSR 릴레이는 **서버 전용 헬퍼 함수 `lib/auth/getSsrAccessToken.ts`** 하나로 구현한다 — Server Component가 `cookies()`로 Refresh 쿠키를 **읽기만** 해서 8.53절의 **비회전 SSR 전용 엔드포인트**를 직접 `fetch`하고 Access Token을 받는다. 브라우저의 회전 경로는 8.7/8.65절 서브도메인 통일 덕분에 **브라우저가 `api.` 도메인의 `/auth/refresh`를 `credentials: 'include'`로 직접 호출**하면 NestJS가 `Domain=.cbmall.co.kr`로 `Set-Cookie`한다. `app/api/` 아래에는 **인바운드 웹훅 수신기(8.23절 `revalidate`) 하나만** 남으며, 이는 BFF가 아니라 별개 범주다(8.66절 O-2). **이 헬퍼는 세 가지를 지킨다(8.69절)**: ① `fetch`에 **`AbortSignal.timeout(3초)`** 를 걸고 실패 시 **재시도 없이** 로그인 리다이렉트 또는 비로그인 셸로 **우아하게 저하**한다(R-2 — Node `fetch`는 기본 타임아웃이 없어 ECS가 멈추면 함수가 `maxDuration`까지 매달린 채 과금·동시성을 소모한다), ② **React `cache()`로 감싸** 한 렌더에서 여러 Server Component가 호출해도 **렌더당 1회**만 실행되게 한다(R-5 — Route Handler였다면 HTTP 호출이라 이 최적화가 불가능했다), ③ 개인화 영역은 **`<Suspense>`로 감싸** 정적 셸을 즉시 flush하고 데이터는 스트리밍으로 채운다(R-4 — 릴레이가 느려도 첫 페인트가 막히지 않고, 저하가 페이지 전체가 아니라 그 영역에만 적용된다) | 이 절이 원래 Route Handler를 둔 이유는 **"Server Component는 쿠키를 쓸 수 없다"** 는 제약 하나였는데, **8.53절이 방안 B를 채택하면서 쿠키 쓰기 자체가 필요 없어져 그 이유가 소멸**했다. 남겨두면 Vercel 함수가 **자기 자신에게 HTTP 요청을 한 번 더 보내는 홉**만 추가된다(SSR 지연·함수 호출 수 증가). 덤으로 `app/api/` 아래 BFF가 0개가 되면 **"프록시를 하나 더 만들까"의 출발점 자체가 사라져** 8.51절 C-6 원칙이 지키기 쉬워진다 |
| 보호된 페이지 접근 제어 | Server Component마다 개별 체크 vs **`middleware.ts`에서 1차 체크** | 마이페이지 등 로그인 필요 라우트는 Next.js `middleware.ts`가 Refresh 쿠키 존재 여부만 가볍게 확인해 없으면 로그인 페이지로 리다이렉트. **최종 권한 검증은 여전히 NestJS**가 수행(미들웨어는 UX 최적화용 1차 방어일 뿐, 보안 경계가 아님). **미들웨어에서 NestJS를 호출하지 않는다(8.66절 O-4)** — BFF Route Handler를 0개로 막으면 로직이 미들웨어로 흘러들기 쉬운데, 미들웨어는 **모든 요청에서 실행**되므로 여기에 네트워크 호출이 들어가면 Route Handler보다 나쁜 결과가 된다 | 미들웨어에서 매번 NestJS를 호출해 완전 검증하면 모든 페이지 이동마다 지연이 생김. 진짜 인가(authorization)는 어차피 각 NestJS API 호출 시점에 재검증되므로 미들웨어는 가벼운 UX 체크로 충분 |
| BFF의 비즈니스 로직 포함 여부 | Route Handler 안에서 데이터 가공/검증 수행 vs **순수 토큰 릴레이만, 로직 없음** | Route Handler는 쿠키 읽기·NestJS 호출·응답 전달만 수행하고 어떤 비즈니스 규칙도 포함하지 않음 | 8.21절에서 정한 "비즈니스 로직은 NestJS에만 존재" 원칙을 프론트 쪽에서도 그대로 지켜, 로직이 두 곳에 분산되는 것을 방지 |
| Server Actions 사용 여부 | Route Handler와 병행 사용 vs **이번엔 Route Handler만 사용** | 폼 제출 등 mutation도 8.7절 그대로 브라우저가 NestJS를 직접 호출(Server Actions로 감싸지 않음), 폼 제출은 8.40절대로 React Hook Form `onSubmit` + TanStack Query mutation으로 고정하고, **`'use server'` 지시문 사용을 금지**한다(8.66절 O-5) | Server Actions까지 도입하면 "이 요청이 Server Action을 타는지, 직접 API 호출인지, Route Handler를 거치는지" 경로가 3갈래로 늘어나 팀이 추적하기 어려워짐. 하나의 경로 규칙(**브라우저는 NestJS 직접 호출, SSR은 서버 전용 헬퍼 함수** — 그 외 경로 없음)만 유지. App Router에서는 `<form action={서버함수}>`를 쓰면 **의도치 않게 Server Action이 만들어지므로**, 금지를 문서에만 두지 않고 O-3의 CI 스크립트에서 `'use server'` 검출을 함께 확인한다 |

Sources:
- [Securing the Client: The BFF Pattern in Next.js](https://dev.to/iprajapatiparesh/securing-the-client-the-bff-pattern-in-nextjs-4lp6)
- [NextJS SSR - JWT (Access/Refresh Token) Authentication with external Backend](https://thewidlarzgroup.com/nextjs-auth/)
- [Refresh Token Rotation in Next.js 16 App Router](https://medium.com/@chiragmehta900/refresh-token-rotation-in-next-js-16-app-router-the-complete-implementation-b2b213dc5717)

### 8.23 캐시 전략 (확정)

> 지금까지 8.11절에서 "Redis 대신 Postgres"를 반복적으로 선택해왔는데, **캐싱은 원래 Redis가 가장 흔히 쓰이는 영역**이라 이번엔 정말 필요한지부터 짚었습니다. 리서치 중 **Next.js 15부터는 fetch가 기본적으로 캐시되지 않음**(과거 13/14와 반대)을 확인해, 명시적으로 캐시 옵션을 켜야 한다는 점도 반영했습니다. **실제 구축이 아닌 결정 단계**입니다.

| 항목 | 검토 대상 | 결정 | 근거 |
|---|---|---|---|
| 별도 캐시 서버(Redis) 도입 | ElastiCache Redis 도입 vs **미도입, Next.js/HTTP 캐시로 대체** | **미도입** | 우리 규모(초기 회원 1,000명/동시접속 100명)에서 DB·API 응답 속도가 이미 충분히 빠르며, Supavisor 풀링(8.15절)까지 갖춰져 있어 캐시로 얻을 이득이 크지 않음. 신규 인프라 비용/운영 부담만 늘어남 — 8.11절과 동일한 판단 기준 |
| 캐싱 대상 | 전체 API 응답 vs **공개·저빈도 변경 데이터로 한정** | 상품 목록/상세, 카테고리, 리뷰 목록 등 **공개 조회 데이터만** 캐싱. 회원 개인화 데이터(주문내역, 마이페이지)·다운로드 엔타이틀먼트·관리자 대시보드는 **캐싱 금지**(`Cache-Control: no-store`) | 개인화 데이터를 캐싱하면 다른 사용자에게 잘못 노출될 위험이 있고, 다운로드 횟수(8.6/8.11절)처럼 원자성이 중요한 데이터는 캐시와 근본적으로 상극임 |
| 캐싱 메커니즘 | 별도 캐시 레이어 직접 구현 vs **Next.js Data Cache(`fetch` 태그 기반) 활용** | 상품 관련 조회를 `next: { tags: ['product:{id}'] }`로 태깅해 Next.js가 결과를 캐시하도록 함 (Next.js 15는 기본 미캐시라 명시적 옵션 필요) | 별도 캐시 서버 없이 이미 채택한 프레임워크(8.3절)의 내장 기능만으로 해결 가능. NestJS 응답을 그대로 캐시하므로 백엔드 코드 변경이 최소화됨 |
| 검색 응답 캐시 | 상품과 동일하게 TTL 적용 vs **캐시 금지** | **검색 엔드포인트는 `Cache-Control: no-store`** — 8.23절 캐시 전략의 공식 예외(8.62절 K-6) | 공유 캐시에 스니펫이 남으면 **캐시 히트가 8.61절 J-4의 분당 30회 제한을 거치지 않고** 본문 조각을 돌려주어 속도 제한이 우회됨. 검색은 쿼리 조합이 무한해 캐시 히트율도 애초에 낮아 얻을 이득이 없음 |
| 캐시 무효화 | 시간 기반(TTL)만 vs **온디맨드 무효화 + 시간 기반 안전망 병행** | 관리자가 상품을 등록/수정/판매상태 변경(FR-2.2/2.5)하면 NestJS 처리 완료 후 Next.js의 `revalidateTag('product:{id}')`를 호출하는 내부 엔드포인트를 트리거. 이 호출이 실패하는 경우를 대비해 **5분 TTL**의 시간 기반 재검증도 함께 설정. **이 엔드포인트는 Secrets Manager에 보관한 공유 시크릿을 검증해야 하며**(8.51절 C-6: 미인증 시 누구나 캐시를 무효화해 DB 부하를 유발할 수 있음), 8.22절의 "Route Handler 최소화" 원칙에 대한 명시적 예외로 둠 | 온디맨드만 쓰면 무효화 호출이 실패했을 때 캐시가 영구히 오래된 채로 남을 위험이 있고, 시간 기반만 쓰면 상품 수정이 최대 5분씩 지연 반영됨 — 두 방식을 병행하면 즉시성과 견고함을 모두 확보 |
| 정적 자산 캐싱 | 매번 재검증 vs **긴 캐시 + 파일명 버전 관리** | S3 공개 버킷(8.10절)의 표지 이미지·샘플 PDF는 `Cache-Control: public, max-age=31536000, immutable` 설정. 파일이 교체되면 같은 키를 덮어쓰지 않고 **새 키(파일명에 버전/해시 포함)**로 업로드. **공개 버킷에는 CORS 규칙도 함께 설정** — 운영·스테이징 도메인뿐 아니라 **8.34절 PR 프리뷰의 `https://*.vercel.app` 오리진까지 `GET` 허용**(8.51절 C-5 + 8.54절 재수정: react-pdf가 브라우저 fetch로 PDF를 읽으므로 CORS가 없으면 미리보기가 실패하고, 프리뷰 오리진을 빠뜨리면 PR 환경에서만 항상 실패함). 오리진별 응답 구분을 위해 `Vary: Origin` 반환도 확인 | 정적 자산은 내용이 바뀌지 않는 한 영구 캐시가 안전하며, 교체 시 새 URL을 쓰면 캐시 무효화 자체가 불필요해짐(무효화 요청을 아예 안 보내도 됨) |
| 판매상태·가격의 캐시-쓰기 정합성 | 캐시된 값을 그대로 결제에 사용 vs **주문/결제 시점엔 항상 캐시 우회, DB 재조회** | 상품 목록/상세 열람은 캐시를 쓰더라도, **주문 생성·결제 승인(8.8절) 시점에는 NestJS가 항상 최신 DB 값으로 가격·판매상태를 재검증** | 캐시가 몇 분간 오래된 값을 보여줘도, 실제 결제는 항상 최신 상태 기준으로 처리되므로 "캐시 때문에 품절/가격변경 상품이 잘못 결제되는" 사고를 원천 차단 |

Sources:
- [Guides: How Revalidation Works | Next.js](https://nextjs.org/docs/app/guides/how-revalidation-works)
- [Guides: ISR | Next.js](https://nextjs.org/docs/app/guides/incremental-static-regeneration)
- [Cache issue in Next JS 15 with App Router (fetch 기본 미캐시)](https://github.com/vercel/next.js/discussions/86538)

### 8.24 모니터링/로깅 전략 (확정)

> 8.3절에서 Sentry를 "에러 트래킹"용으로만 짧게 언급했는데, **평상시 구조화된 로그를 어떻게 남기고 어디에 보관할지**, **서버가 아예 응답하지 않는 상황은 무엇으로 감지할지**는 정하지 않았습니다. 8.20절의 `admin_audit_log`(비즈니스 감사 목적, DB 영구보관)와는 별개로, 디버깅용 시스템 로그 전략을 검토했습니다. **실제 구축이 아닌 결정 단계**입니다.

| 항목 | 검토 대상 | 제안(추천) | 근거 |
|---|---|---|---|
| 로거 라이브러리 | NestJS 기본 로거(단순 텍스트) vs **`nestjs-pino`(구조화 JSON)** | **nestjs-pino** | JSON 구조화 로그라야 CloudWatch Logs Insights 같은 도구로 SQL과 유사하게 검색·집계 가능. `AsyncLocalStorage` 기반이라 하나의 요청에 속한 모든 로그 줄에 requestId가 자동으로 붙어, 특정 요청의 로그만 추적하기 쉬움 |
| 로그 저장소 | 별도 로그 관리 서비스(Datadog 등) 도입 vs **ECS 기본 `awslogs` 드라이버 → CloudWatch Logs** | ECS Fargate 컨테이너의 stdout을 CloudWatch Logs로 전송(AWS 기본 제공, 추가 설정 최소) | 이미 AWS(ECS/S3/Secrets Manager)를 쓰고 있어 신규 벤더·비용 없이 바로 사용 가능. 우리 규모에서 Datadog 같은 전문 로그 서비스는 과설계 |
| 에러·핵심 이벤트 상관관계 | CloudWatch만 사용 vs **Sentry Logs 병행** | 결제 실패·알림 발송 실패 등 지금까지 여러 절(8.8·8.13·8.14·8.16·8.18)에서 "Sentry로 알림"이라 언급된 이벤트들을 Sentry Logs로도 함께 전송해, 에러와 관련 구조화 로그를 Sentry 한 화면에서 상관관계 있게 확인 | CloudWatch는 원시 로그 전체 보관에, Sentry는 "에러 발생 시점 전후 무슨 일이 있었는지"를 빠르게 파악하는 데 특화 — 역할을 분리해 각자 잘하는 일만 하도록 함 |
| 민감정보 로깅 방지 | 별도 규칙 없음 vs **Pino `redact` 옵션으로 자동 마스킹** | `password`, `passwordHash`, `mfaSecretEnc`, `Authorization` 헤더, 토스페이먼츠 시크릿 키 등 민감 필드를 로거 설정에서 자동 마스킹 처리 | 8.7/8.8/8.19절에서 다루는 비밀번호·토큰·TOTP secret이 실수로 평문 로그에 남으면, 로그 저장소(CloudWatch) 접근 권한만으로 계정을 탈취할 수 있는 새로운 취약점이 생김 |
| 로그 보존기간 | 무기한 보관 vs **30일 보존** | CloudWatch Log Group 보존기간을 30일로 설정 | 디버깅 목적의 시스템 로그는 오래된 것까지 보관할 실익이 적고, 무기한 보관 시 비용만 계속 증가함. 장기 보관이 필요한 "누가 무엇을 했는지"는 8.20절 `admin_audit_log`가 DB에 별도로 영구 보관하므로 역할이 겹치지 않음 |
| Cron 조용한 실패 감지 | 에러 로그에만 의존 vs **연속 스킵 횟수 감시** | Cron이 "락 획득 실패로 skip"한 횟수를 구조화 로그로 남기고, **연속 스킵이 비정상적으로 길어지면 Sentry 경고**(8.52절) | 락 누수로 Cron이 영구 정지해도 예외가 발생하지 않아(정상적인 skip 경로) 아무 에러도 남지 않음 — 이 조용한 실패를 잡는 유일한 방법이 스킵 횟수 감시 |
| 알림 상태 집계 기준(8.74절 W-3) | `PENDING`이 아닌 것을 모두 실패로 집계 vs **`CANCELLED` 제외** | 알림 실패율 집계와 **FR-7.7 관리자 수동 재발송 목록에서 `CANCELLED`를 제외**한다 | 8.72절 U-3에서 추가한 `CANCELLED`는 **실패가 아니라 정상적인 종료**다(사용자가 "다시 받기"를 눌러 취소된 건). `FAILED`와 함께 집계하면 **실패율이 부풀려지고**, 재발송 목록에 뜨면 관리자가 **이미 무효인 토큰을 재발송**하게 된다 |
| 커넥션 설정 누락 감지(8.67절 P-2) | 없음 vs **SQLSTATE 기반 Sentry 알림** | `prepared statement ... already exists`(**42P05**)와 `... does not exist`(**26000**)를 Sentry 알림 조건으로 등록 | 이 오류는 `?pgbouncer=true` 누락 시 나는데, **동시 요청이 서로 다른 백엔드 연결에 배정될 때만** 발생해 로컬 개발·단일 요청 테스트에서는 재현되지 않는다. 설정 누락이 **운영에 가서야 간헐적으로** 드러나는 유형이라, 에러 코드를 알림으로 걸어두는 것이 유일한 조기 감지 수단 |
| 본문검색 성능·용량 감시 | 없음 vs **전환 판단 지표 상시 수집**(8.59절 H-4/H-5) | ① **`product_text_chunks` 총 행 수**, ② **본문검색 API p95 응답시간**, ③ **`product_text_chunks_content_trgm_idx` 인덱스 크기**(`pg_relation_size`)를 주기적으로 기록하고, ①이 **50만 행**을 넘거나 ②가 **1초**를 넘으면 전용 검색엔진 전환 검토를 트리거 | 본문검색의 병목은 **상품 수가 아니라 총 본문 분량**이라, 8.27절의 "상품 3,000~5,000건" 기준으로는 열화를 감지하지 못함(상품 300건이어도 권당 600쪽이면 청크 18만 개). 인덱스 크기는 본문 전체를 색인하는 특성상 Supabase DB 용량을 잠식할 수 있어 함께 감시 |
| 서버 다운 자체 감지 | Sentry만으로 대응 vs **외부 업타임 모니터링 병행** | 무료/저가 외부 핑 체크 서비스(예: UptimeRobot, Better Stack)로 프론트(Vercel)·백엔드(ALB 헬스체크 엔드포인트)를 주기적으로 외부에서 확인 | Sentry는 애플리케이션이 살아서 에러를 "보고할 수 있을 때"만 작동함 — 서버/컨테이너 자체가 완전히 응답하지 않으면 Sentry도 조용해짐. 이 사각지대는 외부에서 핑을 보내는 방식으로만 감지 가능 |

Sources:
- [nestjs-pino: Structured Logging with Request Context](https://dev.co/observability/open-source/nestjs-pino)
- [AWS CloudWatch Logs Insights](https://viprasol.com/blog/aws-cloudwatch-logs-insights/)
- [Set Up Logs | Sentry for Nest.js](https://docs.sentry.io/platforms/javascript/guides/nestjs/logs/)

### 8.25 로그인·쿠폰·다운로드·결제 API 반복호출 방어 종합 점검 (확정)

> 네 가지 민감 플로우에 대한 요청횟수 방어가 각 절에 흩어져 결정돼 있어, 이번에 한곳에 모아 빠진 곳이 있는지 점검했습니다. **실제 구축이 아닌 결정 단계**입니다.

| 플로우 | 기존 결정 | 점검 결과 |
|---|---|---|
| 로그인 | 8.7/8.11절: 로그인·비밀번호재설정에 **Postgres 기반 커스텀 ThrottlerStorage**(IP+이메일 기준 분당 5회), ECS 2개 이상 태스크에서도 정확히 동작 | **충분** — 추가 조치 불필요 |
| 다운로드 | 8.6/8.11절: `increment_download_count()` 함수 안에서 **분당 3회 초과 발급 거부**를 원자적으로 처리 | **충분** — 추가 조치 불필요 |
| 결제 | 8.8절: 결제 **승인/취소 API 호출**(서버→토스)에 Idempotency-Key 적용, `orders.toss_order_id`에 UNIQUE 제약 | **구멍 발견**: 이건 "같은 요청의 네트워크 재시도"만 막을 뿐, **사용자가 결제 버튼을 연타해 서로 다른 주문 2건을 만드는 것**은 막지 못함(각각 다른 toss_order_id를 받으므로 UNIQUE 제약을 피해감) |
| 쿠폰 | 8.6절: `redeem_coupon()` 함수가 전역 `usage_limit` 초과를 원자적으로 방지 | **구멍 발견**: 쿠폰 코드를 검증/적용하는 API 자체에는 **속도 제한이 전혀 없음** — 짧은 쿠폰 코드를 자동화 스크립트로 무차별 대입해 유효한 코드를 찾아내는 공격(coupon code brute-forcing)에 노출됨 |

**추가 결정 사항**

| 항목 | 검토 대상 | 결정 | 근거 |
|---|---|---|---|
| 주문 생성 중복 클릭 방지 | 클라이언트 생성 Idempotency-Key 방식 vs 짧은 시간창 Advisory Lock vs **PENDING 주문에 대한 부분 유니크 인덱스** | `orders`에 `cart_hash` 컬럼을 추가하고 **`(user_id, cart_hash) WHERE status='PENDING'` 부분 유니크 인덱스**를 생성 → 같은 장바구니로 중복 주문 생성 시 유니크 위반이 발생하면 새 주문을 만들지 않고 **기존 PENDING 주문을 그대로 반환** | **(8.51절 C-1 반영)** 당초 "5초 Advisory Lock" 안은 8.15절 트랜잭션 모드 풀러와 호환되지 않았고, 트랜잭션 단위 락으로 바꾸면 락 유지 시간이 트랜잭션 길이로 줄어 "수백 ms 간격의 두 번째 클릭"을 막지 못함. 부분 유니크 인덱스는 **PENDING 주문이 존재하는 동안(8.8절 기준 최대 30분) 계속 유효**해 시간창 문제 자체가 사라지고, 락·신규 인프라도 필요 없음 |
| 쿠폰 검증 API 속도 제한 | 무제한 vs **Postgres 기반 ThrottlerStorage 재사용**(8.7/8.11절과 동일 저장소) | 쿠폰 적용 엔드포인트에 사용자(또는 비회원은 세션) 기준 **분당 10회** 제한 적용 | 로그인과 마찬가지로 "코드를 맞힐 때까지 반복 시도"하는 공격 패턴이므로 동일한 방어 메커니즘이 필요. 이미 구축된 저장소를 재사용해 신규 테이블/인프라 불필요 |

Sources: (내부 정책 점검 — 8.6/8.7/8.8/8.11/8.12/8.15절 기존 결정 재검토)

### 8.26 GraphQL 도입 검토 (확정: 미도입)

> 8.3절에서 "REST API로 통신"이라고 짧게만 정하고 깊게 비교하지 않았는데, 이번에 GraphQL 도입 여부를 제대로 검토했습니다. **REST를 그대로 유지하고 GraphQL은 도입하지 않기로 확정**했습니다 — 이미 확정된 여러 결정과 GraphQL이 구조적으로 충돌하기 때문입니다.

| 검토 관점 | REST(현행 유지) | GraphQL(도입 시) |
|---|---|---|
| 8.23절 캐싱 전략과의 정합성 | HTTP GET + `Cache-Control` + Next.js Data Cache 태그가 그대로 적용됨(설계된 그대로 동작) | GraphQL은 보통 단일 엔드포인트에 **POST**로 요청하므로 HTTP 캐싱이 자연스럽게 적용되지 않음 — Persisted Queries(GET 방식) 같은 별도 장치를 새로 도입해야 8.23절 캐싱이 겨우 성립함 |
| 8.11/8.25절 엔드포인트별 속도 제한과의 정합성 | 로그인/다운로드/쿠폰마다 **URL이 달라** 엔드포인트 단위로 서로 다른 제한(분당 5/3/10회)을 그대로 적용 가능 | 단일 엔드포인트라 "어떤 쿼리/뮤테이션이 호출됐는지"를 요청 본문(쿼리 문자열)까지 파싱해서 구분해야 함 — 구현 복잡도 증가 |
| 팀 규모 대비 도입 비용 | 추가 학습 비용 없음(이미 익숙한 방식) | 스키마 설계, 리졸버, N+1 문제 방지용 DataLoader 등 새로운 개념 학습 필요 — 업계 통설상 **팀 인원 10명 미만**이면 도입 비용이 이점을 상회하는 경우가 많음(8.1절 기준 우리 팀 5~6명) |
| 우리 도메인의 실제 필요성 | 상품 목록/상세, 주문, 마이페이지 등 **중첩 관계가 깊지 않은 단순 CRUD** 위주라 오버페칭 문제가 크지 않음 | GraphQL의 핵심 이점(클라이언트가 필요한 필드만 선택, 복잡한 중첩 데이터 한 번에 조회)이 우리 화면 구성에서는 크게 체감되지 않음 |
| 결론 | **REST 유지** | 도입하지 않음(관리자 대시보드처럼 정말 복잡한 조회가 생기면, 그 화면 전용 REST 집계 엔드포인트 1~2개를 추가하는 것으로 충분히 해결 가능) |

> 참고로 우리는 이미 모노레포+TypeScript(8.3/8.21절)를 쓰고 있어, "GraphQL 없이도 프론트-백엔드 타입을 공유하고 싶다"는 요구는 **공유 타입 패키지**(8.17/8.21절에서 이미 언급된 방식)로 REST 안에서도 상당 부분 해결됩니다.

Sources:
- [REST vs GraphQL in Nest.JS: When to Use Which and Why?](https://medium.com/@rajalakshmi160403/rest-vs-graphql-in-nest-js-when-to-use-which-and-why-2462a2d50672)
- [Ecommerce REST vs GraphQL API: Which to Use When](https://blog.wcart.io/ecommerce-rest-vs-graphql-api/)
- [GraphQL vs REST API: Which Should You Use in 2026?](https://kanopylabs.com/blog/graphql-vs-rest-api)

### 8.27 검색 방식 기술 스택 (확정)

> 8.5절에서 FR-2.3(상품 검색)을 "단순 ILIKE로 충분, 카탈로그가 커지면 전문검색 재검토"라고 짧게 정했는데, 이번엔 **한국어 검색 품질**을 기준으로 더 깊게 검토했습니다. `ILIKE '%키워드%'`는 오타에 전혀 관대하지 않고(예: "해리포터"를 "해리포타"로 검색하면 결과 없음), 앞에 `%`가 붙는 패턴은 인덱스를 못 타 카탈로그가 커지면 느려집니다. **실제 구축이 아닌 결정 단계**입니다.

| 항목 | 검토 대상 | 결정 | 근거 |
|---|---|---|---|
| 검색 알고리즘 | 8.5절의 단순 `ILIKE` 유지 vs **PostgreSQL `pg_trgm`(트라이그램 유사도) 확장** vs 전용 검색엔진(Typesense/Meilisearch/Algolia) | **`pg_trgm` 확장 도입**으로 업그레이드 | `pg_trgm`은 Postgres 내장 확장이라 신규 인프라·벤더가 전혀 필요 없음(`CREATE EXTENSION`만 실행). 문자열을 3글자 단위로 쪼개 유사도를 계산해 **가벼운 오타에도 결과가 나오고**, GIN 인덱스를 태울 수 있어 `ILIKE`보다 빠름. 전용 검색엔진은 형태소 분석·오타교정이 더 정교하지만 별도 서버 운영이 필요해, 자사 직접 제작 전자책 위주의 소규모 카탈로그(9.2절 팀 규모 대비)에는 과설계 |
| 초성 검색 | 미지원 vs **지원**(계산된 컬럼) | `Product`에 `title_chosung` 컬럼을 추가해 상품 등록/수정 시 제목의 초성만 미리 추출해 저장, 검색어가 전부 초성(예: "ㅎㄹㅍㅌ")으로만 구성된 경우 이 컬럼과 매칭 | 국내 쇼핑몰 이용자에게 익숙한 검색 방식이며, `pg_trgm`이나 전용 검색엔진 모두 초성 검색을 기본 지원하지 않아 규모와 무관하게 별도 구현이 필요함. 별도 인프라 없이 컬럼 하나로 해결 가능 |
| 향후 전환 기준 | 정해두지 않음 vs **카탈로그 규모 기준선 설정** | **이 기준은 제목·설명 검색에만 적용된다**(8.59절 H-4) — 상품 수가 **약 3,000~5,000건**을 넘어서거나 `pg_trgm` 검색 성능/품질 불만이 누적되면 그때 Typesense 등 전용 검색엔진 도입을 재검토. **본문검색(8.31절)의 전환 기준은 상품 수가 아니라 총 청크 수·응답시간**이므로 별도로 관리한다 | 8.5절의 "커지면 재검토"를 구체적인 숫자로 못박아, 나중에 재검토 시점을 판단하기 쉽게 함. 자사 직접 제작 콘텐츠 특성상 수천 건 규모에 도달하는 데 상당한 시간이 걸릴 것으로 예상 |
| 엔드포인트 구성(8.62절 K-4) | 제목용·본문용 분리 vs **단일 `GET /products/search`** | **단일 엔드포인트로 통합**하고 응답에 `matchedIn`(`title`\|`description`\|`content`)·`searchedFields`를 담는다. 본문 매칭일 때만 `snippet`·`page` 포함(8.61절 J-5 형태), 같은 책이 제목·본문 양쪽에 매칭되면 **제목 매칭 우선으로 1건 병합** | 제목(`products`)과 본문(`product_text_chunks`)은 테이블이 다를 뿐 사용자에게는 하나의 검색이므로, 병합·중복제거를 프론트에 떠넘기지 않음. `searchedFields`가 있어야 **2자 검색으로 본문검색이 빠졌을 때**(8.59절 H-2) 프론트가 "본문검색은 3글자 이상부터"를 정확한 근거로 안내할 수 있음 — 없으면 사용자는 "이 책엔 그 내용이 없구나"로 오해함 |
| 정렬(판매순/신상품순, FR-2.3) | 검색 알고리즘과 결합해 복잡하게 처리 vs **검색 결과에 별도 ORDER BY 적용** | 유사도 검색으로 후보를 추린 뒤, 정렬 옵션(판매순=주문건수 집계, 신상품순=`created_at`)은 별도 `ORDER BY`로 적용 | 검색 관련성과 정렬 기준을 억지로 하나의 점수로 합치려 하면 복잡해짐 — "먼저 후보를 찾고, 사용자가 고른 기준으로 정렬"로 관심사를 분리 |

Sources:
- [PostgreSQL pg_trgm: The Extension That Lets You Search Like the Docs Do, Without Standing Up Elasticsearch](https://www.runxbuild.com/blog/postgresql-pg-trgm-extension/)
- [Postgres Can Do Typo-Tolerant Search. You Don't Need Elasticsearch Yet.](https://dev.to/chizee/postgres-can-do-typo-tolerant-search-you-dont-need-elasticsearch-yet-2k6)
- [PostgreSQL Full-Text Search vs Dedicated Search Engines](https://nomadz.pl/en/blog/postgres-full-text-search-or-meilisearch-vs-typesense)

### 8.28 API Rate Limiting 종합 정책 (확정)

> 지금까지 로그인(8.7/8.11)·다운로드(8.6/8.11)·쿠폰·주문생성(8.25)처럼 **민감한 엔드포인트만** 개별적으로 속도 제한을 정해왔고, 그 외 나머지 엔드포인트(상품 검색·리뷰 작성·문의 등록 등)에는 **전역 기본 제한이 전혀 없다**는 공백을 발견했습니다. 8.27절에서 추가한 검색 API도 아직 무방비 상태입니다. **실제 구축이 아닌 결정 단계**입니다.

**계층 구조**: 애플리케이션 레벨(NestJS `@nestjs/throttler`)과 엣지 레벨(AWS WAF)을 함께 두어 이중 방어합니다.

| 계층 | 항목 | 제안(추천) | 근거 |
|---|---|---|---|
| 애플리케이션(전역 기본) | 모든 엔드포인트 무제한 vs **전역 기본 속도 제한** | `@nestjs/throttler`를 **전역 가드(`APP_GUARD`)**로 등록, IP 기준 **분당 100회** 기본 적용(결정). 저장소는 **인메모리**(8.11절과 달리 정밀함이 필요 없는 baseline 방어라 ECS 태스크별로 다소 느슨해져도 무방). **실효 한도 = `분당 100회 × 태스크 수`**(8.51절 C-9: 태스크 2개면 실제로는 분당 200회까지 허용됨 — 의도된 트레이드오프이며, 정밀 제한이 필요한 엔드포인트는 아래 오버라이드에서 Postgres 저장소를 사용) | 지금까지 명시적으로 다루지 않은 모든 엔드포인트(상품 조회, 리뷰, 문의 등)에 최소한의 안전망을 깔아둠. 정밀한 방어가 필요한 엔드포인트는 아래처럼 개별 오버라이드로 이미 해결되어 있어, 전역치까지 Postgres에 보낼 필요는 없음 |
| 애플리케이션(엔드포인트별 오버라이드) | — | 로그인·비밀번호재설정(8.7/8.11: 분당5, Postgres) / 다운로드 URL 발급(8.6/8.11: 분당3, Postgres) / 쿠폰 검증(8.25: 분당10, Postgres) / 주문 생성(8.25: 5초 Advisory Lock) / 관리자 업로드(8.11: 기본 인메모리) / **검색(8.27, 신규): 분당 30회, 인메모리** + **검색어 길이 상한 50자**(8.61절 J-4) + **`%`·`_`·`\` 이스케이프 후 `ESCAPE ''` 명시**(8.62절 K-2) / **결제 웹훅 수신(8.8/8.71절 T-3): 재조회를 수반하는 요청에만 속도 제한**(8.74절 W-2) — 서명이 없어 엔드포인트가 완전히 공개이므로 가짜 `paymentKey`로 **재조회 호출을 유발하는 DoS 표면**이 된다. 다만 **전체 요청에 제한을 걸면 안 된다** — T-1이 **의도적으로 5xx를 반환해 유도한 토스 재시도**가 장애 시 몰려와 **우리가 설계한 복구 경로를 스스로 막기** 때문이다. 아래 싼 필터를 통과한 건(=실제 재조회가 필요한 건)에만 제한을 걸면 정상 재시도는 닿지 않고 가짜 `paymentKey` 폭주만 걸린다. 토스가 공개하는 발신 IP가 있으면 제외 목록에 추가한다. 재조회 전에 싸게 거르는 순서를 함께 고정한다: ① 페이로드의 `orderId`가 우리 DB에 없으면 **재조회 없이 200**, ② 8.9절 `payment_webhook_events`에 이미 본 이벤트면 **재조회 없이 200**, ③ 그 외에만 재조회 / **SSR 릴레이 엔드포인트(8.53/8.69절 R-6): IP가 아니라 공유 시크릿 단위로 제한**(정상 호출자는 Vercel 함수뿐이며 트래픽이 페이지뷰에 비례), 시크릿 불일치는 **즉시 401 + 별도 카운터**로 집계해 8.24절 Sentry 알림에 연결 | `@Throttle()` 데코레이터로 전역 기본값을 각 엔드포인트 특성에 맞게 오버라이드. 검색은 `pg_trgm` 유사도 계산이 단순 PK 조회보다 DB 부하가 커서 전역 기본(분당100)보다 타이트하게 설정. **와일드카드 이스케이프는 속도 제한과 별개로 반드시 필요**(8.62절 K-2) — `q=%` 하나로 모든 청크가 매칭되어 8.59절이 없앤 전수 스캔이 되살아나며, Prisma `$queryRaw` 태그드 템플릿은 SQL 인젝션만 막고 LIKE 와일드카드는 막지 못함 |
| 엣지(ALB 앞단) | 미도입 vs **AWS WAF(Rate-based rule + 관리형 공통 룰셋)** | **도입** — ALB 앞에 WAF Web ACL 추가 — 5분간 동일 IP 임계치 초과 시 자동 차단 + SQLi/XSS 등 일반 웹 공격 패턴을 막는 AWS 관리형 룰셋 적용(월 약 $13~30) | 애플리케이션 레벨 제한은 요청이 이미 ECS 태스크까지 도달한 뒤에 거부하므로 CPU·커넥션을 일부 소모함. WAF는 요청이 우리 서버에 닿기 전에 엣지에서 차단해 대규모 플러딩에 더 안전하고, 비용도 우리 규모에서 감당 가능한 수준 |
| 네트워크(L3/L4) | 별도 조치 | 조치 불필요 | **AWS Shield Standard**가 모든 AWS 리소스에 무료로 자동 적용되어 SYN flood 등 네트워크 계층 DDoS는 기본 방어됨(별도 설정 없음) |
| 초과 시 응답 규격 | 임의 에러 메시지 vs **표준 `429` + `Retry-After`** | 모든 속도 제한 초과 응답을 `429 Too Many Requests`와 `Retry-After` 헤더로 통일 | 프론트가 재시도 타이밍을 일관되게 처리할 수 있고, 표준 HTTP 시맨틱을 따름 |

Sources:
- [How to understand and estimate combined AWS WAF and AWS Shield Advanced costs](https://aws.amazon.com/blogs/aws-cloud-financial-management/how-to-estimate-your-aws-waf-and-aws-shield-advanced-costs/)
- [AWS WAF Pricing: Web Application Firewall Costs](https://wring.co/blog/aws-waf-pricing-guide)

### 8.29 샘플 PDF 미리보기 뷰어 기술 스택 (확정)

> FR-2.4에 "샘플 PDF 뷰어"라는 문구는 있었지만, **브라우저에서 실제로 어떻게 보여줄지**는 정해진 적이 없었습니다. 참고로 이 뷰어는 **샘플 PDF 미리보기 전용**입니다 — 구매한 전체 PDF는 8.4/8.10절 결정대로 계속 "다운로드" 방식이며, 인앱 뷰어로 읽는 기능은 이번 범위가 아닙니다. **실제 구축이 아닌 결정 단계**입니다.

| 항목 | 검토 대상 | 제안(추천) | 근거 |
|---|---|---|---|
| 뷰어 구현 방식 | 브라우저 네이티브 `<iframe>`/`<embed>` vs **`react-pdf`(PDF.js 래퍼)** | **`react-pdf`** | 네이티브 뷰어는 브라우저마다 UI가 다르고 모바일에서 레이아웃이 일관되지 않음(6절 반응형 요구사항과 상충). `react-pdf`는 캔버스에 직접 렌더링해 데스크톱/모바일에서 동일한 UI(페이지 네비게이션, 확대 등)를 제공하고 사이트 디자인과 통일감 있게 커스터마이징 가능 |
| SSR 처리 | 일반 컴포넌트처럼 사용 vs `next/dynamic`의 `ssr: false` vs **`'use client'` 래퍼 안에서 `ssr: false`** | **(8.64절 M-1)** App Router는 **Server Component 안에서 `ssr: false`를 허용하지 않는다**(`ssr: false is not allowed with next/dynamic in Server Components`). 이 뷰어가 놓일 상품 상세 페이지는 8.43절 `generateMetadata`를 쓰는 **Server Component**라 정확히 이 조건에 걸린다. 따라서 **`'use client'`를 선언한 래퍼 컴포넌트(`PdfViewerLoader.tsx`)를 한 겹 두고 그 안에서 `dynamic(..., { ssr: false })`를 호출**하며, 상품 상세 페이지는 이 래퍼만 import한다(미리보기 버튼 자체가 클라이언트 컴포넌트여야 하므로 아래 "로딩 시점" 결정과도 맞물림). `react-pdf`는 캔버스 등 브라우저 API에 의존해 서버에서 렌더링할 수 없으므로, `dynamic(() => import('./SampleViewer'), { ssr: false })`로 클라이언트 전용 로드 | Next.js SSR 환경에서 `react-pdf`를 그냥 쓰면 하이드레이션 에러가 발생하는 것으로 알려진 표준 제약 — 우리도 동일하게 적용해야 함 |
| 로딩 시점 | 상품 상세 페이지 진입 즉시 로드 vs **"미리보기" 버튼 클릭 시 지연 로드** | 사용자가 미리보기 버튼을 눌렀을 때만 `react-pdf`(및 내부 `pdfjs-dist` 워커)를 동적 import | `react-pdf`/`pdfjs-dist` 번들이 가벼운 편이 아니라서, 상품 상세 페이지를 열 때마다 무조건 불러오면 페이지 로드 성능(6절 2초 이내 목표)에 불리함. 실제로 미리보기를 여는 사용자에게만 비용을 지불시킴 |
| 다운로드 버튼 | 뷰어 내 다운로드 숨김 vs **뷰어 UI에 다운로드 버튼 노출** | `react-pdf` 커스텀 툴바에 8.10절 공개 URL로 연결되는 다운로드 버튼 포함 | 샘플 PDF는 8.10절에서 이미 공개 마케팅 자료로 결정되어 자유롭게 내려받을 수 있어야 함 — 뷰어가 오히려 다운로드를 막을 이유가 없음 |
| 워커 파일 호스팅(8.64절 M-4/M-5) | react-pdf의 전이 의존성에 기대기 vs CDN 참조 vs **직접 의존성 + 자체 호스팅** | `pdfjs-dist`를 **앱의 직접 의존성으로 명시**하고 워커 파일을 **`postinstall` 스크립트로 `public/`에 복사**해 자체 호스팅한다. `workerSrc`는 **react-pdf 컴포넌트를 사용하는 바로 그 모듈에서 설정**한다 | 8.36절에서 택한 **pnpm은 엄격한 `node_modules` 구조**라 전이 의존성 경로(`pdfjs-dist/build/pdf.worker.min.mjs`)가 앱에서 해석되지 않는다. CDN 참조는 외부 의존과 **버전 드리프트**를 동시에 안아 "The API version does not match the Worker version" 실패를 부른다. `postinstall` 자동 복사면 `pdfjs-dist` 업그레이드 시 워커 버전이 저절로 따라온다. `workerSrc`를 별도 파일에 두면 **모듈 실행 순서 때문에 기본값에 덮어써지는** 함정도 있어 사용처와 같은 모듈에 둔다 |
| 미지원 브라우저 대응(8.64절 M-7) | 폴리필 추가 vs **그레이스풀 디그레이드** | 미리보기 실패 시 **"브라우저를 업데이트해 주세요" 안내 + 샘플 PDF 직접 다운로드 링크**로 대체한다 | 최신 pdf.js는 `Promise.withResolvers`를 사용해 구형 환경에서 통째로 실패한다. 다만 미리보기는 **핵심 구매 동선이 아니므로**(샘플 전용) 폴리필로 번들을 키우기보다 대체 경로를 주는 편이 낫다 — 8.16절 웹 푸시에서 쓴 것과 같은 방식 |
| 워터마크 | 샘플에도 워터마크 적용 여부 재확인 | **불필요** — 8.10절 결정 유지 | 8.10절의 워터마킹 논의는 유료 원본 PDF(비공개) 보호 목적이었고, 샘플은 애초에 자유배포용 마케팅 자료라 보호할 대상이 아님 |

Sources:
- [How to Build a React PDF Viewer for Next.js in Minutes](https://dev.to/9haroon/how-to-build-a-react-pdf-viewer-for-nextjs-in-minutes-3pj)
- [react-pdf - npm](https://www.npmjs.com/package/react-pdf)
- [Next.js PDF viewer with React-PDF and Nutrient SDK](https://www.nutrient.io/blog/how-to-build-a-nextjs-pdf-viewer/)

### 8.30 암호화 키/시크릿 로테이션 정책 (확정)

> 지금까지 여러 절에서 키·시크릿을 "AWS Secrets Manager에 보관"이라고만 정했지, **주기적으로 어떻게 교체할지**는 정한 적이 없습니다. 이번에 지금까지 등장한 모든 키를 모아 인벤토리를 만들고 로테이션 정책을 검토했습니다. **실제 구축이 아닌 결정 단계**입니다.

**키 인벤토리**

| 키/시크릿 | 용도 | 로테이션 제안 |
|---|---|---|
| JWT Access Token 서명 시크릿(8.7절) | Access Token 서명(HS256) | **90일 주기 교체**. Access Token이 최대 30분 단명이므로 RS256+`kid` 다중키 같은 복잡한 구조 없이 단일 시크릿 교체로 충분 — 교체 직후 잠깐 검증 실패하는 요청은 DB 기반 Refresh Token(8.7절)으로 자동 재발급됨 |
| MFA TOTP secret 암호화 키(8.19절) | 관리자 TOTP secret 암호화(AES-256-GCM) | **AWS KMS로 전환** 후 KMS의 자동 연간 로테이션 활성화(아래 상세) |
| 토스페이먼츠 시크릿 키(8.8절) | 결제 승인/취소 API | 벤더 콘솔에서 재발급 → Secrets Manager 갱신 → ECS 롤링 재시작. **연 1회** 또는 유출 의심 시 즉시 |
| 솔라피 API 키(8.14절) | SMS 발송 | 위와 동일 절차, **연 1회** |
| Resend API 키(8.13절) | 이메일 발송 | 위와 동일 절차, **연 1회** |
| Firebase 서비스 계정 키(8.16절) | FCM 발송 | 위와 동일 절차, **연 1회** |
| DB 비밀번호 — `cbmall_app`(8.15절) | 런타임 DB 연결 | `ALTER ROLE`로 교체 → Secrets Manager 갱신 → ECS 롤링 재시작. **분기별 수동 교체**(자동화는 MVP 이후 검토) |

| 항목 | 검토 대상 | 제안(추천) | 근거 |
|---|---|---|---|
| MFA secret 암호화 방식 | 현재처럼 애플리케이션이 직접 AES-256-GCM 키를 들고 암복호화 vs **AWS KMS 봉투암호화(envelope encryption)로 전환** | 8.19절의 "Secrets Manager 평문 대칭키 + 앱이 직접 암복호화"를 **KMS CMK**로 교체 | KMS는 CMK를 로테이션해도 **기존 암호화된 데이터를 재암호화할 필요가 없고 애플리케이션 코드 변경도 불필요**함 — KMS가 과거 키 버전을 자동 보관해 복호화 시 알아서 올바른 버전을 사용함. 반면 현재 방식(원문 키 직접 보관)은 키를 바꿀 때마다 모든 관리자의 `mfaSecretEnc`를 수동으로 재암호화해야 해서 "로테이션"이 사실상 불가능한 구조였음 |
| 다운타임 없는 교체 | 전체 재시작 vs **ECS 롤링 배포 활용** | DB 비밀번호·벤더 API 키 교체 시 8.11절에서 이미 확보한 **2개 이상 태스크**를 한 번에 하나씩 순차 재시작 | 신규 인프라 없이 이미 가용성 목적으로 갖춰둔 다중 태스크 구조를 키 로테이션에도 재사용 가능 |
| 유출 의심 시 대응 | 정기 주기까지 대기 vs **즉시 로테이션 절차 마련** | 8.20절 감사 로그에서 이상 징후 발견 시: ① 해당 키 즉시 재발급/교체, ② 영향받은 사용자의 Refresh Token 전체 무효화(강제 재로그인), ③ Sentry/관리자에게 알림 | 정기 로테이션만으로는 실제 유출 사고에 대응 속도가 너무 느림 — 8.7절 Refresh Token 회전/무효화 메커니즘을 그대로 활용해 즉시 세션을 끊을 수 있음 |

Sources:
- [Rotate AWS KMS keys - AWS Key Management Service](https://docs.aws.amazon.com/kms/latest/developerguide/rotate-keys.html)
- [AWS KMS Envelope Encryption Deep Dive](https://tricksumo.com/aws-kms-envelope-encryption-deep-dive/)

### 8.31 PDF 텍스트 추출/검색 인덱싱 (확정)

> **범위 확인**: 원본 PDF 전체 텍스트를 색인하는 전문검색으로 확정하셨습니다. 이는 8.27절의 "상품 제목" 검색을 넘어 **책 본문 전체**를 검색 대상으로 삼는 것이라, 8.10절에서 "원본 PDF는 결제 전 비공개"로 정한 콘텐츠 보호 원칙과 정면으로 만나는 지점입니다. 그래서 **검색 결과에 본문을 얼마나 노출할지**가 이번 검토의 핵심 결정 사항입니다. **실제 구축이 아닌 결정 단계**입니다.

| 항목 | 검토 대상 | 제안(추천) | 근거 |
|---|---|---|---|
| 텍스트 추출 라이브러리 | 직접 파서 구현 vs **`pdf-parse`** | `pdf-parse`(TypeScript, Node/브라우저 겸용, 텍스트·메타데이터 추출) | 표준 텍스트 레이어가 있는 PDF(자사 직접 제작 전자책, 워드/인디자인 등에서 export되므로 대부분 텍스트 기반일 것으로 예상)에서 안정적으로 동작하는 활발히 유지보수되는 라이브러리. 스캔 이미지 기반 PDF(OCR 필요)는 이번 범위에서 제외 — 자사 제작 콘텐츠 특성상 해당 사례가 드물 것으로 예상되며, 필요시 실패 건만 예외 처리 |
| 추출 시점 | 업로드 API 응답을 블로킹하고 동기 처리 vs **비동기 이벤트 기반 처리** | 관리자가 원본 PDF 업로드 완료 시 `product.pdf_uploaded` 이벤트를 발행(8.17/8.21절 패턴), 별도 리스너가 백그라운드에서 텍스트 추출 후 결과를 저장. **추출은 페이지 단위로 수행한다(8.60절 I-5)** — `pdf-parse` v2의 `getText({ partial: [n] })`로 페이지를 순회하되 **`PDFParse` 인스턴스는 한 번만 생성해 재사용**하고(페이지마다 새로 로드하면 600쪽 책에서 600번 파싱) 종료 시 **`destroy()`** 로 메모리 해제. 페이지 텍스트를 이어붙이며 **각 페이지의 시작 오프셋을 기록**해두면 청크가 걸치는 페이지 범위(`page_from`/`page_to`)가 그대로 계산됨 | 대용량 PDF(최대 100MB, 8.10절) 추출은 수 초~수십 초 걸릴 수 있어 업로드 응답을 블로킹하면 관리자 UX가 나빠짐. 8.17절에서 이미 도입한 이벤트 기반 패턴을 재사용해 신규 인프라 없이 해결 |
| 인덱싱 방식 | `similarity()` 기반 유사도 검색 vs `tsvector` 전문검색 vs **`pg_trgm` GIN으로 가속한 `ILIKE` 부분문자열 검색** | **`pg_trgm` GIN 인덱스 + `ILIKE '%검색어%'`**, 스니펫은 **애플리케이션에서 매칭 위치 ±15~20자를 잘라 생성** | **(8.51절 C-3 → 8.54절 재수정)** ① `similarity()`는 두 문자열 **전체**의 트라이그램 비율이라 수십만 자 본문에서는 값이 0에 수렴해 사용 불가(문제는 확장이 아니라 **연산자 선택**이었음). ② 대체안이던 `tsvector`(`simple` 설정)는 **공백 단위 토큰화라 한국어 조사가 붙으면 매칭 실패**("해리포터"로 "해리포터를"을 찾지 못함). 게다가 품질 이전에 **동작 보장 자체가 안 된다** — `to_tsvector` 결과가 **1,048,575바이트를 넘으면 `string is too long for tsvector` 에러**가 나는데, 한국어는 조사 탓에 고유 렉심이 폭증해("데이터/데이터를/데이터가/데이터의"가 전부 별개) 300쪽급 전자책 한 권이 이 한도에 걸릴 수 있고 그 순간 추출 작업이 실패한다(8.59절 H-3). pg_trgm GIN에는 이런 행 단위 크기 한도가 없다. ③ 부분문자열 매칭은 조사가 붙어도 원형을 포함하므로 한국어에 오히려 적합하고, pg_trgm GIN 인덱스가 `ILIKE`를 가속해줌. **한계**: 오타 허용·관련도 순위는 포기. **전환 재검토 기준은 8.59절 H-4에서 상품 수가 아닌 '총 청크 50만 행' 또는 '검색 p95 1초'로 교체**(본문검색의 병목은 상품 수가 아니라 총 본문 분량) |
| **색인 단위(8.59절 H-1)** | 책 1권 = `product_full_texts` 1행에 GIN vs **청크 단위 다중 행에 GIN** | **`product_text_chunks` 테이블 신설**(1권 N행, **청크당 목표 1,000자**(8.60절 I-7) + `page_from`/`page_to`/`chunk_index`)하고 **GIN 인덱스를 청크 컬럼에** 건다. `product_full_texts`는 추출 상태·재시도 관리용으로 유지 | pg_trgm GIN은 **후보 행만** 돌려주고 Postgres가 실제 `ILIKE`를 **재검사(recheck)** 한다. 1권 1행 구조에서는 "데이터"·"있습니" 같은 흔한 트라이그램이 **거의 모든 책에 존재**해 후보 = 전체 행이 되고, 1MB 이상인 본문을 전부 TOAST에서 꺼내 압축 해제한 뒤 재검사하게 되어 **인덱스를 탔는데 순차 스캔보다 느려진다**. 청크로 쪼개면 ① 재검사·TOAST 비용이 사라지고 ② 트라이그램 선택도가 회복되며 ③ 스니펫을 청크 내 오프셋으로 생성하고 ④ 덤으로 검색 결과에 **"이 책 N쪽"** 을 표시할 수 있다. 200권 × 약 300청크 = 6만 행으로 규모 부담 없음 |
| **최소 검색어 길이(8.59절 H-2)** | 제한 없음 vs **본문검색은 3자 이상만** | **2자 이하 검색어에는 본문검색 쿼리를 아예 발행하지 않고** 8.27절의 제목·설명·초성 검색만 수행. UI에 "본문검색은 3글자 이상부터 지원합니다" 안내 | `ILIKE '%검색어%'`에서 와일드카드 사이 글자가 3자 미만이면 **추출 가능한 트라이그램이 없어** 공식 문서 표현대로 *full-index scan* 으로 퇴화한다(GIN 전체를 훑고 후보를 전부 재검사하므로 순차 스캔보다 나쁨). 한국어 검색어는 "주식·요리·영어·수학·투자·육아"처럼 **2글자가 매우 흔해** 방치하면 상시 발생하는 병목이 된다. 제목·설명 컬럼은 짧아 순차 스캔이 저렴하므로 2자 검색은 그쪽으로 넘긴다 |
| 추출 실패 처리 | 상품 등록 자체를 차단 vs **실패해도 등록은 허용, 본문검색만 미지원** | 텍스트 추출이 실패(암호화된 PDF, 손상 파일 등)해도 상품 판매에는 영향 없음 — 제목 검색(8.27절)은 계속 동작, Sentry로 관리자에게만 알림 | 검색 품질 저하일 뿐 핵심 판매 기능 장애가 아니므로, 실패를 이유로 상품 등록 자체를 막을 이유가 없음 |
| **검색 결과 본문 노출 범위(핵심 결정)** | ① 매칭 여부만 표시 vs ② **짧은 스니펫(앞뒤 15~20자) 노출** vs ③ 전체 문단 노출 | **② 짧은 스니펫 노출**로 확정 — 검색어 앞뒤 15~20자만 하이라이트해 보여주고, 전체 본문 텍스트는 어떤 API 응답에도 절대 포함하지 않음 | ①은 너무 보수적이라 사용자가 왜 이 책이 검색됐는지 알 수 없어 UX가 나쁘고, ③은 사실상 유료 콘텐츠를 무료로 읽게 해주는 것과 다름없어 8.10절의 보호 원칙을 무력화함. ②는 구글 도서 미리보기와 유사한 절충점으로, "이 단어가 이 책에 나온다"는 확인 + 구매 유도 효과는 유지하면서 실질적 콘�一츠 대체 열람은 막음 |
| 스니펫 스크래이핑 방지 | 별도 조치 없음 vs 속도 제한만 vs **속도 제한 + 검색어 길이 상한 + 응답 정보 최소화** | **(8.61절 J-4)** ① **검색어 길이 상한 50자**를 서버에서 강제(초과 시 400) — 1회 요청으로 얻는 **신규 정보를 최대 20자로 고정**하는 핵심 방어, ② 스니펫 길이·페이지당 결과 수(20건)를 **서버 상수로 고정**하고 클라이언트 파라미터로 받지 않음, ③ **매칭 횟수("이 책에 N번 등장")를 응답에 넣지 않음** — 그 자체가 본문에 대한 정보, ④ 8.28절 검색 속도 제한(분당 30회) 유지 | 짧은 스니펫이라도 다양한 검색어로 반복 조회해 조각조각 이어붙이면 상당 분량이 재구성될 위험이 있음. **속도 제한만으로는 부족하다** — 스니펫 길이 = 검색어 길이 + 앞뒤 약 40자이므로, 이미 확보한 문장 끝부분을 검색어로 넣어 그 다음 20자를 얻는 식으로 순차 재구성이 가능하다. 검색어 상한이 있어야 1회당 획득량이 고정되어 속도 제한이 실제로 의미를 갖는다 |
| 원문 저장 데이터의 접근 통제 | 일반 API로 전체 본문 컬럼 조회 가능 vs **애플리케이션 레벨에서 전체 본문 반환 금지 + RLS 적용** | 추출된 전체 텍스트가 담긴 컬럼/테이블은 검색(스니펫 생성) 용도로만 내부에서 사용하고, 어떤 API 응답 DTO에도 전체 필드를 포함하지 않도록 코드 컨벤션으로 강제하고, **더 나아가 검색 쿼리가 `SUBSTRING`으로 스니펫만 돌려주게 해 애플리케이션이 본문 전체를 손에 쥘 일 자체를 없앤다**(8.61절 J-1 — 컨벤션은 `select: { content: true }` 한 줄이면 뚫리므로 쿼리 레벨의 구조적 보장으로 격상). 8.6절과 동일하게 RLS도 적용. **이 원칙은 `product_full_texts.extracted_text`뿐 아니라 8.59절에서 신설한 `product_text_chunks.content`에도 똑같이 적용된다** — 청크도 본문의 일부이므로 목록·상세 API가 청크를 통째로 반환하지 않도록 주의(반환 가능한 것은 매칭 위치 ±15~20자 스니펫, PDF 기준 페이지, 매칭 오프셋뿐) | 사실상 책 전체 사본이 DB에 존재하게 되므로, "검색에만 쓰고 절대 외부로 통째로 내보내지 않는다"는 원칙을 코드 레벨에서 명확히 해야 함 |

Sources:
- [pdf-parse - Pure JavaScript PDF Parser for Node.js and Browser](https://mehmet-kozan.github.io/pdf-parse/)
- [Parsing PDFs in Node.js - LogRocket Blog](https://blog.logrocket.com/parsing-pdfs-node-js/)

### 8.32 PDF 텍스트 추출 실패율 모니터링/알림 (확정)

> 8.31절은 "실패하면 Sentry로 개별 알림"까지만 정했는데, 점검해보니 세 가지가 빠져 있었습니다: ① 일시적 오류(S3 지연 등)로 실패해도 **재시도가 전혀 없어** 바로 영구 실패 처리됨, ② 이벤트 리스너가 죽는 등의 이유로 `PENDING` 상태에 **영원히 멈춰있는 건을 감지할 방법이 없음**, ③ 개별 알림만 있고 "최근 실패율이 갑자기 치솟았는지"(라이브러리 버그 등 시스템적 문제의 신호) 같은 **집계 관점의 모니터링이 없음**. **실제 구축이 아닌 결정 단계**입니다.

| 항목 | 검토 대상 | 제안(추천) | 근거 |
|---|---|---|---|
| 추출 결과 저장 단위(8.59절 H-1) | `product_full_texts.extracted_text` 한 덩어리 vs **`product_text_chunks` 다중 행** | 추출 성공 시 본문을 **정규화(8.60절 I-1)한 뒤 목표 1,000자 청크로 분할해 `product_text_chunks`에 저장**. **재추출은 upsert가 아니라 `DELETE FROM product_text_chunks WHERE product_id = ?` → `createMany()` 통째 교체**(8.60절 I-4: 새 청크 수가 줄면 옛 청크가 남아 삭제된 본문이 계속 검색됨). `product_full_texts`는 상태·재시도·클레임 관리용으로 유지 | 검색 성능 때문에 색인 단위를 바꿨으므로(8.59절 H-1) 추출 파이프라인의 최종 산출물도 청크여야 함. 청크 경계는 페이지 경계를 우선 따라 `page_from`/`page_to`를 채워 검색 결과에 쪽수를 표시한다 |
| 일시적 오류 재시도 | 1회 실패 시 즉시 영구 FAILED vs **자동 재시도(최대 2회)** | 추출 실패 시 8.12절 Cron 패턴으로 **최대 2회** 재시도(예: 10분 간격) 후에도 실패하면 최종 `FAILED` 처리 | S3 일시 장애, 네트워크 타임아웃처럼 재시도하면 성공할 수 있는 원인과, PDF 자체가 손상된 영구적 실패 원인을 구분해줌. 8.18절의 알림 재시도와 동일한 사고방식 |
| `PENDING` 정체(stuck) 감지 | 방치 vs **오래된 PENDING 자동 전환+알림** | 8.12절 Cron에 작업 추가: **1시간 이상** `PENDING`으로 남아있는 건은 `FAILED`로 전환하고 Sentry에 "정체 감지" 이벤트 발송 | 이벤트 리스너(8.31절 `product.pdf_uploaded`)가 배포 중 재시작되며 유실되는 등의 이유로 영원히 처리되지 않는 건이 조용히 쌓일 수 있음 — 이 자체가 하나의 장애 신호 |
| 집계 실패율 알림 | 개별 실패만 Sentry 알림 vs **일 1회 실패율 집계 + 임계치 알림** | 8.12절 Cron 패턴으로 매일 1회, 최근 24시간 내 처리 건 중 실패 비율을 계산해 **20% 초과 시** Sentry에 별도 "집계 경고" 이벤트 발송(개별 실패 알림과는 별개) | 하루 1~2건의 개별 실패는 "그 파일만의 문제"로 넘기기 쉽지만, 실패율이 급등하면 `pdf-parse` 라이브러리 이슈나 PDF 제작 도구 변경처럼 **여러 파일에 영향을 주는 시스템적 문제**일 가능성이 높음 — 개별 알림으로는 이 패턴이 잘 안 보임 |
| 운영 가시성 | Sentry 알림에만 의존 vs **관리자 대시보드에 위젯 추가(FR-7.1 확장)** | FR-7.1 대시보드에 "본문 추출 대기/실패 건수" 위젯을 추가해, 관리자가 사고 발생 여부와 무관하게 상시 확인 가능하게 함 | Sentry는 "사고가 터졌을 때" 반응하는 채널이고, 대시보드는 "지금 상태가 정상인가"를 언제든 확인할 수 있는 상시 채널 — 8.24절에서 정한 Sentry(에러 반응)와 CloudWatch(원시 로그) 역할 분리와 같은 맥락으로 대시보드가 "비즈니스 상태 요약" 역할을 추가로 담당 |

Sources: (내부 정책 점검 — 8.12/8.18/8.24/8.31절 기존 결정 재검토)

### 8.33 PDF 텍스트 추출 처리량/성능 부하 (확정)

> 점검해보니 8.31절의 "비동기 이벤트 기반 처리"는 **작업을 API 응답과 분리했을 뿐, 실제로는 여전히 같은 NestJS 프로세스 안에서 실행**됩니다. PDF 파싱은 CPU를 많이 쓰는 작업인데, Node.js는 싱글 스레드 이벤트 루프라서 큰 PDF 하나를 파싱하는 동안 **그 태스크가 처리 중인 로그인·결제·다운로드 등 다른 모든 요청이 함께 멈출 수 있습니다.** 8.31절에서 "비동기"라고 부른 것은 "API 응답을 막지 않는다"는 뜻이었지, "메인 스레드를 막지 않는다"는 뜻이 아니었다는 게 이번에 드러난 구멍입니다. **실제 구축이 아닌 결정 단계**입니다.

| 항목 | 검토 대상 | 제안(추천) | 근거 |
|---|---|---|---|
| 실행 위치 | 이벤트 리스너 안에서 메인 스레드로 직접 실행 vs **Node.js `worker_threads`로 분리** | `piscina` 워커 풀 라이브러리로 **PDF 파싱 + 본문 정규화 + 청크 분할까지 일괄**(`pdf-parse` 호출 포함)을 **별도 스레드**에서 실행하고, 메인 스레드는 **워커가 돌려준 청크 배열을 DB에 쓰는 일만** 담당 | PDF/문서 렌더링·파싱은 worker_threads의 대표적인 사용 사례로 꼽힘. **(8.60절 I-8)** 수십만 자 문자열에 정규식을 여러 번 돌리는 정규화·청킹도 파싱과 똑같이 CPU 바운드라, 이것만 메인 스레드에 남기면 이 절이 막으려던 이벤트 루프 블로킹이 그대로 재발함 — 그래서 워커의 책임 범위를 청크 배열 반환까지로 넓힘(반환값 최대 1.8MB 수준이라 구조화 복제 비용은 수십 ms로 무시 가능). 워커는 자체 V8 인스턴스·이벤트 루프를 가져 메인 스레드(HTTP 요청 처리)를 전혀 막지 않음. Node.js 표준 모듈+경량 라이브러리 하나로 해결되어 신규 인프라(별도 서비스·큐 등) 불필요 |
| 워커 풀 크기 | 호스트 코어 수 기준 설정 vs **ECS Fargate 태스크의 vCPU 할당량 기준 설정** | 태스크에 할당된 vCPU 수보다 적게(예: 1vCPU 태스크라면 `maxThreads: 1`) 설정해 이벤트 루프·GC용 여유를 남김 | 컨테이너 환경에서는 호스트 전체 코어 수가 아니라 **컨테이너에 할당된 CPU 쿼터**를 기준으로 워커 수를 정해야 한다는 것이 업계 통설 — 과도한 워커 수는 오히려 컨텍스트 스위칭 비용만 늘림 |
| 타임아웃 | 무제한 대기 vs **작업당 타임아웃(예: 60초) 설정** | 워커 작업이 60초를 넘기면 강제 종료하고 8.32절의 재시도/실패 처리로 넘김 | 손상된 PDF가 파서를 무한루프에 빠뜨리는 등의 상황에서 워커가 영원히 자원을 점유하는 것을 방지 |
| 동시 처리 개수 제어 | 업로드되는 대로 무제한 동시 실행 vs **워커 풀이 자연스럽게 큐잉** | `piscina`가 대기열을 자동으로 관리해, 워커 풀 크기를 넘는 요청은 자동으로 대기했다가 순차 처리 | 8.1절 기준 관리자가 5~6명뿐이라 대량 동시 업로드 가능성은 낮지만, 워커 풀 자체가 큐 역할을 겸해 별도 동시성 제어 로직을 직접 짤 필요가 없음 |
| ECS 태스크 리소스 | 1 vCPU로 시작 vs **2 vCPU / 4GB로 산정** | **태스크당 2 vCPU / 4GB**로 시작하고 `piscina`의 `maxThreads: 1` 유지 → PDF 파싱 워커가 한 코어를 쓰는 동안 **나머지 한 코어가 API 요청 처리와 GC를 담당** | **(8.51절 C-7 반영)** 워커 스레드는 이벤트 루프 블로킹만 막을 뿐 CPU 총량을 늘려주지 않으므로, 1 vCPU 태스크에서는 PDF 파싱 중 같은 코어를 두고 경합해 API 응답이 느려짐. 또한 워커는 같은 컨테이너 메모리를 공유하므로 대용량 PDF 파싱 시의 메모리 스파이크까지 감안해 4GB로 여유를 둠 |

Sources:
- [Worker Threads in Node.js: When and How to Use Them for CPU-Intensive Tasks (2026)](https://dev.to/young_gao/worker-threads-in-nodejs-when-and-how-to-use-them-2jdm)
- [GitHub - piscinajs/piscina: A fast, efficient Node.js Worker Thread Pool implementation](https://github.com/piscinajs/piscina)
- [Node.js Worker Pools with Piscina in 2026: CPU at Scale](https://www.hirenodejs.com/blog/nodejs-piscina-worker-pools-2026)

### 8.34 개발-개발검토-운영 환경 구성 (확정)

> 지금까지 8.8/8.15/8.30절 등에서 "테스트 키/운영 키 분리"를 산발적으로 언급했지만, **환경이 정확히 몇 단계이고 각 벤더(Vercel/Supabase/ECS/토스 등)가 환경별로 어떻게 분리되는지** 전체 그림을 그린 적이 없었습니다. **실제 구축이 아닌 결정 단계**입니다.

| 항목 | 검토 대상 | 제안(추천) | 근거 |
|---|---|---|---|
| 환경 단계 | 2단계(개발/운영) vs **3단계(PR 프리뷰 → 개발검토(스테이징) → 운영)** | **PR 프리뷰(프론트엔드만 격리, 백엔드·DB는 스테이징 공유 — 8.56절)** → **스테이징(상시)** → **운영** | 9.1절 마일스톤(M5, QA·결제 실연동 검증 2주)이 실제로 진행될 "운영과 유사하지만 진짜 운영은 아닌" 공간이 필요함. 2단계로는 이 검증 공간이 없어 운영에서 처음 결제 테스트를 하게 되는 위험이 생김 |
| DB(Supabase) | 프로젝트 하나를 스키마로 구분 vs 환경별 별도 프로젝트 + PR별 임시 브랜치 vs **스테이징·운영 프로젝트 2개만** | **스테이징·운영 Supabase 프로젝트 2개만** 운영하고, **PR 프리뷰는 스테이징 DB를 공유**(Supabase Branching 미사용) | **(8.56절)** 당초 PR별 임시 브랜치를 쓰려 했으나, ① Supabase 브랜칭은 **Supabase CLI 마이그레이션 기반**이라 Prisma를 쓰는 우리 스키마가 브랜치에 생성되지 않고, ② 백엔드가 Vercel이 아닌 ECS라 **단일 스테이징 서비스가 PR별 DB를 바라볼 수 없음**. 운영 데이터 격리(스테이징≠운영)라는 핵심 목적은 프로젝트 2개로 충분히 달성됨 |
| 프론트(Vercel) | 단일 배포 vs **Git 브랜치 기반 3단계 자동 매핑** | PR → **자동 Preview 배포**(스테이징 백엔드를 바라봄), `main`/`staging` 브랜치 → **스테이징 배포**, 릴리즈 태그 → **운영 배포** | Vercel의 Preview 배포만으로 PR별 화면 확인이라는 목적은 충분히 달성됨(8.56절) |
| 백엔드(ECS) | PR마다 별도 백엔드 기동 vs **스테이징 백엔드·DB 공유** | PR 프리뷰는 **스테이징 ECS 서비스와 스테이징 DB를 그대로 사용** | PR마다 ECS 서비스를 띄우면 기동 시간·비용이 커지고, 단일 서비스가 PR별 DB를 바라보는 것은 구조적으로 불가능(8.56절 E-2). 프리뷰의 목적은 **화면·동작 확인**이므로 백엔드 공유로 충분하며, 여러 PR이 같은 스테이징 데이터를 공유한다는 점은 수용(합성 데이터만 존재) |
| Vercel 함수 리전·실행시간(8.69절 R-1/R-3) | 기본값 사용 vs **명시적 고정** | **함수 리전을 `icn1`(서울)로 고정**(`vercel.json`의 `regions` 또는 프로젝트 설정)하고, 보호된 페이지 라우트에 **`export const maxDuration = 10`** 을 명시한다. 함수 리전 지정은 **Pro 플랜 이상 기능**이므로 플랜 전제로 함께 둔다 | **리전**: Vercel 기본 함수 리전은 `iad1`(워싱턴 DC)인데 우리 S3·ECS는 `ap-northeast-2`(서울)라, SSR 릴레이의 2회 순차 호출이 **매번 태평양을 왕복**한다(왕복 ~180ms × 2 = 렌더 시작 전 360ms+). `icn1`은 `ap-northeast-2`에 대응하므로 사실상 리전 내 통신이 된다. **실행시간**: 기본값은 플랜·Fluid Compute 설정에 따라 15~300초로 달라지는데, **300초가 걸리면 장애 시 SSR 페이지가 5분씩 매달려 함수 동시성이 고갈**되고 정상 페이지까지 느려진다 — 빠르게 실패하는 편이 낫다 |
| Supabase Data API 노출(8.68절 Q-2) | 기본값(PostgREST가 `public` 스키마 노출) vs **노출 스키마에서 `public` 제거** | **스테이징·운영 두 프로젝트 모두에서 Data API 노출 스키마에서 `public`을 제거**한다(프로젝트 설정 → API → Exposed schemas) | 우리는 **PostgREST를 전혀 쓰지 않는다** — 모든 DB 접근은 NestJS → Prisma → `cbmall_app` 경로뿐이다. 그런데 Supabase는 `https://<ref>.supabase.co/rest/v1/`를 자동으로 열어두고, **`anon` 키는 설계상 공개되는 값**이며 **`service_role` 키는 BYPASSRLS를 가져 RLS를 무력화**한다. 쓰지도 않는 문을 열어두고 자물쇠(RLS)만 채우는 대신 **표면 자체를 없애면** 두 위험이 동시에 사라진다. RLS는 그 뒤의 안전망으로 유지 |
| 환경별 도메인(8.65절 N-1/N-6) | 환경마다 임의 URL vs **모든 환경을 같은 상위 도메인 아래로** | 운영 `app.`/`api.`, 스테이징 `stg.`/`api-stg.`, **PR 프리뷰 `pr-<번호>.preview.`** (CI에서 `vercel alias set`으로 부여, DNS에 와일드카드 CNAME). 인증서는 프론트(`app.`·`stg.`·`*.preview.`)는 **Vercel이 도메인 등록 시 자동 발급**, 백엔드(`api.`·`api-stg.`)는 **AWS ACM 발급 후 ALB 연결**(무료) | 8.7절 서브도메인 통일은 **모든 환경에서 성립해야** 의미가 있다 — 한 환경만 다른 사이트가 되면 그 환경에서는 인증이 통째로 동작하지 않는다(8.65절 N-1). 와일드카드가 필요한 쪽은 프리뷰뿐이라 백엔드는 개별 호스트명으로 충분 |
| 로컬 개발 환경(8.65절 N-5) | `localhost:3000` / `localhost:3001` vs **`app.localhost:3000` / `api.localhost:3001`** | 로컬도 운영과 **같은 서브도메인 구조**를 쓴다(Chrome·Firefox는 `*.localhost`를 루프백으로 자동 해석하고, `Secure` 쿠키도 localhost에는 http로 허용됨). **사파리는 `*.localhost`를 자동 해석하지 않으므로** macOS 개발자는 `/etc/hosts` 항목이 필요하다는 점을 온보딩 문서에 명시 | 포트만 다른 `localhost`는 쿠키 동작이 운영과 달라, `Domain` 속성을 환경별로 분기하는 코드가 생긴다 — **운영에서만 재현되는 쿠키 버그**의 전형적인 발생 원인 |
| 외부 벤더 키 | 모든 환경에서 동일 키 사용 vs **환경별 테스트/라이브 키 분리** | 스테이징(및 PR 프리뷰)은 토스페이먼츠 테스트 키(8.8절 기결정)·솔라피/Resend/Firebase의 테스트·샌드박스 모드 사용. **운영만 라이브 키** | 결제·SMS처럼 실제 비용이 발생하거나 실제 고객에게 영향을 주는 벤더 호출이 검증 단계에서 실행되면 안 됨. 8.8절에서 이미 정한 원칙을 전체 벤더로 확장 |
| 마이그레이션 자격증명 배치(8.70절 S-3) | 런타임 시크릿과 함께 주입 vs **CI에만 주입** | **`DIRECT_DATABASE_URL`은 GitHub Actions에만 주입하고 ECS 태스크 정의에는 넣지 않는다** | 마이그레이션은 8.35절에 따라 **CI에서만** 실행하므로, 런타임 태스크가 마이그레이션용 커넥션 자격증명을 들고 있을 이유가 없다. 8.15절 최소권한 원칙과 같은 취지 |
| 시크릿 네임스페이스 | 환경 구분 없이 저장 vs **경로 기반 구분** | AWS Secrets Manager 경로를 `/cbmall/staging/*`, `/cbmall/production/*`로 구분해 저장 | 8.8/8.15/8.30절에서 언급된 여러 시크릿(토스·솔라피·Resend·Firebase·DB비밀번호·KMS 등)을 환경별로 명확히 분리 관리 |
| 스테이징 데이터 정책 | 운영 데이터 복제 사용 vs **합성 시드 데이터만 사용** | 스테이징 DB에는 **실제 고객 데이터를 절대 복제하지 않고**, 익명의 시드 상품·테스트 계정만 사용 | 운영 데이터를 스테이징에 복제하면 8.20절 등에서 다뤄온 개인정보 보호 원칙과 충돌하고, 법적 리스크(개인정보보호법)도 생김 |
| 운영 배포 승인 | main 배포 시 자동으로 운영까지 반영 vs **스테이징→운영은 수동 승인 게이트** | 릴리즈 태그 생성 후 GitHub Actions의 **environment protection rule**로 사람이 승인해야 운영 배포가 진행되도록 설정 | 결제·다운로드처럼 되돌리기 어려운 기능이 많아, 마지막 단계는 자동화보다 사람의 최종 확인을 거치는 것이 안전. 별도 CD 툴 없이 이미 쓰는 GitHub Actions 기능만으로 구현 가능 |

Sources:
- [Supabase branching vs. separate staging projects](https://www.supaclone.io/blog/supabase-branching-vs-separate-projects)
- [Branching | Supabase Docs](https://supabase.com/docs/guides/deployment/branching)
- [The Vibe Coder's Guide to Supabase Environments](https://supabase.com/blog/the-vibe-coders-guide-to-supabase-environments)

### 8.35 CI/CD 파이프라인 기술 스택 (확정)

> 8.34절에서 환경 구성(PR 프리뷰→스테이징→운영)은 정했지만, **그 사이를 잇는 CI/CD 파이프라인 자체**(무엇을 검사하고, 어떻게 배포하고, 실패하면 어떻게 되돌리는지)는 아직 정하지 않았습니다. **실제 구축이 아닌 결정 단계**입니다.

| 항목 | 검토 대상 | 제안(추천) | 근거 |
|---|---|---|---|
| CI 도구 | 별도 CI 서비스(CircleCI 등) 도입 vs **GitHub Actions** | **GitHub Actions** | 저장소가 GitHub라면 별도 계정·연동 없이 바로 사용 가능하고 무료 티어로 우리 규모 충분. 8.34절에서 이미 environment protection rule(승인 게이트)을 GitHub Actions 기준으로 설계해뒀음 |
| 모노레포 빌드 최적화 | 매번 전체 재빌드 vs **Turborepo Remote Cache** | 8.3절에서 도입한 Turborepo의 원격 캐시(Vercel Remote Cache 무료 티어)를 CI에도 연결 | 변경 없는 패키지(예: 백엔드만 고친 PR의 프론트 빌드)를 매번 재빌드하지 않아 CI 시간 단축. 신규 인프라 없이 이미 선택한 도구의 기능만 활성화 |
| PR 단계 검사 항목 | 육안 리뷰에만 의존 vs **자동 게이트 파이프라인** | 매 PR마다 **Lint → 타입체크(`tsc`) → 단위/통합 테스트 → `dependency-cruiser`(8.21절 모듈 경계 검사) → 빌드 검증** 순으로 실행, 하나라도 실패하면 머지 차단 | 8.21절에서 어렵게 정한 모듈 경계 규칙도 CI에서 강제되지 않으면 시간이 지나며 흐트러짐. 자동 게이트가 사람이 매번 확인하는 것보다 훨씬 안정적 **아키텍처 경계 검증도 이 단계에 포함한다(8.66절 O-3)** — ① `app/api/**/route.ts` 파일 목록을 **허용 목록(`revalidate` 하나)과 대조**해 새 BFF Route Handler가 생기면 실패, ② 소스에서 **`'use server'` 지시문 검출 시 실패**(8.66절 O-5), ③ 마이그레이션에 **`CREATE TABLE`이 있으면 같은 파일에 그 테이블의 `ENABLE ROW LEVEL SECURITY`와 `CREATE POLICY app_all ... TO cbmall_app`이 함께 있는지** 검사(8.68절 Q-5 — 하나만 빠뜨려도 그 테이블이 보호에서 조용히 벗어난다), ④ 마이그레이션에서 **`SECURITY DEFINER` 검출 시 실패**(8.68절 Q-6). "범용 프록시를 만들지 않는다"(8.51절 C-6)는 규율로만 두면 파일 업로드 중계·이미지 프록시·서드파티 키 숨기기처럼 **전부 "이번만 예외"로 보이는 요청**에 밀려 반드시 샌다. 허용 목록을 고치는 PR이 강제되므로 **추가가 리뷰 대상으로 드러난다** — 8.21절 `dependency-cruiser`와 같은 접근. |
| AWS 인증 방식 | GitHub Secrets에 장기 AWS Access Key 저장 vs **OIDC(OpenID Connect) 기반 임시 자격증명** | GitHub Actions ↔ AWS IAM Role을 **OIDC**로 연동해, 워크플로마다 **15분짜리 단기 자격증명**을 발급받아 사용. IAM 신뢰 정책의 `sub` 조건은 브랜치가 아니라 **`environment:production` 기반**으로 구성(8.57절 F-7: 릴리즈 태그 배포는 `refs/tags/...` 형태라 브랜치 조건과 맞지 않으며, environment 기반은 8.34절 승인 게이트와 맞물려 **승인된 배포만 AWS 자격증명을 얻게** 됨) | 장기 액세스 키는 유출·로그 노출 위험이 있고 수동 로테이션이 필요함(8.30절 문제의식과 동일). OIDC는 아예 장기 키 자체가 없어 **로테이션할 대상이 사라짐** — 8.30절보다 근본적인 해결책 |
| 백엔드 배포 절차 | 수동 이미지 빌드/배포 vs **Docker 빌드→ECR 푸시→ECS 롤링 업데이트 자동화** | GitHub Actions가 NestJS Docker 이미지를 빌드해 ECR에 푸시 → 새 태스크 정의로 ECS 서비스 업데이트 → 8.11절에서 확보한 **2개 이상 태스크**로 무중단 롤링 배포 | 이미 가용성 목적으로 갖춘 다중 태스크 구조가 배포 무중단성에도 그대로 활용됨 |
| 마이그레이션 실패 시 진단 안내(8.70절 S-5) | 원시 오류만 출력 vs **안내 문구 추가** | 마이그레이션 스텝 실패 시 다음을 출력한다 — "다이렉트 커넥션은 IPv6 전용이며 GitHub Actions는 IPv4만 지원합니다. `DIRECT_DATABASE_URL`이 **세션 모드 풀러**(`...pooler.supabase.com:5432`)를 가리키는지 확인하세요 — PRD 8.70절" | `ENETUNREACH`나 연결 타임아웃은 네트워크·자격증명·방화벽 어디서든 날 수 있어, 처음 겪으면 원인 특정에 시간이 걸린다. 한 줄이지만 **같은 함정에 두 번 빠지지 않게 하는** 비용 대비 효과가 크다 |
| DB 마이그레이션 적용 시점 | 애플리케이션과 동시에 적용 vs **승인 → 마이그레이션 → 배포 순서로 고정** | **운영 승인 게이트(8.34절)를 통과한 뒤에** `prisma migrate deploy`를 실행하고(8.15절 `postgres` 롤 **다이렉트 커넥션** 필수 — 8.57절 F-5), 성공해야 ECS 배포로 진행. 실패 시 파이프라인 중단 | 승인 전에 DB를 먼저 바꾸면 승인이 지연되는 동안 **구코드×신스키마 구간이 무한정 길어짐**(8.57절 F-4). 또한 Prisma 마이그레이션은 세션 advisory lock을 쓰므로 풀러를 거치면 실패함 |
| 마이그레이션 하위호환 강제 | 원칙 문장으로만 명시 vs **확장-수축(expand/contract) 규율 + CI 검출** | 마이그레이션은 **① 확장(추가만, 컬럼은 nullable 또는 기본값 포함) → ② 신·구 코드가 모두 동작하는 상태로 배포 → ③ 다음 릴리즈에서 수축(삭제)** 3단계로 진행. `DROP COLUMN`·`RENAME`·`NOT NULL` 추가 같은 **파괴적 DDL은 CI가 검출해, 수축 단계임을 PR에 명시하지 않으면 머지 차단** | **(8.57절 F-1)** 8.11절에 따라 태스크가 2개 이상이라 롤링 교체 중 **구버전 코드가 이미 바뀐 스키마 위에서 서비스**하는 구간이 반드시 생김 — 파괴적 변경이 하나만 섞여도 배포할 때마다 운영 장애가 됨. 원칙 문장만으로는 막을 수 없어 CI 검출로 강제 |
| 배포 직렬화 | 별도 장치 없음 vs **환경별 `concurrency` 그룹** | GitHub Actions `concurrency` 그룹을 환경별로 지정해 같은 환경의 배포가 한 번에 하나만 진행되도록 직렬화 | **(8.57절 F-6)** 두 PR이 연달아 머지되면 마이그레이션이 동시에 실행돼 **advisory lock 10초 타임아웃으로 실패**하거나 ECS 배포가 겹침 |
| 배포 실패 시 롤백 | 수동 원상복구 vs **플랫폼 내장 롤백 기능 활용** | ECS는 이전 태스크 정의 리비전으로 즉시 되돌리고, Vercel은 대시보드에서 이전 배포로 원클릭 롤백 | 두 플랫폼 모두 롤백 기능을 기본 제공하므로 별도 롤백 도구를 만들 필요가 없음. 단, DB 마이그레이션은 대부분 "앞으로만" 적용하는 방식이라 롤백 시 코드만 되돌리고 스키마는 하위호환 상태를 유지하도록 설계해야 함 |

Sources:
- [How to Deploy a Container from GitHub to AWS ECR through OIDC](https://dev.to/syed_omair/how-to-deploy-a-container-from-github-to-aws-ecr-through-oidc-2ma5)
- [GitHub Actions for AWS: OIDC Setup, ECS Deployment, and CodePipeline Comparison](https://cloudwebschool.com/docs/aws/devops-and-cicd/github-actions-for-aws/)
- [Automated deployments with GitHub Actions for Amazon ECS Express Mode](https://aws.amazon.com/blogs/containers/automated-deployments-with-github-actions-for-amazon-ecs-express-mode)

### 8.36 모노레포 디렉토리 구조 (확정)

> 지금까지 8.3(Turborepo+pnpm)·8.17/8.21(공유 타입, 도메인 모듈)·8.30(prisma 폴더)·8.33(워커 파일)·8.35(CI/CD 대상)에서 나온 조각들을 하나의 실제 디렉토리 트리로 종합했습니다. `prisma/`는 이 결정에 따라 **`apps/api/prisma/`로 실제 이동 완료**했습니다.

```
cb-mall/
├── apps/
│   ├── web/                       # Next.js 프론트엔드 (8.3, 8.22)
│   │   ├── app/
│   │   │   ├── api/auth/refresh/route.ts   # 8.22 BFF Route Handler(SSR 토큰 릴레이 전용)
│   │   │   ├── (customer)/...              # 고객용 라우트
│   │   │   └── (admin)/...                 # 관리자용 라우트 — 별도 앱 아닌 라우트 그룹으로 확정
│   │   ├── middleware.ts          # 8.22 쿠키 존재 여부 1차 체크
│   │   └── next.config.ts
│   └── api/                       # NestJS 백엔드 (8.3, 8.21)
│       ├── prisma/                # ← 리포지토리 최상위에서 이동 완료(확정)
│       │   ├── schema.prisma
│       │   ├── migrations/
│       │   └── setup/roles.sql
│       ├── src/
│       │   ├── modules/           # 8.21 도메인별 모듈 (auth, products, orders, payments,
│       │   │                      #   entitlements, coupons, points, reviews, support,
│       │   │                      #   notifications(React Email 템플릿 포함), admin-accounts ...)
│       │   ├── common/            # guards, decorators, filters 등 진짜 횡단 관심사만
│       │   ├── workers/           # 8.33 piscina PDF 텍스트 추출 워커
│       │   └── main.ts
│       └── Dockerfile             # 8.35 ECR 빌드 대상
├── packages/
│   ├── shared-types/              # 8.17/8.21 프론트-백엔드 공유 DTO/타입
│   ├── eslint-config/
│   └── tsconfig/
├── .github/workflows/             # 8.35 CI/CD
├── dependency-cruiser.config.js   # 8.21 모듈 경계 자동 검증
├── turbo.json
└── pnpm-workspace.yaml
```

| 결정 사항 | 결정 | 근거 |
|---|---|---|
| `prisma/` 폴더 위치 | 리포지토리 최상위 → **`apps/api/prisma/`로 이동 완료**(실제 파일 이동 반영됨) | 8.3/8.7절에서 "NestJS만 DB에 접근하고 Next.js는 API 호출만 한다"고 확정했으므로, Prisma는 사실상 `apps/api` 전용 도구임. 8.21절의 "각 도메인/앱이 자기 것을 소유한다"는 모듈 경계 원칙을 리포지토리 최상위 구조에도 동일하게 적용 |
| 관리자 화면 위치 | 별도 앱(`apps/admin`) 대신 **`apps/web` 안 라우트 그룹(`(admin)`)** | 8.1절 팀 규모(5~6명)와 3개월 일정에서 별도 앱은 배포 파이프라인·서브도메인(8.7절엔 `app.`/`api.`만 있음)을 하나 더 만드는 비용이 듦. 같은 Next.js 앱 안에서 `/admin` 경로 + 8.20절 권한(SUPER_ADMIN/ADMIN) 체크로 충분히 분리 가능 |
| 이메일 템플릿(8.13절 React Email) 위치 | 별도 패키지 대신 **`apps/api` 내부**(`apps/api/src/modules/notifications/templates/`) | React Email 템플릿은 발송 주체인 NestJS(`apps/api`)에서만 쓰이고 프론트엔드가 참조할 일이 없음 — 재사용처가 하나뿐인데 별도 패키지로 분리하면 관리 부담만 늘어남 |

Sources: (내부 종합 — 8.3/8.7/8.17/8.20/8.21/8.30/8.33/8.35절 기존 결정 정리)

### 8.37 프론트엔드 상태관리 라이브러리 (확정)

> 8.7절에서 "Access Token은 클라이언트 메모리에 보관"이라고 정했지만 **정확히 어디에**(어떤 저장소/패턴으로) 보관할지는 정한 적이 없었고, FR-3.1의 "비회원도 세션 기반 장바구니 이용"도 클라이언트 상태를 어떻게 유지할지 정해진 바 없습니다. **실제 구축이 아닌 결정 단계**입니다.

| 항목 | 검토 대상 | 제안(추천) | 근거 |
|---|---|---|---|
| 재시도 정책(8.63절 L-4) | TanStack Query 기본값(`retry: 3`) 유지 vs **4xx는 재시도 금지** | `retry`를 조건 함수로 바꿔 **4xx(특히 429)는 재시도하지 않고** 5xx·네트워크 오류만 재시도 | 검색은 분당 30회 제한이 걸려 있어(8.28/8.61절 J-4) **429를 3회 재시도하면 제한 상황을 더 악화**시키고, 사용자는 원인 모를 빈 화면만 본다. 400·401·404도 재시도해서 나아질 여지가 없음 |
| 서버 상태 관리 | 각 컴포넌트에서 개별 `fetch`+`useState` vs **TanStack Query** | 상품 필터링/무한스크롤, 장바구니-서버 동기화, 리뷰 작성 후 목록 갱신 등 **클라이언트에서 발생하는 서버 데이터 상호작용**에 TanStack Query 도입 | 로딩·에러·캐시·재검증을 표준화된 방식으로 처리해 직접 구현할 코드가 크게 줄어듦. 2026년 기준 Next.js와 함께 사실상 표준 조합으로 자리잡음 |
| 서버 상태 vs 8.23절 캐싱 경계 | 역할 중복 vs **명확히 분리** | **8.23절 Next.js Data Cache**는 서버 컴포넌트의 최초 SSR 데이터 캐싱을 담당하고, **TanStack Query**는 그 이후 클라이언트에서 일어나는 재요청·뮤테이션을 담당 | 두 캐싱 레이어가 같은 데이터를 각자 다르게 캐싱하면 오히려 혼란스러움 — "최초 로드는 서버가, 이후 상호작용은 클라이언트가" 원칙으로 역할을 나눔 |
| 클라이언트 전역 상태(UI/세션) | Redux Toolkit vs React Context API vs **Zustand** | **Zustand** | Redux Toolkit은 보일러플레이트가 우리 규모(8.1절 5~6명, 3개월)에 비해 과함. Context API는 값이 바뀔 때마다 구독 컴포넌트 전체가 리렌더되는 문제가 있어 Access Token처럼 자주 갱신되는 값에는 부적합. Zustand는 선택적 구독으로 이 문제를 피하면서도 API가 간단함 |
| 스토어 생성 방식(8.64절 M-2) | 모듈 최상위 `create()` vs **`createStore()` + 요청별 Context Provider** | **Provider 패턴**으로 요청마다 스토어 인스턴스를 생성하고, **"서버 렌더링 경로에서는 어떤 스토어에도 쓰지 않는다"** 를 원칙으로 둔다. SSR에 필요한 데이터는 스토어가 아니라 **props로 내려보낸다** | JS 모듈은 한 번 평가되면 캐시되므로 모듈 스코프 스토어는 **Next.js 서버 프로세스 전체가 공유**한다(Zustand 공식 문서도 전역 정의를 금지). 우리 설계에서 특히 위험한 이유는 구체적이다 — 이 절이 Access Token을 스토어에 보관하고 8.22절이 **SSR 중 토큰 릴레이**를 수행하므로, 릴레이된 토큰을 스토어에 넣는 (자연스러워 보이는) 구현을 하면 **그 값이 다음 요청의 다른 사용자에게 노출**된다 |
| Access Token 보관 위치 | 별도 저장소 신설 vs **Zustand 스토어(비영속)** | Access Token을 Zustand 스토어에 저장하되 **`persist` 미들웨어는 적용하지 않음**(새로고침 시 소실) | 8.7절에서 이미 "클라이언트 메모리 보관, 새로고침 시 소실"로 원칙을 정해뒀음 — 소실 시에는 8.22절의 SSR BFF 릴레이 또는 클라이언트의 Refresh 호출로 재발급받으면 되므로 영속화가 필요 없고, 오히려 영속화하지 않는 편이 8.7절이 막고자 했던 XSS 시 토큰 탈취 위험을 낮춤 |
| 장바구니 상태 보관 | 서버 세션에만 저장 vs **Zustand + `persist`(localStorage)** | **(8.64절 M-3)** `persist` 옵션에 **`skipHydration: true`** 를 주고 클라이언트 마운트 후 `useEffect`에서 **`rehydrate()`를 명시적으로 호출**한다. 복원 전까지는 서버와 동일한 초기값(빈 장바구니)을 렌더하고, 장바구니 배지 등은 복원 완료 전까지 렌더를 보류한다 — 서버에는 `localStorage`가 없어 빈 장바구니로 HTML을 렌더하는데 클라이언트는 복원된 값으로 렌더하므로, 그냥 두면 **hydration mismatch가 반드시 발생**한다. 장바구니는 Zustand 스토어에 저장하고 `persist` 미들웨어로 localStorage에 영속화 | FR-3.1(비회원도 세션 기반 장바구니 이용)을 만족하려면 새로고침·재방문 후에도 장바구니가 유지돼야 함. Access Token과 달리 장바구니 내용은 민감정보가 아니라 영속화해도 보안 리스크가 낮음 |
| URL 상태(8.63절 L-2) | 검색·필터 조건을 Zustand에 보관 vs **URL 쿼리스트링이 단일 진실 원천** | 검색 파라미터(`q`/`sort`/`page`/`categoryId`)는 **Zustand에 넣지 않고** `useSearchParams`로 읽고 `router.replace`로 쓴다. 이 값들로 TanStack Query의 `queryKey`를 구성해 **URL이 곧 캐시 키**가 되게 한다 | 8.62절 K-1에서 스니펫 색인을 막으려 검색 결과를 클라이언트 페치로 바꿨는데, 상태를 메모리에만 두면 **공유·새로고침·뒤로가기가 전부 깨진다** — 상품 상세를 보고 뒤로 갔을 때 검색 결과가 사라지는 건 이탈로 직결되는 동선. "서버 상태/클라이언트 상태" 2분법에 들어가지 않는 **제3의 범주**임을 명시 |
| 상태관리 경계 원칙 | 서버에서 받은 데이터를 전역 스토어에도 복사 vs **서버 데이터는 TanStack Query에만 존재** | Zustand에는 "진짜 클라이언트 전용" 상태(Access Token, 장바구니, 모달/사이드바 열림 여부 등)만 두고, 상품·주문 같은 서버 데이터를 Zustand로 복사하지 않음 | 같은 데이터가 두 곳(TanStack Query 캐시 + Zustand)에 존재하면 동기화가 어긋나는 버그의 근원이 됨 — "서버 데이터는 TanStack Query, 클라이언트 전용 데이터는 Zustand"로 역할을 명확히 분리 |

Sources:
- [TanStack and Next.js: The De Facto Frontend Logic Layer for 2026](https://dev.to/mericcintosun/tanstack-and-nextjs-the-de-facto-frontend-logic-layer-for-2026-4mal)
- [TanStack Query in 2026: Server State Management That Gets Out of Your Way](https://blog.codercops.com/blog/tanstack-query-server-state-2026)
- [My Frontend Stack In 2026](https://thetshaped.dev/p/my-frontend-stack-in-2026-react-nextjs-pnpm-vite-ts-tailwind-storybook-tanstack-zustand-zod-oxlint-oxfmt-msw-vitest-playright-sentry)

### 8.38 테스트 전략 (확정)

> 8.35절 CI 파이프라인에서 "단위/통합 테스트" 단계를 언급했지만, **어떤 프레임워크로, 어디까지 테스트할지**는 정한 적이 없었습니다. **실제 구축이 아닌 결정 단계**입니다.

| 항목 | 검토 대상 | 제안(추천) | 근거 |
|---|---|---|---|
| 프론트(`apps/web`) 단위 테스트 | Jest vs **Vitest** | **Vitest** + React Testing Library — 클라이언트 컴포넌트, 동기 Server Component, Zustand 스토어(8.37절), 유틸 함수 대상 | Next.js 공식 문서가 App Router 환경에서 우선 권장하는 조합. Vite 기반이라 실행 속도가 빠르고 설정이 가벼움 |
| 프론트 E2E 테스트 | 생략 vs **Playwright** | 핵심 구매 흐름(탐색→장바구니→주문→결제→다운로드)과 로그인/MFA(8.19절) 같은 인증 플로우를 Playwright E2E로 커버 | **비동기 Server Component는 Vitest가 아직 지원하지 않아** E2E로만 검증 가능 — 2026년 기준 업계에서 "Vitest(단위)+Playwright(E2E, 비동기 서버컴포넌트·인증)" 조합이 사실상 표준으로 자리잡음 |
| 백엔드(`apps/api`) 테스트 | 프론트와 동일하게 Vitest 통일 vs **NestJS 기본값인 Jest 유지** | **Jest** | NestJS는 `nest new` 스캐폴딩부터 Jest가 기본 내장되어 있고 `@nestjs/testing` 모듈과의 통합이 가장 매끄러움. 모노레포 안에서 앱마다 각 생태계의 기본값을 따르는 편이 억지로 통일하는 것보다 유지보수가 쉬움 |
| 테스트 범위(우선순위) | 전체 코드 커버리지 목표치 설정 vs **핵심 경로 우선** | 결제 승인(8.8절), 다운로드 원자적 카운터(8.6절), 알림 재시도 백오프 계산(8.18절)처럼 **한 번 틀리면 돈·콘텐츠 유출로 직결되는 로직**은 반드시 단위 테스트, 나머지 UI는 E2E 핵심 흐름으로만 커버 | 8.1절 팀 규모(5~6명)·9.1절 일정 일정에서 전체 커버리지 목표(예: 80%)를 강제하면 일정에 비해 과한 투자가 됨 — 리스크가 큰 로직에 집중하는 것이 합리적 |

Sources:
- [Next.js Testing 2026: Vitest and Playwright Setup Guide](https://medium.com/@securestartkit/next-js-testing-in-2026-vitest-playwright-0caf6dd1f829)
- [Guides: Testing | Next.js 공식 문서](https://nextjs.org/docs/app/guides/testing)

### 8.39 UI 스타일링/컴포넌트 라이브러리 (확정)

> 모든 화면(고객용/관리자용, 8.36절)에 쓰일 스타일링 방식이 아직 정해지지 않았습니다. **실제 구축이 아닌 결정 단계**입니다.

| 항목 | 검토 대상 | 제안(추천) | 근거 |
|---|---|---|---|
| CSS 방식 | CSS Modules/styled-components vs **Tailwind CSS v4** | **Tailwind CSS v4** | Next.js 15/16과 공식적으로 궁합이 맞고, 클래스 기반이라 컴포넌트별 CSS 파일을 따로 관리할 필요가 없어 소규모 팀의 생산성에 유리 |
| 컴포넌트 라이브러리 | 완성형 라이브러리(HeroUI/DaisyUI 등) vs **shadcn/ui** | **shadcn/ui** | 2026년 기준 Next.js 생태계의 사실상 기본 선택지. **npm 패키지가 아니라 컴포넌트 소스 코드를 프로젝트에 직접 복사**하는 방식이라, 지금까지 일관되게 유지해온 "불필요한 신규 의존성/벤더 최소화" 원칙과 잘 맞음 — 필요한 컴포넌트만 가져와 자유롭게 수정 가능 |
| 접근성 기반 | 직접 구현 vs **Radix/Base UI 프리미티브(shadcn/ui 내장)** | shadcn/ui가 내부적으로 사용하는 접근성 프리미티브를 그대로 활용(포커스 관리, 키보드 내비게이션, ARIA 속성 자동 처리) | 6절 NFR의 "웹 접근성 기본 준수"를 별도 구현 없이 상당 부분 충족 |
| 고객용/관리자용 디자인 통일 여부 | 완전히 다른 디자인 시스템 vs **같은 컴포넌트 세트 공유** | 8.36절에서 관리자 화면을 같은 Next.js 앱의 라우트 그룹으로 두기로 했으므로, shadcn/ui 컴포넌트 세트를 고객/관리자 화면이 함께 사용 | 두 화면을 완전히 다른 디자인 시스템으로 만들면 관리 포인트가 늘어남 — 같은 컴포넌트를 재사용하면서 레이아웃/톤만 구분 |

Sources:
- [Best Tailwind Component Libraries (2026): 12 Options Ranked](https://designrevision.com/blog/best-tailwind-component-libraries)
- [Best React UI Component Libraries for Next.js in 2026](https://ui.bidhex.com/blog/best-react-ui-component-libraries-nextjs-2026)

### 8.40 폼 처리/검증 (확정)

> 회원가입·로그인·상품등록·쿠폰발급 등 폼이 많은데 처리 방식이 정해진 적이 없었습니다. 8.39절에서 정한 shadcn/ui가 특정 폼 라이브러리 조합을 전제로 한 공식 `<Form>` 컴포넌트를 제공하고 있어, 그 조합을 기준으로 검토했습니다. **실제 구축이 아닌 결정 단계**입니다.

| 항목 | 검토 대상 | 제안(추천) | 근거 |
|---|---|---|---|
| 프론트 폼 라이브러리 | 직접 `useState`로 폼 관리 vs **React Hook Form + Zod** | **React Hook Form + Zod** — 8.39절 shadcn/ui의 공식 `<Form>` 컴포넌트가 이 조합을 전제로 만들어짐 | React Hook Form은 비제어(uncontrolled) 컴포넌트 방식이라 입력마다 리렌더가 발생하지 않아 성능이 좋고, Zod는 타입 안전한 스키마 검증을 제공. 2026년 기준 Next.js 생태계의 표준 조합 |
| 폼 제출 방식 | Next.js Server Actions vs **8.22절 그대로: 브라우저→NestJS 직접 호출** | React Hook Form의 `onSubmit`에서 TanStack Query(8.37절) mutation으로 NestJS API를 직접 호출 | 8.22절에서 이미 "클라이언트 상호작용은 Server Actions로 감싸지 않고 직접 호출"이라는 경로 규칙을 정해뒀음 — 폼 제출도 예외를 두지 않고 같은 규칙을 따름 |
| 검증 스키마 공유 | 프론트/백엔드 각자 검증 로직 작성 vs **`packages/shared-types`에 Zod 스키마 정의 후 양쪽에서 재사용** | 회원가입/주문/쿠폰 등의 Zod 스키마를 공유 패키지에 두고, 프론트는 React Hook Form의 resolver로, 백엔드는 검증 파이프로 **같은 스키마**를 사용 | 8.17/8.21절에서 만든 공유 타입 패키지의 목적(타입이 프론트/백엔드에서 어긋나지 않게 함)을 검증 로직까지 확장 — 스키마가 하나뿐이라 "프론트는 통과했는데 백엔드는 거부" 같은 불일치가 구조적으로 사라짐 |
| 백엔드 검증 방식 | NestJS 기본값(`class-validator`+DTO 클래스) 유지 vs **`nestjs-zod`로 전환** | **`nestjs-zod`**로 전환해 위 공유 Zod 스키마를 NestJS 검증 파이프에서 그대로 사용 | `class-validator`를 유지하면 결국 같은 검증 규칙을 프론트(Zod)와 백엔드(class-validator)에 **두 번 다른 문법으로** 작성해야 해서 스키마 공유의 이점이 반감됨. `nestjs-zod`는 Zod 스키마를 NestJS DTO/파이프로 그대로 연결해주는 라이브러리라 전환 비용이 크지 않음 |
| 비동기 초기값 주입(8.64절 M-6) | `defaultValues`에 API 응답을 전달 vs **`reset(data)`로 주입** | 관리자 상품 수정 폼(FR-7.2)처럼 **기존 값을 API로 받아 채우는 화면**은 `defaultValues`가 아니라 **`reset(data)`** 로 주입하거나, 데이터가 도착할 때까지 폼 자체를 렌더하지 않는다(TanStack Query 로딩 상태 활용) | React Hook Form의 `defaultValues`는 **첫 렌더에만 반영**된다. 데이터가 도착하기 전에 폼이 먼저 렌더되면 **빈 폼이 그대로 고정**되어, 관리자가 수정하려던 기존 값이 사라진 채로 저장되는 사고로 이어진다 |
| 검색 파라미터 스키마(8.62절 K-7) | 정해진 바 없음 vs **Zod 스키마로 고정** | `q`: 필수, trim 후 **2~50자** / `sort`: `recent`\|`sales`(8.27절) / `page`: **1~10**(8.62절 K-5의 깊은 OFFSET 방어) / `categoryId`: UUID, 선택. 위반 시 400 | 2자 미만은 결과가 무의미하면서 부하만 크고, 50자 상한은 8.61절 J-4의 스니펫 이어붙이기 방어에서 온 값. **검색어가 전부 초성(`ㄱ`~`ㅎ`)인 경우 초성 컬럼 매칭으로 분기**(8.27절)하는 것도 서비스 계층에 명시 |
| 신뢰 경계 | 프론트 검증 통과 시 서버 재검증 생략 vs **서버 재검증 항상 필수** | 클라이언트 Zod 검증은 UX(즉각적인 오류 표시) 목적일 뿐, 실제 **보안 경계는 항상 NestJS의 서버측 검증**(같은 스키마, `nestjs-zod`) | 클라이언트 검증은 우회 가능(직접 API 호출 등)하므로, 서버 검증 없이 클라이언트 검증만 믿는 것은 8.11/8.25/8.28절에서 다뤄온 여러 보안 원칙과 상충함 |

Sources:
- [React Hook Form - shadcn/ui 공식 문서](https://ui.shadcn.com/docs/forms/react-hook-form)
- [React Hook Form + Zod Complete Guide (2026)](https://stacknotice.com/blog/react-hook-form-zod-guide-2026)

### 8.41 API 문서화 (확정)

> 8.40절에서 백엔드를 `nestjs-zod`로 전환하기로 하면서 자연스럽게 따라오는 결정입니다. NestJS는 `@nestjs/swagger`를 공식 지원하지만 기본적으로는 Zod DTO를 인식하지 못해, 이번에 연동 방법까지 확인했습니다.

| 항목 | 결정 | 근거 |
|---|---|---|
| API 문서 생성 방식 | `@nestjs/swagger` + `nestjs-zod`의 `createZodDto` 연동, 부트스트랩 시 `patchNestjsSwagger()` 적용 | `createZodDto`로 만든 DTO 하나가 **검증 로직·TypeScript 타입·OpenAPI 스키마**를 동시에 제공함 — 8.40절에서 이미 확보한 "스키마는 한 곳에만" 원칙이 API 문서에도 그대로 이어짐. `patchNestjsSwagger()`를 부트스트랩에서 호출해야 Swagger 모듈이 Zod DTO를 인식함 |
| 문서 공개 범위 | 운영 환경에도 공개 vs **스테이징/개발 환경에만 노출** | Swagger UI는 8.34절의 **PR 프리뷰·스테이징 환경에서만** 활성화하고, **운영(production)에서는 비활성화** | API 구조가 외부에 노출되면 공격 표면 정찰에 악용될 수 있음 — 사내 개발·QA 목적이면 스테이징까지로 충분 |

### 8.42 환경변수 검증 (확정)

> 8.40/8.41절에서 Zod를 백엔드 검증·문서화의 공통 언어로 채택했으므로, **환경변수 누락/오타로 인한 배포 후 장애**도 같은 도구로 조기에 잡는 방법을 확인했습니다.

| 항목 | 결정 | 근거 |
|---|---|---|
| 프론트(`apps/web`) 환경변수 검증 | **`@t3-oss/env-nextjs`**(Zod 기반)로 필수 환경변수 스키마 정의, 빌드 시점에 누락되면 빌드 자체가 실패 | 8.7/8.16/8.22절 등에서 다루는 여러 공개 설정값(Firebase config, API Base URL 등)이 실제로는 빠져도 빌드가 조용히 성공해버리는 사고를 방지 — "설정이 잘못되면 아예 배포가 안 되게" 만드는 것이 런타임에 조용히 깨지는 것보다 안전 |
| 백엔드(`apps/api`) 환경변수 검증 | NestJS `ConfigModule`에 **Zod 스키마 기반 `validate` 함수** 연결 — 8.8/8.13/8.14/8.16/8.30절의 여러 시크릿(토스·솔라피·Resend·Firebase·KMS 등) 키 이름과 형식을 부팅 시점에 검사 | 필수 시크릿이 하나라도 없거나 형식이 잘못되면 **애플리케이션이 아예 기동하지 않도록** 만들어, "일부 기능만 조용히 고장난 채 운영되는" 상황을 방지. NestJS 공식 `ConfigModule`이 커스텀 validate 함수를 표준으로 지원해 신규 인프라 없이 적용 가능 |
| DB 커넥션 URL 검증(8.70절 S-2/S-3) | 존재 여부만 확인 vs **형식까지 검증해 혼동 차단** | `DATABASE_URL`은 **`pgbouncer=true`를 반드시 포함**해야 하고(8.67절 P-1), `DIRECT_DATABASE_URL`은 **`:6543`이나 `pgbouncer=true`를 포함하면 거부**한다. `DIRECT_DATABASE_URL`은 **CI 컨텍스트에서만 필수**로 분류한다(ECS 런타임에는 주입하지 않음 — 8.70절 S-3) | `directUrl`이 비면 Prisma는 **런타임 풀러 URL로 마이그레이션을 시도**해 세션 advisory lock이 성립하지 않고 10초 타임아웃으로 실패한다(8.57절 F-5). 두 URL은 형식상 구분이 없어 **서로 바꿔 넣는 실수를 사람이 잡기 어렵다** — 스키마가 잡으면 부팅·CI 단계에서 즉시 드러난다 |
| 환경별 필수 항목 구분 | 모든 환경에서 동일하게 필수 vs **환경별로 필수 여부 분리** | 운영·스테이징 **백엔드**는 전체 시크릿을 필수로 강제. **PR 프리뷰는 프론트엔드 변수만 검증 대상**(8.56절에서 프리뷰가 스테이징 백엔드를 공유하게 되어, 프리뷰 전용 백엔드 env 자체가 없어짐) | 8.55절 D-6은 "프리뷰 백엔드가 시크릿 부족으로 못 뜬다"는 문제였는데, 8.56절에서 프리뷰가 백엔드를 따로 띄우지 않게 되면서 문제의 전제가 사라짐 |
| 검증 실패 시 동작 | 경고 로그만 남기고 계속 실행 vs **즉시 프로세스 종료** | 프론트는 빌드 실패, 백엔드는 부팅 시 `process.exit(1)` | 8.35절의 CI/CD 파이프라인이 "실패하면 다음 단계로 진행하지 않는다"는 원칙(마이그레이션 게이트와 동일)을 환경변수 검증에도 동일하게 적용 |

Sources:
- [nestjs-zod - npm](https://www.npmjs.com/package/nestjs-zod)
- [How I Built a Type-Safe API with Auto-Generated Documentation Using Zod + NestJS + OpenAPI](https://medium.com/@gildniy/how-i-built-a-type-safe-api-with-auto-generated-documentation-using-zod-nestjs-openapi-f91c2abd8f08)

### 8.43 SEO/메타데이터 (확정)

> 전자책을 자사 직접 제작·판매하는 서비스 특성상 **검색 유입이 중요한 고객 획득 채널**이 될 가능성이 높은데, 지금까지 SEO 관련 결정이 전혀 없었습니다. **실제 구축이 아닌 결정 단계**입니다.

| 항목 | 검토 대상 | 제안(추천) | 근거 |
|---|---|---|---|
| 메타데이터 관리 | 별도 SEO 라이브러리 도입 vs **Next.js 내장 Metadata API** | 상품 상세 페이지는 `generateMetadata`로 **동적 생성**(title=책 제목, description=책 소개 요약, OG 이미지=8.10절 공개 버킷의 표지 이미지) | Next.js App Router가 메타태그·OG태그·sitemap·robots까지 파일 기반으로 기본 제공해, 별도 SEO 패키지 없이도 충분히 구현 가능 |
| 구조화 데이터(리치 스니펫) | 미적용 vs **JSON-LD(`Book` + `Offer` 스키마)** | 상품 상세 페이지 컴포넌트에 JSON-LD `<script>`를 직접 삽입해 가격·판매상태(FR-2.5)·평점(FR-5.1 리뷰)을 구조화 데이터로 노출 | 구글 검색결과에 가격·평점이 리치 스니펫으로 노출되면 클릭률이 올라감. JSON-LD는 `generateMetadata`가 아니라 **페이지 컴포넌트 안에 별도 스크립트**로 넣어야 함(구현 시 유의점) |
| sitemap.xml | 수동 관리 vs **Next.js 내장 `sitemap.ts`로 동적 생성** | 전체 판매중 상품 목록 기반으로 동적 생성, 8.23절과 유사하게 **1시간 주기 재검증** | 상품이 추가/판매중지될 때마다 수동으로 sitemap을 갱신하는 부담을 없앰. 카탈로그 규모(8.27절 기준 초기 수백 건)에서 매 요청마다 생성해도 부담 없음 |
| robots.txt | 전체 공개 vs **경로별 차단 정책 적용** | `app/robots.ts`로 `/admin`, `/mypage`, `/api/*`(**8.66절 O-6: BFF 프록시가 0개가 된 뒤에도 이 규칙은 유지한다** — 남는 것은 `revalidate` 웹훅 수신기뿐이지만, 차단을 유지해야 향후 웹훅이 추가될 때 자동으로 보호된다. "빈 경로를 막는 쓸모없는 규칙"으로 오해해 지우지 말 것), 8.4절 다운로드/8.7절 인증 관련 경로, **`/search`(8.62절 K-1)** 는 크롤링 차단, 상품 목록/상세는 허용. **AI 크롤러(GPTBot 등)는 허용**(공개 자료만 노출되므로 차단 실익이 적고, AI 검색 노출도 잠재적 유입 경로) | 8.20절 관리자 화면, 8.7절 인증·개인화 페이지가 검색결과에 노출되는 사고를 방지. **`/search`를 막는 이유는 별개다(8.62절 K-1)** — 검색 결과에는 유료 전자책 본문 스니펫이 실리므로, 색인되면 8.31절이 속도 제한까지 걸어 막으려던 본문 유출이 크롤러 경로로 뚫린다. 검색 URL은 조합이 무한해 크롤 예산 낭비·중복 콘텐츠 문제도 함께 발생. 노출돼도 되는 정보(샘플/표지/설명, 8.10절)만 원래 공개 대상이므로 AI 크롤러를 막을 이유가 적음 |
| 민감 페이지 색인 방지 | robots.txt 차단에만 의존 vs **페이지별 `noindex` 메타데이터 병행** | 주문완료/결제/마이페이지 등 개인화 페이지와 **검색 결과 페이지(8.62절 K-1)** 에 `generateMetadata`로 `robots: { index: false, follow: false }` 명시. **검색 결과는 추가로 SSR하지 않고 클라이언트에서 페치**(TanStack Query)해 스니펫이 애초에 HTML 소스에 존재하지 않게 함 — `noindex`를 무시하는 크롤러·아카이브·공유 링크 미리보기까지 막는 세 번째 방어선 | robots.txt는 크롤링 자체를 막을 뿐 이미 다른 경로로 알려진 URL의 색인을 완전히 보장하지 못함 — 페이지 자체에 `noindex`를 명시하는 이중 방어가 표준 관행 |
| 중복 콘텐츠 방지 | 필터/정렬 파라미터 URL을 그대로 색인 허용 vs **canonical 태그로 정규화** | 카테고리/가격/정렬 파라미터가 붙은 상품 목록 URL(예: `?category=x&sort=price`)에 canonical을 파라미터 없는 기본 URL로 설정 | 같은 상품 목록이 파라미터 조합마다 별도 페이지로 색인되면 검색엔진이 콘텐츠를 중복으로 판단해 순위가 분산됨 |

Sources:
- [Next.js JSON-LD Schema: App Router Patterns & Rich Results (2026)](https://schemavalidator.org/guides/nextjs-schema-markup)
- [The NextJS app router SEO features: Metadata, sitemaps, robots.txt, structured data](https://www.kodaps.dev/en/blog/nextjs-app-seo-features)
- [Metadata Files: sitemap.xml | Next.js 공식 문서](https://nextjs.org/docs/app/api-reference/file-conventions/metadata/sitemap)

### 8.44 분석/애널리틱스 (확정)

> 1.3절에 "회원가입 전환율 5%", "장바구니→결제 완료율 40%" 같은 구체적 성공 지표가 있는데, 지금까지 **이 지표를 실제로 측정할 도구가 하나도 없었습니다.** FR-7.1 대시보드(8.32절)는 매출·주문건수 같은 내부 트랜잭션 데이터만 다루고, "방문자가 어디서 이탈하는지" 같은 행동 분석은 다른 도구의 영역입니다. **실제 구축이 아닌 결정 단계**입니다.

| 항목 | 검토 대상 | 제안(추천) | 근거 |
|---|---|---|---|
| 기본 트래픽/성능 분석 | 미적용 vs **Vercel Analytics** | 방문자 수·유입경로·Core Web Vitals(6절 성능 목표와 연계) 측정에 **Vercel Analytics** 활성화 | 8.3절에서 이미 쓰고 있는 Vercel의 부가 기능이라 **신규 벤더가 아님** — 지금까지 일관되게 지켜온 벤더 최소화 원칙과 가장 잘 맞는 선택 |
| 전환 퍼널/이벤트 분석 | Vercel Analytics만으로 충분 vs **GA4 병행 도입** | 1.3절의 회원가입·장바구니→결제 전환율처럼 **단계별 이벤트 기반 퍼널 분석**은 GA4의 전자상거래 이벤트 프리셋(`sign_up`, `view_item`, `add_to_cart`, `begin_checkout`, `purchase`)으로 측정 | Vercel Analytics는 페이지뷰·트래픽 수준에 강하지만 세부 전환 퍼널 분석 기능은 제한적. GA4는 무료이고 이런 용도에 표준적으로 쓰이는 도구라, 없는 지표(퍼널)만 보완하는 최소 추가로 충분 |
| GA4 연동 방식 | 수동 스크립트 삽입 vs **`@next/third-parties/google`** | Vercel이 공식 유지보수하는 `GoogleAnalytics` 컴포넌트로 로드, 커스텀 이벤트는 `sendGAEvent`로 전송 | 로딩 전략이 최적화되어 있어 6절 성능 목표(페이지 로드 2초 이내)에 불리한 영향을 최소화. Next.js 공식 통합 경로 |
| GA4와 FR-7.1 대시보드의 역할 분리 | 하나로 통합 vs **역할 분리 유지** | **GA4**는 "방문자가 어떻게 행동하고 어디서 이탈하는지"(마케팅/제품 관점), **FR-7.1 대시보드**는 "실제 매출·주문이 얼마나 발생했는지"(운영/재무 관점)를 각각 담당 | 두 도구가 보는 데이터 성격이 달라 억지로 하나로 합치면 오히려 각 목적에 최적화되지 못함. 8.24절의 Sentry(에러)/CloudWatch(로그) 역할 분리와 같은 맥락 |
| `search` 이벤트의 검색어 전송(8.62절 K-8) | 검색어를 그대로 전송 vs **PII 패턴 감지 시 검색어 생략** | `search`는 탐색성 이벤트이므로 8.50절 원칙대로 **클라이언트에서 전송**하되, 전송 직전 **이메일·전화번호 패턴이 감지되면 검색어 파라미터를 빼고** 이벤트만 보냄 | 사용자가 검색창에 이메일·전화번호를 입력하는 일이 실제로 있고, GA4는 PII 수집을 정책상 금지함. 검색어 길이는 이미 50자로 제한(8.61절 J-4)되어 있어 추가 절단은 불필요 |
| 개인정보 처리 | 별도 고지 없음 vs **개인정보처리방침에 명시 + IP 익명화 확인** | 7절의 개인정보처리방침에 GA4 사용 사실과 수집 항목을 명시하고, GA4의 IP 익명화(기본 적용) 설정을 확인 | GA4는 쿠키·식별자 기반 추적 도구라 개인정보 수집에 해당 — 국내 개인정보보호법상 고지 의무를 충족해야 함(세부 문구는 법률 검토 권장 범위) |

Sources:
- [Using Google Analytics with Next.js (through `@next/third-parties/google`) | Next.js 공식 문서](https://nextjs.org/docs/messages/next-script-for-ga)
- [10 Best Analytics Tools for Next.js in 2026](https://amplitude.com/compare/best-analytics-tools-nextjs)

### 8.45 Vitest 테스트 도구 상세 설정 (확정)

> 8.38절에서 프론트 단위테스트로 Vitest를 확정했지만, **실제 실행 환경·API 모킹 방식·TanStack Query/Zustand(8.37절) 테스트 방법**까지는 정하지 않았습니다. **실제 구축이 아닌 결정 단계**입니다.

| 항목 | 검토 대상 | 제안(추천) | 근거 |
|---|---|---|---|
| 테스트 환경 | `happy-dom`(더 빠름) vs **`jsdom`** | **`jsdom`** | Vite 네이티브 프로젝트라면 happy-dom이 더 빠르지만, **Next.js는 Vite 네이티브가 아니라서** 공식적으로 jsdom 환경을 전제로 함 — 호환성 문제를 피하기 위해 표준 선택지를 따름 |
| 설정 위치 | 모노레포 루트에 단일 설정 vs **`apps/web/vitest.config.ts` 개별 설정** | `apps/web` 안에 `@vitejs/plugin-react` + jsdom + `@testing-library/react`/`jest-dom` 조합으로 개별 설정 | 8.38절에서 이미 "앱마다 각 생태계 기본값을 따른다"(백엔드는 Jest 유지)고 정했으므로, 프론트 테스트 설정도 `apps/web` 소유로 두는 것이 8.21/8.36절의 "각 앱이 자기 것을 소유" 원칙과 일치 |
| NestJS API 모킹 | 실제 스테이징 API 호출 vs **MSW(Mock Service Worker)** | 컴포넌트/훅 테스트에서 NestJS API 호출을 **MSW**로 가로채 모킹 | 8.22절에서 프론트가 NestJS를 직접 호출하는 구조로 정했으므로, 단위 테스트에서 실제 네트워크(심지어 스테이징 서버)를 타면 테스트가 느려지고 외부 상태에 의존하게 됨. MSW는 네트워크 레벨에서 가로채므로 실제 호출 코드를 변경하지 않고도 모킹 가능 |
| TanStack Query(8.37절) 테스트 | 프로덕션과 동일한 QueryClient 사용 vs **테스트 전용 QueryClient** | 테스트마다 `retry: false`, 캐시 비활성화로 구성한 별도 `QueryClient`를 주입 | 재시도 로직이 켜진 채로 테스트하면 실패 케이스 테스트가 불필요하게 오래 걸리고 타이밍에 취약해짐 |
| Zustand(8.37절) 스토어 테스트 | 전역 싱글턴 스토어 직접 사용 vs **테스트마다 스토어 초기화** | 각 테스트 시작 시 Zustand 스토어를 초기 상태로 리셋 | 테스트 간 상태가 누수되면(예: 이전 테스트의 장바구니 데이터가 다음 테스트에 남음) 실패 원인을 찾기 어려워짐 |
| 커버리지 측정 | 미측정 vs **`@vitest/coverage-v8`** | 커버리지 리포트를 생성하되, 8.38절 원칙대로 **목표 수치를 강제하지는 않고** 결제·다운로드 등 핵심 로직 위주로 참고 지표로만 활용 | 커버리지 %를 CI 게이트로 강제하면 형식적인 테스트만 늘어나는 부작용이 있어, 8.38절의 "핵심 경로 우선" 원칙을 유지 |
| CI 실행 방식 | 개별 `vitest run` 직접 호출 vs **`turbo run test`로 통합** | `turbo run test`로 실행하되 **단위 테스트만 캐싱하고, 8.46절 E2E 태스크는 `"cache": false`로 캐시에서 제외** | 변경되지 않은 패키지의 단위 테스트는 캐시로 건너뛰어 CI 시간을 단축. 다만 **(8.57절 F-2)** E2E는 배포된 프리뷰 URL이라는 **외부 상태**를 대상으로 하므로 소스 해시로 캐싱하면 **배포된 앱이 달라져도 "통과" 캐시를 재사용해 테스트가 한 번도 실행되지 않을 수 있음** |

Sources:
- [How to Set Up Vitest in Next.js 15 (Step-by-Step Guide)](https://www.wisp.blog/blog/setting-up-vitest-for-nextjs-15)
- [Testing React Apps in 2026: Vitest, React Testing Library, MSW](https://nirajiitr.com/blog/react-testing-2026-vitest-rtl-msw)
- [Testing: Vitest | Next.js 공식 문서](https://nextjs.org/docs/app/guides/testing/vitest)

### 8.46 Playwright E2E 테스트 도구 상세 설정 (확정)

> 8.38절에서 "핵심 구매 흐름·인증 플로우를 Playwright로 커버"라고 정했지만, **실제 토스 결제창까지 자동화할지, 로그인을 매번 새로 할지, 어느 환경을 대상으로 돌릴지**는 정하지 않았습니다. **실제 구축이 아닌 결정 단계**입니다.

| 항목 | 검토 대상 | 제안(추천) | 근거 |
|---|---|---|---|
| 테스트 대상 환경 | 로컬 실행 전용 vs **8.34절 PR 프리뷰 URL 대상** | CI에서는 Vercel Preview 배포가 끝난 후 **그 프리뷰 URL**을 대상으로 Playwright 실행 | 실제 배포된 환경(프론트+백엔드+8.34절 Supabase 임시 브랜치)에서 검증해야 "로컬에서는 됐는데 배포하니 깨지는" 사고를 잡을 수 있음. 로컬 실행도 별도로 가능하게 두되 CI 기준은 프리뷰 URL |
| 스키마 변경 PR의 프리뷰 | 프리뷰에서 전체 검증 vs **UI 확인용으로 한정** | 마이그레이션은 **머지 후 스테이징 배포 시에만 적용**하고, 새 컬럼을 요구하는 기능은 **머지 후 스테이징에서 검증**(8.57절 F-3) | 프리뷰가 스테이징 DB를 공유하므로(8.56절), 머지 전에 마이그레이션을 적용하면 **병합되지도 않은 스키마가 공용 DB에 남아** 다른 PR과 스테이징에 영향을 줌 |
| 테스트 데이터 정리 | 정리 로직 불필요(임시 브랜치 자동 삭제) vs **명시적 정리 로직 필요** | **테스트 전용 접두사**(예: `e2e-` 로 시작하는 계정·주문·상품)를 사용하고, 테스트 종료 시 해당 데이터만 삭제하는 단계를 둠 | **(8.56절 반영)** 당초에는 "PR 종료 시 Supabase 임시 브랜치가 삭제되므로 정리 불필요"로 정했으나, 8.56절에서 **브랜칭을 포기하고 스테이징 DB를 공유**하게 되면서 그 전제가 사라짐 — 정리하지 않으면 E2E 실행마다 스테이징에 테스트 데이터가 계속 누적됨 |
| 로그인 상태 재사용 | 테스트마다 매번 로그인 vs **`storageState`로 재사용** | 글로벌 셋업에서 고객/관리자(8.20절 SUPER_ADMIN·ADMIN) 계정으로 **역할별 로그인 상태 파일**을 한 번 생성해 재사용, **CI 실행마다 새로 생성**(git에 커밋하지 않음). 저장 직전 **장바구니 localStorage 키는 제거**(8.51절 C-8: 8.37절 Zustand `persist` 장바구니가 `storageState`에 함께 캡처되면 이전 테스트의 장바구니가 다음 테스트로 새어 들어감) | 매 테스트 로그인은 시간 낭비이자 8.11절 로그인 속도 제한에도 불필요하게 부딪힘. `storageState`는 쿠키(8.7절 Refresh Token)를 캡처하지만 **Access Token(메모리 보관, 8.37절)은 캡처되지 않으므로** 앱이 기동 시 자동으로 재발급받는 기존 흐름(8.22절)에 자연스럽게 의존 |
| 결제(토스) 플로우 테스트 | 실제 토스 테스트 결제창까지 자동화 vs **`page.route()`로 토스 API 응답 모킹** | 자동화 E2E는 **`page.route()`**로 토스 결제 성공/실패 응답을 가로채 시뮬레이션. 실제 토스 테스트 결제창까지 완주하는 검증은 자동화하지 않고 **9.1절 M5(QA·결제 실연동 검증) 수동 테스트**로 수행 | 외부 PG사의 결제창 UI에 의존하는 자동화 테스트는 토스 쪽 UI 변경만으로도 깨지기 쉬워 불안정(flaky)해짐. `page.route()`로 결제 성공/실패 두 경로를 안정적으로 재현하는 편이 CI 신뢰도가 높고, 진짜 PG 연동 자체의 정합성은 8.8절 로직 단위테스트(8.38절)와 M5 수동 QA가 이미 담당 |
| 브라우저 범위 | Chromium/Firefox/WebKit 전체 vs **Chromium 단독** | CI에서는 **Chromium 하나만** 실행 | 8.1절 팀 규모(5~6명)·3개월 일정에서 3개 엔진을 매번 돌리면 CI 시간이 배 이상 늘어남. 대부분의 사용자가 Chromium 기반 브라우저를 사용하므로 우선순위가 높은 조합만 자동화하고, 나머지는 필요시 수동 확인 |
| 셀렉터 전략 | CSS/XPath 셀렉터 vs **`getByRole` 우선** | Playwright 공식 권장대로 `getByRole` → `getByLabel`/`getByText` → `getByTestId` 순으로 사용 | `getByRole`은 접근성 트리 기준으로 요소를 찾으므로, 테스트 작성 자체가 8.39절의 접근성 목표(ARIA 구조가 올바른지)를 겸사겸사 검증해줌 |
| SSR/하이드레이션 결함 검출(8.64절 M-8) | 단위 테스트에 의존 vs **E2E 시나리오 + 콘솔 에러 수집** | 세 시나리오를 추가한다: ① 상품 상세에서 **PDF 미리보기 열기**(M-1), ② 장바구니에 담고 **새로고침 후 배지 유지** 확인(M-3), ③ 로그인 상태로 SSR 페이지 진입. ③은 **`page.on('console')`로 에러를 수집해 hydration 에러가 있으면 테스트를 실패**시킨다 | M-1은 빌드 단계, M-2·M-3은 **서버 렌더와 클라이언트 렌더가 만나는 지점**에서만 드러나므로 8.45/8.47절의 Vitest·Testing Library로는 재현되지 않는다. hydration 에러는 화면이 결국 그려지기 때문에 사람 눈으로도 놓치기 쉬워, 콘솔을 테스트 실패 조건으로 삼는 것이 유일한 자동 검출 수단 |
| 시각적 회귀 테스트 | 스크린샷 비교 도입 vs **도입하지 않음** | 이번 범위에서는 기능 동작 검증에 집중하고 픽셀 단위 시각적 회귀는 다루지 않음 | 스크린샷 비교는 사소한 디자인 변경에도 계속 깨져 유지보수 비용이 큼 — 8.1절 팀 규모·3개월 일정 대비 우선순위가 낮음 |

Sources:
- [Playwright Best Practices: 8 Patterns for Stable E2E (2026)](https://getautonoma.com/blog/playwright-best-practices-2026)
- [TesterArmy: Testing Authentication in E2E: The Complete Playwright Guide](https://tester.army/blog/how-to-handle-authentication-in-playwright-e2e-tests)

### 8.47 Testing Library(React Testing Library) UI 컴포넌트 테스트 상세 (확정)

> 8.38/8.45절에서 "Vitest + React Testing Library"라고만 정하고, **정확히 무엇을 테스트 대상으로 삼고 어떻게 렌더링할지**는 정한 적이 없었습니다. **실제 구축이 아닌 결정 단계**입니다.

| 항목 | 검토 대상 | 제안(추천) | 근거 |
|---|---|---|---|
| 테스트 대상 범위 | shadcn/ui 컴포넌트까지 포함한 전체 UI vs **우리가 작성한 비즈니스 로직 컴포넌트만** | 8.39절 shadcn/ui 컴포넌트 자체(버튼·다이얼로그 등)는 테스트하지 않고, 그 위에 **우리가 조합한 화면/폼 컴포넌트**(장바구니, 결제 폼, 상품 카드 등)만 대상으로 함 | shadcn/ui는 이미 검증된 라이브러리 소스를 그대로 가져다 쓰는 것(8.39절)이라 재검증할 실익이 적음 — 우리 비즈니스 로직에 테스트 자원을 집중 |
| Server/Client 컴포넌트 경계 | 모든 컴포넌트를 RTL로 시도 vs **동기 컴포넌트만 RTL, 비동기 Server Component는 제외** | RTL은 **Client Component**와 **동기 Server Component**만 대상으로 하고, 비동기 Server Component(데이터 페칭 포함)는 8.46절 결정대로 **Playwright E2E**가 검증 | 8.38절에서 이미 "Vitest는 비동기 Server Component를 지원하지 않는다"고 확인했음 — 8.47절은 그 경계를 컴포넌트 테스트 레벨에서 다시 한 번 명확히 함 |
| 공통 렌더 함수 | 매 테스트마다 Provider를 개별적으로 감싸기 vs **`renderWithProviders()` 공통 헬퍼** | 8.45절의 테스트 전용 `QueryClient`(TanStack Query)와 리셋된 Zustand 스토어를 자동으로 감싸주는 공통 렌더 함수를 만들어 모든 컴포넌트 테스트에서 재사용 | 컴포넌트 대부분이 8.37절 상태관리(TanStack Query/Zustand)에 의존하므로, 테스트마다 Provider 보일러플레이트를 반복하면 유지보수 비용이 커짐 |
| 이벤트 시뮬레이션 | `fireEvent` vs **`userEvent`** | 클릭·입력 등 모든 상호작용에 **`@testing-library/user-event`** 사용(비동기 API라 `await` 필요) | `userEvent`가 실제 브라우저 사용자 조작(포커스 이동, 키 입력 순서 등)에 더 가깝게 이벤트를 발생시켜 `fireEvent`보다 신뢰도 높은 테스트를 만듦 |
| 쿼리 전략 | CSS 셀렉터/`data-testid` 위주 vs **`getByRole` 우선** | 8.46절 Playwright와 동일하게 `getByRole` → `getByLabelText`/`getByText` → `getByTestId` 순서로 통일 | 두 테스트 도구(RTL/Playwright)의 쿼리 전략을 통일하면 팀이 규칙을 하나만 기억하면 되고, 접근성 트리 기준 쿼리가 8.39절 접근성 목표 검증도 겸함 |
| 접근성 자동 검사 | 수동 확인에만 의존 vs **`jest-axe`(Vitest에서도 동작) 도입** | 회원가입·결제 폼처럼 **입력이 많은 핵심 컴포넌트**에 한해 `jest-axe`로 ARIA 위반을 자동 검사 | 8.39절(shadcn/ui의 Radix/Base UI 접근성 프리미티브)과 6절(웹 접근성 기본 준수) 목표를 코드로 검증하는 최소한의 안전망. 전체 컴포넌트에 강제하면 3개월 일정에 부담이므로 핵심 폼에만 한정 |
| 스냅샷 테스트 | 도입 vs **도입하지 않음** | HTML 구조 스냅샷(`toMatchSnapshot`)도 8.46절과 같은 이유로 미도입 | 8.46절에서 이미 "시각적 회귀 테스트 미도입"을 결정한 것과 같은 논리 — 스냅샷은 사소한 마크업 변경에도 깨져 유지보수 비용이 테스트 가치보다 커지기 쉬움 |
| Next.js 훅 모킹 | 컴포넌트마다 개별 모킹 vs **공통 테스트 유틸에서 `next/navigation` 등 표준 모킹 제공** | `useRouter`/`usePathname` 등 Next.js 훅을 공통 유틸에서 한 번만 모킹해 재사용 | App Router의 Next.js 전용 훅은 RTL 테스트 환경에서 기본 동작하지 않으므로 표준 모킹이 필요 — 이를 컴포넌트마다 반복하지 않도록 공통화 |

Sources:
- [How to Configure React Testing Library](https://oneuptime.com/blog/post/2026-01-24-react-testing-library-configuration/view)
- [Test Strategy in the Next.js App Router Era: Jest, RTL, and Playwright](https://shinagawa-web.com/en/blogs/nextjs-app-router-testing-setup)

### 8.48 ESLint 정적 코드 검사 (확정)

> 8.35절 CI 파이프라인에 "Lint" 단계가 있고 8.36절에 `packages/eslint-config`가 트리에 그려져 있었지만, **실제로 어떤 버전·구조·규칙**으로 구성할지는 정한 적이 없었습니다. **실제 구축이 아닌 결정 단계**입니다.

| 항목 | 검토 대상 | 제안(추천) | 근거 |
|---|---|---|---|
| 버전/설정 형식 | 레거시 `.eslintrc` vs **ESLint 최신 버전(flat config, `eslint.config.mjs`)** | 최신 ESLint(2026년 기준 9는 유지보수 모드로 전환되어 최신 버전 사용) + flat config 형식 채택 | flat config가 현재 표준이며, Next.js 15/16도 이를 기본 지원함. 레거시 형식으로 새로 시작할 이유가 없음 |
| 모노레포 설정 공유 | 앱마다 독립적인 설정 vs **`packages/eslint-config`에서 base/next/nestjs로 분리 공유** | 8.36절에서 이미 자리를 잡아둔 `packages/eslint-config` 안에 `base.js`(공통 TS 규칙) + `next.js`(`eslint-config-next` 확장) + `nestjs.js`(NestJS 데코레이터 패턴 고려)로 나누고, 각 앱은 필요한 것만 import | 업계에서 권장하는 모노레포 ESLint 공유 패턴과 일치. 두 앱(`apps/web`, `apps/api`)이 공통 규칙은 재사용하면서 프레임워크별 특화 규칙만 다르게 가져갈 수 있음 |
| Prettier와의 역할 분리 | ESLint가 포맷팅까지 검사 vs **포맷팅은 Prettier, 품질은 ESLint** | `eslint-config-prettier`로 ESLint의 포맷팅 관련 규칙을 비활성화해 Prettier와 충돌 방지 | 두 도구가 같은 것(들여쓰기, 줄바꿈 등)을 서로 다르게 검사하면 저장할 때마다 규칙이 충돌하는 경험을 하게 됨 — 역할을 명확히 나눠야 함 |
| `dependency-cruiser`(8.21절)와의 역할 분리 | 중복 검사 vs **역할 분리 유지** | ESLint는 코드 품질·버그 패턴(예: 미사용 변수, 잘못된 훅 사용)을, **`dependency-cruiser`는 모듈 경계**(8.21절, 도메인 간 잘못된 import)를 각자 담당 | 두 도구 모두 "import 관련 검사"를 할 수 있어 겹치는 것처럼 보이지만, ESLint는 파일 단위 규칙에 강하고 dependency-cruiser는 프로젝트 전체 의존성 그래프 분석에 특화되어 있어 상호보완적 |
| 보안 규칙 강제(8.63절 L-3) | 코드 리뷰로 잡음 vs **`react/no-danger`를 error로 추가** | Next.js 설정에 **`react/no-danger`를 `error`** 로 넣어 `dangerouslySetInnerHTML` 사용 자체를 차단 | 8.61절 J-5는 XSS를 막으려 "서버는 스니펫 오프셋만 주고 프론트가 문자열을 쪼개 감싼다"로 정했는데, 프론트에서 가장 흔한 하이라이트 구현(`snippet.replace(new RegExp(q,'gi'), '<mark>$&</mark>')` + `dangerouslySetInnerHTML`)을 쓰면 **정확히 J-5가 막으려던 상태로 되돌아간다**. 자사 콘텐츠에 HTML·JS 교재가 있으면 본문에 실제로 `<script>`가 들어 있으므로, 규율이 아니라 린트로 막는다 |
| 커밋 전 검사 | CI에서만 검사 vs **Husky + lint-staged로 커밋 전 로컬 검사 병행** | 커밋 시 **변경된 파일만** 대상으로 ESLint 실행 | 8.35절 CI에서야 린트 오류를 발견하면 되돌아가 수정하고 다시 푸시하는 왕복 비용이 생김 — 커밋 시점에 조기 발견하면 CI 실패 자체를 줄일 수 있음. 전체 파일이 아닌 변경분만 검사해 커밋 속도 저하도 최소화 |
| 신규 초고속 린터(oxlint) 도입 | 병행 도입 vs **이번엔 도입하지 않음** | Rust 기반 oxlint는 매우 빠르지만 2026년 기준 일부 ESLint 플러그인이 아직 대응하지 않은 상태 — ESLint 단독 유지 | 8.1절 팀 규모·3개월 일정에서 툴체인을 하나 더 얹는 리스크(플러그인 미지원으로 인한 예외 처리 등)가 이득보다 큼. CI 린트 속도가 실제 병목이 되면 그때 재검토 |

Sources:
- [How to use Eslint v9 in a monorepo with flat config file format](https://medium.com/@felipeprodev/how-to-use-eslint-v9-in-a-monorepo-with-flat-config-file-format-8ef2e06ce296)
- [ESLint | Turborepo 공식 문서](https://turborepo.dev/docs/guides/tools/eslint)
- [Share Configurations - ESLint 공식 문서](https://eslint.org/docs/latest/extend/shareable-configs)

### 8.49 Prettier 포맷팅 도구 상세 설정 (확정)

> 8.48절에서 "포맷팅은 Prettier, 품질은 ESLint"로 역할만 나눴을 뿐, **Prettier 자체를 모노레포에서 어떻게 구성할지**는 정한 적이 없었습니다. **실제 구축이 아닌 결정 단계**입니다.

| 항목 | 검토 대상 | 제안(추천) | 근거 |
|---|---|---|---|
| 설정 위치 | ESLint(8.48절)처럼 앱별로 분리(base/next/nestjs) vs **리포지토리 루트에 단일 설정 파일 하나** | 루트에 `prettier.config.js` 하나만 두고 `apps/web`·`apps/api` 모두 동일하게 적용 | 포맷팅 규칙(들여쓰기, 따옴표 등)은 프레임워크와 무관하게 보편적이라, ESLint처럼 앱별 규칙을 분리할 실익이 없음 — 오히려 하나로 통일하는 편이 일관성 있음 |
| 스타일 값(세미콜론/따옴표 등) | 팀 취향에 맞춰 커스터마이징 vs **Prettier 기본값 그대로 사용** | 옵션을 거의 건드리지 않고 기본값 유지 | Prettier의 존재 이유 자체가 "스타일 논쟁을 없애는 것" — 커스터마이징을 늘릴수록 그 취지가 옅어짐. 8.1절 팀 규모에서 스타일 값으로 시간을 쓸 이유가 없음 |
| Tailwind 클래스 정렬 | 수동 정렬 vs **`prettier-plugin-tailwindcss`(공식 플러그인)** | 8.39절에서 확정한 Tailwind CSS v4에 맞춰 `tailwindStylesheet` 옵션으로 CSS 엔트리 포인트를 지정하고 클래스 순서를 자동 정렬 | Tailwind Labs가 직접 유지보수하는 공식 플러그인이라 신뢰도가 높고, 클래스가 길어지는 Tailwind 특성상 정렬이 안 되면 가독성이 빠르게 나빠짐 |
| import 문 정렬 | Prettier 플러그인으로 처리 vs **8.48절 ESLint가 계속 담당** | import 순서 정렬은 Prettier가 아니라 **ESLint**(`packages/eslint-config`) 쪽 규칙으로 유지 | import 정렬은 포맷팅이 아니라 "코드 구조 규칙"에 가까워 8.48절에서 이미 정한 ESLint/Prettier 역할 분리 원칙(품질=ESLint, 포맷=Prettier)에 따라 ESLint 쪽에 둠 |
| Prisma 스키마 포맷팅 | Prettier가 처리 vs **Prisma 자체 CLI(`prisma format`)가 담당** | `apps/api/prisma/schema.prisma`(8.36절)는 Prettier 대상에서 제외하고 Prisma CLI의 자체 포맷터를 사용 | Prisma 스키마는 Prettier가 이해하는 문법이 아니므로 애초에 대상이 될 수 없음 — 명시적으로 범위 밖임을 문서화해 혼동 방지 |
| 커밋/CI 검사 | Husky+lint-staged(8.48절)에만 의존 vs **CI에 `prettier --check` 안전망 병행** | 로컬 커밋 시 lint-staged가 자동 포맷하되, 8.35절 CI 파이프라인에도 `prettier --check` 단계를 추가 | 로컬 훅은 `--no-verify`로 건너뛰거나 팀원이 훅을 설치하지 않은 상태로 푸시할 수 있음 — CI가 최종 안전망 역할을 함 |
| 에디터 설정 공유 | 팀원 각자 에디터 설정 vs **`.vscode/settings.json` 공유** | "저장 시 자동 포맷" 설정을 리포지토리에 커밋해 팀 전체가 동일하게 적용 | 팀원마다 저장 시 포맷 여부가 다르면 커밋마다 불필요한 포맷 변경 diff가 섞여 리뷰가 지저분해짐 |

Sources:
- [prettier-plugin-tailwindcss - GitHub (Tailwind Labs 공식)](https://github.com/tailwindlabs/prettier-plugin-tailwindcss)
- [Boost Your Tailwind Workflow with ESLint and Prettier](https://ouassim.tech/notes/boost_your_tailwind_workflow_with_eslint_and_prettier/)

### 8.50 GA4 제품 분석 이벤트 추적 상세 설계 (확정)

> 8.44절에서 GA4 도입과 이벤트 프리셋 이름(`sign_up`, `view_item` 등)만 나열했는데, **가장 중요한 `purchase`(매출) 이벤트를 클라이언트에서만 보내면 광고 차단기·페이지 이탈로 유실될 수 있다**는 점을 이번에 확인했습니다. 이 문제를 포함해 전체 이벤트를 FR 기준으로 구체화했습니다. **실제 구축이 아닌 결정 단계**입니다.

| 항목 | 검토 대상 | 제안(추천) | 근거 |
|---|---|---|---|
| 이벤트 전송 위치 이원화 | 모든 이벤트를 클라이언트(`sendGAEvent`)에서만 전송 vs **매출 핵심 이벤트는 서버 전송 병행** | 탐색성 이벤트(`view_item`, `add_to_cart` 등)는 8.44절 그대로 클라이언트 전송. **`purchase`는 NestJS가 8.8절 결제 승인 성공 시점에 GA4 Measurement Protocol로 서버 전송** | 광고 차단기가 GA4 스크립트 자체를 막는 경우가 흔하고, 결제 완료 직후 사용자가 바로 탭을 닫으면 클라이언트 이벤트가 전송되기 전에 유실될 수 있음. `purchase`는 매출 집계의 근거가 되는 가장 중요한 이벤트라 **서버가 신뢰할 수 있는 소스**에서 직접 전송하는 것이 안전 |
| 서버-클라이언트 세션 연결 | 서버 이벤트를 별도 세션으로 취급 vs **`client_id`를 프론트→백엔드로 전달해 연결** | 결제 요청 시 프론트가 GA4의 `client_id`(브라우저 `_ga` 쿠키 기반)를 NestJS에 함께 전달하고, Measurement Protocol 페이로드에 포함 | `client_id` 없이 서버에서 임의로 이벤트를 보내면 GA4가 그 사용자의 기존 탐색 이력(view_item, add_to_cart 등)과 연결하지 못해 퍼널 분석이 끊어짐 |
| 이벤트 분류 체계 | GA4 표준 프리셋만 사용 vs **표준 이벤트 + 서비스 고유 커스텀 이벤트 병행** | 아래 "이벤트 매핑표" 참고 — 표준 전자상거래 이벤트 외에 `sample_preview_opened`(8.29절), `download_completed`(8.4/8.11절), `coupon_applied`(FR-6.1), `search_no_results`(8.27/8.31절) 같은 서비스 고유 이벤트 추가 | 표준 이벤트만으로는 "샘플을 본 사람이 실제로 얼마나 구매하는지", "검색했는데 결과가 없었던 검색어가 무엇인지"처럼 이 서비스 특유의 질문에 답할 수 없음 |
| 이벤트 정의 관리 | 각자 하드코딩 vs **`packages/shared-types`에 이벤트 이름/파라미터 타입 공유** | 8.17/8.40절과 같은 패턴으로, 이벤트 이름 상수와 파라미터 타입을 공유 패키지에 정의해 프론트(클라이언트 전송)·백엔드(서버 전송) 양쪽에서 재사용 | 이벤트 이름에 오타가 나거나 프론트/백엔드가 같은 이벤트를 다른 파라미터로 보내면 GA4 리포트에서 데이터가 갈라짐 — 하나의 정의만 존재하게 함 |
| 개인정보 포함 금지 | 이메일 등 식별정보를 파라미터에 포함 vs **PII 절대 미포함** | 이벤트 파라미터에는 상품ID·카테고리·금액처럼 비식별 정보만 포함하고, 이메일·실명·전화번호는 어떤 이벤트에도 담지 않음 | GA4 자체 이용약관이 PII 전송을 금지하며, 8.44절의 개인정보처리방침 고지 범위를 벗어나지 않기 위한 원칙 |

**이벤트 매핑표(발췌)**

| 이벤트 | 발생 시점 | 전송 위치 | 관련 절 |
|---|---|---|---|
| `sign_up` | 회원가입 완료 | 클라이언트 | FR-1.1 |
| `view_item` / `view_item_list` | 상품 상세/목록 조회 | 클라이언트 | FR-2.3, FR-2.4 |
| `search` / `search_no_results` | 검색 실행 / 결과 없음 | 클라이언트 | 8.27/8.31절 |
| `sample_preview_opened` | 샘플 PDF 미리보기 오픈 | 클라이언트 | 8.29절 |
| `add_to_cart` / `view_cart` | 장바구니 담기/조회 | 클라이언트 | FR-3.1 |
| `coupon_applied` | 쿠폰 적용 | 클라이언트 | FR-6.1 |
| `begin_checkout` | 주문서 진입 | 클라이언트 | FR-3.2 |
| **`purchase`** | **결제 승인 성공** | **서버(NestJS, Measurement Protocol)** | 8.8절 |
| `download_completed` | 다운로드 URL 발급 성공 | 클라이언트 | 8.4/8.11절 |

Sources:
- [Measurement Protocol reference | Google Analytics 공식 문서](https://developers.google.com/analytics/devguides/collection/protocol/ga4/reference)
- [GA4 Measurement Protocol: Implementation Guide](https://ga4experts.com/ga4-measurement-protocol-complete-implementation-guide/)
- [How to Validate GA4 Measurement Protocol Ecommerce](https://anatainc.com/blog/validate-ga4-measurement-protocol-ecommerce)

### 8.51 기술 스택 상호 호환성 감사 (확정 — 9건 전부 수정 반영)

> 8.1~8.50에서 내린 결정들을 **개별적으로는 타당하지만 서로 맞물렸을 때 실제로 깨지는 지점**이 있는지 교차 검증했습니다. 그 결과 **설계대로 구현하면 동작하지 않는 심각 충돌 3건**과 보완이 필요한 6건을 발견했습니다.

#### 심각 — 현재 설계대로면 동작하지 않음 (**3건 모두 수정 확정, 관련 절·스키마에 반영 완료**)

| # | 충돌 지점 | 문제 | 제안 수정 |
|---|---|---|---|
| **C-1** | **8.12/8.25(세션 단위 Advisory Lock) × 8.15(Supavisor 트랜잭션 모드 풀러)** | 8.12절은 `pg_try_advisory_lock()`+`pg_advisory_unlock()`(**세션 단위** 락)을 쓰고, 8.25절 주문 중복방지도 이를 재사용함. 그런데 8.15절에서 런타임 커넥션을 **트랜잭션 모드 풀러**로 정했기 때문에, 매 트랜잭션이 서로 다른 백엔드 커넥션에 배정될 수 있음 → **락을 건 커넥션과 해제하는 커넥션이 달라져 락이 영구히 남거나(누수), 반대로 의도한 상호배제가 전혀 걸리지 않음** | 세션 단위 락 대신 **트랜잭션 단위 락(`pg_advisory_xact_lock` / `pg_try_advisory_xact_lock`)** 으로 전환. 트랜잭션 종료 시 자동 해제되므로 풀러 환경에서 안전하며, 명시적 unlock 호출 자체가 불필요해짐 |
| **C-2** | **8.7(Refresh 쿠키 `Path=/auth/refresh` 제한) × 8.22(BFF Route Handler `/api/auth/refresh` + middleware)** | 브라우저는 쿠키 `Path` 조건에 맞는 요청에만 쿠키를 보냄. 8.22절 BFF 핸들러 경로는 `/api/auth/refresh`라 `/auth/refresh` 조건에 **맞지 않아 쿠키가 전송되지 않고**, 더 근본적으로 8.22절 `middleware.ts`가 `/mypage` 같은 보호 경로에서 "Refresh 쿠키 존재 여부"를 확인하려 해도 **그 경로에는 애초에 쿠키가 오지 않음** → SSR 토큰 릴레이와 미들웨어 1차 체크가 둘 다 무력화 | **`Path` 제한을 제거**하고 `Domain=.도메인; HttpOnly; Secure; SameSite=Lax`로만 보호. Path 제한은 어차피 XSS·CSRF 방어의 주된 수단이 아니며(HttpOnly/SameSite가 담당), 8.22절 설계를 살리려면 제거가 불가피 |
| **C-3** | **8.31(`pg_trgm`으로 책 본문 전문검색) × pg_trgm의 동작 원리** | `pg_trgm` 유사도는 **두 문자열 전체의 트라이그램 집합 비율**로 계산됨. 수십만 자 분량의 책 본문과 검색어 몇 글자를 비교하면 유사도가 0에 수렴해 **"이 단어가 책에 있는가"를 판별하지 못함**(짧은 제목 검색인 8.27절에서는 정상 동작). 또한 본문 전체에 GIN 트라이그램 인덱스를 걸면 인덱스 크기가 본문보다 몇 배 커질 수 있음 | 8.31절만 **PostgreSQL 표준 전문검색(`tsvector` + GIN)** 으로 변경. 덤으로 **`ts_headline`이 검색어 주변 스니펫 생성을 기본 제공**해 8.31절이 요구한 "앞뒤 15~20자 스니펫"을 직접 구현할 필요가 없어짐. **8.27절(제목·초성)은 pg_trgm 그대로 유지**(짧은 문자열에는 적합) |

#### 보완 필요 (**6건 모두 수정 확정, 관련 절에 반영 완료**)

| # | 충돌 지점 | 문제 | 적용한 수정 |
|---|---|---|---|
| C-4 | 8.34(Supabase PR 프리뷰 브랜치) × 8.15(수동 생성 `cbmall_app` 롤) | 롤은 클러스터 단위 객체라 `roles.sql`을 수동 1회 실행하는 방식으로는 **PR마다 새로 만들어지는 프리뷰 브랜치에 롤이 존재하지 않음** → 프리뷰 환경 백엔드가 접속 실패 | **8.56절에서 근본 해소** — 심층 검토 결과 Supabase 브랜칭 자체가 우리 구성(Prisma 마이그레이션 + ECS 백엔드)과 맞지 않아 **브랜칭을 포기**했고, 프리뷰가 스테이징 DB를 공유하게 되면서 "프리뷰에 롤이 없는" 문제 자체가 사라짐 |
| C-5 | 8.29(react-pdf) × 8.10(S3 공개 버킷) | react-pdf(PDF.js)는 브라우저에서 **fetch로 PDF를 가져와 캔버스에 렌더링**하므로 크로스 오리진 요청이 발생 — S3 버킷에 CORS 설정이 없으면 로드 실패(단순 `<iframe>`이었다면 불필요했던 제약) | 8.23절 정적 자산 항목에 **공개 버킷 CORS 규칙(프론트 도메인 `GET` 허용)** 명시, 11절 다음 단계에도 설정 작업 추가 |
| C-6 | 8.23(NestJS→Next.js `revalidateTag` 호출) × 8.22("Route Handler는 인증 릴레이 1개만") | 캐시 무효화를 받으려면 Next.js에 **두 번째 Route Handler**가 필요한데 8.22절 원칙과 충돌하고, 이 엔드포인트의 **인증 수단도 정의되지 않음**(누구나 호출하면 캐시 무효화 남용 가능) | 8.22절 원칙을 "**범용 BFF 프록시는 만들지 않는다**"로 정정하고 인증 릴레이·캐시 무효화 2개를 명시적 예외로 지정. 8.23절에 **공유 시크릿(Secrets Manager) 검증 필수** 추가 |
| C-7 | 8.33(worker_threads) × 1 vCPU 태스크 가정 | 워커 스레드는 **이벤트 루프 블로킹은 막지만 CPU 총량을 늘려주지는 않음** — 1 vCPU 태스크에서 대용량 PDF를 파싱하면 같은 코어를 두고 경합해 API 응답 지연이 발생 | ECS 태스크 스펙을 **2 vCPU / 4GB로 확정**(`maxThreads: 1` 유지 → 한 코어는 PDF 파싱, 다른 코어는 API·GC 담당). 8.33절에 미정으로 남아 있던 "메모리는 부하 테스트 후 확정" 항목도 함께 해소 |
| C-8 | 8.37(Zustand `persist` 장바구니) × 8.46(Playwright `storageState`) | `storageState`는 localStorage를 함께 저장하므로, 로그인 상태를 재사용하면 **이전 테스트의 장바구니 데이터까지 딸려 들어와** 테스트 간 간섭 발생 가능 | 8.46절에 **`storageState` 저장 직전 장바구니 localStorage 키 제거** 단계 명시 |
| C-9 | 8.28(전역 rate limit 인메모리) × 8.11(2개 이상 태스크) | 이미 8.28절에서 수용한 트레이드오프지만, **실효 한도가 `분당 100회 × 태스크 수`** 라는 점이 문서에 없어 혼선 소지 | 8.28절에 **실효 한도 계산식 명시**(정책 변경 없음, 문서화만) |

Sources: (내부 교차 검증 — 8.6/8.7/8.10/8.11/8.12/8.15/8.22/8.23/8.25/8.27/8.28/8.29/8.31/8.33/8.34/8.37/8.46절 상호 대조)

### 8.52 세션 단위 Advisory Lock × 풀러 비호환 심층 검토 (확정)

> 8.51절 C-1에서 "세션 단위 락 → 트랜잭션 단위 락(`pg_try_advisory_xact_lock`)"으로 수정했는데, **그 수정이 모든 Cron 작업에 대해 성립하는지** 다시 검증했습니다. 결론: **원인 분석과 방향은 맞지만, 외부 I/O를 하는 작업 2건(8.18 알림 재시도, 8.32 PDF 추출 재시도)에는 그대로 적용하면 또 다른 안티패턴이 됩니다.** **실제 구축이 아닌 결정 단계**입니다.

#### 왜 깨지는가 (메커니즘)

| 단계 | 일어나는 일 |
|---|---|
| 1 | `pg_try_advisory_lock(k)`는 **백엔드 프로세스(세션)** 에 락을 붙임 — 트랜잭션이 끝나도 유지됨 |
| 2 | Supavisor 트랜잭션 모드는 **트랜잭션이 끝나면 백엔드를 풀에 반납** — 락은 그 백엔드에 **붙은 채로** 남음 |
| 3 | 뒤이은 `pg_advisory_unlock(k)`는 **다른 백엔드**에 배정될 수 있음 → `false` 반환, 원래 락은 **해제되지 않고 누수** |
| 4 | 더 나쁜 경우: 락이 남은 그 백엔드를 **다른 요청이 물려받아**, 자신이 건 적 없는 락을 보유한 상태가 됨 |

**증상**: 첫 실행 이후 모든 `pg_try_advisory_lock`이 실패해 **해당 Cron이 영구히 스킵**되거나, 무관한 요청이 락을 상속받음. 예외도 에러 로그도 남지 않아(정상적으로 "락 못 얻음 → skip" 경로를 타므로) **조용히 죽는 것이 가장 위험한 점**.

#### C-1 수정안 재검증 — 조건부로만 옳음

`pg_try_advisory_xact_lock`은 트랜잭션 종료 시 자동 해제되므로 풀러 환경에서 안전합니다. **단, 상호배제가 실제로 성립하려면 작업 본문 전체가 그 트랜잭션 안에서 끝나야 합니다.** 락만 잡고 커밋한 뒤 별도 트랜잭션에서 일하면 락은 이미 풀린 상태라 아무 보호도 되지 않습니다.

그래서 **작업이 길거나 외부 호출을 포함하면** 이 방식은 "장시간 트랜잭션 + 트랜잭션 내 네트워크 I/O"라는 별도의 안티패턴이 됩니다(DB 커넥션 점유, VACUUM 지연, Prisma 인터랙티브 트랜잭션 기본 타임아웃에도 저촉).

#### 작업 유형별 최종 정책

| 유형 | 해당 작업 | 방식 | 근거 |
|---|---|---|---|
| **A. 짧은 순수 DB 작업** | PENDING 주문 취소(8.8) · `rate_limit_buckets` 정리(8.11) · 만료 토큰 정리(8.7) · 추출 정체 감지·실패율 집계(8.32) | **`pg_try_advisory_xact_lock` + 짧은 트랜잭션**(C-1 그대로 유지) | 단일 UPDATE/DELETE 수준이라 트랜잭션 안에서 전부 끝남 — 락 유지 구간과 작업 구간이 일치해 상호배제가 실제로 성립 |
| **B. 외부 I/O 포함 작업** | **알림 재시도(8.18: Resend·솔라피·FCM 호출)** · **PDF 추출 재시도(8.32: S3 다운로드 + 파싱)** | **Advisory Lock 사용하지 않음. `FOR UPDATE SKIP LOCKED` 행 클레이밍으로 전환** | 짧은 트랜잭션에서 처리할 행만 **원자적으로 선점**(status→처리중, `claimed_at` 기록)하고 **커밋 후 트랜잭션 밖에서** 외부 호출 → 장시간 트랜잭션이 사라짐. 덤으로 태스크들이 **서로 다른 행을 나눠 동시에 처리**할 수 있어, 락 방식(한 태스크만 일함)보다 처리량도 좋아짐 |

**B형 처리 흐름**: ① 짧은 트랜잭션에서 `SELECT ... WHERE 처리대상 ORDER BY ... FOR UPDATE SKIP LOCKED LIMIT n` → 선점 표시 후 커밋 → ② 트랜잭션 밖에서 외부 API 호출 → ③ 결과를 별도 짧은 트랜잭션으로 기록. 처리 도중 태스크가 죽으면 `claimed_at`이 일정 시간 지난 행을 **다시 선점 가능**하게 해 복구. **재선점 대기 시간은 알림 outbox = 1분**으로 둔다(8.58절 G-4: 8.18절 긴급 프로파일이 1·3·10분 간격으로 총 14분 안에 끝나야 하므로, 5분 대기는 워커가 한 번만 죽어도 긴급 예산을 통째로 날림. 외부 API 타임아웃보다는 충분히 길어 정상 처리 중인 행을 뺏지 않음). 다만 **PDF 텍스트 추출(8.32절)은 추출 자체가 수 분 걸릴 수 있으므로 10분을 유지**한다 — 같은 B형이라도 대기 시간은 "작업 1건의 정상 처리 시간"보다 길게 잡아야 중복 처리가 생기지 않는다. **이벤트 기반 즉시 발송도 반드시 이 클레임 절차를 거친다**(8.58절 G-1). **외부 API 호출 타임아웃은 10초로 고정한다**(8.72절 U-5) — 이 값이 정해져 있지 않으면 8.18절 재시도 예산의 기준이 없어진다(4회면 타임아웃만으로 최대 수 분이 추가된다). 8.69절 R-2에서 SSR 릴레이에 3초를 건 것과 같은 취지로, **빠르게 실패해야 재시도 예산이 지켜진다**.

#### 검증·모니터링 방법

| 항목 | 방법 |
|---|---|
| 락 누수 감시 | `SELECT * FROM pg_locks WHERE locktype = 'advisory'` 를 주기적으로 확인 — 실행 중인 Cron이 없는데 advisory 락이 남아 있으면 누수 신호 |
| 상호배제 동작 검증 | 스테이징(8.34절)에서 **태스크 2개가 동시에 같은 Cron을 실행**하도록 유도해, A형은 한쪽만 수행되는지 / B형은 서로 다른 행을 나눠 갖는지 확인 |
| 조용한 실패 감지 | Cron이 "락 획득 실패로 skip"한 횟수를 8.24절 구조화 로그로 남기고, **연속 스킵이 비정상적으로 길어지면 Sentry 경고**(락 누수 시 조용히 죽는 것을 잡는 안전망) |

Sources:
- [Postgres advisory locks, PgBouncer, and pool (락 누수·상속 사례)](https://www.snowinch.com/en/blog/postgres-advisory-lock-connection-pool-leak)
- [PgBouncer is useful, important, and fraught with peril](https://jpcamara.com/2023/04/12/pgbouncer-is-useful.html)
- [PostgreSQL Advisory Locks for Distributed Job Scheduling](https://dev.to/software_mvp-factory/postgresql-advisory-locks-for-distributed-job-scheduling-15kp)

### 8.53 Cookie Path 제한 × BFF SSR 토큰 릴레이 심층 검토 (확정)

> 8.51절 C-2에서 "쿠키 `Path` 제한 제거"로 수정했는데, 이것이 **8.7절의 Refresh 토큰 회전(rotation)·재사용 탐지와 맞물렸을 때도 성립하는지** 재검증했습니다. 결론: **Path 제거는 필요조건일 뿐이고, 그것만으로는 SSR 릴레이가 오히려 사용자를 강제 로그아웃시킵니다.** **실제 구축이 아닌 결정 단계**입니다.

#### 재검증 결과 — Path 제거만으로는 부족

| 단계 | 현재 설계대로 진행하면 |
|---|---|
| 1 | Server Component가 SSR 중 BFF 릴레이로 NestJS `/auth/refresh` 호출 |
| 2 | NestJS는 8.7절 정책대로 **Refresh 토큰을 회전**하고 새 토큰을 `Set-Cookie`로 응답 |
| 3 | 그런데 **Next.js는 Server Component에서 쿠키 쓰기를 금지**함 → 회전된 새 토큰이 **그대로 버려짐**(브라우저에 전달 안 됨) |
| 4 | 브라우저에는 **이미 무효화된 구 토큰**만 남음 |
| 5 | 다음 요청에서 그 구 토큰을 제시 → 8.7절 **재사용 탐지 발동 → 해당 사용자의 전체 세션 강제 로그아웃** |

즉, 보호된 페이지를 SSR로 한 번 여는 것만으로 로그아웃되는 구조입니다. 게다가 이는 "보안 침해 탐지"로 기록되므로 **정상 사용자가 침해 사고로 오탐**되기까지 합니다.

#### 해결 방안 비교

| 방안 | 내용 | 평가 |
|---|---|---|
| A. 미들웨어에서 회전 수행 | `middleware.ts`는 쿠키 쓰기가 가능하므로 여기서 회전하고 Access Token은 요청 헤더로 전달 | 회전 의미론은 지키지만, **보호된 페이지를 열 때마다 회전이 발생**해 DB 쓰기가 늘고, 프리페치·병렬 탭처럼 **동시 요청이 같은 Refresh 토큰을 동시에 소비하면 한쪽이 재사용 탐지에 걸려** 같은 문제가 재현됨 |
| **B. SSR 전용 비회전 엔드포인트(추천)** | NestJS에 **회전하지 않고 검증만 하는** SSR 전용 토큰 발급 엔드포인트를 두고, 쿠키를 **읽기만** 하는 Server Component가 이를 호출 | 쿠키 쓰기가 아예 필요 없어 Next.js 제약과 무관해지고, 회전 경쟁 조건도 발생하지 않음. 대신 **서버 간 공유 시크릿**(C-6 revalidate와 동일 패턴)으로 호출자를 Next.js 백엔드로 제한하고, 발급되는 Access Token의 수명을 짧게 유지 |
| C. 개인화 데이터 SSR 포기 | 마이페이지 등은 클라이언트에서 조회 | 가장 단순하지만 8.22절에서 SSR 릴레이를 도입한 목적 자체가 사라짐 |

#### 추가 보완 — 회전 유예 기간

방안 B를 택해도 **브라우저 경로에서의 동시 refresh**(예: 여러 API 호출이 동시에 401을 받아 각자 refresh 시도)는 여전히 재사용 탐지 오탐을 유발할 수 있습니다. 이를 막기 위해 8.7절 회전 정책에 **유예 기간(예: 30초)** 을 추가합니다 — 회전 직후 짧은 시간 안에 구 토큰이 다시 오면 침해로 간주하지 않고 **이미 발급된 새 토큰을 그대로 반환**하고, 유예 시간이 지난 뒤 재사용되면 그때 침해로 처리합니다. `RefreshToken` 모델의 `revokedAt`·`replacedById`(8.7절)만으로 구현 가능해 **스키마 변경은 필요 없습니다**.

#### 정리된 최종 구성

| 구성 요소 | 결정 |
|---|---|
| 쿠키 속성 | `Domain=.도메인; HttpOnly; Secure; SameSite=Lax`, **Path 제한 없음**(C-2 유지) |
| SSR 인증 데이터 조회 | **비회전 SSR 전용 엔드포인트**(서버 간 공유 시크릿 + 짧은 TTL Access Token) |
| 브라우저 refresh | 기존 회전·재사용 탐지 유지 + **30초 유예 기간** 추가 |
| `middleware.ts` | 쿠키 **존재 여부만** 확인하는 1차 체크(토큰 회전은 하지 않음, 8.22절 유지) |
| CSRF 관점 | 일반 API는 `Authorization` 헤더 기반이라 쿠키 CSRF와 무관하고(8.7절), 쿠키를 쓰는 엔드포인트는 refresh 계열뿐이라 노출면이 좁음. **다만 `SameSite=Lax`만으로는 부족하다(8.65절 N-4)** — `Lax`는 **크로스사이트** 요청만 막을 뿐, 서브도메인 통일로 쿠키가 사이트 전체에 공유되므로 **탈취되거나 외부에 위임된 형제 서브도메인**(8.65절 N-3)에서 오는 요청은 같은 사이트라 그대로 통과한다. 따라서 쿠키만으로 동작하는 엔드포인트(`/auth/refresh`, 로그아웃, 이 절의 SSR 전용 엔드포인트)에는 **`Origin` 헤더 검증을 추가**하고, 허용 목록은 8.51절 C-5의 CORS 오리진 목록(`app.cbmall.co.kr`, `*.preview.cbmall.co.kr`)을 재사용해 관리 지점을 늘리지 않는다 |

Sources:
- [next-app: token refresh calls cookies().set() from a Server Component — the render throws, no Set-Cookie is emitted](https://github.com/civitai/civitai-app-starters/issues/323)
- [Help me with Refresh Token Rotation on Next.js 15 (vercel/next.js Discussion)](https://github.com/vercel/next.js/discussions/77558)
- [Token Rotation in Next.js: A Learning Story with Code and Mistakes](https://amritsapkota.medium.com/token-rotation-in-next-js-a-learning-story-with-code-and-mistakes-c0e8abccd202)

### 8.54 C-3~C-9 수정안 재검증 (확정)

> 8.52·8.53절에서 C-1·C-2 수정안이 모두 "절반만 옳았던" 것으로 드러나, 나머지 7건도 같은 강도로 재검증했습니다. **2건은 수정안 자체가 틀렸고(C-3·C-5), 1건은 비용 트레이드오프를 명시해야 하며(C-7), 4건은 유지하되 한계를 문서화**해야 합니다.

#### C-3 — **수정안이 틀림**: 한국어에서는 `tsvector`도 동작하지 않음

| 구분 | 내용 |
|---|---|
| 8.51절에서 내린 진단 | "pg_trgm은 긴 문서 검색에 부적합" |
| **진단의 오류** | 정확히는 **`similarity()`/`%` 연산자**가 긴 문서에 부적합한 것이고, **pg_trgm GIN 인덱스로 `ILIKE '%검색어%'`를 가속하는 것은 별개의 정상 기능**임 — 확장 자체가 아니라 **연산자 선택이 문제**였음 |
| 대체안(`tsvector`)의 문제 | `to_tsvector('simple', ...)`는 **공백 단위로만 토큰화**함. 한국어는 조사가 붙어("해리포터**를**", "해리포터**가**") 각각 다른 토큰이 되므로, "해리포터"로 검색하면 **하나도 매칭되지 않음**. 한국어 형태소 분석에는 PGroonga·pg_bigm 같은 별도 확장이 필요한데, 3개월 일정에 추가 확장 도입은 부담이고 Supabase 지원 여부도 확인 필요 |
| **대체안의 더 근본적인 문제(8.59절 H-3 추가)** | 조사 문제 이전에 **동작 보장 자체가 안 됐다** — `to_tsvector` 결과가 **1,048,575바이트를 넘으면 `string is too long for tsvector` 하드 에러**. 한국어는 조사 탓에 고유 렉심이 폭증해 300쪽급 전자책 한 권이 이 한도에 걸릴 수 있고, 그러면 색인이 **에러로 실패**한다. 즉 `tsvector`는 품질이 나쁜 선택지가 아니라 **애초에 선택지가 될 수 없었다** |
| **재수정안** | **pg_trgm을 유지하되 용법을 바꿈** — `similarity()` 대신 **GIN 인덱스로 가속되는 `ILIKE '%검색어%'` 부분문자열 검색**으로 필터링. 부분문자열 매칭은 조사가 붙어도("해리포터를") 원형("해리포터")을 그대로 포함하므로 **한국어에 오히려 잘 맞음**. 스니펫은 `ts_headline` 대신 **애플리케이션에서 매칭 위치를 찾아 앞뒤 15~20자를 잘라내면** 끝(8.31절 요구사항 충족) |
| 남는 한계 | 오타 허용과 관련도 순위(ranking)는 포기(부분문자열 정확 매칭). 8.27절과 동일하게 카탈로그 3,000~5,000건 초과 시 전용 검색엔진 재검토 |

#### C-5 — **수정안이 불완전**: PR 프리뷰 오리진이 차단됨

| 구분 | 내용 |
|---|---|
| 문제 | C-5에서 "공개 버킷 CORS에 프론트 도메인(`app.*`) `GET` 허용"이라고만 정했는데, 8.34절의 **PR 프리뷰는 `*.vercel.app` 형태의 매번 다른 임시 오리진**을 사용함 → 프리뷰 환경에서 **샘플 PDF 미리보기(8.29절)가 항상 실패** |
| **재수정안** | CORS `AllowedOrigins`에 **운영/스테이징 도메인 + Vercel 프리뷰 패턴(`https://*.vercel.app`)** 을 함께 등록. 공개 버킷에는 마케팅용 자료만 있으므로(8.10절) 프리뷰 오리진 허용에 따른 정보 노출 위험이 없음 |
| 추가 확인 | 8.23절에서 이 자산에 `immutable, max-age=31536000`을 걸어두었으므로, CORS 응답이 오리진별로 구분되도록 **`Vary: Origin`이 함께 반환되는지** 확인 필요(S3는 기본 반환) |

#### C-7 — 수정안은 유효하나 **비용 트레이드오프를 명시해야 함**

2 vCPU/4GB는 기술적으로 옳지만, **PDF 파싱은 관리자가 가끔 업로드할 때만 발생하는 드문 작업**인데 이를 위해 2개 태스크의 컴퓨팅을 상시 2배로 잡는 셈입니다(대략 월 $71 → $142 수준). 대안은 **1 vCPU를 유지하고 업로드 중 짧은 지연을 감수**하는 것이며, 이미 8.31절에서 추출이 비동기이고 8.32절에 재시도가 있어 실패하더라도 복구됩니다. **어느 쪽이든 의식적인 선택이어야 하므로 이번에 확정이 필요합니다.**

#### 유지되는 수정안(한계만 문서화)

| # | 재검증 결과 |
|---|---|
| C-4 | **유지.** 단 프리뷰가 기본 롤을 쓰므로 **권한 누락(GRANT 빠짐)은 PR 단계에서 드러나지 않고 스테이징에서야 발견**됨 — 운영 전에는 반드시 걸러지므로 수용 가능하나, "프리뷰에선 됐는데 스테이징에서 권한 오류"가 정상 동작임을 팀에 공유 필요 |
| C-6 | **유지.** 단 NestJS가 호출할 **revalidate 대상 URL이 환경별로 달라야 함**(스테이징 NestJS → 스테이징 Next.js). 환경변수로 분리하고 8.42절 검증 대상에 포함 |
| C-8 | **유지.** `storageState`는 쿠키+localStorage만 저장하고 sessionStorage는 저장하지 않으며, 우리 영속 상태는 장바구니뿐이라 그 키만 제거하면 충분(추가 문제 없음) |
| C-9 | **유지.** 덧붙여 **실질적인 전역 상한은 8.28절의 AWS WAF rate-based 규칙**임(엣지에서 집계하므로 태스크 수와 무관하게 정확) — 인메모리 제한은 어디까지나 애플리케이션 레벨 보조 장치임을 명시 |

Sources:
- [PostgreSQL: Controlling Text Search (공식 문서)](https://www.postgresql.org/docs/current/textsearch-controls.html)
- [Migrate multilingual full-text search from SQL Server to PostgreSQL (AWS, CJK 토큰화 한계)](https://aws.amazon.com/blogs/database/migrate-multilingual-full-text-search-from-sql-server-to-postgresql/)

### 8.55 2차 전면 점검 — C-1~C-9 외 나머지 결정 (확정)

> 8.51절 감사는 "절 사이의 충돌"에 초점을 맞췄기 때문에, 각 절 **내부의 빈틈**은 상대적으로 덜 검토됐습니다. 이번에 나머지 결정들을 같은 기준(실제로 돌렸을 때 깨지는가)으로 훑어 **6건**을 찾았습니다. **실제 구축이 아닌 결정 단계**입니다.

#### 심각

| # | 지점 | 문제 | 제안 수정 |
|---|---|---|---|
| **D-1** | 8.25/8.51 C-1 재수정(주문 중복방지 부분 유니크 인덱스) × FR-3.1(비회원 주문) | 인덱스 조건이 `(user_id, cart_hash) WHERE status='PENDING' AND user_id IS NOT NULL`이라, **`user_id`가 NULL인 비회원 주문에는 중복 방지가 걸리지 않음** | **현행 유지 결정(리스크 수용).** 근거: 비회원이 연타해도 **결제까지 두 번 완료해야만** 중복 결제가 되며, 완료되지 않은 나머지 주문은 8.8절 배치가 30분 뒤 자동 취소함 → 실제 피해는 "만료될 PENDING 주문 1건"에 그침. 다만 **비회원 중복 결제 가능성이 0은 아니라는 점은 수용된 리스크로 기록**하고, CS 문의가 반복되면 `COALESCE(user_id::text, guest_email)` 표현식 인덱스로 확장 |
| **D-2** | 8.8(결제 승인) × 8.13/8.17/8.18(알림 outbox) | Outbox 패턴의 핵심은 **비즈니스 트랜잭션과 outbox INSERT를 같은 트랜잭션에 묶는 것**인데, 현재 설계는 8.17절 인메모리 이벤트(`EventEmitter2`)로 알림을 트리거함 → **결제 승인은 커밋됐는데 리스너 실행 전에 프로세스가 죽으면 `notification_outbox` 행 자체가 생기지 않아 알림이 영구 유실**. 주문완료 메일에는 다운로드 링크가 들어 있어(FR-3.4) **고객이 결제한 상품을 받지 못하는 상태**가 됨 | 결제 승인 트랜잭션 **안에서** `notification_outbox` 행을 INSERT(같은 커밋에 포함)하고, 실제 발송은 8.52절 행 클레이밍 워커가 집어가도록 분리. 이벤트는 "즉시 발송 시도"를 앞당기는 최적화 용도로만 사용 |

#### 보완 필요

| # | 지점 | 문제 | 제안 수정 |
|---|---|---|---|
| D-3 | 8.19(MFA 필수) × 8.20(관리자 초대) | 초대받은 관리자가 비밀번호만 설정하고 **MFA를 아직 등록하지 않은 상태**에서 관리자 기능에 접근 가능한지 규칙이 없음. 또한 SUPER_ADMIN이 **자기 자신 또는 마지막 SUPER_ADMIN을 비활성화**하면 아무도 관리자 계정을 관리할 수 없는 잠김(lockout) 상태가 됨 | ① MFA 미등록 관리자는 **MFA 등록 엔드포인트 외 모든 관리자 API 차단**, ② **활성 SUPER_ADMIN이 최소 1명 남도록** 비활성화·권한강등을 거부 |
| D-4 | 8.6/8.11(다운로드 URL 발급 시점에 횟수 차감) × FR-4.2(10회 한도) | 네트워크 실패·사용자 취소로 **실제로 받지 못했는데 횟수만 차감**될 수 있음. 그런데 관리자 기능(FR-7)에 **다운로드 횟수를 복구·증량할 수단이 없어** CS 대응이 불가능 | FR-7에 **다운로드 한도 조정(재부여)** 기능 추가 — 8.20절 감사 로그 대상에도 포함 |
| D-5 | 8.10(관리자 업로드 최대 100MB) × Vercel 요청 본문 제한 | 업로드는 브라우저 → **`api.` 도메인(ALB/ECS)** 직접 호출이라 현재 설계는 안전하지만, 구현 중 무심코 Next.js Route Handler를 경유시키면 **Vercel의 요청 본문 크기 제한(약 4.5MB)에 걸려 대용량 업로드가 실패**함 | 8.10절에 "**업로드는 반드시 `api.` 도메인 직접 호출, Next.js 경유 금지**"를 명시적 제약으로 기록 |
| D-6 | 8.42(필수 env 없으면 기동 실패) × 8.34(PR 프리뷰) | 프리뷰 환경에 토스 테스트키·Resend 키 등 필수 시크릿이 없으면 **백엔드가 부팅 자체를 못 해 프리뷰가 뜨지 않음** | **프리뷰용 최소 env 세트**를 정의(외부 연동 키는 더미/샌드박스 값 허용)하고, 8.42절 스키마에서 환경별로 필수 여부를 구분 |

Sources: (내부 2차 점검 — 8.6/8.8/8.10/8.11/8.13/8.17/8.18/8.19/8.20/8.25/8.34/8.42절 및 FR-3.1/3.4/4.2/7절 대조)

### 8.56 Supabase 브랜칭 × PR 프리뷰 심층 검토 (확정 — 브랜칭 포기)

> 8.51절 C-4(프리뷰 브랜치에 커스텀 롤이 없음)를 깊게 파보니, **롤 문제는 증상일 뿐이고 8.34절의 PR 프리뷰 구성 자체가 성립하지 않는다**는 것이 드러났습니다. **실제 구축이 아닌 결정 단계**입니다.

#### 발견된 문제

| # | 문제 | 근거 |
|---|---|---|
| **E-1** | **프리뷰 브랜치는 빈 DB로 생성된다** — Supabase 브랜칭은 스키마를 복제하지 않고 **`./supabase/migrations`(Supabase CLI 마이그레이션)를 실행해 구성**함. 그런데 우리는 8.3/8.15/8.35절에서 **Prisma Migrate**를 쓰기로 했으므로, Supabase는 우리 마이그레이션의 존재를 알지 못함 → **브랜치에 테이블이 하나도 생기지 않음** | Supabase 공식 문서: "자체 ORM으로 마이그레이션을 관리하려면 **프리뷰 브랜치 생성 후 GitHub Actions에서 직접 실행**해야 한다" |
| **E-2** | **단일 스테이징 백엔드는 PR별 DB를 바라볼 수 없다** — 8.34절은 "PR 프리뷰에서 **백엔드는 스테이징을 공유**하고 DB만 그 PR의 브랜치로 바꿔치기"로 정했는데, 스테이징 ECS 서비스는 `DATABASE_URL` **하나로 기동된 상시 서비스**임. PR이 3개 열리면 각각 다른 브랜치 DB를 봐야 하는데 **한 서비스가 요청마다 DB를 갈아탈 수 없음** | 8.3절에서 백엔드를 Vercel이 아닌 **AWS ECS**에 두기로 했기 때문에, Vercel-Supabase 통합이 프리뷰 배포에 자동 주입해주는 브랜치 자격증명의 혜택을 **백엔드는 받지 못함** |
| **E-3** | 위 두 가지를 동시에 해결하려면 **PR마다 ECS 서비스를 따로 띄워야** 하는데, 이는 8.34절이 "기동 시간·비용이 커서 하지 않는다"고 이미 배제한 방식 | — |

#### 해결 방안

| 방안 | 내용 | 평가 |
|---|---|---|
| **A. 브랜칭 포기(추천)** | **PR 프리뷰는 프론트엔드만 격리**(Vercel Preview)하고, **백엔드·DB는 스테이징을 공유**. Supabase 브랜칭 미사용. **단, 프리뷰에는 반드시 같은 상위 도메인의 서브도메인을 부여한다**(8.65절 N-1) — Vercel 프로젝트에 `*.preview.cbmall.co.kr`을 등록하고 DNS에 와일드카드 CNAME을 둔 뒤, CI에서 배포 후 `vercel alias set <deployment-url> pr-<번호>.preview.cbmall.co.kr`을 실행 | 가장 단순하고 지금 아키텍처와 모순이 없음. 덤으로 **C-4의 롤 문제가 소멸**하고(프리뷰가 스테이징 DB를 쓰므로 `cbmall_app` 권한이 그대로 검증됨), 8.54절에서 지적한 "권한 오류가 스테이징에서야 발견된다"는 한계도 사라짐. 브랜칭용 상위 요금제 필요성도 없어짐. **다만 기본 `*.vercel.app` URL 그대로는 이 구성이 성립하지 않는다**(8.65절 N-1) — `vercel.app`은 Public Suffix List 등재 도메인이라 `cbmall.co.kr`과 다른 사이트가 되어 Refresh 쿠키가 서드파티 쿠키로 취급되고, 사파리는 설정 자체를 차단한다. 즉 **프리뷰에서 로그인도 SSR 토큰 릴레이도 동작하지 않아** 이 결정과 8.46절 E2E가 함께 무너진다. Vercel의 Preview Deployment Suffix 기능은 Pro 위에 **월 $100 유료 애드온**이라, 같은 결과를 CI 스텝 하나(`vercel alias`)로 얻는 방식을 택함 |
| B. 브랜칭 유지 + CI 보완 | 프리뷰 브랜치 생성 후 GitHub Actions에서 해당 브랜치 커넥션으로 `prisma migrate deploy` 실행 | E-1은 해결되지만 **E-2(백엔드가 PR별 DB를 못 봄)는 해결 불가** — 프론트만 브랜치 DB를 보고 백엔드는 스테이징 DB를 보는 **데이터 불일치 상태**가 됨 |
| C. PR별 백엔드까지 배포 | PR마다 ECS 서비스 생성 | 완전한 격리는 되지만 비용·기동 시간·파이프라인 복잡도가 8.34절 판단 기준을 벗어남 |

#### 방안 A 채택 시 연쇄 수정 사항

| 절 | 수정 내용 |
|---|---|
| 8.34 | 환경 구성을 **"PR 프리뷰(프론트만) → 스테이징(백엔드·DB 공유) → 운영"** 으로 정정, Supabase 프로젝트는 **스테이징·운영 2개만** 유지 |
| 8.15 / C-4 | 프리뷰도 스테이징 DB를 쓰므로 **모든 환경이 `cbmall_app`을 사용** — 환경별 롤 분기 자체가 불필요해짐 |
| **8.46** | **E2E 테스트 데이터 정리 로직이 다시 필요함** — 8.46절은 "PR 종료 시 브랜치가 삭제되므로 정리 불필요"를 근거로 정리 코드를 생략했는데, 그 전제가 사라짐. 테스트 전용 계정·상품 접두사를 정해 **테스트 종료 시 해당 데이터만 삭제**하는 단계 추가 |
| 8.42 / D-6 | 프리뷰 전용 최소 env 세트 논의가 **프론트엔드 변수로 한정**됨(백엔드는 스테이징 설정 그대로 사용) |

Sources:
- [Branching | Supabase Docs (프리뷰 브랜치는 스키마를 복제하지 않고 마이그레이션으로 구성)](https://supabase.com/docs/guides/deployment/branching)
- [Working with branches | Supabase Docs (자체 ORM 사용 시 GitHub Actions에서 직접 실행 필요)](https://supabase.com/docs/guides/deployment/branching/working-with-branches)

### 8.57 CI/CD 파이프라인 심층 검토 (확정)

> 8.35절은 "무엇을 검사하고 어떻게 배포하는가"를 정했지만, **배포 순간에 실제로 무슨 일이 벌어지는지**(구코드와 신스키마가 공존하는 구간, 캐시가 테스트를 건너뛰는 경우 등)는 검증하지 않았습니다. **실제 구축이 아닌 결정 단계**입니다.

#### 심각

| # | 문제 | 상세 | 제안 수정 |
|---|---|---|---|
| **F-1** | **롤링 배포 중 "구코드 × 신스키마" 구간이 반드시 발생** | 8.35절은 마이그레이션을 ECS 배포보다 **먼저** 실행하도록 정했고, 8.11절에 따라 태스크가 2개 이상이라 롤링 교체 동안 **구버전 코드가 이미 바뀐 스키마 위에서 계속 서비스**함. 파괴적 변경(컬럼 삭제·이름 변경·NOT NULL 추가)이 하나라도 들어오면 **배포할 때마다 운영 장애**. 8.35절에 "하위호환되게 작성한다"는 원칙 문장은 있지만 **강제 수단이 없음** | **확장-수축(expand/contract) 규율을 명시적 규칙으로 승격**: ① 추가만 하는 마이그레이션(컬럼 추가는 nullable 또는 기본값 포함) → ② 신·구 코드가 모두 동작하는 상태로 배포 → ③ 구코드가 사라진 다음 릴리즈에서야 삭제(수축). **파괴적 DDL(`DROP COLUMN`/`RENAME`/`NOT NULL` 추가)은 CI에서 검출해 경고**하고, 수축 단계임을 PR에 명시하지 않으면 머지 차단 |
| **F-2** | **Turborepo 캐시가 E2E 테스트를 통째로 건너뛸 수 있음** | 8.45절에서 CI를 `turbo run test`로 돌려 Remote Cache 혜택을 보기로 했는데, 8.46절 Playwright는 **배포된 프리뷰 URL**(외부 상태)을 대상으로 함. Turborepo는 입력 해시로 캐시하므로 **소스가 안 바뀌면 "통과" 캐시를 재사용** — 배포된 앱이나 스테이징 데이터가 달라져도 **E2E가 실제로는 한 번도 실행되지 않을 수 있음** | E2E 태스크는 **`"cache": false`로 지정**해 캐시 대상에서 제외하고, 단위 테스트(Vitest/Jest, 외부 의존 없음)만 캐싱 |
| **F-3** | **스키마 변경 PR의 프리뷰 동작 정책이 없음**(8.56절 결정의 후속) | 8.56절에서 프리뷰가 **스테이징 DB를 공유**하게 되면서, 새 컬럼을 요구하는 PR의 프리뷰는 **아직 없는 컬럼을 조회해 실패**함. 반대로 머지 전에 그 마이그레이션을 스테이징에 적용하면 **병합되지도 않은 스키마가 공용 DB에 반영**되어 다른 PR·스테이징에 영향 | **마이그레이션은 머지 후 스테이징 배포 시에만 적용**하고, **스키마 변경이 포함된 PR의 프리뷰는 "UI 확인용"으로만 사용**한다고 명문화(해당 기능 검증은 머지 후 스테이징에서 수행) |

#### 보완 필요

| # | 문제 | 제안 수정 |
|---|---|---|
| F-4 | **마이그레이션 실행 시점과 운영 승인 게이트(8.34절)의 순서가 미정** — 승인 전에 마이그레이션이 돌면 승인이 지연되는 동안 구코드×신스키마 구간이 무한정 길어짐 | **승인 게이트를 통과한 뒤에 마이그레이션 → 배포** 순으로 고정(승인 이전에는 DB를 건드리지 않음) |
| F-5 | **Prisma는 마이그레이션 시 세션 단위 advisory lock을 사용**(타임아웃 10초, 조정 불가)하므로 **풀러를 거치면 마이그레이션 자체가 실패**함 — 8.15절에서 "마이그레이션은 다이렉트 커넥션" 결정이 사실상 필수 조건인데, 정작 `schema.prisma`에 **`directUrl`이 선언돼 있지 않아** 구현 시 누락되기 쉬움 | `schema.prisma`의 `datasource`에 **`directUrl` 명시**(런타임은 풀러 URL, 마이그레이션은 다이렉트 URL) |
| F-6 | **동시 배포 직렬화 장치 없음** — 두 PR이 연달아 머지되면 마이그레이션이 동시에 돌아 advisory lock 10초 타임아웃으로 실패하거나 ECS 배포가 겹침 | GitHub Actions **`concurrency` 그룹을 환경별로 지정**해 같은 환경의 배포가 항상 한 번에 하나만 진행되도록 직렬화 |
| F-7 | **OIDC 신뢰 정책을 브랜치로만 제한하면 릴리즈 태그 배포에서 맞지 않음**(태그의 `sub` 클레임은 `refs/tags/...` 형태) | 신뢰 정책 조건을 **`environment:production` 기반**으로 구성 — 8.34절 승인 게이트와도 자연스럽게 맞물려, 승인된 배포만 AWS 자격증명을 얻게 됨 |

Sources:
- [Development and production | Prisma Documentation (마이그레이션 advisory lock·10초 타임아웃)](https://www.prisma.io/docs/orm/prisma-migrate/workflows/development-and-production)
- [Prisma running multiple advisory lock queries and failing migration (prisma/prisma#27636)](https://github.com/prisma/prisma/issues/27636)
- [Prisma With Managed Postgres: Setup Guide (풀러 사용 시 `directUrl` 필요)](https://www.pandastack.ai/blog/how-to-use-prisma-with-a-managed-postgres-database/)

### 8.58 알림 아키텍처 심층 검토 (확정)

> 8.55절 D-2에서 "outbox INSERT를 결제 승인 트랜잭션에 묶는다"로 바꾸면서 **트랜잭션 경계와 발송 주체가 동시에 달라졌는데**, 8.17/8.18/8.52절은 그 변화를 반영해 재검토된 적이 없습니다. 이번에 이어붙인 부분들을 점검해 **5건**을 찾았습니다. **실제 구축이 아닌 결정 단계**입니다.

#### 심각

| # | 문제 | 상세 | 제안 수정 |
|---|---|---|---|
| **G-1** | **같은 알림이 두 번 발송될 수 있음** | D-2 이후 구조는 "**트랜잭션 안에서 outbox 행 INSERT** + **이벤트로 즉시 발송 시도**"인데, 8.52절 재시도 워커도 `status='PENDING'`인 같은 행을 `FOR UPDATE SKIP LOCKED`로 집어감 → **이벤트 리스너와 워커가 동일 행을 동시에 발송**할 수 있음(주문완료 메일 중복 발송) | 즉시 발송 경로도 **반드시 동일한 클레이밍 절차를 거치게** 함 — 이벤트 리스너가 직접 발송하지 않고 "해당 행을 클레임 시도 → 성공한 경우에만 발송"하는 **같은 함수를 호출**하도록 통일 |
| **G-2** | **롤백된 결제의 알림이 나갈 수 있음** | `@nestjs/event-emitter`는 기본적으로 **동기 실행**이라, 트랜잭션 **커밋 전에** 이벤트를 발행하면 리스너가 곧바로 발송을 시도함. 이후 트랜잭션이 롤백되면 **결제가 성사되지 않았는데 주문완료 메일이 이미 나간 상태** | 이벤트는 **트랜잭션이 커밋된 뒤에만 발행**(`$transaction` 반환 후 발행)하도록 규칙화. 트랜잭션 내부에서는 outbox INSERT까지만 수행 |
| **G-3** | **토스 승인 성공 ↔ DB 커밋 사이에 프로세스가 죽으면 결제만 되고 아무것도 생성되지 않음** | 8.8절 흐름상 토스 승인(외부 호출)은 트랜잭션 **밖**에서 일어나야 하는데(트랜잭션 내 네트워크 I/O 금지, 8.52절), 승인 성공 직후·커밋 직전에 태스크가 죽으면 **돈은 결제됐는데 주문은 PENDING, 엔타이틀먼트·알림 모두 없음** → 고객이 구매한 상품을 받지 못함. 8.8/8.9절 웹훅은 **중복 수신 방지**만 다루고 있어 이 상태를 복구한다는 규정이 없음 | 웹훅 핸들러가 단순 상태 반영이 아니라 **결제 이후 워크플로 전체(주문 PAID 전이 + 엔타이틀먼트 생성 + outbox 행 생성)를 멱등하게 완결**하도록 명시. 이미 완료된 부분은 건너뛰고 빠진 부분만 채움 |

#### 보완 필요

| # | 문제 | 제안 수정 |
|---|---|---|
| G-4 | **재클레임 대기(5분)가 긴급 알림 재시도 주기(1·3·10분)보다 길다** — 8.52절은 워커 사망 시 `claimed_at` 5분 경과 후 재선점하도록 했는데, 8.18절 긴급 프로파일(비밀번호 재설정·이메일 인증·비회원 다운로드 링크)은 **총 14분 안에 4회**를 시도해야 함. 워커가 한 번 죽으면 5분을 통째로 날려 긴급 예산을 소진 | `notification_outbox`의 재클레임 대기 시간을 **1분으로 단축**(긴급 프로파일의 첫 재시도 간격과 정렬). 외부 API 타임아웃(수 초~수십 초)보다 충분히 길어 정상 처리 중인 행을 뺏지 않음. 단, **`product_full_texts`(PDF 추출)는 10분을 유지** — 추출 1건이 수 분 걸릴 수 있어 1분으로 줄이면 정상 처리 중인 행을 다른 워커가 뺏어 중복 추출이 발생함(반영: 8.52절) |
| G-5 | **outbox `payload`에 다운로드 링크·토큰이 평문으로 영구 보관됨** — 8.13절이 CS 대응을 위해 발송 이력을 남기기로 했는데, 8.4/8.7절은 토큰을 해시로만 저장한다는 원칙임. 발송 이력이 쌓이면 **DB에 유효한 다운로드 링크가 평문으로 누적** | `payload`에는 **엔타이틀먼트 ID 등 식별자만 저장**하고 실제 링크는 **발송 시점에 생성**. 부득이하게 민감값이 들어간 경우 **발송 성공 후 payload를 비우는** 정리 단계를 재시도 Cron에 추가 |

Sources: (내부 심층 점검 — 8.4/8.7/8.8/8.9/8.13/8.17/8.18/8.52/8.55절 및 D-2 수정 결과 대조)

---

### 8.59 PDF 본문검색 엔진 심층 검토: pg_trgm vs tsvector (확정)

> 8.31절에서 본문 색인을 `pg_trgm` GIN + `ILIKE`로 정했고 8.54절 C-3에서 한 번 재수정했는데, 이번엔 **"엔진 선택"이 아니라 "선택한 엔진이 우리 데이터 모양에서 실제로 빠른가"** 를 기준으로 다시 파고들었습니다. 결론: **엔진 선택(pg_trgm)은 맞지만, 지금의 "전자책 1권 = `product_full_texts` 1행" 구조에서는 GIN 인덱스가 제 역할을 하지 못합니다.** 전자책 한 권은 본문이 30만~60만 자(UTF-8 기준 약 1~1.8MB)에 달해, 일반적인 "게시글 한 건" 크기를 전제로 한 트라이그램 색인의 가정이 깨집니다. **실제 구축이 아닌 결정 단계**입니다.

#### 세 방식 비교 (전자책 1권 = 수십만 자 기준)

| 기준 | `pg_trgm` GIN + `ILIKE` (현행 8.31절) | `tsvector` + GIN | PGroonga (TokenBigram) |
|---|---|---|---|
| 한국어 조사 대응 | **가능** — 부분문자열이라 "해리포터를"에 "해리포터"가 매칭 | **불가** — `simple` 설정은 공백 토큰화라 조사가 붙으면 실패(8.54절 C-3) | **가능** — 바이그램 토큰화 |
| 2글자 검색어("주식", "요리") | **인덱스 못 탐**(H-2) | 가능(토큰이 일치하는 경우에 한해) | **가능** — 바이그램이라 2자면 충분 |
| 긴 문서(수십만 자) | **느림** — 재검사(recheck) 비용이 지배적(H-1) | **동작 불가** — 1,048,575바이트 초과 시 하드 에러(H-3) | 길이 제약·재검사 없음 |
| 인덱스 크기 | **가장 작음**(기준 1.0) | 약 1.6배 | 약 5배 |
| 오타 허용 | 포기(`similarity()`는 긴 본문에서 0에 수렴, 8.54절) | 미지원 | 유사검색 연산자 별도 제공 |
| 인프라 비용 | `CREATE EXTENSION`만 | Postgres 내장 | Supabase 지원 확장이나 **신규 운영 대상 추가** |

> **참고로 못박아 둘 것(H-6)**: PGroonga·pg_bigm 진영 비교 문서에는 "pg_trgm은 비ASCII 문자를 지원하지 않아 한국어·일본어에 쓸 수 없다"는 서술이 있는데, 이는 **오래된 내용**입니다. PostgreSQL 9.1 이후 pg_trgm은 멀티바이트 문자를 정상 처리하며 국내 활용 사례도 다수입니다. 정확히 말하면 **"지원은 하지만 한글에 특화된 최적화가 없을 뿐"** 이고, 그 결과가 아래 H-2입니다. 이 구분을 적어두지 않으면 나중에 이 비교 문서를 접한 사람이 8.31절 결정을 오판할 수 있습니다.

#### 즉시 수정 필요

| # | 문제 | 왜 문제인가 | 제안 수정 |
|---|---|---|---|
| **H-1** | **"책 1권 = 1행" 구조에서는 GIN 인덱스가 사실상 전수 스캔으로 퇴화** | pg_trgm GIN은 **후보 행만** 돌려주고 Postgres가 실제 `ILIKE`를 **재검사(recheck)** 한다. 전자책 1권이 1행인 구조에서는 "데이터"·"있습니" 같은 흔한 트라이그램이 **거의 모든 책에 존재**하므로 후보 = 전체 행이 되고, 결국 모든 행의 `extracted_text`(1MB 이상 → TOAST에 압축 저장)를 **디토스트해서 전부 재검사**하게 된다. 인덱스를 태웠는데 순차 스캔보다 느려지는 전형적 사례이며, PGroonga 공식 비교 문서도 "pg_trgm은 매칭 문서가 많고 각 문서가 길 때 느리다"고 같은 이유를 든다 | 본문을 **청크 단위 다중 행으로 분할 색인**한다. `product_full_texts`(1권 1행)는 추출 상태 관리용으로 유지하고, 실제 검색 대상은 **`product_text_chunks`(1권 N행 + `page_from`/`page_to`/`chunk_index`)** 로 분리해 GIN 인덱스를 **청크 컬럼에** 건다(청크 크기·경계 규칙은 8.60절에서 확정). ① 행이 작아 TOAST·재검사 비용이 사라지고 ② 트라이그램 선택도가 회복되며(특정 청크만 매칭) ③ 스니펫을 청크 내 오프셋으로 자연스럽게 생성하고 ④ 덤으로 검색 결과에 **"이 책 N쪽"** 을 표시할 수 있다. 규모는 200권 × 약 300청크 = 6만 행 수준으로 Postgres에 전혀 부담이 없다 |
| **H-2** | **2글자 검색어가 인덱스를 못 탄다 — 한국어에서 매우 흔한 패턴** | pg_trgm은 `ILIKE '%검색어%'` 패턴에서 트라이그램을 추출해 인덱스를 조회하는데, 와일드카드 사이 글자가 **3자 미만이면 추출 가능한 트라이그램이 없다**. 공식 문서: "a pattern with no extractable trigrams will degenerate to a full-index scan." 이 경우 GIN 전체를 훑은 뒤 **모든 후보를 다시 디토스트 재검사**하므로 순차 스캔보다도 나쁘다. 문제는 한국어 검색어가 **"주식·요리·영어·수학·투자·육아"처럼 2글자인 경우가 매우 흔하다**는 점 | **본문검색은 3자 이상 검색어에만 적용**하고, 2자 이하는 8.27절의 **제목·설명·초성 검색만** 수행한다(이 컬럼들은 짧아서 순차 스캔이 저렴). UI에는 "본문검색은 3글자 이상부터 지원합니다"를 안내하고, API는 2자 검색어에 대해 본문검색 쿼리를 **아예 발행하지 않는다**(조건 분기를 애플리케이션에서 강제). 대안인 PGroonga 도입은 2자 문제를 근본 해결하지만 인덱스 5배 + 신규 운영 대상이라 현 규모에 과함 |
| **H-3** | **`tsvector`는 품질 이전에 "전자책 길이에서 동작 보장이 안 된다"** | `to_tsvector` 결과가 **1,048,575바이트를 넘으면 `string is too long for tsvector` 에러**가 난다. 한국어는 조사가 붙어 고유 렉심 수가 폭증하므로(같은 단어가 "데이터/데이터를/데이터가/데이터의"로 전부 별개 렉심) 300쪽급 전자책 한 권이 이 한도에 걸릴 수 있고, 그 순간 **추출 작업이 에러로 실패**해 8.32절 실패율 알림만 울린다. 8.54절 C-3은 "조사 때문에 검색이 안 맞는다"는 **품질** 근거만 적었는데, 실제로는 그보다 앞선 **동작 보장** 문제가 있었다 | 8.31/8.54절에 이 근거를 **추가로 명시**해 "tsvector로 되돌리자"는 재논의가 나오지 않도록 못박는다. pg_trgm GIN에는 이런 행 단위 크기 한도가 없다. (H-1의 청크 분할을 적용하면 1MB 한도 자체는 사라지지만 **조사 문제는 그대로 남으므로 결론은 불변** — 이 점도 함께 적는다) |

#### 보완 필요

| # | 문제 | 제안 수정 |
|---|---|---|
| H-4 | **전환 기준 지표가 잘못 잡혀 있다** — 8.27/8.31절은 "상품 3,000~5,000건 초과 시 전용 검색엔진 재검토"를 본문검색에도 그대로 적용했는데, **본문검색의 병목은 상품 수가 아니라 총 본문 분량**이다. 상품이 300건이어도 권당 600쪽이면 청크는 18만 개가 된다 | 기준 지표를 이원화한다. **제목 검색(8.27절)**: 상품 수 3,000~5,000건 기준 유지. **본문 검색(8.31절)**: **총 청크 행 수(약 50만 행)** 또는 **검색 API p95 응답시간(1초)** 중 먼저 도달하는 쪽을 재검토 트리거로 삼고, 8.24절 모니터링에 이 두 지표를 넣는다 |
| H-5 | **GIN 인덱스 운영 파라미터가 정해져 있지 않다** — GIN은 기본값이 `fastupdate=on`이라 INSERT를 pending list에 모아뒀다가 나중에 반영한다. 이 정리 작업을 **검색 쿼리가 떠안게 되면 특정 요청만 갑자기 느려지고**, 상품 등록 직후 검색 결과가 늦게 반영될 수도 있다 | cb몰은 **쓰기(상품 등록)가 드물고 읽기(검색)가 중요**하므로 본문 청크 GIN 인덱스는 **`fastupdate=off`** 로 생성한다(`WITH (fastupdate = off)`). 또한 본문 전체를 색인하므로 **Supabase DB 용량 모니터링에 인덱스 크기 항목을 추가**하고, 대량 초기 색인(기존 상품 일괄 재추출) 시에는 `maintenance_work_mem`을 일시 상향한다 |

Sources:
- [PostgreSQL: pg_trgm — support for similarity of text using trigram matching](https://www.postgresql.org/docs/current/pgtrgm.html)
- [PGroonga versus textsearch and pg_trgm](https://pgroonga.github.io/reference/pgroonga-versus-textsearch-and-pg-trgm.html)
- [PGroonga: Multilingual Full Text Search | Supabase Docs](https://supabase.com/docs/guides/database/extensions/pgroonga)
- [Understanding Postgres GIN Indexes: The Good and the Bad | pganalyze](https://pganalyze.com/blog/gin-index)
- [PostgreSQL error: string is too long for tsvector (DefectDojo #8299)](https://github.com/DefectDojo/django-DefectDojo/issues/8299)
- [Behavior of a pg_trgm index for 2 (or < 3) character LIKE queries — pgsql-general](https://postgrespro.com/list/thread-id/1821635)
- [PostgreSQL에서 LIKE 검색 속도를 높여주는 pg_trgm 활용해보기](https://yahwang.github.io/posts/80)

---

### 8.60 본문 청크 분할 로직: 경계 규칙·페이지 매핑 (확정)

> 8.59절 H-1에서 "청크 1,000~2,000자 + `page_from`/`page_to`"까지만 정하고 **어떻게 쪼갤지**는 비워뒀습니다. 파보니 이 빈칸에 **검색이 아예 안 되는 경우**가 여럿 숨어 있습니다. PDF는 파일 포맷 차원에서 "단어"나 "문단" 개념이 없고 글자를 좌표에 찍어둔 것뿐이라, 추출 텍스트에는 레이아웃 기준의 줄바꿈이 그대로 들어옵니다. **실제 구축이 아닌 결정 단계**입니다.

#### 즉시 결정 필요

| # | 문제 | 왜 문제인가 | 제안 |
|---|---|---|---|
| **I-1** | **줄바꿈이 검색어를 반으로 쪼갠다** | PDF에서 뽑은 텍스트에는 **화면 줄바꿈 위치에 그대로 개행**이 들어간다. "해리포터"가 줄 끝에서 넘어가면 추출 결과는 `해리포\n터`가 되고, `ILIKE '%해리포터%'`는 **매칭에 실패**한다. 인덱스·청크를 아무리 잘 설계해도 이 단계에서 검색어가 사라진다. 게다가 PDF는 공백을 저장하지 않거나 글자마다 넣는 경우도 있어 띄어쓰기를 신뢰할 수 없다 | **단일 `content` 컬럼 + 정규화 파이프라인**으로 확정(검색 전용 공백제거 컬럼은 두지 않음). 색인 전 다음 순서로 정규화한다: ① `normalize('NFC')`(자모 분리형 한글 방어) → ② soft hyphen(`­`)·ZWSP(`​`)·BOM·non-breaking space 등 유령 문자 제거 → ③ 영문 하이픈 분철(`com-\npany`) 결합 → ④ **한글과 한글 사이의 개행은 공백 없이 결합**(8.61절 J-7에서 검증: 한국어 기본 줄바꿈은 음절 단위가 맞다 — UAX #14에서 한글은 기본 ID 클래스이고 한컴오피스·Word도 "한글 단어 잘림 허용"이 기본값. 대가로 **줄바꿈이 어절 경계였던 자리는 공백 없이 붙어 보인다**("만났다그런데"). 어절 중간이었는지 경계였는지 구분할 방법이 없어 검색을 살리는 쪽을 택한 것이며, 스니펫이 짧아 영향은 제한적)(`해리포\n터`→`해리포터`), 그 외 개행은 공백으로 → ⑤ 연속 공백 1개로 축약. **수용한 한계**: 책에 원래 띄어쓰기가 있는 표현("해리 포터")은 붙여 쓴 검색어("해리포터")로 찾지 못한다. 이를 잡으려면 공백을 전부 제거한 검색 전용 컬럼을 따로 두고 스니펫 오프셋을 역매핑해야 하는데, 저장 2배 + 로직 복잡도에 비해 자사 제작 전자책(표기가 일관될 가능성이 높음)에서의 이득이 불확실해 **MVP에서는 수용**한다. 검색 불만이 누적되면 이 컬럼 분리를 1순위 개선안으로 재검토 |
| **I-2** | **청크 경계에 걸친 검색어를 놓친다** | 1,000자에서 딱 자르면 경계를 걸친 "해리포터"는 **앞 청크에도 뒤 청크에도 온전히 존재하지 않아** 영구히 검색되지 않는다. 책 한 권에 경계가 300개면 300군데에 구멍이 뚫린다 | 인접 청크를 **50자 오버랩**해 생성(허용 검색어 최대 길이 이상). 같은 매칭이 두 청크에서 잡히는 부작용은 **검색 결과가 이미 상품 단위**라 `DISTINCT product_id` + 대표 1건 스니펫으로 자연 해소. 저장 증가는 약 5% |
| **I-3** | **고정 길이로 자르면 스니펫이 문장 중간에서 끊긴다** | 단순히 1,000자마다 자르면 청크 시작/끝이 "…했다. 그런데 이 문"처럼 어색해지고, 매칭이 청크 가장자리에 걸리면 앞뒤 15~20자 스니펫(8.31절)이 반쪽만 나온다 | **목표 1,000자, 허용 ±200자**로 두고 그 구간에서 **문단(`\n\n`) → 문장 종결(`.`/`!`/`?`/`다.`/`요.`) → 공백** 순으로 가장 가까운 경계를 찾아 자른다. 구간 내에 아무 경계도 없으면(표·수식 등) 1,200자에서 강제 컷 |
| **I-4** | **재추출 시 옛 청크가 남아 "없어진 본문"이 계속 검색된다** | FR-2.6 오탈자 수정본으로 파일을 교체하면 재추출이 도는데, 새 청크 수가 이전보다 **적으면** `chunk_index`가 큰 옛 청크가 그대로 남는다. `(product_id, chunk_index)` upsert로는 절대 못 잡는 누락이며, **삭제된 문장이 검색 결과에 계속 뜨는** 형태로 드러난다 | 재추출은 upsert가 아니라 **같은 트랜잭션에서 `DELETE FROM product_text_chunks WHERE product_id = ?` → `createMany()`** 로 통째 교체한다. 600행 삽입은 `createMany` 한 번이면 충분하고 트랜잭션도 1MB 남짓이라 부담 없다 |

#### 보완 필요

| # | 문제 | 제안 |
|---|---|---|
| I-5 | **페이지별 텍스트를 얻는 방법이 정해져 있지 않다** — `page_from`/`page_to`를 채우려면 페이지 단위 텍스트가 필요한데 8.31절은 "`pdf-parse`로 추출"까지만 적혀 있다. 전체를 한 번에 뽑으면 페이지 경계 정보가 사라진다 | `pdf-parse` v2의 **`getText({ partial: [n] })`** 로 페이지를 순회한다. 단 **`PDFParse` 인스턴스는 한 번만 생성해 재사용**하고(페이지마다 새로 로드하면 600쪽 책에서 600번 파싱), 끝나면 반드시 **`destroy()`** 로 메모리를 해제한다. 페이지별 텍스트를 이어붙이면서 **각 페이지의 시작 오프셋을 기록**해두면, 청크 `[start, end)`가 걸치는 페이지 범위를 그대로 계산할 수 있다(짧은 페이지는 한 청크가 여러 장을 걸치고, 긴 페이지는 한 장이 여러 청크가 되는 양방향 모두 자연 처리) |
| I-6 | **`page_from`이 가리키는 "쪽"이 책에 인쇄된 쪽번호와 다르다** — PDF 1페이지는 보통 표지이고 판권지·서문(로마숫자)이 이어지므로, PDF 물리 페이지 12가 책에는 "3쪽"으로 찍혀 있을 수 있다. 검색 결과에 "125쪽"이라 표시했는데 실제로 펼치면 다른 내용이 나오는 혼란 | MVP에서는 **"PDF 기준 N페이지"로 표기**해 기대치를 맞춘다(별도 데이터 입력 불필요). 향후 `Product`에 `page_offset` 하나를 두면 인쇄 쪽번호로 보정 가능하도록 여지만 남긴다 |
| I-7 | **청크가 2,000자면 TOAST 외부 저장이 그대로 일어나 H-1의 이득이 절반만 남는다** — Postgres는 행이 **2,032바이트**를 넘으면 큰 컬럼을 압축·외부 저장한다. 한글은 UTF-8 3바이트라 **2,000자 = 6,000바이트**로, 압축해도 임계를 넘겨 별도 TOAST 테이블 조회가 붙는다. 재검사(recheck) 비용을 없애려고 청크로 쪼갠 것인데 비용이 남는다 | 청크 목표 크기를 **1,000자**로 고정하고(2,000자 옵션 폐기), `content` 컬럼에 **`SET STORAGE MAIN`** 을 적용한다. MAIN은 압축은 하되 **외부 저장을 최후 수단으로만** 쓰므로, 압축 후 행이 페이지에 들어가는 한 본문이 힙 튜플에 함께 남아 재검사가 추가 I/O 없이 끝난다 |
| I-8 | **정규화·청킹을 어느 스레드에서 할지 정해져 있지 않다** — 8.33절은 `piscina` 워커로 "PDF 파싱"만 분리했는데, 수십만 자 문자열에 정규식을 여러 번 돌리는 정규화·청킹도 똑같이 CPU 바운드다. 이걸 메인 스레드에서 하면 8.33절이 막으려던 이벤트 루프 블로킹이 그대로 재발한다 | 워커의 책임 범위를 **"파싱 + 정규화 + 청킹"까지**로 넓혀 명시하고, 메인 스레드는 **워커가 돌려준 청크 배열을 DB에 쓰는 일만** 한다. 반환값은 최대 1.8MB 수준이라 구조화 복제 비용은 수십 ms로 무시 가능 |

Sources:
- [pdf-parse — getText / partial page options (mehmet-kozan)](https://github.com/mehmet-kozan/pdf-parse)
- [PostgreSQL: TOAST — TOAST_TUPLE_THRESHOLD and storage strategies](https://www.postgresql.org/docs/current/storage-toast.html)
- [TOAST: Where PostgreSQL hides big values | boringSQL](https://boringsql.com/posts/postgresql-toast/)
- [Why Does PDF Text Extraction Fail? — line breaks, hyphenation, missing spaces](https://texttopdf.net/blog/pdf-to-text-guide-why-text-extraction-works-fails)
- [Extract Text from a PDF — pypdf (PDF has no concept of words/paragraphs)](https://pypdf.readthedocs.io/en/4.0.2/user/extract-text.html)

---

### 8.61 검색 스니펫 생성 로직 (확정)

> 8.31절은 "애플리케이션에서 매칭 위치 ±15~20자를 잘라 생성"까지만 정했습니다. 그런데 스니펫은 **유료 콘텐츠를 조금씩 내보내는 창구**이자 **본문 전체가 API로 새어나갈 수 있는 유일한 경로**라, 자르는 방법보다 **어디서 자르고 무엇까지 돌려주느냐**가 훨씬 중요합니다. **실제 구축이 아닌 결정 단계**입니다.

#### 즉시 결정 필요

| # | 문제 | 왜 문제인가 | 제안 |
|---|---|---|---|
| **J-1** | **"본문 전체를 응답에 넣지 않는다"가 코드 컨벤션으로만 보장되고 있다** | 8.31절은 전체 본문 미노출을 **코드 컨벤션으로 강제**한다고 했는데, 이는 Prisma `select`에 `content: true`를 한 줄 추가하거나 `include`를 무심코 쓰는 **실수 한 번이면 뚫린다**. 유료 전자책 전문이 그대로 나가는 사고이므로 규율에만 맡길 수준이 아니다. 성능 측면에서도 결과 20건 × 1,000자를 API 서버로 끌고 와서 자바스크립트로 자르는 셈이라 낭비다 | **스니펫을 DB에서 잘라 반환한다** — `SUBSTRING(content FROM ... FOR ...)`로 쿼리 자체가 **스니펫만 돌려주게** 만들어, 애플리케이션이 본문 전체를 손에 쥘 일 자체를 없앤다. 컨벤션이 아니라 **쿼리 레벨의 구조적 보장**이 된다. Prisma 모델 API로는 `SUBSTRING`/`strpos`를 표현할 수 없으므로 이 검색 쿼리는 **`$queryRaw` 사용을 명시적 예외로 허용**한다(파라미터는 반드시 태그드 템플릿으로 바인딩) |
| **J-2** | **`ILIKE` 필터와 위치 계산의 대소문자가 어긋나 스니펫이 엉뚱한 곳에서 잘린다** | 필터는 `content ILIKE '%React%'`(대소문자 무시)인데 위치는 `POSITION('React' IN content)`(대소문자 구분)로 찾으면, 본문이 `react`일 때 **필터는 통과하고 위치는 0**이 나온다. 그대로 `SUBSTRING`에 넣으면 스니펫이 **문서 맨 앞에서 잘려** 검색어와 무관한 내용이 표시된다. 자사 제작 전자책에 프로그래밍·어학 교재가 있다면 바로 드러나는 버그다 | 위치 계산을 **`strpos(lower(content), lower($1))`** 로 양쪽 모두 소문자화해 필터와 기준을 일치시킨다. 그래도 0이 나오는 경우에 대비해 **`GREATEST(pos, 1)`로 clamp**해 잘못된 오프셋이 쿼리를 깨뜨리지 않게 방어한다 |
| **J-3** | **같은 책이 검색 결과에 여러 번 나온다** | 8.60절 I-2에서 넣은 **50자 오버랩** 때문에 경계에 걸친 매칭은 청크 2개에서 잡히고, 본문에 검색어가 50번 나오면 청크 50개가 매칭된다. 검색 결과는 **상품 단위**여야 하는데 그대로 두면 같은 전자책이 화면을 도배한다 | **`DISTINCT ON (product_id)`** + `ORDER BY product_id, chunk_index`로 **책에서 가장 먼저 나오는 위치**를 대표 스니펫으로 선정한다(결정적이라 같은 검색어에 항상 같은 결과). **주의**: `DISTINCT ON`은 `ORDER BY` 첫 컬럼이 `DISTINCT ON` 컬럼이어야 하므로, 8.27절의 정렬 옵션(판매순·신상품순)은 이 결과를 **서브쿼리로 감싼 바깥 쿼리에서** 다시 적용해야 한다 |
| **J-4** | **스니펫을 이어붙여 본문을 재구성하는 공격에 검색어 길이 상한이 없다** | 8.31절은 이 위험을 인지하고 **분당 30회 속도 제한**만 방어선으로 뒀다. 그런데 **스니펫 길이 = 검색어 길이 + 앞뒤 약 40자**이므로, 공격자가 이미 확보한 문장의 끝부분을 검색어로 넣으면 **그 다음 20자를 얻고**, 이를 반복해 본문을 순차적으로 이어붙일 수 있다. 속도 제한만으로는 하루에 상당 분량이 재구성된다 | **검색어 길이 상한 50자**를 서버에서 강제(초과 시 400)하는 것이 핵심 방어다 — 한 번의 요청으로 얻는 **신규 정보가 최대 20자로 고정**되어, 속도 제한과 곱해졌을 때 실질 크롤링 속도가 크게 떨어진다. 함께: ② 스니펫 길이·페이지당 결과 수를 **서버 상수로 고정**(클라이언트 파라미터로 받지 않음), ③ **"이 책에 N번 등장" 같은 매칭 횟수는 응답에 넣지 않음**(그 자체가 본문에 대한 정보), ④ 8.28절 분당 30회 제한 유지 |

#### 보완 필요

| # | 문제 | 제안 |
|---|---|---|
| J-5 | **서버가 하이라이트를 HTML로 만들면 XSS 통로가 된다** — 서버가 `<mark>`를 끼워 넣어 반환하면 프론트는 `dangerouslySetInnerHTML`을 써야 하고, 그러면 본문에 들어 있던 문자열이 그대로 실행된다. 자사 콘텐츠에 **HTML·JavaScript 교재가 있다면 본문에 `<script>`가 실제로 존재**한다 | 서버는 **순수 텍스트 + 매칭 오프셋만** 반환한다 — `{ snippet, matchStart, matchLength, page }`. 프론트에서 문자열을 세 조각으로 쪼개 가운데만 `<mark>`으로 감싸면 React가 자동 이스케이프하므로 `dangerouslySetInnerHTML`이 아예 필요 없다 |
| J-6 | **스니펫 양 끝이 음절 중간에서 잘려 읽기 나쁘다** — `±20자` 고정이면 "…는 해리포터를 만"처럼 끝난다 | 자른 뒤 앞뒤를 **가장 가까운 공백 경계까지** 조정하고(최대 10자 범위 내), 잘려나간 쪽에 `…`를 붙인다. 8.60절 I-1에서 개행을 이미 정리했으므로 스니펫에 줄바꿈이 섞여 들어올 일은 없다 |
| J-7 | **8.60절 I-1 개행 결합이 스니펫에 남기는 흔적** — 검증 결과 **한국어 기본 줄바꿈은 음절 단위**가 맞았다(UAX #14에서 한글은 기본 ID 클래스, 한컴오피스·Word도 "한글 단어 잘림 허용"이 기본). 따라서 I-1 ④의 "한글 사이 개행은 공백 없이 결합"은 **옳은 기본값**이다. 다만 줄바꿈이 어절 중간이었는지 어절 경계였는지 구분할 방법이 없어, **어절 경계였던 자리는 공백 없이 붙어 보인다**("만났다그런데") | 검색을 살리기 위한 **의도된 대가**로 8.60절에 수용 한계를 명시한다. 스니펫은 짧아(약 60자) 붙은 구간이 드물게 보이는 정도이며, 매칭어가 `<mark>`으로 강조되므로 가독성 손실은 제한적이다. 어절 복원은 형태소 분석이 필요해 현 범위를 벗어난다 |
| J-8 | **스니펫 계산이 매칭된 모든 청크에서 실행되면 낭비다** — 흔한 검색어는 청크 수천 개가 매칭되는데, 그 전부에 `strpos`+`SUBSTRING`을 돌린 뒤 대표 1건만 남기면 대부분이 버려진다 | 쿼리 단계를 **① GIN 필터 → ② `DISTINCT ON`으로 상품당 대표 청크 선정 → ③ 바깥에서 `strpos`+`SUBSTRING`** 순서로 고정해, 스니펫 계산을 **최종 결과 건수(페이지당 20건)만큼만** 수행한다 |

Sources:
- [PostgreSQL: String Functions — substring, strpos, position](https://www.postgresql.org/docs/current/functions-string.html)
- [PostgreSQL: SELECT — DISTINCT ON and its ORDER BY requirement](https://www.postgresql.org/docs/current/sql-select.html)
- [Prisma: Raw queries — `$queryRaw` and parameter binding](https://www.prisma.io/docs/orm/prisma-client/using-raw-sql/raw-queries)
- [UAX #14: Unicode Line Breaking Algorithm — Hangul defaults to class ID](https://unicode.org/reports/tr14/tr14-51.html)
- [한글 단어 잘림 허용 — 한컴오피스 도움말](http://help.hancom.com/hoffice/multi/ko_kr/show/format/paragraph_typography_wraptext.htm)

---

### 8.62 검색 API 엔드포인트 설계 (확정)

> 8.27(제목·설명·초성)·8.59(본문)·8.61(스니펫)·8.28(속도 제한)이 **실제로 만나는 지점**이 검색 엔드포인트입니다. 각 절은 따로 보면 일관된데, 하나의 API로 합치는 순간 **두 절의 결정이 서로를 무력화하는 곳**이 드러납니다. **실제 구축이 아닌 결정 단계**입니다.

#### 즉시 결정 필요

| # | 문제 | 왜 문제인가 | 제안 |
|---|---|---|---|
| **K-1** | **검색 결과 페이지가 색인되면 본문 스니펫이 검색엔진에 그대로 남는다** | 8.43절은 robots.txt에서 `/admin`·`/mypage`·`/api/*`·다운로드/인증 경로만 차단하고 **상품 목록·상세는 허용, AI 크롤러(GPTBot 등)도 허용**으로 정했다. 검색 결과 페이지는 **아무 언급이 없어 허용 대상**이다. 이 페이지를 SSR하면 **유료 전자책 본문 스니펫이 HTML에 렌더링되어 구글·AI 크롤러에 색인**된다. 8.31절이 속도 제한(8.61절 J-4)까지 걸어 막으려던 본문 유출이, **크롤러가 URL만 훑으면 공짜로 가져가는 경로**로 뚫리는 것이다. 덤으로 검색 URL은 조합이 무한해 크롤 예산 낭비·중복 콘텐츠 문제까지 따라온다 | **세 겹으로 막는다**: ① `app/robots.ts`에 **`/search` Disallow 추가**, ② 검색 페이지에 `robots: { index: false, follow: false }` 명시(8.43절이 이미 채택한 이중 방어 패턴), ③ **검색 결과를 SSR하지 않고 클라이언트에서 페치**(TanStack Query) — 그래야 스니펫이 애초에 HTML 소스에 존재하지 않는다. ③은 8.22절 "브라우저가 `api.` 도메인을 직접 호출" 원칙과도 자연스럽게 맞물린다 |
| **K-2** | **`%`·`_` 이스케이프 누락 — 검색어 한 글자로 전수 스캔을 유발할 수 있다** | 사용자가 `q=%` 하나만 보내면 쿼리가 `ILIKE '%%%'`가 되어 **모든 청크가 매칭**된다. GIN이 전부 후보로 돌려주고 재검사까지 수행하므로, 8.59절 H-1이 청크 분할까지 해가며 없앤 **전수 스캔을 사용자가 임의로 되살릴 수 있다**. 속도 제한이 분당 30회여도 매 호출이 전수 스캔이면 치명적이다. 정확도 문제도 있다 — `_`는 임의의 한 글자라 "홀로`_`그램" 같은 정상 검색어가 엉뚱하게 동작한다. **주의**: Prisma `$queryRaw` 태그드 템플릿은 SQL 인젝션은 막아주지만 **LIKE 와일드카드는 전혀 막지 못한다** — 별개의 처리다 | 검색어에서 `\`·`%`·`_`를 **이스케이프**한 뒤 패턴을 조립하고 쿼리에 **`ESCAPE '\'`** 를 명시한다. 이 처리는 8.61절 J-1의 `$queryRaw` 검색 쿼리와 8.27절 제목 검색 **양쪽 모두에 적용**한다 |
| **K-3** | **검색어 정규화가 색인 정규화와 다르면 조용히 매칭에 실패한다** | 8.60절 I-1에서 본문은 `NFC` 정규화 + 유령문자 제거 + 공백 축약을 거쳐 저장된다. 그런데 **검색어에 같은 처리를 하지 않으면** 자모 분리형(NFD) 한글을 붙여넣은 사용자는 **아무 결과도 못 받는다**(macOS에서 복사한 텍스트가 대표 사례). 에러도 안 나고 결과만 0건이라 원인 파악이 특히 어렵다 | 검색어에도 색인과 **동일한 정규화**(NFC → 유령문자 제거 → trim → 공백 축약)를 적용하고, **색인용과 질의용이 같은 공유 함수를 호출**하도록 구현한다(한쪽만 바뀌면 검색이 조용히 깨지는 것을 구조적으로 방지). 이 함수는 서버 전용이므로 `packages/shared-types`가 아니라 백엔드 공용 유틸에 둔다 |
| **K-4** | **제목 검색과 본문 검색이 서로 다른 테이블 쿼리인데 하나의 결과로 합쳐야 한다** | 8.27절은 `products`(제목·설명·초성), 8.59절은 `product_text_chunks`(본문)를 각각 검색한다. 같은 책이 양쪽에 매칭되면 중복되고, 사용자는 **이 책이 왜 검색됐는지** 알 수 없다. 더 큰 문제는 **2자 검색어일 때 본문검색이 빠진다**는 점(8.59절 H-2)인데, 그냥 결과가 적게 나오면 사용자는 "이 책엔 그 내용이 없구나"로 오해한다 | **단일 엔드포인트 `GET /products/search`** 로 통합하고 응답에 **`matchedIn`**(`title`\|`description`\|`content`)을 담는다. 본문 매칭일 때만 `snippet`·`page`를 포함(8.61절 J-5의 `{ snippet, matchStart, matchLength, page }` 형태). 양쪽에 매칭되면 **제목 매칭을 우선**해 1건으로 합친다(관련도가 높고 스니펫 노출도 줄어듦). 또한 응답에 **`searchedFields`** 를 함께 반환해, 2자 검색 시 프론트가 "본문검색은 3글자 이상부터 지원합니다"를 **정확한 근거로** 안내할 수 있게 한다 |

#### 보완 필요

| # | 문제 | 제안 |
|---|---|---|
| K-5 | **총 건수 계산이 검색 쿼리를 한 번 더 돌린다** — 8.61절 J-3의 `DISTINCT ON` + 바깥 정렬 구조에서 정확한 총 건수를 내려면 같은 서브쿼리를 반복해야 하는데, 본문검색은 청크 수천 개가 매칭될 수 있어 비싸다 | **상한 카운트**를 쓴다 — `LIMIT 201`로 세어 200건을 넘으면 정확한 수 대신 **"200+"** 로 표시. **이 200은 임의의 값이 아니라 `페이지 상한 10 × 페이지당 20건`에서 나온 값이다(8.74절 W-6)** — 세 값 중 하나만 바꾸면 "200+인데 10페이지까지밖에 못 감" 또는 "11페이지가 비어 있음" 같은 어긋남이 생기므로 **반드시 함께 조정한다**. 페이지네이션은 우리 규모(상품 수백~수천)에서 **OFFSET으로 충분**하며 커서 페이징은 과설계다. 다만 **`page` 상한을 10**으로 두어 깊은 OFFSET 호출을 막는다 |
| K-6 | **검색 응답이 공유 캐시에 남으면 속도 제한이 우회된다** — 8.23절은 상품 상세에 5분 TTL을 적용했는데, 같은 감각으로 검색에 캐시를 걸면 **캐시 히트가 8.61절 J-4의 분당 30회 제한을 거치지 않고** 스니펫을 돌려준다. 검색은 쿼리 조합이 무한해 히트율도 애초에 낮다 | 검색 응답에 **`Cache-Control: no-store`** 를 명시하고, 이를 **8.23절 캐시 전략의 공식 예외**로 기록한다 |
| K-7 | **요청 스키마가 정해져 있지 않다** — 8.40절에서 Zod + `nestjs-zod`를 쓰기로 했으나 검색 파라미터 규칙이 비어 있다 | `q`: 필수, trim 후 **2~50자**(2자 미만은 결과가 무의미하고 부하만 크며, 50자 상한은 8.61절 J-4) / `sort`: `recent`\|`sales`(8.27절) / `page`: 1~10(K-5) / `categoryId`: UUID, 선택. 위반 시 400. **검색어가 전부 초성(`ㄱ`~`ㅎ`)인 경우 초성 컬럼 매칭으로 분기**(8.27절)하는 것도 서비스 계층에 명시 |
| K-8 | **GA4 `search` 이벤트에 검색어를 그대로 보내면 개인정보가 쌓일 수 있다** — 사용자가 검색창에 이메일·전화번호를 입력하는 일이 실제로 있고, GA4는 PII 수집을 정책상 금지한다 | `search` 이벤트는 탐색성이므로 8.50절 원칙대로 **클라이언트에서 전송**하되, 전송 전 **이메일·전화번호 패턴이 감지되면 검색어를 생략**하고 이벤트만 보낸다. 검색어 길이는 이미 50자로 제한되어 있어 추가 절단은 불필요 |

Sources:
- [PostgreSQL: Pattern Matching — LIKE/ILIKE and the ESCAPE clause](https://www.postgresql.org/docs/current/functions-matching.html)
- [LIKE in PostgreSQL: ILIKE, Wildcards, and Why It Ignores Your Index](https://www.runxbuild.com/blog/like-sql-postgres/)
- [Next.js: Metadata API — robots and per-page indexing control](https://nextjs.org/docs/app/api-reference/functions/generate-metadata)
- [Google Search Central: Block search result pages from being crawled](https://developers.google.com/search/docs/crawling-indexing/block-indexing)
- [GA4: Best practices to avoid sending personally identifiable information](https://support.google.com/analytics/answer/6366371)

---

### 8.63 검색 결과 화면 프론트엔드 구성 (확정)

> 8.62절까지는 서버 쪽 이야기였고, 이번엔 그 결정들이 **화면에서 어떻게 드러나는지**입니다. 검토해보니 이 절의 진짜 내용은 UI 취향이 아니라 **앞 절들의 제약이 프론트 구현 방식에 따라 정상 사용자를 차단하거나, 방어 장치를 무력화한다**는 점이었습니다. **실제 구축이 아닌 결정 단계**입니다.

#### 즉시 결정 필요

| # | 문제 | 왜 문제인가 | 제안 |
|---|---|---|---|
| **L-1** | **입력하면서 실시간 검색하면 정상 사용자가 분당 30회 제한에 스스로 걸린다** | 쇼핑몰 검색창은 보통 타이핑 중 실시간으로 결과를 보여준다. 그런데 8.28/8.61절 J-4에서 검색은 **분당 30회**로 제한했고, 8.62절 K-7에서 **2자부터 검색이 성립**한다. "해리포터를 찾아보자"처럼 지우고 고쳐가며 입력하면 debounce 300ms를 걸어도 **한 번의 탐색에서 10~20회 요청**이 나가고, 몇 번 검색하면 **정상 사용자가 429로 자기 자신을 차단**한다. 게다가 매 요청이 본문검색(청크 GIN + 재검사)을 수반해 DB 부하도 크다 | **제출형 검색으로 확정한다** — 엔터 또는 검색 버튼에서만 요청. 타이핑 중 자동완성(suggest)은 **MVP 범위에서 제외**하고, 향후 도입한다면 8.62절 K-4의 통합 엔드포인트가 아니라 **제목 전용 경량 엔드포인트**(본문검색 제외, 캐시 가능, 별도 완화된 제한)를 따로 두는 것을 전제로 한다. 본문검색은 구조상 "타이핑마다 호출"에 맞지 않는 연산이다 |
| **L-2** | **SSR을 뺐는데 검색 상태를 URL에 두지 않으면 공유·새로고침·뒤로가기가 전부 깨진다** | 8.62절 K-1에서 스니펫 색인을 막으려 **검색 결과를 클라이언트 페치**로 바꿨다. 이때 검색어·정렬·페이지를 Zustand 같은 메모리 상태로만 관리하면 **URL에 아무 정보가 없어** 결과를 공유할 수 없고, 새로고침하면 초기화되며, 상품 상세를 보고 뒤로 가면 검색 결과가 사라진다 — 쇼핑몰에서 이탈로 직결되는 동선이다 | 검색 상태(`q`/`sort`/`page`/`categoryId`)는 **URL 쿼리스트링을 단일 진실 원천**으로 삼는다(`useSearchParams` 읽기 + `router.replace` 쓰기). TanStack Query의 `queryKey`를 이 값들로 구성하면 **URL이 곧 캐시 키**가 되어 뒤로가기 시 재요청 없이 복원된다. 8.37절이 "서버 상태=TanStack Query, 클라이언트 상태=Zustand"로 나눴는데, **검색 파라미터는 둘 중 어느 쪽도 아닌 URL 상태**임을 명시한다(Zustand에 넣지 않음). 색인은 이미 8.62절 K-1에서 막았으므로 URL이 공유돼도 SEO 문제는 없다 |
| **L-3** | **하이라이트를 검색어 정규식 치환으로 구현하면 8.61절 J-5가 무의미해진다** | J-5는 XSS를 막으려 **서버가 오프셋만 주고 프론트가 문자열을 쪼개 감싸라**고 정했다. 그런데 프론트에서 흔히 쓰는 `snippet.replace(new RegExp(q, 'gi'), '<mark>$&</mark>')` + `dangerouslySetInnerHTML` 구현을 하면 **정확히 J-5가 막으려던 상태로 되돌아간다**. 검색어에 정규식 특수문자가 있으면 깨지는 문제까지 덤으로 따라온다 | 스니펫 렌더링을 **`slice` 3조각**으로 고정한다 — `snippet.slice(0, matchStart)` / `slice(matchStart, matchStart + matchLength)` / `slice(matchStart + matchLength)`, 가운데만 `<mark>`. 그리고 **문서가 아니라 린트로 강제**한다: 8.48절 ESLint flat config에 **`react/no-danger`를 error로 추가**해 `dangerouslySetInnerHTML` 사용 자체를 차단 |
| **L-4** | **결과가 0건일 때 사용자가 이유를 알 수 없다** | 8.62절 K-4가 `searchedFields`를 응답에 넣기로 한 이유가 바로 이 화면이다. **2자로 검색하면 본문검색이 빠지는데**(8.59절 H-2) 아무 안내 없이 결과만 적게 나오면 사용자는 "이 책엔 그 내용이 없구나"로 오해한다. 또 **429를 받았을 때** TanStack Query가 기본값(`retry: 3`)대로 재시도하면 **제한 상황을 더 악화**시키고 사용자는 빈 화면만 본다 | 빈 결과를 **세 갈래로 구분**해 안내한다: ① `searchedFields`에 `content`가 없으면 → "본문검색은 3글자 이상부터 지원합니다" + 재검색 유도, ② 3자 이상인데 0건 → 같은 카테고리 인기 상품을 대신 노출(추가 쿼리 없이 기존 목록 API 재사용), ③ **429** → "잠시 후 다시 시도해 주세요" 전용 화면. ③을 위해 **TanStack Query에 `retry` 조건을 두어 4xx는 재시도하지 않도록** 설정한다(특히 429) |

#### 보완 필요

| # | 문제 | 제안 |
|---|---|---|
| L-5 | **페이지네이션 UI 방식이 정해져 있지 않다** — 무한 스크롤을 택하면 8.62절 K-5의 `OFFSET` 페이징과 충돌한다(스크롤 중 상품이 추가·판매중지되면 중복·누락이 생기고, `page` 상한 10과도 맞지 않으며, 뒤로가기 복원이 어렵다) | **번호 페이지네이션**으로 확정(K-5의 `OFFSET` + `page` 1~10과 자연스럽게 일치하고, 결과 위치를 기억·공유하기 쉬워 쇼핑몰 검색의 표준 패턴). 총 건수는 K-5의 상한 카운트에 맞춰 **200건 초과 시 "200+"** 로 표기하고, 10페이지에 도달하면 **"검색어를 더 구체적으로 입력해 보세요"** 를 노출한다 |
| L-6 | **클라이언트 페치라 첫 진입 화면이 비어 있고 페이지 이동 때마다 깜빡인다** — 8.62절 K-1에서 SSR을 뺀 직접적인 대가다 | 첫 로딩에는 **결과 카드 형태의 스켈레톤**을 표시하고(레이아웃이 미리 잡혀 CLS 방지), 페이지 이동 시에는 TanStack Query의 **`placeholderData: keepPreviousData`** 로 이전 결과를 유지한 채 로딩 표시만 겹쳐 보여준다 |
| L-7 | **결과 카드 높이가 제각각이라 레이아웃이 흔들린다** — `matchedIn`이 `title`이면 스니펫이 없고 `content`면 있으므로(8.62절 K-4) 카드마다 높이가 다르다. 6절 성능 목표·8.43절 Core Web Vitals의 CLS에 직접 영향 | 결과를 **리스트형 카드로 통일**하고 **스니펫 영역을 2줄 고정 높이**(`line-clamp-2`)로 잡아, 스니펫이 없는 항목도 같은 높이를 유지하게 한다. 제목 매칭 항목에는 스니펫 자리에 카테고리·짧은 소개를 넣어 빈칸으로 보이지 않게 한다 |
| L-8 | **검색 결과의 접근성 처리가 비어 있다** — 8.47절에서 `jest-axe`를 도입했지만 검색 결과는 정적 검사로 안 잡히는 동적 함정이 있다: 결과 개수가 바뀌어도 스크린리더 사용자는 알 수 없고, 정렬을 바꾸면 포커스가 사라진다 | 결과 요약 영역(“총 N건”)에 **`role="status"` + `aria-live="polite"`** 를 부여해 검색·정렬·페이지 변경을 음성으로 알리고, 정렬 변경 후 **포커스를 결과 목록 헤딩으로 이동**시킨다. `<mark>`는 하이라이트에 대한 표준 시맨틱 요소이므로 그대로 사용한다 |

Sources:
- [TanStack Query: `retry` and `placeholderData: keepPreviousData` (v5)](https://tanstack.com/query/latest/docs/framework/react/guides/paginated-queries)
- [Next.js: `useSearchParams` — URL as the source of truth](https://nextjs.org/docs/app/api-reference/functions/use-search-params)
- [eslint-plugin-react: `react/no-danger`](https://github.com/jsx-eslint/eslint-plugin-react/blob/master/docs/rules/no-danger.md)
- [MDN: `<mark>` — the Mark Text element](https://developer.mozilla.org/en-US/docs/Web/HTML/Element/mark)
- [WAI-ARIA: `aria-live` and the `status` role for dynamic result counts](https://www.w3.org/WAI/ARIA/apg/practices/live-regions/)

---

### 8.64 App Router × Zustand / React Hook Form / react-pdf 호환성 점검 (확정)

> 8.37(Zustand)·8.40(React Hook Form)·8.29(react-pdf)는 각각 다른 시점에 정해졌고, **App Router 환경에서 함께 동작할 때의 제약은 점검된 적이 없습니다**. 핵심 오해 하나를 먼저 정리하면: **`'use client'`는 "서버에서 실행하지 않는다"는 뜻이 아니라 "클라이언트에도 번들로 보낸다"는 뜻**입니다. 클라이언트 컴포넌트도 초기 HTML은 **서버에서 렌더링**되며, 아래 문제 대부분이 이 지점에서 출발합니다. **실제 구축이 아닌 결정 단계**입니다.

#### 즉시 수정 필요

| # | 문제 | 왜 문제인가 | 제안 수정 |
|---|---|---|---|
| **M-1** | **8.29절의 `dynamic(..., { ssr: false })`는 App Router에서 그대로는 빌드에 실패한다** | 8.29절은 react-pdf를 `dynamic(() => import('./SamplePdfViewer'), { ssr: false })`로 불러오기로 했다. 그런데 App Router에서 **Server Component 안에서 `ssr: false`를 쓰면 빌드 에러**다(`ssr: false is not allowed with next/dynamic in Server Components`). 이 뷰어가 놓일 **상품 상세 페이지는 8.43절 `generateMetadata`를 쓰는 Server Component**이므로 **정확히 이 조건에 해당한다** — 지금 적힌 대로 구현하면 첫 빌드에서 막힌다 | **`'use client'` 래퍼 컴포넌트를 한 겹 둔다** — `PdfViewerLoader.tsx`에 `'use client'`를 선언하고 그 안에서 `dynamic(..., { ssr: false })`를 호출한 뒤, Server Component인 상품 상세 페이지는 **이 래퍼만 import**한다. 8.29절의 "미리보기 버튼 클릭 시 지연 로드" 결정과도 자연스럽게 맞물린다(버튼 자체가 클라이언트 컴포넌트여야 하므로) |
| **M-2** | **Zustand 스토어를 모듈 최상위에 두면 서버에서 모든 요청이 같은 스토어를 공유한다** | JS 모듈은 한 번 평가되면 캐시되므로, `create()`로 만든 스토어를 모듈 스코프에 두면 **Next.js 서버 프로세스 전체가 하나의 인스턴스를 공유**한다. Zustand 공식 문서도 "스토어를 전역 변수로 정의하지 말고 요청마다 생성하라"고 명시한다. 우리 설계에서 이게 위험한 이유는 구체적이다 — 8.37절은 **Access Token을 Zustand에 보관**하기로 했고, 8.22절은 **SSR 중 토큰 릴레이**를 수행한다. SSR 경로에서 릴레이된 토큰을 스토어에 넣는 구현(자연스러워 보이는 구현이다)을 하면 **그 값이 서버 프로세스에 남아 다음 요청의 다른 사용자에게 노출**된다 | **두 가지를 함께 적용한다**: ① 스토어를 `createStore()`로 만들어 **Context Provider가 요청마다 인스턴스를 생성**하도록 바꾸고(Zustand 공식 Next.js 가이드 패턴), ② **"서버 렌더링 경로에서는 어떤 스토어에도 쓰지 않는다"** 를 원칙으로 못박는다. SSR에 필요한 데이터는 스토어가 아니라 **props로 내려보낸다**. 8.22절이 이미 "Route Handler에는 비즈니스 로직을 두지 않는다"로 정한 것과 같은 결의 규율이다 |
| **M-3** | **`persist`로 저장한 장바구니는 SSR과 반드시 어긋난다(hydration mismatch)** | 8.37절은 장바구니를 `persist` 미들웨어로 `localStorage`에 저장하기로 했다. 서버에는 `localStorage`가 없어 **빈 장바구니로 HTML을 렌더**하는데, 클라이언트는 하이드레이션 시 `localStorage`에서 복원한 값으로 렌더한다. **두 결과가 다르므로 hydration mismatch**가 발생하고, React는 해당 서브트리를 버리고 클라이언트에서 다시 그린다 — 헤더의 장바구니 배지가 깜빡이거나 콘솔이 에러로 가득 찬다 | `persist` 옵션에 **`skipHydration: true`** 를 주고, 클라이언트 마운트 후 `useEffect`에서 **`rehydrate()`를 명시적으로 호출**한다. 복원 전까지는 **서버와 동일한 초기값(빈 장바구니)** 을 렌더해 첫 HTML과 첫 클라이언트 렌더를 일치시킨다. 배지 같은 UI는 복원 완료 전까지 렌더를 보류한다 |
| **M-4** | **pnpm 모노레포에서 pdf.js 워커 경로와 Zod 버전이 각각 다른 이유로 깨진다** | ① **워커 경로**: react-pdf는 `pdfjs-dist/build/pdf.worker.min.mjs`를 참조해야 하는데, **pnpm은 엄격한 `node_modules` 구조**라 앱에서 이 경로가 해석되지 않는다(react-pdf의 전이 의존성이라 앱의 `node_modules`에 없음). 8.36절에서 **pnpm + Turborepo**를 택했으므로 우리에게 그대로 해당한다. ② **Zod 버전**: 8.40절은 Zod 스키마를 `packages/shared-types`에 두고 **프론트(`@hookform/resolvers`)·백엔드(`nestjs-zod`) 양쪽에서 재사용**하기로 했는데, pnpm은 패키지마다 요구 버전이 다르면 **서로 다른 Zod를 각각 설치**한다. 그러면 공유 스키마가 한쪽에서는 낯선 객체가 되어 검증이 조용히 어긋난다 | ① `pdfjs-dist`를 **앱의 직접 의존성으로 명시**하고(전이 의존성에 기대지 않음), 워커 파일을 **빌드 시 `public/`으로 복사**해 자체 호스팅한다(CDN 참조는 외부 의존과 버전 드리프트를 동시에 안음). ② **`pnpm.overrides`로 `zod`를 단일 버전에 고정**하고, `@hookform/resolvers`·`nestjs-zod`가 같은 메이저를 지원하는지 도입 시점에 확인한다 |

#### 보완 필요

| # | 문제 | 제안 |
|---|---|---|
| M-5 | **`workerSrc` 설정이 어긋나면 런타임에서야 드러난다** — 워커 버전이 `pdfjs-dist` 버전과 다르면 "The API version does not match the Worker version"로 실패하고, `workerSrc`를 별도 파일에 설정하면 **모듈 실행 순서 때문에 기본값에 덮어써지는** 함정도 있다 | `workerSrc`는 **react-pdf 컴포넌트를 사용하는 바로 그 모듈에서 설정**하고, 경로는 M-4의 자체 호스팅 파일을 가리키게 한다. 워커 복사는 수동이 아니라 **`postinstall` 스크립트로 자동화**해 `pdfjs-dist` 업그레이드 시 버전이 저절로 따라오게 한다 |
| M-6 | **React Hook Form의 `defaultValues`는 첫 렌더에만 반영된다** — 관리자 상품 수정 폼(FR-7.2)처럼 **기존 값을 API로 받아 채우는 화면**에서, 데이터가 도착하기 전에 폼이 먼저 렌더되면 **빈 폼이 그대로 고정**된다. 8.40절에는 이 주의점이 없다 | 비동기로 받은 초기값은 `defaultValues`가 아니라 **`reset(data)`** 로 주입한다. 또는 데이터가 도착할 때까지 폼 자체를 렌더하지 않는다(TanStack Query의 로딩 상태 활용). 수정 폼이 있는 모든 화면에 적용되는 규칙으로 8.40절에 명시 |
| M-7 | **pdf.js가 쓰는 `Promise.withResolvers`가 구형 환경에서 없다** — 최신 pdf.js는 이 API를 사용하는데, 6절 브라우저 호환 목표(최신 Chrome·Safari·Edge·모바일)의 **"최신"이 실제로 어디까지인지** 정해져 있지 않다. 지원 범위를 벗어난 사파리에서는 미리보기가 통째로 실패한다 | 미리보기는 **핵심 구매 동선이 아니므로**(8.29절: 샘플 전용) 폴리필을 추가하기보다 **실패 시 "브라우저를 업데이트해 주세요" 안내와 샘플 PDF 직접 다운로드 링크로 대체**한다(8.16절 웹 푸시에서 쓴 그레이스풀 디그레이드와 같은 방식). 6절 브라우저 호환 항목에 **"최근 2개 메이저 버전"** 이라는 기준선을 함께 명시한다 |
| M-8 | **이 부류의 결함은 단위 테스트로 잡히지 않는다** — M-1은 빌드 단계, M-2·M-3은 서버 렌더와 클라이언트 렌더가 만나는 지점에서만 드러나므로 Vitest·Testing Library(8.45/8.47절)로는 재현되지 않는다 | 8.46절 Playwright E2E에 **① 상품 상세에서 미리보기 열기, ② 장바구니에 담고 새로고침 후 배지 유지 확인**(M-3), ③ 로그인 상태로 SSR 페이지 진입 시 콘솔에 hydration 에러가 없을 것을 시나리오로 추가한다. ③은 `page.on('console')`로 에러를 수집해 **테스트를 실패시키는** 방식으로 강제한다 |

Sources:
- [Guides: Lazy Loading | Next.js — `ssr: false` must be in a Client Component](https://nextjs.org/docs/app/guides/lazy-loading)
- [Setup with Next.js — Zustand 공식 문서(요청마다 스토어 생성)](https://zustand.docs.pmnd.rs/learn/guides/nextjs)
- [Using Zustand in React Server Components — pmndrs/zustand Discussion #2200](https://github.com/pmndrs/zustand/discussions/2200)
- [Mismatching worker version with pdfjs-dist as a direct dependency — wojtekmaj/react-pdf #1520](https://github.com/wojtekmaj/react-pdf/discussions/1520)
- [Can't resolve 'pdfjs-dist/build/pdf.worker.min.mjs' in Next.js App Router — react-pdf #1855](https://github.com/wojtekmaj/react-pdf/issues/1855)
- [Promise.withResolvers is not a function — vercel/next.js #70239](https://github.com/vercel/next.js/issues/70239)

---

### 8.65 서브도메인 통일의 실제 적용 범위와 대가 (확정)

> "프론트(`app.`)와 백엔드(`api.`)의 상위 도메인을 통일한다"는 **8.7절에 이미 결정으로 들어가 있습니다**. 이번 검토는 그 원칙을 **실제로 적용했을 때 어디까지 영향이 가는지**를 봤고, 결론은 **지금 상태로는 이 원칙이 한 군데에서 깨진다**는 것입니다. 쿠키의 동작 기준은 "같은 오리진"이 아니라 **"같은 사이트(eTLD+1)"** 라는 점이 논의의 출발점입니다 — `app.`과 `api.`는 오리진은 다르지만 사이트는 같아서 `SameSite=Lax` 쿠키가 정상 전송됩니다. **실제 구축이 아닌 결정 단계**입니다.

#### 즉시 결정 필요

| # | 문제 | 왜 문제인가 | 제안 |
|---|---|---|---|
| **N-1** | **Vercel 프리뷰 배포(`*.vercel.app`)에서는 이 원칙이 성립하지 않아 로그인이 아예 안 된다** | `vercel.app`은 **Public Suffix List에 등재된 도메인**이라, `pr-12-cb-mall.vercel.app`은 `cbmall.co.kr`과 **완전히 다른 사이트**다. 프리뷰 화면에서 `api-stg.cbmall.co.kr`로 로그인하면 Refresh 쿠키는 **서드파티 쿠키**가 되어 `SameSite=Lax` 조건상 이후 요청에 실리지 않고(사파리는 설정 자체를 차단), **8.22/8.53절의 SSR 토큰 릴레이도 동작하지 않는다**. 이게 두 결정을 동시에 무너뜨린다 — **8.56절**은 "프리뷰는 프론트만 격리하고 백엔드·DB는 스테이징 공유"로 정했고, **8.46절**은 "프리뷰 URL을 대상으로 `storageState` 로그인을 재사용"하도록 정했는데, **둘 다 프리뷰에서 인증이 되는 것을 전제**로 한다 | 프리뷰 배포에 **같은 상위 도메인의 서브도메인을 부여**한다 — Vercel 프로젝트에 `*.preview.cbmall.co.kr`을 등록하고, CI에서 배포 후 **`vercel alias set <deployment-url> pr-<번호>.preview.cbmall.co.kr`** 한 줄을 실행한다(DNS에 와일드카드 CNAME 필요). Vercel의 **Preview Deployment Suffix 기능은 Pro 위에 월 $100 유료 애드온**이라, 같은 결과를 CI 스텝 하나로 얻는 이 방식이 우리 규모에 맞다 |
| **N-2** | **`Domain=.cbmall.co.kr`가 필요한 이유가 문서에 없고, 그래서 더 안전한 선택지가 검토되지 않았다** | 8.7절은 쿠키를 `Domain=.cb몰도메인`으로 설정한다고 적었는데, **사실 `app.`→`api.` 호출만 생각하면 Domain 속성은 필요 없다** — `api.cbmall.co.kr`에 host-only로 설정해도 그 도메인으로 가는 요청에는 정상 전송되고, 범위가 좁아 더 안전하다. **Domain 속성이 반드시 필요한 유일한 이유는 8.22/8.53절의 SSR 토큰 릴레이**다(Next.js 서버가 `app.` 도메인으로 들어온 요청에서 쿠키를 읽어야 하므로). 이 연결이 기록돼 있지 않으면, 나중에 누군가 "범위를 좁히자"며 Domain을 떼는 순간 **SSR 경로가 조용히 깨진다** | Domain 속성을 유지하되 **그 이유(SSR 릴레이 의존)를 8.7절에 명시**해 임의 변경을 막는다. 함께 따라오는 제약도 적는다 — **`__Host-` 접두사는 Domain 속성과 함께 쓸 수 없으므로 사용 불가**하며, 대신 **`__Secure-` 접두사**를 쓴다(Secure 강제는 되지만 도메인 고정 보장은 포기) |
| **N-3** | **이 쿠키는 현재·미래의 모든 서브도메인에 전송된다** | `Domain=.cbmall.co.kr` 쿠키는 `app.`·`api.`뿐 아니라 **앞으로 만들 모든 서브도메인**에 실려 나간다. 흔한 사고 경로가 둘 있다 — ① 헬프센터·블로그·상태페이지를 외부 SaaS에 **CNAME으로 위임**하면 사용자의 **Refresh Token이 그 외부 업체 서버로 전송**된다, ② 쓰지 않게 된 서브도메인의 DNS 레코드를 남겨두면(dangling DNS) 제3자가 그 호스트를 선점해 토큰을 수집할 수 있다. 8.13절이 이미 발신 전용 `mail.` 서브도메인을 계획하고 있어 서브도메인은 계속 늘어난다 | **서브도메인 운영 정책을 8.7절에 못박는다**: ① 외부 SaaS에 서브도메인을 위임할 때는 **쿠키가 전송된다는 점을 전제로 검토**하고, 가급적 **별도 상위 도메인**을 쓴다, ② **사용을 중단한 DNS 레코드는 즉시 삭제**한다, ③ 새 서브도메인 추가는 **인증 담당자 확인을 거친다**(8.20절 관리자 권한 체계와 같은 결의 규율) |

#### 보완 필요

| # | 문제 | 제안 |
|---|---|---|
| N-4 | **`SameSite=Lax`가 형제 서브도메인으로부터의 CSRF는 막지 못한다** — 8.53절은 "쿠키를 쓰는 엔드포인트는 refresh 계열뿐이라 노출면이 좁다"고 판단했는데, 서브도메인 통일로 쿠키가 **같은 사이트 전체에 공유**되면 사정이 달라진다. `Lax`는 **크로스사이트** 요청을 막을 뿐, 탈취되거나 위임된 형제 서브도메인(N-3)에서 오는 요청은 **같은 사이트라 그대로 통과**시킨다 | 쿠키만으로 동작하는 엔드포인트(`/auth/refresh`, 로그아웃, 8.53절 SSR 전용 엔드포인트)에 **`Origin` 헤더 검증을 추가**한다 — 허용 목록(`app.cbmall.co.kr`, `*.preview.cbmall.co.kr`)과 일치하지 않으면 거부. 8.51절 C-5에서 정한 CORS 허용 오리진 목록을 그대로 재사용하면 관리 지점이 늘지 않는다 |
| N-5 | **로컬 개발 환경만 구조가 달라 쿠키 설정이 분기된다** — `localhost:3000`과 `localhost:3001`은 포트만 다른 같은 호스트라 쿠키 동작이 운영과 달라지고, `Domain` 속성 분기 코드가 생기면 **운영에서만 재현되는 버그**의 온상이 된다 | 로컬도 **`app.localhost:3000` / `api.localhost:3001`** 로 운영과 같은 구조를 만든다(Chrome·Firefox는 `*.localhost`를 루프백으로 자동 해석하며, `Secure` 쿠키도 localhost에는 http로 허용된다). **사파리는 `*.localhost`를 자동 해석하지 않으므로** macOS 개발자는 `/etc/hosts` 항목이 필요하다는 점을 온보딩 문서에 적는다 |
| N-6 | **인증서 발급 주체가 프론트·백엔드에서 다르다** | 프론트(`app.`, `*.preview.`)는 **Vercel이 도메인 등록 시 자동 발급**하고, 백엔드(`api.`, `api-stg.`)는 **AWS ACM에서 발급해 ALB에 연결**한다(ACM 인증서는 무료). 와일드카드가 필요한 쪽은 프리뷰뿐이므로 백엔드는 개별 호스트명으로 충분하다 |
| N-7 | **도메인 확정이 "다음 단계"가 아니라 개발 착수 선행 작업이 됐다** — 10절에 "서비스 도메인 확정 및 DNS·인증서 설정"이 적혀 있지만, N-1 때문에 **도메인이 없으면 PR 프리뷰와 E2E 인증 테스트가 아예 돌지 않는다**. 게다가 도메인은 8.13절 SPF/DKIM/DMARC, 토스페이먼츠 상점 등록에도 선행 조건이다 | 도메인 확정·DNS·인증서 설정을 **9.1절 마일스톤의 1주차 작업으로 끌어올린다**. 확정 전까지는 프리뷰에서 인증이 필요 없는 화면(상품 목록·상세·검색)만 검증 가능하다는 점을 팀에 공유한다 |

Sources:
- [Preview Deployment Suffix — Vercel(유료 애드온)](https://vercel.com/docs/deployments/preview-deployment-suffix)
- [Wildcard subdomains in preview URLs? — vercel/community #475(`vercel alias` 대안)](https://github.com/vercel/community/discussions/475)
- [Can I Set a Cookie from My Vercel Project Subdomain to Vercel.app? — Vercel KB](https://vercel.com/kb/guide/can-i-set-a-cookie-from-my-vercel-project-subdomain-to-vercel-app)
- [MDN: Set-Cookie — Domain 속성과 `__Host-`/`__Secure-` 접두사](https://developer.mozilla.org/en-US/docs/Web/HTTP/Reference/Headers/Set-Cookie)
- [MDN: SameSite cookies — 사이트(eTLD+1) 기준 동작](https://developer.mozilla.org/en-US/docs/Web/HTTP/Reference/Headers/Set-Cookie/SameSite)
- [Public Suffix List](https://publicsuffix.org/)

---

### 8.66 BFF 범위 재확정: Route Handler를 몇 개까지 둘 것인가 (확정)

> "범용 프록시가 아닌 SSR 토큰 릴레이 전용"은 **8.22절에 이미 원칙으로 들어가 있습니다**. 다만 그 절은 Route Handler **2개**(인증 릴레이 + 캐시 무효화 수신)를 예외로 두고 있고, 그 뒤 **8.53절에서 SSR 릴레이 방식이 바뀌었는데 8.22절이 갱신되지 않았습니다**. 다시 세어보니 **필요한 BFF Route Handler는 1개가 아니라 0개**입니다. **실제 구축이 아닌 결정 단계**입니다.

#### 즉시 수정 필요

| # | 문제 | 왜 문제인가 | 제안 수정 |
|---|---|---|---|
| **O-1** | **`app/api/auth/refresh/route.ts`는 존재 이유가 이미 사라졌다** | 8.22절이 이 Route Handler를 둔 이유는 딱 하나였다 — **"Server Component는 쿠키를 쓸 수 없다"** 는 제약을 우회하기 위해서다. 그런데 **8.53절이 방안 B(비회전 SSR 전용 엔드포인트)를 채택하면서 쿠키 쓰기 자체가 필요 없어졌다.** 이유가 소멸했는데 결론만 남아 있는 상태다. 두 경로를 다시 따라가 보면 둘 다 Route Handler를 거칠 이유가 없다 — ① **SSR 경로**: Server Component가 `cookies()`로 읽고 NestJS 비회전 엔드포인트를 직접 `fetch`하면 끝(쿠키 쓰기 없음), ② **브라우저 회전 경로**: 8.7/8.65절 서브도메인 통일 덕분에 브라우저가 `api.cbmall.co.kr/auth/refresh`를 `credentials: 'include'`로 **직접 호출**하면 NestJS가 `Domain=.cbmall.co.kr`로 `Set-Cookie`한다. Route Handler는 **자기 자신에게 HTTP 요청을 한 번 더 보내는 홉**만 추가한다 | **BFF Route Handler를 0개로 하고, SSR 릴레이는 서버 전용 헬퍼 함수 하나(`lib/auth/getSsrAccessToken.ts`)로 대체**한다. 8.22절도 이미 "Route Handler(**또는 동등한 서버 전용 헬퍼 함수**)"라고 여지를 남겨뒀으므로 결정의 방향을 바꾸는 것이 아니라 **둘 중 하나로 확정**하는 것이다. 얻는 것: SSR마다 발생하던 **자기 자신 호출 홉 제거**(지연·Vercel 함수 호출 수 감소), `app/api/` 디렉터리 자체가 사라져 **"프록시를 하나 더 만들까"라는 유혹의 출발점이 없어짐** |
| **O-2** | **캐시 무효화 엔드포인트를 BFF로 분류해 둔 탓에 "BFF는 하나" 규칙이 참이 될 수 없다** | 8.22절은 이것을 BFF 예외 2개 중 하나로 적었지만, **이건 BFF가 아니다.** BFF는 브라우저 요청을 백엔드로 중계하는 것인데 이 엔드포인트는 **NestJS가 Next.js로 보내는 인바운드 웹훅**이라 방향이 반대다. 게다가 **`revalidateTag()`는 Next.js 런타임 안에서만 호출할 수 있어** 대체 수단이 없다 — 없애면 8.23절의 온디맨드 무효화 결정이 함께 무너진다. 즉 "하나로 줄이자"를 문자 그대로 적용하면 **줄일 수 없는 것을 줄이려다 캐시 전략이 깨진다** | **삭제가 아니라 재분류한다.** `app/api/` 아래 파일을 두 범주로 나누어 규칙을 각각 건다: **① BFF(브라우저→백엔드 중계) = 0개**, **② 인바운드 웹훅 수신기 = `revalidate` 하나만 허용**(8.51절 C-6의 공유 시크릿 검증 필수). 이렇게 해야 "BFF는 만들지 않는다"가 **예외 없는 규칙**이 되어 지키기 쉬워진다 |
| **O-3** | **"범용 프록시 금지"가 규율로만 존재해 시간이 지나면 반드시 새는다** | 이 원칙은 8.51절 C-6에서 정해진 뒤 문서에만 있다. 실제로 Route Handler는 **그럴듯한 이유와 함께 하나씩 늘어난다** — 파일 업로드 중계, 외부 이미지 프록시, 서드파티 API 키 숨기기 같은 요청은 전부 "이번만 예외"처럼 보인다. 8.21절이 모듈 경계를 `dependency-cruiser`로 **자동 검증**하기로 한 것과 같은 문제인데, 여기만 수동이다 | **CI에서 자동 검증한다** — `app/api/**/route.ts` 파일 목록을 **허용 목록(`revalidate` 하나)과 대조**해 새 파일이 생기면 실패시키는 스크립트를 8.35절 Lint 단계에 추가한다. 예외가 필요하면 허용 목록을 고치는 PR이 강제되므로, **추가가 리뷰 대상으로 드러난다** |

#### 보완 필요

| # | 문제 | 제안 |
|---|---|---|
| O-4 | **`middleware.ts`가 BFF 비대화의 다른 경로가 된다** — 개수 규칙을 Route Handler에만 걸면, 막힌 로직이 미들웨어로 흘러든다. 미들웨어는 **모든 요청에서 실행**되므로 여기에 NestJS 호출이 들어가면 Route Handler보다 나쁜 결과가 된다 | 미들웨어의 범위를 8.22절 결정대로 **"Refresh 쿠키 존재 여부 확인과 리다이렉트만"** 으로 못박고, **미들웨어에서 NestJS를 호출하지 않는다**를 명시적 금지 항목으로 추가한다(8.22절이 "보안 경계가 아니라 UX 1차 방어"로 정한 것과 같은 취지) |
| O-5 | **Server Actions는 막아뒀지만 실수로 생기기 쉽다** — 8.22절이 "Server Actions 미사용"으로 정했으나, App Router에서는 `<form action={서버함수}>`를 쓰면 **의도치 않게 Server Action이 만들어진다**. 그러면 8.22절이 피하려던 "요청 경로 3갈래" 상황이 조용히 생긴다 | 8.40절이 이미 **React Hook Form의 `onSubmit` + TanStack Query mutation**을 폼 제출 방식으로 고정했으므로, 여기에 **`'use server'` 지시문 사용 금지**를 명시적으로 덧붙인다. O-3의 CI 스크립트에서 `'use server'` 문자열 검출을 함께 확인하면 비용 없이 강제된다 |
| O-6 | **BFF가 사라지면 8.43절 robots.txt의 `/api/*` 차단 항목이 실제와 어긋난다** | `app/api/` 아래 남는 것은 `revalidate` 웹훅 하나뿐이지만, **차단 규칙은 그대로 두는 것이 맞다** — 경로가 비어 있어도 차단을 유지하면 향후 웹훅이 추가될 때 자동으로 보호된다. 다만 8.43절에 **"BFF 프록시가 없으므로 이 규칙은 웹훅 수신기 보호가 목적"** 이라고 의도를 적어, 나중에 "쓸모없는 규칙"으로 오해해 지우지 않도록 한다 |

Sources:
- [Next.js: Route Handlers](https://nextjs.org/docs/app/api-reference/file-conventions/route)
- [Next.js: `revalidateTag` — Next.js 런타임에서만 호출 가능](https://nextjs.org/docs/app/api-reference/functions/revalidateTag)
- [Next.js: `cookies()` — Server Component에서는 읽기 전용](https://nextjs.org/docs/app/api-reference/functions/cookies)
- [Next.js: Middleware](https://nextjs.org/docs/app/api-reference/file-conventions/middleware)
- (내부 교차 검토 — 8.7/8.22/8.23/8.35/8.40/8.43/8.51 C-6/8.53/8.65절 대조)

---

### 8.67 Prisma × Supavisor 트랜잭션 모드의 Prepared Statement 지원 여부 (확정)

> 8.15절이 런타임 연결에 `?pgbouncer=true`를 붙이기로 한 전제가 지금도 유효한지 확인했습니다. **결론: 유효합니다.** 다만 근거가 문서에 적혀 있지 않아, **공식 문서를 읽은 사람이 이 플래그를 떼려 할 가능성이 높은** 상태입니다 — Prisma 공식 문서와 Supavisor 공식 문서 **둘 다 "요즘은 필요 없다"로 읽히기 때문**입니다. **실제 구축이 아닌 결정 단계**입니다.

#### 확인 결과

| 질문 | 답 |
|---|---|
| Supavisor 트랜잭션 모드(6543)는 named prepared statement를 지원하는가? | **소프트웨어로는 지원한다** — Supavisor는 `named_prepared_statements` **기능 플래그**가 켜져 있을 때 트랜잭션 모드에서도 지원한다(`NAMED_PREPARED_STATEMENTS_ENABLED` 환경변수 또는 테넌트별 설정) |
| 그러면 우리는 쓸 수 있는가? | **아니다.** 그 플래그는 **Supavisor 서버 측 설정**이라 **Supabase 호스팅 고객이 켤 수 없다**. Supabase 공식 문서도 현재 "트랜잭션 모드는 prepared statement를 지원하지 않으니 `pgbouncer=true`를 붙이라"고 안내한다 |
| Prisma 문서의 "PgBouncer 1.21+ 에서는 `pgbouncer=true`를 쓰지 마라"는? | **우리에게 해당하지 않는다.** ① 그건 **PgBouncer** 이야기이고 우리는 **Supavisor**를 쓴다, ② 그 조건인 `max_prepared_statements > 0` 역시 **우리가 제어할 수 없는 서버 측 설정**이다 |
| **결론** | **8.15절의 `?pgbouncer=true` 유지가 맞다.** 근거는 "PgBouncer 일반론"이 아니라 **"Supabase 호스팅에서 서버 측 플래그를 켤 수 없다"** 는 현재 제약이다 |

#### 반영 필요

| # | 문제 | 제안 |
|---|---|---|
| **P-1** | **근거가 없으면 누군가 반드시 이 플래그를 뗀다** — 위 표에서 보듯 Prisma 공식 문서("1.21+ 에서는 쓰지 마라")와 Supavisor 공식 문서("트랜잭션 모드에서 지원함")를 각각 읽으면 **제거가 옳은 것처럼 보인다**. 8.15절에는 플래그만 적혀 있고 "왜 여전히 필요한가"가 없다 | 8.15절에 **"제거 금지 + 이유"** 를 명시한다 — 서버 측 플래그(`NAMED_PREPARED_STATEMENTS_ENABLED` / `max_prepared_statements`)를 **우리가 켤 수 없기 때문**이며, **Supabase가 이를 노출하기 전까지는 제거하지 않는다**. 8절 의존 관계 표에도 8.15를 기반 결정으로 이미 올려뒀으므로, 변경 시 함께 볼 절이 연결된다 |
| **P-2** | **빠뜨렸을 때의 증상이 간헐적이라 개발 단계에서 안 잡힌다** | 플래그 없이 돌리면 `prepared statement "s0" already exists`(SQLSTATE **42P05**) 또는 `...does not exist`(**26000**)가 난다. 문제는 **동시 요청이 서로 다른 백엔드 연결에 배정될 때만** 발생해 로컬 개발이나 단일 요청 테스트에서는 재현되지 않는다는 점이다. → 8.24절 모니터링에 **이 두 SQLSTATE를 Sentry 알림 조건으로 추가**해, 설정 누락이 운영에서 즉시 드러나게 한다 |
| **P-3** | **`connection_limit=1`을 같이 복사하면 성능이 죽는다** — 인터넷의 Supabase+Prisma 설정 예시는 대부분 `?pgbouncer=true&connection_limit=1`을 함께 제시하는데, **`connection_limit=1`은 Vercel·Lambda 같은 서버리스 전제**다(함수 인스턴스마다 별도 풀을 들고 있으면 커넥션이 폭증하므로 1로 묶는다). 우리 백엔드는 **ECS 상주 프로세스**(8.3절)라 그대로 쓰면 **태스크당 DB 커넥션이 1개로 묶여 동시성이 무너진다** | **`connection_limit`을 지정하지 않고 Prisma 기본값을 쓴다**(`물리 CPU 수 × 2 + 1`). 8.51절 C-7의 **2 vCPU** 기준 태스크당 5개, 태스크 2개로 **총 10개** — Supavisor 풀 한도 안에서 여유가 있다. 8.15절에 **"서버리스용 권장값을 복사하지 말 것"** 을 주석으로 남긴다 |
| P-4 | **prepared statement를 못 쓰는 대가가 평가된 적이 없다** — 쿼리 계획이 서버에 캐시되지 않아 매 실행마다 parse·plan이 반복된다 | 우리 규모(동시접속 100명)에서는 무시 가능한 수준이다. 다만 **8.59절 청크 GIN 검색**처럼 계획 수립이 상대적으로 무거운 쿼리는 영향을 받을 수 있으므로, 8.24절에 이미 넣어둔 **검색 API p95 지표**로 함께 관측한다(별도 지표 추가 불필요) |

#### 대안 — 세션 모드로 바꾸면?

| 방안 | 내용 | 평가 |
|---|---|---|
| **A. 트랜잭션 모드 유지(추천)** | 현행대로 6543 + `?pgbouncer=true` | prepared statement는 포기하지만 **커넥션 멀티플렉싱이라 ECS 태스크를 늘려도 DB 커넥션이 선형으로 늘지 않는다**. 8.52절의 `pg_try_advisory_xact_lock`(트랜잭션 단위)도 그대로 성립 |
| B. 세션 모드로 전환 | 세션 모드 포트 사용 → prepared statement 정상 동작, `pgbouncer=true` 불필요 | 세션 모드는 **클라이언트 연결 1개가 DB 연결 1개를 점유**한다. 지금 규모(총 10개)에서는 감당되지만 **ECS 오토스케일 여지를 그대로 깎아먹고**, 8.15절이 풀러를 도입한 이유 자체가 사라진다. 얻는 것(계획 캐시)에 비해 잃는 것이 크다 |

Sources:
- [Troubleshooting Prisma errors | Supabase Docs — 트랜잭션 모드는 prepared statement 미지원, `pgbouncer=true` 추가](https://supabase.com/docs/guides/database/prisma/prisma-troubleshooting)
- [Prisma | Supavisor 공식 문서 — `named_prepared_statements` 기능 플래그](https://supabase.github.io/supavisor/orms/prisma/)
- [Configure Prisma Client with PgBouncer | Prisma Documentation](https://www.prisma.io/docs/orm/prisma-client/setup-and-configuration/databases-connections/pgbouncer)
- [Disabling Prepared Statements | Supabase Troubleshooting](https://supabase.com/docs/guides/troubleshooting/disabling-prepared-statements-qL8lEL)
- [Connect to your database | Supabase Docs — 포트별 풀 모드](https://supabase.com/docs/guides/database/connecting-to-postgres)

---

### 8.68 RLS × `cbmall_app` BYPASSRLS 상호작용 점검 (확정)

> 8.6절은 RLS를 **"방어적 이중화"** 로 도입했고 8.15절은 런타임 롤 `cbmall_app`에 **BYPASSRLS**를 부여했습니다. 이 둘을 같이 놓고 보면 **"방어적 이중화"라는 설명이 실제 효과와 어긋납니다** — 애플리케이션이 RLS를 우회하므로 앱 경로에는 RLS가 **아무 방어도 하지 않습니다**. 실측해보니 구현은 의도대로 되어 있었고(전 26개 테이블 RLS 활성, 정책 0개, 함수는 모두 `SECURITY INVOKER`), **문제는 그 구성이 무엇을 막고 있는지가 기록돼 있지 않다는 점**이었습니다. **실제 구축이 아닌 결정 단계**입니다.

#### 현재 구현 실측 결과

| 확인 항목 | 결과 |
|---|---|
| RLS 활성화 커버리지 | **26개 테이블 전부 적용** — 누락 없음 |
| RLS 정책(`CREATE POLICY`) 수 | **0개** → 비우회 롤에게는 **전면 거부**(deny-all) |
| PL/pgSQL 함수의 보안 모드 | 전부 **`SECURITY INVOKER`**(기본값) — 권한 상승 경로 없음 |
| 앱 경로에 대한 RLS의 실제 방어 효과 | **없음**(`cbmall_app`이 BYPASSRLS로 우회) |

#### 즉시 결정 필요

| # | 문제 | 왜 문제인가 | 제안 |
|---|---|---|---|
| **Q-1** | **"방어적 이중화"라는 설명이 실제와 다르고, 그 오해가 구멍을 만든다** | 앱은 RLS를 우회하므로 이중화가 아니다. **RLS가 실제로 막고 있는 것은 Supabase가 자동으로 노출하는 PostgREST Data API**다 — `https://<ref>.supabase.co/rest/v1/`가 열려 있고 **`anon` 키는 설계상 공개되는 값**이다. 우리는 Supabase Auth를 쓰지 않고 자체 인증(8.7절)을 쓰므로, **RLS 활성 + 정책 0개 = anon 전면 거부**가 정확히 맞는 구성이다. 위험은 여기서 나온다 — 나중에 누군가 "RLS를 켜놨는데 정책이 하나도 없네?"라며 **선의로 정책을 추가하면 그 순간 공개 키로 읽을 수 있는 테이블이 생긴다** | 8.6절의 설명을 **"앱 경로의 이중 방어"가 아니라 "PostgREST/anon 경로 차단"** 으로 바로잡고, **"정책을 추가하지 않는다 — 정책 0개가 의도된 상태"** 를 명시적 규칙으로 못박는다 |
| **Q-2** | **RLS에만 기대면 `service_role` 키 유출에 무방비다** | Supabase의 `service_role` 롤은 **BYPASSRLS를 가지고** PostgREST를 통해 모든 테이블을 읽고 쓴다. 이 키가 유출되면 **RLS는 아무 소용이 없다**. 그런데 우리는 **PostgREST를 전혀 사용하지 않는다** — 모든 DB 접근은 NestJS → Prisma → `cbmall_app` 경로뿐이다. 쓰지도 않는 문을 열어두고 자물쇠(RLS)만 채워둔 셈 | **Data API 노출 스키마에서 `public`을 제거**해 **표면 자체를 없앤다**(Supabase 프로젝트 설정 → API → Exposed schemas). RLS는 그대로 두되, 이제 **1차 방어선은 "API가 우리 테이블을 아예 보지 못한다"** 가 되고 RLS는 그 뒤의 안전망이 된다. 설정 변경 하나로 `anon`·`service_role` 양쪽 위험이 동시에 사라진다 |
| **Q-3** | **`CREATE ROLE ... BYPASSRLS`가 Supabase에서 실행될지 확인된 바 없다** | PostgreSQL 규칙상 **BYPASSRLS는 superuser이거나 자신이 BYPASSRLS를 가진 롤만 부여할 수 있고, `CREATEROLE`만으로는 부여할 수 없다**. Supabase의 `postgres` 롤은 **superuser가 아니다**(superuser는 `supabase_admin` 하나뿐). Supabase가 이를 허용하도록 패치했다는 보고가 있으나 **버전·플랜에 따라 다를 수 있어 실측 전에는 단정할 수 없다**. `roles.sql`이 M0 ②에서 첫 줄부터 실패하면 그날 환경 구축이 멈춘다 | **M0 ②의 체크리스트 항목으로 승격**해 프로젝트 생성 직후 실제로 실행해본다. 실패할 경우의 **대안을 미리 정해둔다** — 아래 "대안" 표 참고 |

#### 대안 — BYPASSRLS가 부여되지 않을 경우

| 방안 | 내용 | 평가 |
|---|---|---|
| **A. `cbmall_app` 전용 허용 정책** | BYPASSRLS 대신 각 테이블에 `CREATE POLICY app_all ON <table> FOR ALL TO cbmall_app USING (true) WITH CHECK (true);`(26개 테이블을 `DO` 블록으로 일괄 생성) | **BYPASSRLS 없이 동일한 효과**를 내면서 `anon`·`authenticated`에는 여전히 정책이 없어 전면 거부가 유지된다. 오히려 **"누가 통과하는지"가 정책에 명시되어** 의도가 코드에 드러난다. 대가: 쿼리마다 정책 평가 오버헤드(상수 `true`라 무시 가능)와 신규 테이블마다 정책 추가 필요(Q-5의 CI 검사로 강제) |
| B. `postgres` 롤로 런타임 접속 | 런타임도 `postgres`를 쓰면 소유자라 RLS를 자동 우회 | **8.15절의 최소권한 설계가 통째로 무너진다** — 런타임이 DDL 권한까지 갖게 되어 SQL 인젝션 한 건의 피해 범위가 극대화된다. 채택하지 않음 |

#### 보완 필요

| # | 문제 | 제안 |
|---|---|---|
| Q-4 | **Supabase 기본 권한으로 `anon`·`authenticated`에 GRANT가 남아 있을 수 있다** — Supabase는 `public` 스키마에 대해 이 롤들에 기본 권한을 설정해 둔다. Q-2로 API 표면을 없애면 경로는 막히지만 권한 자체는 남는다 | `roles.sql`에 **`REVOKE ALL ON ALL TABLES IN SCHEMA public FROM anon, authenticated;`** 와 해당 default privileges 회수를 추가한다. Q-2(경로 차단) + Q-4(권한 회수) + RLS(정책 0개) 세 겹이면 어느 하나가 실수로 풀려도 나머지가 버틴다 |
| Q-5 | **신규 테이블에서 RLS 활성화를 빠뜨리면 조용히 노출된다** — 지금은 26/26 완전하지만, 이건 매번 사람이 기억해서 맞춘 결과다. 마이그레이션 한 건만 빠뜨려도 그 테이블은 보호에서 벗어난다 | 8.66절 O-3의 CI 스크립트에 **"마이그레이션에 `CREATE TABLE`이 있으면 같은 파일에 해당 테이블의 `ENABLE ROW LEVEL SECURITY`가 있는지"** 검사를 추가한다. 방안 A를 택한 경우 **정책 생성 여부**도 함께 확인한다 |
| Q-6 | **`SECURITY DEFINER` 함수가 추가되면 최소권한 설계가 무너진다** — 현재 8.6절 함수들은 모두 `SECURITY INVOKER`(기본값)로 안전하지만, 앞으로 누군가 `SECURITY DEFINER`를 붙이면 그 함수는 **소유자(`postgres`) 권한으로 실행**되어 `cbmall_app`의 최소권한을 우회한다 | 8.6절에 **`SECURITY DEFINER` 사용 금지**를 명시하고(불가피한 경우 `search_path`를 함수 내에서 고정), Q-5의 CI 검사에 `SECURITY DEFINER` 문자열 검출을 함께 넣는다 |

Sources:
- [PostgreSQL: Role Attributes — BYPASSRLS는 superuser 또는 BYPASSRLS 보유자만 부여 가능](https://www.postgresql.org/docs/current/role-attributes.html)
- [PostgreSQL: CREATE ROLE — `CREATEROLE`은 BYPASSRLS를 부여할 수 없음](https://www.postgresql.org/docs/current/sql-createrole.html)
- [Postgres Roles | Supabase Docs — `service_role`이 RLS를 우회](https://supabase.com/docs/guides/database/postgres/roles)
- [Roles, superuser access and unsupported operations | Supabase Docs](https://supabase.com/docs/guides/database/postgres/roles-superuser)
- [Row Level Security | Supabase Docs](https://supabase.com/docs/guides/database/postgres/row-level-security)
- (내부 실측 — `apps/api/prisma/migrations/` 전수 검사: RLS 26/26, 정책 0개, 함수 전부 `SECURITY INVOKER`)

---

### 8.69 Vercel 함수 타임아웃 × SSR 토큰 릴레이 (확정)

> 8.66절에서 BFF Route Handler를 없애고 SSR 릴레이를 서버 전용 헬퍼 함수로 확정했는데, **그 함수가 Vercel 함수 안에서 네트워크를 건너 AWS ECS를 호출한다는 사실**이 성능·장애 관점에서 검토된 적이 없습니다. 검토해보니 **보호된 페이지 한 번 여는 데 태평양을 두 번 왕복하게 되어 있습니다.** **실제 구축이 아닌 결정 단계**입니다.

#### 현재 SSR 릴레이 경로

| 단계 | 내용 | 비용 |
|---|---|---|
| 1 | 브라우저 → Vercel 함수(`app.` 도메인) | — |
| 2 | Server Component가 `cookies()`로 Refresh 쿠키 읽기 | 로컬 |
| 3 | **Vercel → NestJS 비회전 SSR 엔드포인트**(8.53절) → Access Token 수령 | **네트워크 왕복 1회 + DB 조회** |
| 4 | **Vercel → NestJS 데이터 API**(Access Token 사용) | **네트워크 왕복 2회째 + DB 조회** |
| 5 | 렌더 → 응답 | — |

#### 즉시 수정 필요

| # | 문제 | 왜 문제인가 | 제안 수정 |
|---|---|---|---|
| **R-1** | **Vercel 함수 리전이 지정돼 있지 않아 기본값인 워싱턴 DC(`iad1`)로 간다** | PRD는 S3를 **`ap-northeast-2`(서울)** 로 못박았고(8.10절) ECS도 같은 리전을 전제하는데, **Vercel 함수 리전은 문서 어디에도 없다**. Vercel의 기본 함수 리전은 **`iad1`(워싱턴 DC)** 이므로, 위 3·4단계가 **매번 태평양을 건넌다** — 왕복 1회에 약 180ms, **2회 순차라 렌더를 시작하기도 전에 360ms 이상**이 깔린다. 여기에 각 단계의 DB 조회가 더해진다. 6절의 **"주요 페이지 로드 2초 이내"** 목표를 로그인 사용자 페이지에서 먼저 잠식한다. 같은 실수로 대시보드가 5초씩 걸린 사례가 공개적으로 보고돼 있다 | Vercel 프로젝트의 **함수 리전을 `icn1`(서울)로 고정**한다(`vercel.json`의 `regions` 또는 프로젝트 설정). `icn1`은 AWS `ap-northeast-2`에 대응하므로 ECS·S3와 **같은 지역**이 되어 3·4단계가 사실상 리전 내 통신이 된다. **함수 리전 지정은 Pro 플랜 이상 기능**이므로 8.34절 환경 구성에 플랜 전제로 함께 적는다 |
| **R-2** | **SSR 릴레이 `fetch`에 타임아웃이 없어 ECS 지연이 그대로 504가 된다** | Node의 `fetch`는 **기본 타임아웃이 없다**. ECS가 느리거나 멈추면 Vercel 함수는 **자신의 `maxDuration`까지 그대로 대기**하다가 `FUNCTION_INVOCATION_TIMEOUT`(504)을 뱉는다. 그동안 함수는 **과금되고 동시성 슬롯을 점유**한다. 사용자에게는 마이페이지가 흰 화면으로 수십 초 멈춘 뒤 오류로 끝나는 경험이 된다 | 릴레이 `fetch`에 **`AbortSignal.timeout()`을 `maxDuration`보다 짧게**(예: 3초) 건다. 타임아웃·실패 시 **504로 터뜨리지 않고 우아하게 저하**한다 — 로그인 페이지로 리다이렉트하거나 비로그인 상태의 셸을 렌더한다. **재시도는 하지 않는다**(SSR 경로에서 재시도는 지연을 배로 늘리고 DB 부하만 키운다) |
| **R-3** | **`maxDuration`이 정해져 있지 않고, 기본값은 플랜·컴퓨트 설정에 따라 바뀌어 왔다** | Vercel의 기본 함수 실행 시간은 플랜과 Fluid Compute 사용 여부에 따라 **15초에서 300초까지** 달라진다. **300초가 걸리면 우리에게는 오히려 해롭다** — 장애 상황에서 SSR 페이지가 5분간 매달려 있으면 함수 동시성이 고갈되어 **정상 페이지까지 함께 느려진다**. 기본값에 맡길 값이 아니다 | 보호된 페이지 라우트에 **`export const maxDuration = 10`** 을 명시한다. R-2의 `fetch` 타임아웃(3초) × 2단계 + 렌더 시간을 감안한 값으로, 실패가 **빠르게** 끝나도록 한다 |

#### 보완 필요

| # | 문제 | 제안 |
|---|---|---|
| R-4 | **릴레이를 `await`하면 그 지연이 TTFB에 전부 실린다** — 지금 구조는 Access Token을 받고 데이터를 받을 때까지 **아무것도 브라우저로 나가지 않는다**. R-1을 고쳐도 최선의 경우가 "빨라진 대기"일 뿐이다 | 개인화 영역을 **`<Suspense>`로 감싸** 정적 셸(헤더·레이아웃·스켈레톤)을 **즉시 flush**하고 개인화 데이터는 스트리밍으로 채운다. 이러면 릴레이가 느려도 **첫 페인트가 막히지 않고**, R-2의 우아한 저하도 페이지 전체가 아니라 그 영역에만 적용된다. App Router의 기본 기능이라 추가 라이브러리가 필요 없다 |
| R-5 | **한 번의 렌더에서 릴레이가 여러 번 호출될 수 있다** — 여러 Server Component가 각자 `getSsrAccessToken()`을 부르면 **같은 렌더 안에서 3단계가 중복 실행**되어 왕복과 DB 조회가 배로 늘어난다 | `getSsrAccessToken`을 **React `cache()`로 감싸** 요청 스코프에서 **렌더당 1회**만 실행되게 한다(8.66절 O-1에서 헬퍼 함수로 정한 덕분에 이 최적화가 자연스럽게 가능하다 — Route Handler였다면 HTTP 호출이라 캐시가 어렵다) |
| R-6 | **릴레이 엔드포인트가 속도 제한 대상에 들어 있지 않다** — 8.53절은 공유 시크릿으로 호출자를 Next.js 백엔드로 제한했지만, 이 구간은 **공개 인터넷**이고 8.28절 Rate Limiting 표에도 이 엔드포인트가 없다 | 8.28절에 **SSR 릴레이 엔드포인트 항목을 추가**한다. 정상 호출자는 Vercel 함수뿐이므로 **IP가 아니라 공유 시크릿 단위로** 제한을 걸고(정상 트래픽은 페이지뷰에 비례), 시크릿 불일치 요청은 **즉시 401 + 별도 카운터**로 집계해 8.24절 Sentry 알림에 연결한다 |

Sources:
- [Configuring Maximum Duration for Vercel Functions](https://vercel.com/docs/functions/configuring-functions/duration)
- [Vercel Functions Limits](https://vercel.com/docs/functions/limitations)
- [Configuring regions for Vercel Functions](https://vercel.com/docs/functions/configuring-functions/region)
- [Global network and regions — `icn1` = Seoul](https://vercel.com/docs/regions)
- [Why My Vercel Deployment Was Slow: The Pitfall of Function Regions](https://zenn.dev/mountain1009/articles/00c8a4542e6194?locale=en)
- [Next.js: Loading UI and Streaming (`<Suspense>`)](https://nextjs.org/docs/app/api-reference/file-conventions/loading)
- [React: `cache()` — 요청 스코프 메모이제이션](https://react.dev/reference/react/cache)

---

### 8.70 마이그레이션 커넥션의 실제 도달 가능성 (확정)

> "`prisma migrate deploy`는 풀러가 아닌 다이렉트 커넥션(5432)을 쓴다"는 **8.15절에 이미 확정**되어 있고 `schema.prisma`의 `directUrl`로 구현돼 있습니다. 이번 검토는 그 결정이 **우리 실행 환경에서 실제로 연결되는지**를 봤고, 결론은 **연결되지 않는다**입니다 — 결정 자체는 옳지만 **"다이렉트 커넥션"이 가리키는 호스트를 바꿔야** 합니다. **실제 구축이 아닌 결정 단계**입니다.

#### 즉시 수정 필요

| # | 문제 | 왜 문제인가 | 제안 수정 |
|---|---|---|---|
| **S-1** | **Supabase의 다이렉트 커넥션은 IPv6 전용인데, GitHub Actions 러너는 IPv4 전용이다** | Supabase는 AWS의 IPv4 과금 정책 이후 **다이렉트 커넥션(`db.<ref>.supabase.co:5432`)을 IPv6 전용**으로 전환했고, IPv4로 쓰려면 **유료 애드온**이 필요하다. 그런데 **GitHub 호스티드 러너는 IPv6 아웃바운드가 아예 없다**(오래된 미해결 이슈). 8.35절이 "승인 → 마이그레이션 → 배포" 순서로 **CI에서 `prisma migrate deploy`를 실행**하도록 정했으므로, **첫 배포에서 `ENETUNREACH`/타임아웃으로 멈춘다**. 증상이 "연결 안 됨"이라 **"Supabase가 죽었나?"로 오진하기 쉽다** | **`DIRECT_DATABASE_URL`을 Supavisor 세션 모드 풀러**(`aws-0-<region>.pooler.supabase.com:5432`)로 지정한다 — **전 플랜에서 IPv4를 지원**한다. 중요한 건 **세션 모드는 연결이 세션 내내 전용 서버 연결에 고정**된다는 점이다. 8.57절 F-5가 요구한 **Prisma Migrate의 세션 단위 advisory lock이 정상 동작**하므로, 트랜잭션 모드(6543)와 달리 마이그레이션에 안전하다. 즉 **"풀러를 피하라"가 아니라 "트랜잭션 모드를 피하라"** 가 정확한 규칙이었다 |
| **S-2** | **`DIRECT_DATABASE_URL`이 환경변수 검증(8.42절)에 없어, 누락되면 조용히 잘못된 연결로 마이그레이션한다** | `directUrl`이 비면 Prisma는 **`DATABASE_URL`(트랜잭션 모드 풀러)로 마이그레이션을 시도**한다. 그러면 세션 advisory lock이 성립하지 않아 **10초 타임아웃으로 실패**하거나 더 나쁜 경우 부분 적용된다(8.57절 F-5). 두 URL을 **서로 바꿔 넣는 실수**도 형식상 구분이 없어 막을 방법이 없다 | 8.42절 Zod 스키마에 **`DIRECT_DATABASE_URL`을 필수로 추가**하고, **형식으로 혼동을 차단**한다 — `DATABASE_URL`은 **`pgbouncer=true`를 반드시 포함**해야 하고(8.67절 P-1), `DIRECT_DATABASE_URL`은 **`:6543`이나 `pgbouncer=true`를 포함하면 거부**한다. 두 값을 바꿔 넣으면 **부팅·CI 단계에서 즉시 실패**한다 |

#### 보완 필요

| # | 문제 | 제안 |
|---|---|---|
| S-3 | **ECS 런타임 태스크에 마이그레이션용 커넥션 정보를 줄 이유가 없다** — 8.35절에서 마이그레이션은 **CI에서만** 실행하기로 했는데, 시크릿을 한 묶음으로 주입하면 런타임 태스크가 불필요하게 다이렉트 커넥션 자격증명을 들고 있게 된다 | 8.34절 시크릿 네임스페이스에서 **`DIRECT_DATABASE_URL`은 CI(GitHub Actions)에만 주입하고 ECS 태스크 정의에는 넣지 않는다**. 8.42절 환경변수 검증도 이에 맞춰 **CI 컨텍스트에서만 필수**로 분류한다(8.42절이 이미 "환경별 필수 항목 구분"을 두고 있으므로 그 틀을 그대로 쓴다) |
| S-4 | **로컬 개발에서도 같은 제약이 적용되는데 안내가 없다** — 개발자가 `prisma migrate dev`를 돌릴 때 회사망·VPN이 IPv6를 막고 있으면 같은 증상이 난다 | `.env.example`에 두 URL을 **분리해 주석과 함께** 제공하고, `DIRECT_DATABASE_URL`의 기본 예시를 **세션 모드 풀러 주소**로 둔다(IPv6 가용성과 무관하게 동작). 8.36절 온보딩 문서에도 같은 내용을 적는다 |
| S-5 | **실패했을 때 원인이 드러나지 않는다** — `ENETUNREACH`나 연결 타임아웃은 네트워크·자격증명·방화벽 어느 쪽이든 날 수 있어, 처음 겪으면 원인 특정에 시간이 걸린다 | 8.35절 CI 마이그레이션 스텝에 **실패 시 안내 문구**를 출력한다(예: "다이렉트 커넥션은 IPv6 전용입니다. `DIRECT_DATABASE_URL`이 세션 모드 풀러(`...pooler.supabase.com:5432`)를 가리키는지 확인하세요 — PRD 8.70절"). 한 줄이지만 **같은 함정에 두 번 빠지지 않게 하는 비용 대비 효과가 크다** |

#### M0에서 실측할 것

세션 모드 풀러가 **Prisma Migrate에서 실제로 문제없이 동작하는지**는 M0 ②에서 확인한다. "세션 풀러도 결국 풀링이므로 Migrate/Introspect에서 주의가 필요하다"는 의견이 있으나, 세션 모드의 정의상 세션 advisory lock은 성립한다. 만약 실측에서 문제가 드러나면 **차선책은 Supabase IPv4 애드온**(유료)이며, 이 경우에만 진짜 다이렉트 커넥션(`db.<ref>.supabase.co:5432`)을 쓴다.

Sources:
- [Connect to your database | Supabase Docs — 다이렉트 연결/세션 모드/트랜잭션 모드 구분](https://supabase.com/docs/guides/database/connecting-to-postgres)
- [Supabase & Your Network: IPv4 and IPv6 compatibility | Supabase Troubleshooting](https://supabase.com/docs/guides/troubleshooting/supabase--your-network-ipv4-and-ipv6-compatibility-cHe3BP)
- [Prisma | Supabase Docs](https://supabase.com/docs/guides/database/prisma)
- [IPv6 on GitHub-hosted runners — actions/runner #402](https://github.com/actions/runner/issues/402)
- [Migrations with Supabase Supavisor in transaction mode don't work — prisma/prisma #22779](https://github.com/prisma/prisma/issues/22779)

---

### 8.71 서명 없는 결제 웹훅의 운영 규칙 (확정)

> "`PAYMENT_STATUS_CHANGED`는 서명이 없으므로 페이로드를 신뢰하지 않고 `paymentKey`로 재조회 검증한다"는 **8.8절에 이미 확정**되어 있습니다. 전제도 **여전히 유효**합니다 — 토스페이먼츠가 `tosspayments-webhook-signature` 헤더를 제공하긴 하지만 **지급대행(`payout.changed`·`seller.changed`) 웹훅에만** 붙고, 결제 웹훅에는 없습니다. 이번 검토는 그 결정을 **실제로 구현할 때 필요한 규칙**을 봤고, **돈이 걸린 공백 두 개**를 찾았습니다. **실제 구축이 아닌 결정 단계**입니다.

#### 토스 웹훅의 확인된 동작

| 항목 | 값 |
|---|---|
| 서명 헤더 | `payout.changed` · `seller.changed` **전용** — 결제 웹훅에는 없음 |
| 성공 판정 | **HTTP 200** 응답 시 "성공"으로 기록되고 **재시도 중단** |
| 재시도 | **3일 19시간 동안 최대 7회**, 지수 백오프 |
| 순서 보장 | **없음** |
| IP 허용목록 | 공식 문서에 없음 |

#### 즉시 수정 필요

| # | 문제 | 왜 문제인가 | 제안 수정 |
|---|---|---|---|
| **T-1** | **웹훅 응답 코드 정책이 없어, 일시 장애 한 번에 결제가 영구 유실될 수 있다** | 토스는 **200을 받으면 재시도를 멈춘다**. 그런데 우리 핸들러가 실패하는 경우는 성격이 전혀 다른 셋이다 — ① **중복 수신**(이미 처리됨), ② **재조회 API·DB 일시 장애**, ③ **재조회 결과 불일치**(위조 의심). 이걸 구분하지 않고 일괄 200을 주면 **②에서 토스가 재시도를 멈춰** 그 결제는 영영 처리되지 않는다. 8.58절 G-3이 "웹훅이 결제 이후 워크플로 전체를 멱등하게 완결한다"고 **웹훅에 복구 책임을 몰아뒀기 때문에** 이 실수의 대가가 특히 크다 | **응답 코드를 세 갈래로 분리한다**: ① 중복·이미 처리됨 → **200**(재시도가 무의미), ② **재조회 실패·DB 실패 → 5xx**(토스의 3일 19시간 재시도가 곧 우리의 자동 복구 장치가 된다 — **별도 폴링 안전망을 만들 필요가 없어진다**), ③ 재조회 결과 불일치 → **200 + Sentry 경보**(재시도해도 결과가 같으므로 붙잡아둘 이유가 없고, 위조 시도는 알림으로 다룬다) |
| **T-2** | **주문 자동취소(30분)와 웹훅 재시도 윈도우(3일 19시간)가 어긋나 "결제는 됐는데 주문은 취소된" 상태가 생긴다** | 8.8절은 `PENDING` 주문을 **30분 뒤 자동 취소**하고, 8.12절 Cron이 이를 실행한다. 그런데 토스 웹훅은 **최대 3일 19시간까지 도착할 수 있다**(우리 서버 장애로 재시도가 길어지는 경우 포함). 그 사이 주문은 이미 `CANCELLED`가 된다. 웹훅이 뒤늦게 도착해 재조회하면 **결제는 `DONE`인데 주문은 `CANCELLED`** — 고객은 돈을 냈고 상품은 받지 못한 상태다. 현재 PRD에는 **이 조합을 만났을 때의 처리가 없다** | 웹훅 핸들러에 **"결제 `DONE` × 주문 `CANCELLED`" 분기를 명시**한다. 이 상태는 **자동으로 판단하지 않고** ① 주문을 별도 상태(`NEEDS_REVIEW`)로 표시, ② **관리자에게 즉시 알림**(8.17절 통합 알림, 긴급 프로파일), ③ 고객에게는 "확인 중" 안내. 자동 환불·자동 복구 중 무엇이 맞는지는 아래 결정 사항으로 올린다 |
| **T-3** | **서명이 없다는 것은 엔드포인트가 완전히 공개라는 뜻인데, 속도 제한 대상에 없다** | 누구나 이 엔드포인트에 가짜 `paymentKey`로 POST할 수 있고, 우리는 **성실하게 토스 API를 재조회**한다. 즉 **재조회 호출을 유발하는 DoS 표면**이며, 토스 API 쿼터와 우리 ECS 자원을 함께 소모한다. 8.28절 Rate Limiting 표에 이 엔드포인트가 없다 | **재조회 전에 싸게 거르는 순서**를 명시한다: ① 페이로드의 `orderId`가 **우리 DB에 없으면 재조회 없이 200**(가장 싼 방어), ② 8.9절 `payment_webhook_events`에 **이미 본 이벤트면 재조회 없이 200**, ③ 그 외에만 재조회. 여기에 8.28절에 **웹훅 엔드포인트 속도 제한**(IP 기준)을 추가한다 |

#### 보완 필요

| # | 문제 | 제안 |
|---|---|---|
| T-4 | **재조회로 "무엇을" 대조하는지가 명시돼 있지 않다** — 8.8절은 "조회 API로 검증"까지만 적혀 있다. 대조 항목이 없으면 **금액 위변조 방어라는 핵심이 빠진 채** 구현될 수 있다 | 재조회 응답에서 **네 가지를 모두 대조**한다: `orderId`가 우리 주문과 일치 / `totalAmount`가 **우리가 계산한 주문 금액과 일치**(클라이언트가 보낸 값이 아니라) / `status`가 기대값(`DONE`) / `paymentKey`가 일치. 하나라도 어긋나면 T-1의 ③ 경로(200 + Sentry)로 처리한다 |
| T-5 | **토스의 서명 지원이 확대될 경우를 대비한 기준이 없다** — 이미 지급대행 웹훅에는 서명 메커니즘이 존재하므로 결제 웹훅으로 확대될 여지가 있다 | 8.67절 P-1과 같은 방식으로 **해제 조건을 적어둔다** — 결제 웹훅에 `tosspayments-webhook-signature`가 제공되면 **서명 검증을 추가**한다. 단 **재조회 검증은 그대로 유지**한다(서명은 "토스가 보냈다"만 증명할 뿐, T-6의 순서 문제와 금액 대조는 재조회만이 해결한다) |
| T-6 | **재조회 검증이 순서 미보장 문제까지 해결한다는 점이 기록돼 있지 않다** — 토스 웹훅은 순서를 보장하지 않아, 일반적인 구현에서는 **오래된 상태가 새 상태를 덮어쓰는** 사고가 생긴다 | 우리는 페이로드가 아니라 **재조회 결과(항상 최신 상태)** 를 쓰므로 이 문제가 **구조적으로 발생하지 않는다**. 이 사실을 8.8절에 적어둔다 — 나중에 "재조회는 비용이 드니 페이로드를 믿자"는 제안이 나왔을 때, **성능이 아니라 정합성 때문에 재조회를 한다**는 반박 근거가 된다 |

Sources:
- [웹훅(Webhook) 연결하기 | 토스페이먼츠 개발자센터 — 200 응답·재시도 정책](https://docs.tosspayments.com/guides/v2/webhook)
- [웹훅 이벤트 | 토스페이먼츠 개발자센터 — 서명 헤더는 `payout.changed`·`seller.changed` 전용](https://docs.tosspayments.com/reference/using-api/webhook-events)
- [결제 조회 API | 토스페이먼츠 개발자센터](https://docs.tosspayments.com/reference#결제-조회)

---

### 8.72 긴급 알림 재시도 예산의 실제 소요시간 검증 (확정)

> "30분 만료 토큰을 쓰는 긴급 알림은 재시도를 14분 안에 끝낸다"는 **8.18절에 이미 확정**되어 있습니다(긴급 `1분→3분→10분`, 최대 4회 / 일반 `5분→30분→2시간→6시간`, 최대 5회). 이번 검토는 그 **14분이 실제로 지켜지는지**를 다른 절의 수치와 맞춰 계산했고, 결론은 **지켜지지 않는다**입니다 — 백오프 간격만 더했을 뿐 **스케줄러의 실행 주기가 예산에 반영돼 있지 않습니다.** **실제 구축이 아닌 결정 단계**입니다.

#### 실제 소요시간 계산

8.12절 Cron은 **1분마다** 실행되므로 각 재시도는 예정 시각보다 **최대 1분 늦게** 집힙니다. 현재 설계대로 `next_retry_at = now() + 지연`으로 계산하면 이 지연이 **회차마다 누적**됩니다.

| 회차 | 문서상 예정 | 실제(누적 반영) |
|---|---|---|
| 1차 | t+0 | t+0 |
| 2차 | t+1 | 최대 **t+2** |
| 3차 | t+4 | 최대 **t+6** |
| 4차(마지막) | **t+14** | 최대 **t+17** |

여기에 8.58절 G-4의 **재클레임 대기 1분**(워커 사망 시)과 ESP 수신 지연이 더해집니다. 30분 만료 토큰 기준으로 **사용자에게 남는 시간이 16분에서 13분 이하로** 줄어듭니다.

#### 즉시 수정 필요

| # | 문제 | 왜 문제인가 | 제안 수정 |
|---|---|---|---|
| **U-1** | **백오프를 `now()` 기준으로 잡아 스케줄러 지연이 회차마다 누적된다** | 8.18절은 간격(`1·3·10분`)만 정했고 **스케줄러 주기(8.12절 1분)를 예산에 넣지 않았다**. `next_retry_at = now() + 지연`으로 계산하면 매 회차 최대 1분씩 밀려 **총 14분이 최대 17분**이 된다. 문서의 약속과 실제 동작이 다른 상태이며, 이 오차는 회차를 늘릴수록 커진다 | **백오프를 절대 시각 기준으로 계산한다** — `next_retry_at = 최초 생성 시각(created_at) + 누적 지연`. 이러면 스케줄러 지연이 **누적되지 않고** 각 회차가 최대 1분만 늦어져, 마지막 시도가 **최대 t+15**로 수렴한다. 8.18절의 "총 14분 이내"도 **"최대 15분"** 으로 정정해 문서와 동작을 일치시킨다 |
| **U-2** | **이미 만료된 토큰이 담긴 메일이 발송될 수 있다** | outbox 행이 장애로 오래 묶여 있다가 복구 후 처리되면, **30분이 지난 뒤에 재설정 링크가 발송**된다. 사용자는 메일을 받고 링크를 눌렀는데 "만료되었습니다"를 보게 된다 — 아무 메일도 안 오는 것보다 나쁜 경험이다. 8.58절 G-5에서 **payload에 식별자만 저장하고 링크는 발송 시점에 생성**하기로 했으므로, **발송 직전에 토큰을 조회하는 구조가 이미 갖춰져 있다** — 규칙만 없다 | 발송 직전 **토큰 유효성을 확인**하고, 이미 만료되었으면 **발송하지 않고 `FAILED` 처리**한다(`last_error`에 "token_expired" 기록). 사용자는 8.18절 결정대로 화면에서 "다시 받기"를 누르면 된다. 무의미한 ESP 호출과 잘못된 메일을 동시에 막는다 |
| **U-3** | **"다시 받기"와 진행 중인 재시도가 충돌해 만료된 링크를 보낸다** | 8.18절은 최종 실패 시 사용자가 "다시 받기"를 누르면 된다고 정했다. 그런데 **재시도가 아직 진행 중일 때** 누르면 ① outbox 행이 두 개가 되고 ② 8.7절 정책상 재설정 토큰은 1회용이라 **이전 토큰이 무효화**된다. 그러면 진행 중이던 재시도가 **이미 죽은 토큰이 담긴 메일을 발송**한다(U-2와 같은 결과를 사용자가 직접 유발하는 경로) | 새 토큰을 발급할 때 **같은 사용자·같은 템플릿의 진행 중인 outbox 행을 먼저 취소**한다(`status = 'CANCELLED'`). U-2의 발송 직전 검사와 중복되는 방어지만, 이쪽은 **불필요한 발송 시도 자체를 없애** ESP 호출과 로그 노이즈를 줄인다 |

#### 보완 필요

| # | 문제 | 제안 |
|---|---|---|
| U-4 | **`maxAttempts` 기본값·주석이 프로파일 정의와 어긋난다** — `schema.prisma`의 `NotificationOutbox` 주석은 "이메일=5, SMS/푸시=1"로 **채널 기준**인데, 8.18절은 "긴급=4회, 일반=5회"로 **템플릿(프로파일) 기준**이다. 같은 이메일이라도 `password_reset`은 4회, `order_confirmation`은 5회다 | outbox 행 생성 시 **프로파일 상수 맵에서 `maxAttempts`를 가져와 채운다**(채널 기본값이 아니라). `schema.prisma` 주석도 **"채널별"이 아니라 "템플릿별 프로파일에서 주입"** 으로 정정한다 |
| U-5 | **외부 API 타임아웃이 정해져 있지 않아 예산 계산의 기준이 없다** — 1차 시도가 "실패"로 판정되는 시점은 ESP 호출이 타임아웃될 때인데, 그 값이 어디에도 없다. 30초든 60초든 4회면 **최대 4분이 추가**된다 | 알림 발송의 외부 API 호출 타임아웃을 **10초**로 고정한다(8.69절 R-2에서 SSR 릴레이에 3초를 건 것과 같은 취지 — 빠르게 실패해야 재시도 예산이 지켜진다). 이 값을 8.18절 예산 계산에 포함해 **최종 시도가 t+15분 + 여유**임을 명시한다 |
| U-6 | **긴급 프로파일이 어느 채널에 적용되는지 불명확하다** — 8.14/8.16절은 SMS·푸시를 `maxAttempts=1`(사실상 재시도 없음)로 정했는데, 8.18절 긴급 프로파일은 채널을 언급하지 않는다 | 긴급 프로파일은 **이메일 전용**임을 8.18절에 명시한다. `password_reset`·`email_verification`·`download_link_guest`는 모두 이메일로만 발송되므로 현재 설계와 모순이 없으며, 향후 SMS로 확장할 때 재시도 정책을 함께 검토해야 한다는 점을 적어둔다 |

Sources:
- (내부 교차 계산 — 8.7 토큰 만료 / 8.12 Cron 주기 / 8.13·8.14·8.16 채널별 정책 / 8.17 outbox / 8.18 재시도 프로파일 / 8.52 행 클레임 / 8.58 G-4·G-5 대조)
- [Retrying and Exponential Backoff: Smart Strategies for Robust Software](https://www.pullrequest.com/blog/retrying-and-exponential-backoff-smart-strategies-for-robust-software/)
- [Transactional Email Timing: SLA Guide for Developers](https://resources.mailertogo.com/guide/transactional-email-timing-best-practices)

---

### 8.73 다운로드 한도 × 속도 제한 × URL 수명 조합 검증 (확정)

> FR-4.2(구매일로부터 1년, **최대 10회**), 8.4절(presigned URL **30분**, **발급 시점 카운트**), 8.11절(**분당 3회** 발급 제한)이 각각 다른 시점에 정해졌습니다. 세 수치를 함께 놓고 보면 **"최대 10회"가 우리가 의도한 것을 세고 있지 않습니다.** **실제 구축이 아닌 결정 단계**입니다.

#### 즉시 결정 필요

| # | 문제 | 왜 문제인가 | 제안 |
|---|---|---|---|
| **V-1** | **presigned URL은 만료 전까지 재사용 횟수에 제한이 없다 — "최대 10회"는 다운로드 횟수가 아니다** | S3 presigned URL은 **유효기간 안에서는 몇 번이든 사용할 수 있다.** 8.4절이 카운트를 **발급 시점**에 올리기로 했으므로, FR-4.2의 "최대 10회"는 **URL 발급 10회**를 뜻한다. 실제로는 ① 사용자가 URL 하나로 **30분 동안 무제한 다운로드**할 수 있고, ② 더 중요하게 **그 URL을 공유하면 누구나 유료 전자책을 받을 수 있다**. 즉 8.10절의 "원본 PDF는 비공개" 보호가 **30분 창으로 축소**되고, 10회 한도는 사실상 **"공유 가능한 30분짜리 링크를 10개 만들 수 있다"** 가 된다 | **URL 유효기간을 30분 → 5분으로 단축**한다. 전자책 PDF(최대 100MB)는 정상 회선에서 **수 초~수십 초**면 받으므로 5분이면 충분하고, **공유 창이 6분의 1로 줄어든다**. 근본 차단(백엔드 프록시 스트리밍)은 100MB 파일을 ECS가 중계해야 해 대역폭·메모리 부담이 커 MVP에는 과하다. 함께 **FR-4.2 문구에 "10회는 URL 발급 횟수"임을 명시**해 오해를 없앤다 |
| **V-2** | **정상 사용자가 버튼을 몇 번 눌렀다고 한도가 깎인다** | 8.4절은 **발급 시점에 카운트를 올린다.** 사용자가 다운로드가 시작되지 않는 것 같아 버튼을 세 번 누르면 **한도 3회가 사라진다**. 8.11절의 분당 3회 제한이 이를 막아주지 않는다 — 오히려 **분당 3회씩 소진되도록 허용**한다. 10회 한도가 **3~4분 만에** 바닥날 수 있고, 그때마다 FR-7.10(관리자 한도 재부여) CS가 발생한다 | **직전 발급 URL이 아직 유효하면 새로 발급하지 않고 그 URL을 그대로 반환하며 카운트를 올리지 않는다.** 5분(V-1) 안의 반복 클릭은 **한 번으로 집계**되어, 정상 사용자의 오작동으로 인한 한도 소모가 구조적으로 사라진다. UI에도 "이미 발급된 링크가 N분간 유효합니다"를 표시한다 |
| **V-3** | **8.4절과 8.11절의 속도 제한 수치가 서로 다르다** | 8.4절 남용 방지 항목은 **"예: 1분당 1회"**, 8.11절은 **"분당 3회 초과 시 거부"** 로 적혀 있다. 같은 대상에 대한 두 값이 문서에 공존한다. 게다가 8.11절은 **무엇을 기준으로 세는지**(사용자 단위인지 엔타이틀먼트 단위인지)가 없다 — 사용자 단위라면 **5권을 산 고객이 마이페이지에서 전부 받으려 할 때 3권만 되고 2권은 거부**된다 | **분당 3회로 통일**하고 8.4절 문구를 정정한다. 기준은 **엔타이틀먼트(상품) 단위**로 명시한다 — 여러 상품을 동시에 받는 정상 사용이 막히지 않고, "한 상품을 반복 요청하는" 남용만 걸러진다 |

#### 보완 필요

| # | 문제 | 제안 |
|---|---|---|
| V-4 | **개정판을 받는 데 고객의 다운로드 한도가 소모된다** — FR-2.6은 오탈자 수정본을 기존 구매자에게 **무료로 새 파일 제공**하기로 했다. 그런데 그걸 받으려면 10회 중 1회를 쓴다. **고객 잘못이 아닌 사유로 한도가 깎이는** 구조라 부당하고 CS를 유발한다 | 오탈자 수정본(`ERRATA`) 발행 시 **해당 상품 구매자 전원의 다운로드 한도를 +1 부여**한다(FR-7.10의 재부여 로직을 그대로 재사용). 다운로드 시점에 "개정판인지" 판별해 카운트를 빼는 방식보다 단순하고, 이미 받은 사람에게도 공평하다 |
| V-5 | **만료된 엔타이틀먼트 보유자에게 개정판 알림이 간다** — 구매 후 1년이 지나면 다운로드가 불가능한데, FR-2.6 개정판 알림은 "기존 구매자"에게 발송된다. 받을 수 없는 파일의 알림을 받는 셈이다 | 개정판 알림 대상을 **유효한 엔타이틀먼트 보유자**(구매일+1년 이내)로 한정한다. V-4의 한도 +1 부여 대상도 동일하게 적용한다 |
| V-6 | **비회원은 한도를 회복할 경로가 없다** — FR-7.10(한도 재부여)은 관리자 기능이고, 회원은 마이페이지에서 문의할 수 있지만 **비회원은 계정이 없다**. 네트워크 실패로 한도를 날리면 연락할 방법이 막막하다 | 비회원에게 발송하는 **다운로드 링크 이메일 본문에 문의 경로를 명시**한다(고객센터 이메일 주소 + 주문번호 안내). V-2의 "유효 URL 재사용"이 적용되면 이 상황 자체가 크게 줄지만, 경로는 열어둬야 한다 |

Sources:
- [Amazon S3: Using presigned URLs — 만료 전까지 재사용 가능](https://docs.aws.amazon.com/AmazonS3/latest/userguide/using-presigned-url.html)
- [Sharing objects with presigned URLs | AWS 공식 문서](https://docs.aws.amazon.com/AmazonS3/latest/userguide/ShareObjectPreSignedURL.html)
- (내부 교차 계산 — FR-2.6 / FR-4.2~4.5 / FR-7.10 / 8.4 URL 수명·카운트 시점 / 8.10 보호 원칙 / 8.11 속도 제한 대조)

---

### 8.74 3차 상호 호환성 감사 (확정)

> 8.51절(1차, 9건)·8.55절(2차 전면 점검) 이후 **8.59~8.73절에서 15개 절이 추가**되었습니다. 이번 감사는 **새로 추가된 결정들이 기존 결정과, 그리고 서로와 충돌하는 지점**을 봤습니다. 특이한 점은 **가장 최근 세 절(8.71~8.73)에서 추가한 규칙끼리 부딪히는 경우가 두 건** 나왔다는 것입니다 — 각 절 안에서는 일관됐지만 함께 놓고 보지 않았습니다. **실제 구축이 아닌 결정 단계**입니다.

#### 즉시 수정 필요

| # | 충돌 | 왜 문제인가 | 제안 수정 |
|---|---|---|---|
| **W-1** | **8.73절 V-2(유효 URL 재사용) × 8.11절(분당 3회 제한)이 서로를 무력화한다** | V-2는 "반복 클릭으로 한도가 깎이는 문제"를 구하려고 **재사용 발급은 횟수를 소모하지 않게** 했다. 그런데 8.11절 속도 제한 쿼리는 `download_logs`를 **`counted` 구분 없이 전부** 센다. 결과적으로 사용자가 버튼을 4번 누르면 **한도는 안 깎이지만 4번째는 429로 거부**된다 — V-2가 고치려던 사용자 경험이 **절반만** 개선된 셈이다 | **두 개의 제한으로 분리한다**: ① **차감되는 발급**은 기존대로 **분당 3회**(남용 방지의 본래 목적), ② **전체 요청**(재사용 포함)은 **분당 10회**(자동화 스크립트만 걸리는 느슨한 상한). 반복 클릭하는 정상 사용자는 ①에 닿지 않고 ②에도 여유가 있다 |
| **W-2** | **8.71절 T-1(5xx로 재시도 유도) × T-3(웹훅 IP 속도 제한)이 같은 절 안에서 모순된다** | T-1은 재조회·DB 일시 장애 때 **의도적으로 5xx를 반환해 토스의 재시도를 자동 복구 장치로 쓰기로** 했다. 그런데 T-3은 같은 절에서 **웹훅 엔드포인트에 IP 기준 속도 제한**을 걸었다. 장애 상황에서는 **여러 주문의 웹훅이 동시에 실패해 재시도가 토스 IP에서 몰려 오는데**, 그게 우리 IP 제한에 걸리면 **우리가 설계한 복구 경로가 스스로 막힌다**. 장애가 길수록 밀린 재시도가 쌓여 더 확실하게 막힌다 | 속도 제한을 **요청 수가 아니라 "재조회를 수반하는 요청" 기준으로** 건다 — T-3이 정한 싼 필터(① 모르는 `orderId`, ② 이미 본 이벤트)를 **통과한 건에만** 제한을 적용한다. 정상 재시도는 대부분 ②에서 걸러지거나 실제 처리가 필요한 건이므로 제한에 닿지 않고, **가짜 `paymentKey` 폭주만 걸린다**. 토스가 공개하는 발신 IP가 있으면 제외 목록에 추가한다 |
| **W-3** | **8.72절 `CANCELLED` 상태가 실패 집계와 관리자 화면에 섞인다** | 8.72절 U-3에서 `notification_outbox`에 `CANCELLED`를 추가했는데, 이 값을 **기존 소비처들이 모른다**. ① 8.24절 알림 실패 모니터링이 `FAILED`와 함께 집계하면 **알림 실패율이 부풀려지고**, ② **FR-7.7 관리자 수동 재발송 목록**에 "사용자가 다시 받기를 눌러 취소된 건"이 떠서 관리자가 이미 무효인 토큰을 재발송하게 된다 | `CANCELLED`는 **실패가 아니라 정상적인 종료**임을 명시하고, **8.24절 실패율 집계와 FR-7.7 재발송 목록에서 제외**한다. 8.52절 행 클레임 쿼리는 `status='PENDING'`만 잡으므로 영향이 없다(확인 완료) |

#### 보완 필요

| # | 문제 | 제안 |
|---|---|---|
| W-4 | **DB 커넥션 총량이 한 번도 합산된 적이 없다** — 8.67절 P-3에서 태스크당 5개(2 vCPU 기준), 8.11절에서 ECS 태스크 2개 이상, 8.70절에서 CI 마이그레이션용 세션 모드 커넥션, 8.12절 Cron까지 각각 정해졌지만 **총합이 Supavisor 풀 한도 안에 들어가는지 확인된 적이 없다** | 8.15절에 **커넥션 총량 산정을 명시**한다(런타임 2×5=10 + CI 마이그레이션 1 + 여유). 실제 Supavisor 풀 한도는 플랜·설정에 따라 다르므로 **M0 ② 체크리스트에 "총 커넥션 수 대 풀 한도 대조" 항목을 추가**해 실측한다. 태스크를 늘리거나 vCPU를 올리면 이 값이 함께 커진다는 점도 적어둔다 |
| W-5 | **URL 수명을 5분으로 줄인 것이 대용량 다운로드를 깨뜨리지 않는지 근거가 없다** — 8.73절 V-1에서 30분→5분으로 단축했는데, 100MB 파일이 5분 안에 안 받아지면 어떻게 되는지가 기록돼 있지 않다 | **확인 결과 문제없다** — S3는 **요청 시작 시점에만 서명을 검증**하므로 전송 도중 URL이 만료돼도 **진행 중인 다운로드는 끊기지 않는다**. 단 **연결이 끊겨 재개(Range 재요청)하는 경우는 새 요청**이라 만료되었으면 실패한다. 즉 단축의 실제 대가는 "**끊긴 다운로드를 다시 시작할 여유가 30분→5분으로 줄어든 것**"인데, 5분 안이면 V-2의 재사용 창이 횟수 소모 없이 새 URL을 주므로 대체로 완충된다. 이 근거를 8.4절에 기록한다 |
| W-6 | **검색 결과 상한 세 값이 서로 맞물려 있는데 그 관계가 기록돼 있지 않다** — 8.62절 K-5의 총 건수 표기 상한 **200**, 8.63절 L-5의 페이지 상한 **10**, 페이지당 결과 **20건**은 `10 × 20 = 200`으로 **정확히 일치한다**. 우연처럼 보이면 나중에 한쪽만 바뀌어 "200+인데 10페이지까지밖에 못 감" 또는 "11페이지가 비어 있음" 같은 어긋남이 생긴다 | 세 값이 **하나의 규칙(`페이지 상한 × 페이지당 건수 = 총 건수 표기 상한`)에서 나온 것**임을 8.62절에 명시하고, **하나를 바꾸면 나머지도 함께 바꿔야 한다**고 적는다. 8절 의존 관계 표에도 반영한다 |

#### 확인했고 문제없는 조합

| 조합 | 결과 |
|---|---|
| 8.68 RLS 정책 × 8.59 청크 테이블 신설 순서 | 청크 테이블(`20260920140000`)이 정책 마이그레이션(`20260920150000`)보다 **앞서 생성**되어 `app_all` 정책에 포함됨 |
| 8.71 `NEEDS_REVIEW` × 8.12 주문 자동취소 Cron | Cron은 `PENDING`만 대상으로 하므로 검토 대기 주문을 다시 건드리지 않음 |
| 8.72 `CANCELLED` × 8.52 행 클레임 인덱스 | 클레임 쿼리가 `status='PENDING'` 조건이라 영향 없음 |
| 8.61 J-1 `$queryRaw` × 8.68 `cbmall_app` 정책 | raw 쿼리도 같은 롤로 실행되므로 `app_all` 정책을 그대로 통과 |
| 8.66 BFF 0개 × 8.69 `maxDuration` | 남는 Vercel 함수는 페이지 렌더와 `revalidate` 웹훅뿐이며 둘 다 보호된 라우트 설정과 무관 |

Sources:
- [Download and upload objects with presigned URLs | AWS — 서명은 요청 시작 시점에 검증](https://docs.aws.amazon.com/AmazonS3/latest/userguide/using-presigned-url.html)
- (내부 교차 감사 — 8.4/8.11/8.12/8.15/8.24/8.52/8.59~8.73절 및 FR-4·FR-7.7 대조)

---

### 8.75 웹 푸시 알림 트리거 확정 (확정)

> 8.16절은 FCM 인프라(구독 등록·토큰 저장·발송 API)까지만 만들고 **"어떤 이벤트에 푸시를 보낼지"는 별도 FR로 미뤄**뒀습니다. PRD에 남은 마지막 미결 항목입니다. 검토해보니 이 결정은 **기능 선택이라기보다 두 가지 제약을 어떻게 지킬 것인가**의 문제였습니다 — ① **정보통신망법**상 푸시도 SMS·이메일과 같은 **전자적 전송매체**이고, ② 우리 설계에서 **푸시는 도달을 보장할 수 없는 채널**입니다. **실제 구축이 아닌 결정 단계**입니다.

#### 먼저 정할 원칙 3가지

| # | 원칙 | 근거 |
|---|---|---|
| **P1** | **푸시는 항상 보조 채널이다 — 푸시로만 전달되는 정보는 없다.** 모든 푸시 트리거는 **이메일 트리거의 부분집합**이어야 한다 | 8.18절이 푸시를 `maxAttempts=1`(사실상 재시도 없음)로 뒀고, 8.16절대로 **토큰 무효화(`UNREGISTERED`)·사파리 제약**이 상시 존재한다. 도달이 보장되지 않는 채널에 **단독 책임을 주면 안 된다** |
| **P2** | **MVP는 거래 정보성 알림만** 보낸다. 광고성(쿠폰·프로모션·리뷰 요청·장바구니 리마인드)은 제외 | 광고성 정보는 **사전 동의 + 야간(21시~08시) 별도 동의 + "(광고)" 표기 + 수신거부 수단**이 모두 필요하다. 동의 관리 UI와 이력 보관까지 붙는 작업이라 **MVP 범위를 크게 늘린다**. 거래 정보성(주문·문의 답변 등)은 이 규제 대상이 아니다 |
| **P3** | **푸시 본문에 상품명(책 제목)을 넣지 않는다** | 푸시는 **잠금화면에 그대로 표시**된다. 전자책 제목은 질병·종교·정치·성 등 **민감한 주제일 수 있어**, 어깨너머로 노출되면 8.31절이 본문을 감추려 애쓴 것이 무색해진다. "구매하신 도서의 개정판이 나왔습니다"처럼 **상품을 특정하지 않는 문구**를 쓰고, 상세는 앱에서 확인하게 한다 |

#### 확정 트리거 (3건)

| 트리거 | 관련 FR | 템플릿 | 왜 푸시가 필요한가 |
|---|---|---|---|
| **1:1 문의 답변 등록** | FR-5.2 | `inquiry_answered` | 사용자가 **답을 기다리는 상태**라 즉시성의 가치가 가장 크다. 8.16절이 원래 후보로 든 이벤트이며, 권한 요청 맥락(아래)도 여기서 가장 자연스럽다 |
| **개정판(오탈자 수정본) 발행** | FR-2.6 | `revision_published` | 고객이 **새 파일을 받아야 하는** 이벤트다. 이메일은 묻히기 쉬운데, 8.73절 V-4로 **다운로드 한도까지 +1 부여**되므로 안 받으면 그 혜택이 사라진다 |
| **다운로드 기간 만료 임박(D-30)** | **FR-4.6(신설)** | `entitlement_expiring` | 구매 후 1년이라 사용자는 **완전히 잊고 있는** 상태다. 만료되면 재다운로드가 영구 불가(FR-4.2)이므로 알림의 실질 가치가 가장 높은 축이다 |

**FR-4.6 신설**이 함께 필요합니다 — 현재 FR-4.4는 **화면에 만료일을 표시**할 뿐 알림이 없습니다. 8.12절 Cron에 **일 1회 만료 30일 전 대상 조회** 작업을 추가하고, **이메일(주 채널) + 푸시(보조)** 로 발송합니다.

#### 제외한 후보와 이유

| 후보 | 제외 사유 |
|---|---|
| 주문/결제 완료 | 결제 직후 **화면에 바로 표시**되고 이메일도 간다. 게다가 P3 때문에 상품명을 못 쓰면 "주문이 완료되었습니다" 뿐이라 **정보 가치가 거의 없다** |
| 다운로드 횟수 소진 임박 | 10회 중 8회를 쓴 사용자는 **이미 인지하고 있다**. FR-4.4 화면 표시로 충분 |
| 상품 Q&A 답변(FR-5.3) | 공개 게시판 성격이라 개인 알림의 필요성이 낮다. 2차 확장 후보 |
| 쿠폰·프로모션·리뷰 요청·장바구니 리마인드 | **광고성**(P2) — 동의 관리·야간 차단·수신거부가 필요해 MVP 범위 밖 |

#### 운영 규칙

| 항목 | 결정 | 근거 |
|---|---|---|
| 권한 요청 시점 | **1:1 문의 작성 완료 직후** — "답변이 등록되면 알려드릴까요?" (2차 기회: 마이페이지 알림 설정) | 8.16절이 "컨텍스트형 요청"으로만 정한 것을 구체화. **사용자가 답을 기다리기 시작하는 바로 그 순간**이라 수락률이 가장 높고, 거절해도 맥락상 자연스럽다 |
| 발송 시간대 | `entitlement_expiring`은 **09~21시에만** 발송(Cron 스케줄로 제어). 나머지 둘은 관리자 액션 시점이라 자연히 업무시간 | 거래 정보성은 야간 제한 대상이 아니지만, **발송 시점을 우리가 고르는 알림**을 굳이 밤에 보낼 이유가 없다 |
| 수신 설정 | 마이페이지에 **트리거별 on/off 3개** 제공 | 브라우저 권한이 사실상 동의 역할을 하지만, 일부만 받고 싶은 사용자를 위해 채널 내 세분화를 둔다 |
| outbox 연동 | 세 템플릿 모두 `notification_outbox`에 **EMAIL 행과 PUSH 행을 각각** 생성(8.17절) | P1대로 이메일이 주 채널이므로 두 채널이 항상 쌍으로 생성된다. 푸시만 실패해도 이메일로 전달이 보장된다 |
| 재시도 | 푸시는 **재시도 없음**(`maxAttempts=1`), 실패 시 8.16절대로 토큰 정리 | 8.18절 결정 유지. 긴급 프로파일은 **이메일 전용**이다(8.72절 U-6) |

#### 향후 광고성 푸시를 도입한다면 (지금은 하지 않음)

다음이 모두 필요하다는 점을 기록해 둡니다 — **사전 수신 동의**(동의 시점·방법 이력 보관), **야간 전송 별도 동의**, 매 전송 시 **"(광고)" 표기**와 **수신거부 방법 명시**, 수신거부 즉시 반영. 거래관계 예외(거래일로부터 일정 기간, 동종 재화)를 활용하더라도 **수신거부 수단은 반드시 제공**해야 합니다.

Sources:
- [정보통신망 이용촉진 및 정보보호 등에 관한 법률 제50조](https://www.law.go.kr/LSW/lsLawLinkInfo.do?chrClsCd=010202&lsJoLnkSeq=1000688185&lsId=000030&print=print)
- [앱 푸시 보내기 전 알아야 할 것 — 수신동의 편(서비스알림 vs 광고)](https://openads.co.kr/content/contentDetail?contsId=8549)
- [푸시 운영: 알림 권한과 광고 수신동의, 야간 전송 규정 정리](https://www.digitalmarketer.co.kr/class/app-marketing-campaigns/push-notification-operations)
- [광고성 정보 전송 가이드라인 | APP PUSH](https://developers.fingerpush.com/app-push/guide/ads)

---

## 9. 마일스톤 및 팀 구성

### 9.1 마일스톤 (확정 — 총 16주, 내부 개발팀 기준)

> 기존 12주 산정은 **요구사항이 확정되기 전**에 잡은 값입니다. 이후 8.1~8.74절 상세 설계 과정에서 **FR이 7건 늘었고**(FR-1.6 이메일 인증, FR-2.7 본문 전문검색, FR-7.7~7.10 관리자 4건), MFA(8.19)·웹 푸시(8.16)·검토 대기 주문 처리(8.71)도 추가됐습니다. 아래는 그 증분만 반영한 재산정입니다.

#### 재산정 근거 — 무엇이 늘었나

**중요한 구분**: 8.51~8.74절에서 찾은 설계 결함들(락 방식·쿠키·검색 엔진·BFF·IPv6 등)은 **일정을 늘리는 요인이 아닙니다.** 그것들은 구현 중에 부딪혔을 문제를 미리 제거한 것이라 **오히려 일정 리스크를 줄입니다.** 늘어난 것은 **범위(FR)** 뿐입니다.

| 추가된 범위 | 증분 | 비고 |
|---|---|---|
| **FR-2.7 본문 전문검색**(8.31/8.59~8.63) | **+2주** | 원래 "상품 검색"은 제목 `ILIKE` 한 줄이었음. 지금은 **추출 파이프라인(페이지 순회·정규화·청킹) + 스니펫 쿼리 + 전용 검색 화면**이며 검색 기능 중 가장 큰 덩어리 |
| **관리자 기능 4건**(FR-7.7~7.10) + **MFA**(8.19) + **초대·감사 로그**(8.20) | **+1.5주** | 기존 M4(2주)는 상품·주문·회원 CRUD만 전제했음 |
| **이메일 인증**(FR-1.6) · **웹 푸시**(8.16) · **`NEEDS_REVIEW` 처리**(8.71) | **+1주** | 각각은 작지만 알림·결제·관리자 세 곳에 걸침 |
| **합계** | **+4.5주** | |

기존 12주 + 4.5주 ≈ **16주**(버퍼 포함). M0는 M1과 병행하므로 순증이 아닙니다.

#### 확정안 — 범위 유지, 16주

| 단계 | 내용 | 기간 |
|---|---|---|
| **M0** | **선행 작업 — 도메인·환경 구축 + 기술 스파이크** (아래 표, M1과 병행) | **1주** |
| M1 | 화면 설계(IA/와이어프레임) + 프로젝트 기반(모노레포·CI·DB 스키마) | 2주 |
| M2 | 회원/인증(FR-1, **MFA·이메일 인증**) + **알림 기반**(outbox·이메일 채널) | 3주 |
| M3 | 상품/카테고리/장바구니 + **제목·초성 검색**(8.27) + **관리자 상품 등록 최소 기능**(9.3절) | 2주 |
| M4 | 주문/결제(PG 연동)/디지털 인도(다운로드) | 3주 |
| M5 | **본문 전문검색**(추출 파이프라인 + 검색 API + 검색 화면, FR-2.7) | 2주 |
| M6 | 관리자 기능(FR-7.1~7.10) | 3주 |
| M7 | QA 및 결제 테스트(PG 실연동 검증) | 2주 |
| M8 | 오픈(소프트 런칭) | - |

**M2에 알림 기반을 앞당긴 이유**: 주문완료 메일(M4)·비밀번호 재설정(M2)이 모두 `notification_outbox`에 의존하므로, 이 토대가 늦으면 두 마일스톤이 함께 막힙니다.
**M5를 M4 뒤에 둔 이유**: 본문검색은 **판매에 필수가 아니므로**(제목 검색만으로 구매 가능) 일정이 밀릴 때 **가장 먼저 덜어낼 수 있는 위치**에 두었습니다.
**M3에 관리자 상품 등록을 넣은 이유**(9.3절): 상품 생성·PDF 업로드만 먼저 만들면 M4·M5 동안 **실제 화면으로 테스트 데이터를 만들 수 있어**, 스키마가 바뀔 때마다 시드 스크립트를 고치는 비용이 사라집니다. 나머지 관리자 기능은 M6에 둡니다.

#### 일정이 밀릴 때의 축소 순서 (참고 — 채택하지 않음)

**범위 유지(16주)로 확정**했으나, 진행 중 일정이 밀릴 경우 **어떤 순서로 덜어낼지**를 미리 정해둡니다. 위기 상황에서 즉흥적으로 범위를 자르면 **수습 창구(FR-7.7·7.10)처럼 없으면 안 되는 것부터 잘려나가기** 때문입니다.

| 덜어낼 범위 | 절감 | 영향 |
|---|---|---|
| **FR-2.7 본문 전문검색 전체** | **-2주** | 제목·설명·초성 검색(8.27)은 유지되므로 **구매 동선에 지장 없음**. 8.59~8.63절 설계는 그대로 보존해 오픈 후 착수 |
| **웹 푸시**(8.16) | **-0.5주** | 이메일 알림으로 대체 가능. 사파리 제약(8.16절)으로 도달률도 제한적이었음 |
| **MFA를 SUPER_ADMIN 전용으로 축소**(8.19) | **-0.5주** | 일반 ADMIN은 오픈 후 확대. 관리자 5~6명 규모라 초기 위험이 낮음 |
| **FR-7.6 매출 리포트 엑셀 다운로드** | **-0.5주** | 초기에는 관리자 대시보드(FR-7.1) 조회로 갈음 |
| **FR-5.1 리뷰 · FR-5.3 상품 Q&A** | **-1주** | 카탈로그가 작은 초기에는 콘텐츠가 쌓이지 않아 효용이 낮음. FR-5.2 1:1 문의는 유지(CS 필수) |
| **합계** | **-4.5주** | → 12주(= 기존 목표) |

**덜어내면 안 되는 것**: FR-7.7(수동 재발송)·FR-7.10(한도 재부여)·`NEEDS_REVIEW` 처리는 **돈·전달 실패를 수습하는 창구**라 없으면 CS를 수작업 DB 조작으로 처리하게 됩니다. FR-7.9(감사 로그)도 관리자 권한 사고 시 추적 수단이라 유지합니다.

#### M0 — 1주차 선행 작업

> 8.51~8.70절 교차 검토에서 드러난 **"막히면 그 다음이 전부 멈추는"** 작업들입니다. M1(화면 설계)과 병행하므로 **9.1절의 16주에 순증하지 않지만**, 아래 ①②가 늦어지면 M2부터 실제로 밀립니다.
>
> **실행용 체크리스트는 [`docs/M0-CHECKLIST.md`](docs/M0-CHECKLIST.md)** 에 있습니다 — 항목별 **합격 기준과 실패 시 대안**, 그리고 실측 결과에 따라 설계가 바뀌는 지점(④)을 함께 정리해 뒀습니다.

| # | 작업 | 왜 1주차여야 하나 | 담당 |
|---|---|---|---|
| ① | **도메인 확정 + DNS + 인증서** — 운영 `app.`/`api.`, 스테이징 `stg.`/`api-stg.`, 프리뷰 **`*.preview.` 와일드카드 CNAME**. 백엔드는 ACM 발급 후 ALB 연결 (8.7/8.34/8.65절) | 도메인이 없으면 **PR 프리뷰에서 로그인이 안 되고**(8.65절 N-1: `*.vercel.app`은 다른 사이트라 Refresh 쿠키가 동작하지 않음) 8.46절 인증 E2E가 통째로 막힌다. 8.13절 SPF/DKIM/DMARC와 **토스페이먼츠 상점 등록**도 도메인에 의존한다. 도메인 구입·DNS 전파·ACM 검증은 **대기 시간이 있어** 착수가 늦으면 회복이 안 된다 | PM + 백엔드 |
| ② | **Supabase 프로젝트 2개(스테이징·운영) 생성 → `roles.sql` 실행 → 최초 마이그레이션 → Data API 노출 스키마에서 `public` 제거** (8.15/8.34/8.56/8.68절) | 모든 DB 작업의 전제. `cbmall_app` 최소권한 롤을 **양쪽에 모두** 적용해야 프리뷰 단계부터 운영과 같은 권한으로 검증된다(8.56절: 프리뷰는 스테이징 DB 공유). **실행 순서가 중요하다(8.68절)** — `roles.sql`은 롤 생성과 기본 권한만 다루므로 **최초 마이그레이션보다 먼저**, 기존 객체에 대한 정책·권한 정리는 마이그레이션 `20260920150000_rls_app_policies`가 **그 뒤에** 수행한다. Data API 노출 스키마 제거(8.68절 Q-2)도 이때 함께 처리한다 | 백엔드 |
| ③ | **react-pdf 스파이크(타임박스 0.5일)** — `'use client'` 래퍼 안에서 `ssr: false` 동작 확인, pnpm 환경에서 `pdfjs-dist` 워커 경로 해석, `workerSrc` 버전 정합 (8.29/8.64절 M-1·M-4·M-5) | M-1은 **빌드 자체가 실패**하고 M-4는 pnpm의 엄격한 `node_modules` 구조에서만 드러나는 문제다. FR-2.4 샘플 뷰어 착수 직전에 발견하면 그 주가 통째로 날아간다 | 프론트 |
| ④ | **PDF 텍스트 추출 스파이크(타임박스 1일)** — 실제 자사 전자책 2~3권으로 페이지별 추출(`getText({ partial })`) 후 **줄바꿈이 음절 중간에서 끊기는지 어절 경계에서 끊기는지 실측** (8.60절 I-1) | 8.59~8.63절 검색 설계 전체가 **"한국어 PDF는 음절 단위로 줄바꿈된다"** 는 전제 위에 서 있다(UAX #14·한컴/Word 기본값 근거, 8.61절 J-7). 실제 콘텐츠가 다르게 나오면 **정규화 규칙과 스니펫 가독성 판단이 함께 바뀐다** — 청크 파이프라인을 만들기 전에 확인해야 한다 | 백엔드 |
| ⑤ | **CI 뼈대** — GitHub Actions + OIDC, 환경별 `concurrency` 그룹, 파괴적 DDL 검출, `app/api/**` 허용 목록 검증 (8.35/8.57/8.66절 O-3) | 배포 직렬화(8.57절 F-6)와 확장-수축 검출(F-1)은 **사고가 난 뒤에 넣으면 이미 늦다**. 1주차 말~2주차 초에 걸쳐 진행 가능 | 백엔드 |
| ⑥ | **Vercel 프로젝트 설정** — 함수 리전 `icn1`(서울) 고정, `maxDuration=10`, SSR 릴레이 타임아웃·`<Suspense>`·`cache()` (8.69절) | 기본 리전 `iad1`이면 SSR 릴레이가 **태평양을 2회 왕복**해 6절 성능 목표를 로그인 페이지에서 먼저 잠식한다. 리전 지정은 **Pro 플랜 이상** 기능이라 플랜 확정과 함께 처리해야 한다 | 프론트 |

**M2 범위 주의**: 8.59~8.63절에서 본문검색이 "`ILIKE` 한 줄"에서 **청크 색인 파이프라인 + 정규화 + 스니펫 쿼리 + 전용 화면**으로 커졌습니다. M2의 "검색"은 8.27절 제목·초성 검색만이 아니라 이 범위 전체를 포함하므로, 3주 안에 빠듯하면 **본문검색(FR-2.7)을 M4 이후로 미루는 것**이 우선 조정 대상입니다(제목 검색만으로도 판매에는 지장이 없음).

### 9.2 팀 구성

백엔드가 결제·다운로드 보안 로직을 전담하는 구조라 백엔드 인력을 프론트보다 1명 더 배치했다.

| 역할 | 인원 | 주요 업무 | 참여 시기 | 비고 |
|---|---|---|---|---|
| PM/기획 | 1명 | 일정/범위 관리, QA 총괄, **M0 도메인 확정·외부 서비스 등록**(토스페이먼츠 상점, DNS) | 전체(M0~M6) | 겸직 가능 |
| 프론트엔드 개발자 | 1명 | Next.js 화면 개발, 결제위젯 연동, 반응형 UI, **M0 react-pdf 스파이크** | 전체(M0~M6) | - |
| 백엔드 개발자 | 2명 | NestJS API, 결제 승인/웹훅, 다운로드 토큰·presigned URL, 관리자 API, **M0 환경 구축·PDF 추출 스파이크·CI 뼈대** | 전체(M0~M6), M3 집중 | 최소 1명 가능하나 2명 권장 |
| UI/UX 디자이너 | 1명(PT) | 와이어프레임/IA, UI 디자인 | M1~M2 집중 | 외주 활용 가능 |
| QA | 1명(PT) | 결제 시나리오 테스트, 버그 트래킹 | M5 집중 | 겸직 가능 |

**총 인원: 권장 5~6명 / 최소 4명(겸직 시)** — 16주 재산정(9.1절) 후에도 **구성은 그대로 유지**한다. 인력을 늘려도 결제 승인·다운로드 인도처럼 **순차 의존이 강한 작업은 병렬화되지 않아** 온보딩 비용만 늘어나므로, 늘어난 범위는 **기간으로 흡수**한다. **M0(1주차 선행 작업)는 PM·프론트·백엔드가 각자 맡은 항목을 M1과 병행**하므로 추가 인력이 필요하지는 않으나, 백엔드에 ②④⑤ 세 건이 몰려 있어 **백엔드 1명 구성에서는 M0가 2주로 늘어날 수 있다**. 최소 구성(4명)일 경우 M3·M5 기간이 늘어날 수 있어 3개월 일정이 빠듯할 수 있으며, 백엔드가 1명뿐이라면 8절의 아키텍처를 Next.js 풀스택 단일 구조로 재검토하는 것도 대안이다.


### 9.3 개발 착수 로드맵 (확정)

> 9.1절이 **"언제"** 를 정했다면, 이 절은 **"무엇이 무엇을 막는가"** 와 **"세 명이 동시에 무엇을 하는가"** 를 정합니다. 마일스톤 표만으로는 **한 사람이 대기하는 구간**과 **숨은 선행 작업**이 드러나지 않습니다.

#### 크리티컬 패스 — 밀리면 전체가 밀리는 사슬

```
도메인·DNS(M0①) ─┬─→ 토스 상점 등록 ─────────────────────→ 결제 실연동 검증(M7)
                  └─→ 프리뷰 인증 ─→ 인증 E2E(8.46)
Supabase·roles(M0②) ─→ 마이그레이션 ─→ [모든 DB 작업]
알림 토대(outbox·이메일) ─→ 이메일 인증(FR-1.6) ─→ 회원가입 완결
                          └─→ 주문완료 메일 ─→ 결제 흐름 완결
상품 CRUD ─→ 장바구니 ─→ 주문 ─→ 결제 ─→ 엔타이틀먼트 ─→ 다운로드   ← 일직선, 단축 불가
상품 업로드 ─→ PDF 추출·청킹 ─→ 검색 API ─→ 검색 화면              ← 분기, 뒤로 미룰 수 있음
```

**두 가지만 기억하면 됩니다**: ① **도메인과 Supabase가 M0에서 안 끝나면 그 뒤 전부가 대기**합니다(그래서 M0가 존재합니다). ② **상품→장바구니→주문→결제→다운로드는 일직선**이라 인력을 넣어도 단축되지 않습니다 — 여기가 실질적인 기간의 하한입니다.

#### 숨은 선행 작업 — 마일스톤 표에 안 보이는 것

| 항목 | 언제까지 | 없으면 무슨 일이 | 담당 |
|---|---|---|---|
| **자사 전자책 실물 2~3권 확보** | **M0 시작 전** | M0 ④ 추출 스파이크를 돌릴 대상이 없고, **8.60절 줄바꿈 전제를 검증하지 못한 채 M5 검색을 만들게 된다**. 표지·샘플 PDF도 없어 상품 화면 개발이 목 데이터에 갇힌다 | PM/콘텐츠 |
| **토스페이먼츠 상점 등록 착수** | **M0 ①과 동시** | 심사·계약에 리드 타임이 있다. M4에서 테스트 키가 없으면 결제 개발이 목으로만 진행되고, **실연동 검증(M7)에서 한꺼번에 터진다** | PM |
| **OpenAPI 계약 확정**(8.41절) | **M2 착수 시** | 프론트가 백엔드 완성을 기다리게 된다. 계약이 있으면 **MSW 목**(8.45절)으로 화면을 선행 개발할 수 있다 | 백엔드 |
| **최초 SUPER_ADMIN 시드 스크립트**(8.20절) | **M2 말** | 관리자 초대 체계는 "기존 관리자"를 전제하므로, 시드가 없으면 **관리자 기능 개발 자체를 시작할 수 없다** | 백엔드 |
| **관리자 상품 등록 화면**(FR-7.2 일부) | **M3 말** | M6까지 미루면 M4·M5 내내 **상품·PDF 테스트 데이터를 시드 스크립트로 관리**해야 한다. 스키마가 바뀔 때마다 스크립트를 고치는 비용이 화면 하나 만드는 비용을 넘어선다 | 백엔드+프론트 |

#### 트랙별 병렬 배치

프론트 1명이 백엔드를 기다리지 않도록, **OpenAPI 계약 + MSW 목**으로 화면을 선행합니다(8.41/8.45절에서 이미 결정된 도구입니다).

| 구간 | 백엔드 A | 백엔드 B | 프론트 |
|---|---|---|---|
| M0~M1 | Supabase·roles·마이그레이션, CI 뼈대 | PDF 추출 스파이크(M0④), 알림 토대 설계 | react-pdf 스파이크(M0③), 디자인 시스템·레이아웃, **화면 골격(목 데이터)** |
| M2 | 인증(FR-1, MFA, 쿠키·SSR 헬퍼) | **알림 토대**(outbox·Cron·이메일 채널) | 회원가입/로그인/마이페이지 — **MSW 목으로 선행** |
| M3 | 상품·카테고리 CRUD + **관리자 상품 등록** | 장바구니, 제목·초성 검색 | 상품 목록/상세, 장바구니 |
| M4 | **결제(토스 연동·웹훅·멱등)** | 주문·엔타이틀먼트·다운로드(원자적 함수) | 주문서·결제위젯·다운로드 UI |
| M5 | PDF 추출 파이프라인(청킹·정규화) | 검색 API(스니펫 쿼리) | 검색 화면 |
| M6 | 관리자 API(FR-7.1~7.10) | 감사 로그·알림 이력·`NEEDS_REVIEW` | 관리자 화면 |
| M7 | 결제 실연동 검증, 성능 점검 | 장애·복구 시나리오 | E2E 보강, 접근성 점검 |

**M4가 가장 위험합니다** — 외부 의존(토스)과 돈이 동시에 걸린 유일한 구간입니다. 백엔드 A를 결제에 전담시키고 다른 작업을 섞지 않습니다.

#### 마일스톤 완료 기준(DoD)

"끝났다"의 정의가 없으면 다음 단계가 불완전한 토대 위에서 시작됩니다.

| 단계 | 완료 기준 |
|---|---|
| M0 | `docs/M0-CHECKLIST.md` 전 항목 통과 + **④ 줄바꿈 패턴 관찰 결과가 팀에 공유됨**(8.60절 재검토 여부 판단) |
| M1 | 프리뷰 URL에서 화면 골격이 뜨고, CI가 Lint·타입체크·테스트·아키텍처 경계 검증을 모두 수행 |
| M2 | **가입 → 이메일 인증 → 로그인 → 비밀번호 재설정**이 실제 메일 수신까지 동작. 관리자 시드 계정으로 로그인 가능 |
| M3 | 관리자가 **상품과 PDF를 실제로 등록**하고, 고객이 목록·상세·제목 검색으로 그 상품에 도달 |
| M4 | **토스 테스트 결제 → 주문 PAID → 엔타이틀먼트 생성 → 다운로드 성공**이 한 번에 통과. 웹훅 유실·중복 시나리오 검증 포함 |
| M5 | 실제 전자책 본문에서 검색어를 찾아 **스니펫과 페이지 번호가 표시**됨 |
| M6 | 관리자가 **주문 취소·환불·한도 재부여·알림 재발송**을 화면에서 처리 가능(수작업 DB 조작 불필요) |
| M7 | 실결제 1건 성공 후 환불까지 완료. 8.46절 E2E 전 시나리오 통과 |

#### 첫 2주 착수 순서

| 일자 | 작업 |
|---|---|
| D+0 | 도메인 확보·DNS 등록(리드 타임 때문에 **가장 먼저**), 토스 상점 등록 신청, 전자책 실물 확보 요청 |
| D+1~2 | Supabase 2개 생성 → `roles.sql` → 최초 마이그레이션 → Data API `public` 제거 |
| D+2~3 | 모노레포 스캐폴딩(Turborepo·pnpm), CI 뼈대(OIDC·`concurrency`) |
| D+3~4 | react-pdf 스파이크(③), PDF 추출 스파이크(④) — **④ 결과에 따라 8.60절을 고칠 수 있으므로 늦추지 않는다** |
| D+5 | ACM 인증서·ALB 연결, `vercel alias` 프리뷰 도메인 검증 |
| 2주차 | 화면 설계 확정, OpenAPI 계약 초안, 디자인 시스템 셋업, DB 스키마 리뷰 |

---

## 10. 착수 전 준비물

> 개발 **작업 순서**는 9.3절 로드맵, **1주차 실측 항목**은 `docs/M0-CHECKLIST.md`가 담당합니다. 이 절은 그와 겹치지 않게 **"코드를 쓰기 전에 외부에서 확보해야 하는 것"** 만 모았습니다 — 계정·키·계약·자산처럼 **우리가 통제할 수 없는 리드 타임이 있는 항목**들입니다.
>
> 8.1~8.75절의 기술 의사결정과 FR 39건이 **모두 확정**되었습니다. 남은 미결 사항은 없습니다.

### A. 리드 타임이 있어 가장 먼저 (D+0 착수)

| 항목 | 필요 시점 | 막히면 |
|---|---|---|
| **서비스 도메인 확보** → 운영 `app.`/`api.`, 스테이징 `stg.`/`api-stg.`, 프리뷰 `*.preview.` 와일드카드 CNAME, ACM 인증서·ALB 연결 | **M0** | 프리뷰 인증·E2E·SPF/DKIM·토스 등록이 전부 대기(8.65절 N-1) |
| **자사 전자책 실물 2~3권 확보**(원본 PDF + 표지 + 샘플) | **M0 이전** | M0 ④ 추출 스파이크 대상이 없어 **8.60절 줄바꿈 전제를 검증 못 한 채 M5 검색을 만들게 됨**(9.3절) |
| **토스페이먼츠 가맹점 계약 + 개발자센터 연동 준비**(테스트 키 발급) | **M0 착수, M4 사용** | 심사 리드 타임이 있어, 늦으면 결제를 목으로만 만들다 M7 실연동에서 한꺼번에 터짐 |
| **솔라피 가입 + 발신번호 사전등록**(사업자 서류 인증, 영업일 1~2일) | **M2** | SMS 채널 개발·검증 불가 |

### B. 외부 서비스 계정·키

| 항목 | 필요 시점 |
|---|---|
| Resend 계정/API 키, 발신 전용 `mail.` 서브도메인 SPF/DKIM/DMARC 설정 | M2 |
| Firebase 프로젝트 + FCM 웹 설정, 서비스 계정 키 발급 | M2 |
| Sentry 프로젝트 생성(프론트·백엔드), UptimeRobot(또는 동급) 헬스체크 등록 | M1 |
| GA4 속성 생성 + 전자상거래 이벤트 설계, Vercel Analytics 활성화, Measurement Protocol API secret 발급 | M4 |
| Vercel 프로젝트 — **함수 리전 `icn1`(서울)** 설정(Pro 플랜 이상 기능, 8.69절 R-1) | M1 |

### C. AWS 리소스

| 항목 | 필요 시점 |
|---|---|
| S3 버킷 2개(원본 PDF 비공개 / 샘플·표지 공개) + 버킷 정책, 공개 버킷 **CORS**(Vercel 프리뷰 오리진 포함, 8.51절 C-5) | M3 |
| Secrets Manager 경로 `/cbmall/staging/*`, `/cbmall/production/*` 구성 | M1 |
| 등록할 시크릿 — 토스 테스트/라이브 키, **캐시 무효화 공유 시크릿**(8.51절 C-6), **SSR 비회전 토큰 엔드포인트 공유 시크릿**(8.53절), GA4 API secret, Resend·솔라피·FCM 키 | 각 사용 시점 |
| KMS CMK 생성 + 자동 로테이션(MFA secret 암호화용), 로테이션 일정 캘린더 등록(JWT 서명키 포함, 8.30절) | M2 |
| ECR 리포지토리, ECS 태스크 정의 **2 vCPU / 4GB**(8.51절 C-7), ALB 앞단 **WAF Web ACL**(Rate-based + 관리형 공통 룰셋) | M1 |
| GitHub Actions **OIDC ↔ IAM Role** 신뢰관계(브랜치 조건부 제한) | M1 |

### D. Supabase

| 항목 | 필요 시점 |
|---|---|
| 스테이징·운영 프로젝트 **2개** 생성(**Branching 미사용** — 8.56절) | M0 |
| 각 프로젝트에서 `apps/api/prisma/setup/roles.sql` 실행 → 최초 마이그레이션 → **Data API 노출 스키마에서 `public` 제거**(8.68절 Q-2) | M0 |
| ECS 태스크에는 `cbmall_app`의 **트랜잭션 모드 커넥션**(6543, `?pgbouncer=true`), CI에는 **세션 모드 커넥션**(5432 풀러) 주입(8.67/8.70절) | M1 |

### E. 코드·운영 준비

| 항목 | 필요 시점 |
|---|---|
| 토스페이먼츠 개발자센터에 웹훅(`PAYMENT_STATUS_CHANGED`) 엔드포인트 등록 | M4 |
| 최초 SUPER_ADMIN 시드 스크립트 작성 + 배포 절차 문서화(8.20절) | M2 |
| E2E 테스트 데이터 접두사 규칙(`e2e-`) 및 정리 스크립트(8.46/8.56절) | M1 |
| `dependency-cruiser` 규칙 + CI 아키텍처 경계 검증 스크립트 5종(8.35/8.66/8.68절) | M1 |

### F. 문서·법무

| 항목 | 필요 시점 |
|---|---|
| 개인정보처리방침에 GA4 등 분석 도구 사용 사실 반영(법률 검토 권장) | M7 이전 |
| 전자상거래법·전자금융거래법 관련 고지사항 및 청약철회 정책 문구 확정(7절) | M7 이전 |

### G. 남은 FR 결정

**없습니다.** 마지막 미결 항목이던 웹 푸시 트리거는 **8.75절에서 확정**되었습니다(문의 답변·개정판 발행·만료 임박 3건, FR-4.6 신설).

---

## 부록 A. 개발 접근 방식 비교: 자체 개발 vs 커머스 SaaS (결정 근거 참고)

**결정: 자체 개발로 진행 확정.**

| 항목 | 자체 개발 (커스텀 웹앱) | 커머스 SaaS (카페24/고도몰/Shopify 등) |
|---|---|---|
| 초기 출시 속도 | 느림 (약 4개월 — 9.1절 재산정) | 빠름 (수 주 내 오픈 가능) |
| 초기 비용 | 개발 인력/기간 투입 필요, 상대적으로 높음 | 월 이용료 + 결제 수수료 중심, 초기 비용 낮음 |
| PG/간편결제 연동 | 토스페이먼츠와 직접 계약 후 연동 개발 필요 | 플랫폼 기본 지원 여부 사전 확인 필요 |
| 커스터마이징 자유도 | 높음 — 5절 요구사항을 제약 없이 구현 가능 | 제한적 — 플랫폼 정책/템플릿 범위 내 |
| 확장성 | 자유롭게 설계 가능 | 플랫폼에 따라 제약 있을 수 있음 |
| 유지보수 | 자체 인프라/보안 패치 관리 필요 | 플랫폼 사업자가 관리 |
| 데이터 소유권 | 완전한 소유 | 플랫폼 정책에 종속 |
| 적합한 경우 | 장기적 차별화, 개발 리소스 확보된 경우 | 빠른 시장 검증이 우선인 경우 |

Sources:
- [토스페이먼츠 JavaScript SDK](https://docs.tosspayments.com/sdk/v2/js)
- [결제위젯 이해하기(Version 1)](https://docs.tosspayments.com/guides/payment-widget/overview)
- [30분 안에 결제 페이지 개발하기 (ft. 결제위젯)](https://velog.io/@tosspayments/%EA%B2%B0%EC%A0%9C%EC%9C%84%EC%A0%AF%EC%9C%BC%EB%A1%9C-30%EB%B6%84%EC%95%88%EC%97%90-%EA%B2%B0%EC%A0%9C-%ED%8E%98%EC%9D%B4%EC%A7%80-%EA%B0%9C%EB%B0%9C%ED%95%98%EA%B8%B0)
